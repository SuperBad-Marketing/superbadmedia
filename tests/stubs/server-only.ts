// Stub for `server-only` in vitest. The real package throws at import
// time outside the React Server Components runtime; tests run in plain
// node and need to load server modules normally.
export {};
