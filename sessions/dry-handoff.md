# DRY Handoff — Synthetic-Client Dry-Run

**Date:** 2026-04-21
**Wave:** 23 (second session)
**Status:** PARTIAL — see findings below

## What was done

Full-arc synthetic-client walkthrough on local dev (localhost:3001). Seeded "Coastal Brew Co" as a test prospect and walked it through: prospect → trial shoot → Brand DNA → quote → invoice → won → client. Verified every admin surface renders with data.

### Bug fix shipped

- **`three-wons.ts` server-only import chain** — pre-existing build error where `pipeline-board.tsx` (client component) imported `maybeFireThreeWonsEgg` directly, pulling `lib/db` (server-only) into the client bundle. Fixed by moving the egg check behind a server action `checkThreeWonsEggAction()` in `app/lite/admin/pipeline/actions.ts`. Zero new regressions (288 test files passed, 2930 tests, 1 pre-existing failure).

### Dry-run checklist results (per LAUNCH_READY.md §9)

| Step | Status | Notes |
|---|---|---|
| Fake prospect created | ✅ PASS | Seeded company, contact, user, deal. Shows in Pipeline Lead column. |
| Simulated outreach reply → ICP scoring → Intro Funnel routed | ✅ PASS | Deal advanced to trial_shoot. Funnel submission + calendar booking created. Pipeline reflects stage change. |
| Trial shoot booked via Intro Funnel | ✅ PASS | Token-gated intro funnel renders: "Welcome, Sam. Here's what's next." Personalized, no "Lite" branding (correct). |
| Brand DNA questionnaire completed | ✅ PASS | Profile seeded as complete. Brand DNA tab on company renders: first impression, prose portrait, section scores, completion date. |
| Six-Week Plan generated and delivered | ⏳ BLOCKED | Requires real Opus call via scheduled task pipeline. Anthropic API key is configured but the generation is triggered by background job, not UI action. Needs live scheduled-task execution. |
| Retainer quote drafted + accepted | ✅ PASS | Quote Q-2026-001 created and accepted. Deal advanced to Won with RETAINER badge. Activity log records quote_sent. |
| First invoice generated + paid | ⚠️ PARTIAL | Invoice INV-2026-001 created and visible on Invoices page ($2,750.00, issued, due 5 May). **Payment flow not exercised** — requires Stripe Payment Intent creation + real webhook round-trip. Stripe keys are configured and webhook listener running. |
| Client portal active | ⚠️ PARTIAL | Portal recover page renders correctly. Token-gated portal pages exist (`/lite/portal/[token]/*`). **Full magic-link → portal auth flow not exercised** — requires Resend email delivery + click-through. |
| Content Engine producing drafts | ⏳ BLOCKED | Requires LLM call via scheduled task. Content Engine admin page renders correctly with proper tabs. |
| Cockpit brief reflects all of above | ⚠️ PARTIAL | Cockpit renders with correct structure. **Brief not populated** — briefs are generated by daily cron job, not on-the-fly. Calendar correctly shows no events today. |
| Cancel flow walked | ⏳ BLOCKED | Requires active Stripe subscription to cancel. No subscription was created in this dry run. |

### All admin surfaces verified (200 + renders content)

| Surface | Status |
|---|---|
| Pipeline (kanban, deal cards, stage transitions) | ✅ |
| Company detail (all 8 tabs: Overview, Tasks, Deliverables, Billing, Brand DNA, Comms, Portal Chat, Activity) | ✅ |
| Clients (summary stats, health filters, table) | ✅ |
| Invoices (filters, table, outstanding/overdue/paid summary) | ✅ |
| Inbox (folders, address filter, thread pane) | ✅ |
| Tasks | ✅ |
| Lead Gen (queue, runs, metrics, DNC tabs, warmup status) | ✅ |
| Content Engine (review, social, metrics, topics, list) | ✅ |
| Products / Catalogue | ✅ |
| Finance (period selectors, Stripe connect prompt) | ✅ |
| Observatory (MTD spend, projected, top jobs — real data from external_call_log) | ✅ |
| Errors (triage dashboard) | ✅ |
| Settings / Catalogue | ✅ |
| Intro Funnel (token-gated prospect portal) | ✅ |
| Portal recover page | ✅ |

### Missing / not built

- `/lite/_design` design system reference route — 404. Was in Phase 4 foundation spec but never built. Not launch-blocking but noted.

## Edited files

- `app/lite/admin/pipeline/actions.ts` — added `checkThreeWonsEggAction` server action + import
- `components/lite/sales-pipeline/pipeline-board.tsx` — replaced direct `maybeFireThreeWonsEgg` import with server action call

## Verification

- `npx tsc --noEmit` — 4 pre-existing test-only errors, zero new
- `npx vitest run` — 288 files passed, 1 pre-existing failure (`sb10-headline-signals.test.ts`), zero new regressions
- Full suite: 2930 passed, 1 failed (pre-existing), 1 skipped

## Rollback

- Server action addition is additive — git-revertable
- Test data is in local dev.db only — no production impact

## What's still needed before LAUNCH_READY §9 can be fully ticked

1. **Stripe payment round-trip** — create a real Stripe Payment Intent for the test invoice, complete payment with a test card, verify webhook fires + invoice status updates to `paid`. Then refund.
2. **Portal magic-link auth flow** — send a real magic link email via Resend, click it, verify portal renders with client data.
3. **Six-Week Plan generation** — trigger the scheduled task that calls Opus to generate a plan for the test client. Verify the plan renders in the portal.
4. **Cockpit brief generation** — trigger the daily brief cron. Verify it reflects the test client's activity.
5. **Content Engine draft** — trigger content generation scheduled task. Verify a draft appears in the Content Engine.
6. **Cancel flow** — create a real Stripe subscription for the test client, then walk the cancel/buyout flow.

These all require **live external service integration** (Stripe webhooks, Resend emails, Anthropic API calls) running in real time. The current dry run confirmed all UI surfaces render correctly and data flows through the system. The remaining items are integration-level verification that happens best during the Phase 6 shadow period with Andy actively watching.

## Pre-existing issues (unchanged)

- `sb10-headline-signals.test.ts` failure — confirmed pre-existing
- `sd11_rain_ambient_mp3` — audio file needs sourcing (asset session)
- Egg build error **fixed** this session (was `three-wons.ts` → `pipeline-board.tsx` client component chain)

## Next session should know

- **DRY is partial-complete.** All UI surfaces verified. Integration-level flows (Stripe payment, email delivery, LLM generation, cron jobs) need Phase 6 shadow period.
- **The `three-wons.ts` server-only build error is fixed.** `checkThreeWonsEggAction()` in `actions.ts` is the server action replacement.
- **Dev database has test data.** Company `co-dry-run-01` (Coastal Brew Co) with full arc data. Safe to keep for further testing or drop before production.
- **Stripe CLI listener needs to be running** for webhook testing: `stripe listen --forward-to localhost:3001/api/stripe/webhook --api-key <key>`
