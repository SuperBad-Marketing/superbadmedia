# PATCHES_OWED

Consolidated list of patches owed on existing docs, specs, memories, and code. Every spec session, content mini-session, and brainstorm that identifies a patch owed on another file adds a row here — not just a mention in its handoff.

Phase 3.5 reads this file as the authoritative list of owed patches alongside session handoffs. If a handoff names a patch that isn't here, the 3.5 session is treated as canonical but also adds the missing row.

## How to use

- **Add** a row at the bottom of the relevant section when a session identifies a patch.
- **Strike through or move to "Applied"** when a later session has actually applied the patch.
- **Never delete rows** — keep applied ones in the "Applied" section at the bottom for auditability.
- Columns: target file · what to patch · why · raised by (session id or memory) · raised when (YYYY-MM-DD).

## Pending

### SCOPE.md

- ~~`SCOPE.md` · Add **Branded Invoicing** as a first-class v1.0 feature~~ — **APPLIED 2026-04-13** by Phase 3.5 Batch A stop point: added as §17 in new "Additional v1 features (added 2026-04-13 — Phase 3.5 SCOPE reconciliation)" section.
- ~~`SCOPE.md` · Add **Intro Funnel (paid trial shoot)** as a first-class v1.0 feature~~ — **APPLIED 2026-04-13** by Phase 3.5 Batch A stop point: added as §18. Supersedes the single "Paid intro offer" bullet under §1 Lead Generation.
- ~~`SCOPE.md` · Add **Six-Week Plan Generator** as a first-class v1.0 feature~~ — **APPLIED 2026-04-13** by Phase 3.5 Batch A stop point: added as §19.
- ~~`SCOPE.md` · Hiring Pipeline decision~~ — **APPLIED 2026-04-13** by Phase 3.5 Batch A stop point: added as §20 (Andy chose v1.0 inclusion, not v1.1 deferral).

### FOUNDATIONS.md

Phase-5-gated FOUNDATIONS patches (land alongside the relevant build session, not now — but tracked here for Phase 4 sequencing):

- `FOUNDATIONS.md` §11.2 `sendEmail()` classification enum · Add 3 new values: `six_week_plan_invite`, `six_week_plan_followup`, `six_week_plan_delivery` · Six-Week Plan Generator spec · 2026-04-13 · **gate: Phase 5 Six-Week Plan build session**
- `FOUNDATIONS.md` §11 disciplines list · Add 6 Task Manager / Surprise & Delight build-time disciplines (24–29) — task state transitions, deliverable approval, braindump ingestion, braindump mount discipline, portal-task authorisation, email-send classification-at-call-site · Task Manager + S&D specs · 2026-04-12 · **gate: Phase 5 Task Manager build session**
- `FOUNDATIONS.md` §11 · Document `scheduled_tasks` worker as §11-tier cross-cutting infrastructure primitive (current location: Quote Builder §5.4 / §8.2 as authoritative owner; Foundations needs a one-paragraph pointer for discoverability) · Quote Builder handoff · 2026-04-12 · **gate: Phase 4 Build Plan**
- `FOUNDATIONS.md` §11.2 · Confirm unified send-gate model aligns with Cost & Usage Observatory's actor-attribution extensions (currently compatible but not cross-referenced in Foundations) · Observatory spec · 2026-04-13 · **gate: Phase 5 Observatory build session**
- `docs/specs/cost-usage-observatory.md` prompt-cardinality inventory · Reconcile with `lib/ai/prompts/INDEX.md` (47 prompts across 14 specs); Observatory registered-jobs inventory must include every prompt-keyed job so external-call cost attribution is complete · Phase 3.5 Batch B step 7 · 2026-04-13 · **gate: Phase 5 Observatory build session**
- `FOUNDATIONS.md` §11.2 / §11.5 runtime definition · Cross-cutting primitives `canSendTo(recipient, classification)`, `renderToPdf(htmlOrReactTree, opts)`, `checkBrandVoiceDrift(draftText, brandDnaProfile)` are referenced across ≥5 specs but have no runtime signature in Foundations — only interface hints. Define full TS signatures + return shapes in Phase 4 foundation session · Phase 3.5 Batch B step 7 · 2026-04-13 · **gate: Phase 4 foundation session**
- `docs/specs/daily-cockpit.md` `getWaitingItems()` + `getHealthBanners()` source-spec stubs · Cockpit aggregates from 14 specs but the enumeration of which spec emits which waiting-item kind / health-banner kind is only partially complete — Observatory, Finance, Hiring kinds applied (see Applied below) but several specs still need explicit `emits:` blocks naming their cockpit contributions · Phase 3.5 Batch B step 7 · 2026-04-13 · **gate: Phase 5 Daily Cockpit build session**
- `FOUNDATIONS.md` §11 · Add **Error Reporting & Support Triage** as a cross-cutting primitive. Three components: (1) Sentry wired into Next.js client + server + edge runtimes — captures every unhandled exception with stack, user, URL, session replay; deduped by fingerprint so N users hitting the same bug = 1 ticket. (2) `lib/support/reportIssue(context)` primitive callable from any client-facing surface (portal, cockpit, intro funnel, brand DNA, admin); "Report an issue" button lives in the global footer of every surface; attaches last 30s replay + user + page + recent actions; writes to new `support_tickets` table AND pings Sentry with the ticket id for correlation. (3) New `support_tickets` table (id, user_id, surface, page_url, description, session_replay_url, sentry_issue_id, status, created_at, resolved_at). (4) New Daily Cockpit waiting-item kind `error_triage` — aggregates Sentry issues + open support_tickets, sorted by impact (users_affected × occurrences); single triage inbox, not three. Covers the "efficiency when live, don't hunt errors individually" goal. · Andy question 2026-04-13 (efficient debugging/error reporting process when live) · 2026-04-13 · **gate: Phase 4 foundation session** (land alongside other §11 cross-cutting primitive definitions; Cockpit waiting-item kind lands in Phase 5 Daily Cockpit build session)

### Specs — retroactive updates owed

- ~~All locked specs · Inline cross-spec contract detail currently only in handoffs (Phase 3.5 step 2a)~~ — **APPLIED 2026-04-13** by `phase-3.5-step-2a-self-containment` (see Applied section below)
- ~~All locked specs with autonomy thresholds expressed as literals · Convert to `settings.get()` keys per Phase 3.5 step 7a~~ — **APPLIED 2026-04-13** by Phase 3.5 Batch B step 7a (see Applied section below)
- Locked specs pre-dating the "Voice & delight treatment" convention · Add the section per cross-cutting discipline (Sales Pipeline done 2026-04-13; audit remaining specs) · Cross-cutting principle audit · 2026-04-13
- `docs/specs/design-system-baseline.md` §6 · Add "Won card outcome badge (RETAINER/SAAS)" as either a 9th BHS location or swap one of the existing 8 · flagged in sales-pipeline.md §7.3 and realised in SP-3 `won-badge.tsx` · SP-3 · 2026-04-14
- ~~Locked specs with LLM prompt text inline in prose · Extract prompts to `lib/ai/prompts/<name>.ts` stubs (Phase 3.5 step 3b)~~ — **APPLIED 2026-04-13** by Phase 3.5 Batch A step 3b (see Applied section below)

### Cross-cutting

- ~~`docs/content/` folder · Populate from each content mini-session's output (Phase 3.5 step 3a homes them here)~~ — **APPLIED 2026-04-13** by Phase 3.5 Batch A step 3a (see Applied section below)

### Phase 3.5 Step 11 walkthrough — friction resolutions

- ~~`docs/specs/intro-funnel.md` · Portal-guard recovery flow~~ — **APPLIED 2026-04-13** inline by Phase 3.5 Step 11 Stage 4 closure (new §10.1 block covers guard behaviour, recovery form, email-matched / unmatched paths).
- ~~`docs/specs/intro-funnel.md` §17 notifications + §11 payment / §12 calendar / §13 post-shoot / §15 deliverables · every journey-beat email embeds a fresh magic link~~ — **APPLIED 2026-04-13** inline by Stage 4 closure (consolidated in §10.1 with explicit send-point list; `sendEmail()` wrapper enforces OTT precondition at build time).
- ~~`docs/specs/client-management.md` §10 · Reference `portal-guard` primitive~~ — **APPLIED 2026-04-13** inline by Stage 4 closure (§10.1 Identity and access now names the primitive, settings keys, and retainer-mode journey-email OTT embedding).
- `FOUNDATIONS.md` §11 · Add `portal-guard` as a cross-cutting shared primitive — session-cookie check with magic-link-recovery fallback form. Contract specified in `docs/specs/intro-funnel.md` §10.1 (authoritative). Consumers: Intro Funnel portal, Client Management portal. · F1.a resolution · 2026-04-13 · **gate: Phase 4 foundation session**
- `FOUNDATIONS.md` §11.2 `sendEmail()` classification enum · Add `portal_magic_link_recovery` (transactional — fresh magic link sent from recovery form when prospect's cookie has expired) · F1.a resolution, Stage 4 closure · 2026-04-13 · **gate: Phase 5 Session A (foundation seed)**
- `docs/specs/intro-funnel.md` §4.1 data model · Document new `portal_magic_links` table (columns spec'd in §10.1). Phase 4 foundation generalises it so Client Management `/portal/[token]/*` reuses the table with nullable `submission_id` + new `client_id` column. · F1.a resolution · 2026-04-13 · **gate: Phase 4 foundation session**
- ~~`FOUNDATIONS.md` §11.7 · `ensureStripeCustomer(contactId)` primitive + `contacts.stripe_customer_id` column + business-lifecycle terminology note~~ · F1.c resolution · 2026-04-13 · **APPLIED 2026-04-13** inline (FOUNDATIONS §11.7; `contacts` schema updated in sales-pipeline.md §4.1).
- ~~`docs/specs/sales-pipeline.md` §10.4 · `createDealFromLead()` contact dedupe rule (email + phone fallback, non-destructive merge, reuse Company)~~ · F1.c + Andy's lead-lifecycle lock · 2026-04-13 · **APPLIED 2026-04-13** inline.
- ~~`docs/specs/intro-funnel.md` §3 step 2 · Contact dedupe call-out + business-lifecycle note (Lead/Prospect through trial shoot; only retainer promotes to Client)~~ · F1.c + Andy's lead-lifecycle lock · 2026-04-13 · **APPLIED 2026-04-13** inline.
- ~~`docs/specs/intro-funnel.md` §11.2 · `ensureStripeCustomer()` call before PaymentIntent creation + `customer` field on PaymentIntent~~ · F1.c resolution · 2026-04-13 · **APPLIED 2026-04-13** inline.
- `docs/specs/quote-builder.md` §7 (Stripe integration) · Add `ensureStripeCustomer(deal.primary_contact_id)` call before Checkout session creation + `customer` field on the session. · F1.c resolution · 2026-04-13 · **gate: apply during Phase 3.5 Step 11 Stage 4 (retainer conversion) walkthrough, or Phase 5 Quote Builder build session — whichever fires first**
- `docs/specs/branded-invoicing.md` · Add `ensureStripeCustomer()` precondition to every Stripe-touching path (draft-invoice create, send, chain-stop). · F1.c resolution · 2026-04-13 · **gate: BI-2 build session** — narrowed 2026-04-15: BI-1a/b ship zero Stripe-touching paths (manual-billed pipeline only); all Stripe Payment Element mounting + `payment_intent.succeeded` handling + customer attachment are BI-2 scope.
- `docs/specs/saas-subscription-billing.md` · Add `ensureStripeCustomer()` precondition to Subscription create + one-off Checkout paths. · F1.c resolution · 2026-04-13 · **gate: Phase 5 SaaS Subscription Billing build session**
- `FOUNDATIONS.md` §11.7 + ESLint rule in foundation session · Block direct imports of `stripe.customers.create` from feature code — mirrors the `@anthropic-ai/sdk` no-direct-import rule in §11.6. · F1.c resolution · 2026-04-13 · **gate: Phase 4 foundation session**
- ~~`FOUNDATIONS.md` Phase 3.5 step 8 glossary pass · Canonical term separation: `stripe.Customer` (Stripe payment identity) ≠ **Client** (retainer-holding contact) ≠ **customer** (banned as a standalone term in specs + code). Add to the glossary when step 8 runs.~~ · F1.c resolution · 2026-04-13 · **APPLIED 2026-04-13** by Phase 3.5 Batch C step 8 — FOUNDATIONS §13 Glossary added; 22 "SaaS customer" / "retainer client" literals replaced across 6 specs (onboarding-and-segmentation, saas-subscription-billing, surprise-and-delight, brand-dna-assessment, design-system-baseline, unified-inbox).
- ~~`docs/specs/sales-pipeline.md` §4.1 + §4.1A · `companies.shape` canonical column + activity_log additions (`company_shape_updated`, `shape_mismatch_flagged`) + admin surface + staleness cascade~~ · F1.b resolution · 2026-04-13 · **APPLIED 2026-04-13** inline.
- ~~`docs/specs/intro-funnel.md` §3 step 2 + §4.1 · Canonical shape write rule (update `companies.shape` if null, log `shape_mismatch_flagged` on disagreement) + `intro_funnel_submissions.shape` reframed as historical snapshot~~ · F1.b resolution · 2026-04-13 · **APPLIED 2026-04-13** inline.
- ~~`docs/specs/brand-dna-assessment.md` §8.1 · `shape` reframed as historical snapshot + `needs_regeneration` column + mismatch flagging on profile completion~~ · F1.b resolution · 2026-04-13 · **APPLIED 2026-04-13** inline.
- `docs/specs/six-week-plan-generator.md` §4 (generator pipeline) · Read `companies.shape` as canonical shape input (fall back to `intro_funnel_submissions.shape` if canonical null). Add staleness hook: on `company_shape_updated`, if plan is pre-conversion + not yet approved, mark `needs_regeneration = true` on the plan (column addition owed). · F1.b resolution · 2026-04-13 · **gate: Phase 5 Six-Week Plan build session**
- `docs/specs/client-context-engine.md` §11 / §14.5 (active_strategy lifecycle) · Add `company_shape_updated` as a trigger for `active_strategy` staleness. · F1.b resolution · 2026-04-13 · **gate: Phase 5 Client Context Engine build session**
- `docs/specs/daily-cockpit.md` `getHealthBanners()` or attention-rail · Add a quiet surface for `shape_mismatch_flagged` so Andy sees "prospect answered a different shape than we had on file — reconcile" as a reviewable item. · F1.b resolution · 2026-04-13 · **gate: Phase 5 Daily Cockpit build session**
- `docs/specs/onboarding-and-segmentation.md` · Onboarding personalisation reads `companies.shape` as canonical source. · F1.b resolution · 2026-04-13 · **gate: Phase 5 Onboarding build session**
- `docs/specs/intro-funnel.md` §13.4 (retainer-fit recommendation) · Input context bundle uses `companies.shape` as canonical; `intro_funnel_retainer_fit` gains `needs_regeneration` flag triggered by `company_shape_updated`. · F1.b resolution · 2026-04-13 · **gate: Phase 5 Intro Funnel build session (or Phase 3.5 Batch C if touched sooner)**
- `docs/specs/setup-wizards.md` Phase 4 sequencing · Note: Intro Funnel build session depends on `setup-wizards` `WizardDefinition` primitive (§5.3). Build order must place Setup Wizards foundation before Intro Funnel. · F1.d resolution (Phase 4 sequencing note, no product decision) · 2026-04-13 · **gate: Phase 4 Build Plan**

### Phase 3.5 Step 11 Stage 2 — F2.d (Safety-valve × retainer-fit)

- ~~`docs/specs/intro-funnel.md` §13.4 · Retainer-fit fires even when safety valve triggered. Prompt receives `safety_valve_triggered` flag + safety-valve free-text; output biased to honour negative signal (`'neither'` typical, flags include `safety_valve_triggered`). Hard lock: retainer-fit is internal-only (Andy cockpit + Pipeline panel only) — never reaches prospect via email/SMS/portal/PDF/quote-copy. Build-time discipline: `lib/intro-funnel/retainer-fit.ts` accessor with `// internal-only` JSDoc + ESLint-able marker. Cockpit ordering inherits existing urgent-above-quiet behaviour when both `post_trial_negative_feedback` urgent + `retainer_fit_recommendation_ready` quiet fire on same Deal.~~ · F2.d resolution · 2026-04-13 · **APPLIED 2026-04-13** inline.
- ~~`docs/specs/intro-funnel.md` §0/§18 · Removed mis-categorisation of retainer-fit as "customer-facing" output in §11.5 references. Retainer-fit still passes through drift check as a belt-and-braces voice-consistency safeguard on Andy's surfaces.~~ · F2.d resolution · 2026-04-13 · **APPLIED 2026-04-13** inline.
- ~~`docs/specs/intro-funnel.md` §18 · Removed stub-path narration (was "stubbed baseline that carries SuperBad's brand voice + business context from skills"); now reads "no stub path exists; the gate guarantees the perpetual profile is real before any consumer runs" per F2.b lock.~~ · F2.b/F2.d cross-reference cleanup · 2026-04-13 · **APPLIED 2026-04-13** inline.
- `FOUNDATIONS.md` §11 (build-time disciplines) · Add discipline #N: "internal-only marker on Claude-generated artefacts that must not reach a customer surface — annotated with `// internal-only` JSDoc on the accessor function; lint rule (or comments-as-discipline) blocks consumption from a customer-targeted route/comm path." Foundation session implements (or marks as comments-only if a custom rule is overkill). Initial consumer: `intro_funnel_retainer_fit`. · F2.d resolution · 2026-04-13 · **gate: Phase 4 foundation session**
- `docs/specs/intro-funnel.md` Pipeline Trial Shoot panel UI (cross-spec note to sales-pipeline.md) · Add a "Regenerate retainer-fit" admin action on the Trial Shoot panel — useful when initial generation needs a re-run (e.g. after Andy adds context). One-click from the Deal profile. · F2.d follow-up · 2026-04-13 · **gate: Phase 5 Sales Pipeline / Intro Funnel build session**
- `docs/specs/intro-funnel.md` `lib/intro-funnel/prompts/retainer-fit.ts` content authoring · Prompt must explicitly handle the `safety_valve_triggered = true` branch with appropriate framing (likely `'neither'` / low-confidence; reasoning_text names safety valve as primary input; does not pitch around it). Belongs in Intro Funnel content mini-session. · F2.d resolution · 2026-04-13 · **gate: Intro Funnel content mini-session**

### Phase 3.5 Step 11 Stage 2 — F2.c (Pixieset API spike)

- ~~`docs/specs/intro-funnel.md` §15.3 · Move Pixieset API capability spike from Phase 5 mid-build into Phase 4 prep; document two outcomes (sufficient → inline gallery; insufficient → on-brand link-out fallback in design system + house-spring motion); flag mop-up-brainstorm contingency for Pixieset alternatives evaluation.~~ · F2.c resolution · 2026-04-13 · **APPLIED 2026-04-13** inline.
- `BUILD_PLAN.md` (Phase 4 prep) · 1-session spike: confirm Pixieset API capability (private gallery access, image-URL fetching, auth model, rate limits). Outcome routes the deliverables-reveal build path. Must complete before BUILD_PLAN.md is finalised. · F2.c resolution · 2026-04-13 · **gate: Phase 4 prep**
- ~~(contingent) Phase 4 mop-up brainstorm · If Pixieset spike returns "API insufficient", evaluate Pixieset alternatives (Pic-Time, Cloudspot, ShootProof) before accepting on-brand link-out as v1.0 final. Mop-up only fires if spike fails — not auto-spawned.~~ · F2.c resolution · 2026-04-13 · **DECLINED 2026-04-13** by P0 handoff — all three alternatives share Pixieset's closed-ecosystem posture (no public API, no webhook, no iframe). Switching would not unlock Path A. Revisit only on real v1.0 customer feedback.
- `docs/specs/intro-funnel.md` §15.1 data model · Review whether `deals.pixieset_gallery_id` and `intro_funnel_submissions.pixieset_gallery_id` are still load-bearing under Path B (likely dead — they were Path A artefacts for API calls; URL alone is sufficient). Decide: drop or keep as optional display metadata. · P0 Pixieset spike handoff · 2026-04-13 · **gate: Phase 5 Wave 14 IF-2 build session**
- `docs/specs/setup-wizards.md` SW-5 integration wizards list · Confirm no Pixieset credential / API-key wizard step is specced — Andy doesn't need to authorise anything Pixieset-side under Path B. · P0 Pixieset spike handoff · 2026-04-13 · **gate: Phase 5 Wave 7 SW-5 build session**
- `docs/specs/intro-funnel.md` §15.2 content · Drift-checked (§11.5) brand-voice body copy for the "Your gallery is ready" launch card — new surface not covered by existing deliverables-ready announcement email content. · P0 Pixieset spike handoff · 2026-04-13 · **gate: Phase 5 Wave 14 IF-2 build session**

### Phase 3.5 Step 11 Stage 2 — F2.b (First-Login Brand DNA Gate)

- ~~`docs/specs/intro-funnel.md` §13.3 + §13.4 · Inputs lists rewritten — synthesis + retainer-fit prompts read SuperBad's perpetual Brand DNA profile unconditionally; "stubbed before then" wording removed; references First-Login Brand DNA Gate as the guarantee.~~ · F2.b resolution · 2026-04-13 · **APPLIED 2026-04-13** inline.
- ~~`docs/specs/brand-dna-assessment.md` §11 · Added §11.1 First-Login Brand DNA Gate — middleware behaviour, why-hard-gate rationale, no-stub-anywhere lock, env-var bypass safety net, Phase 4 build-order constraint, onboarding voice direction.~~ · F2.b resolution · 2026-04-13 · **APPLIED 2026-04-13** inline.
- ~~`FOUNDATIONS.md` §11.8 · Added First-Login Brand DNA Gate primitive — owner is Brand DNA Assessment §11.1; foundation session owns middleware + env-var bypass implementation.~~ · F2.b resolution · 2026-04-13 · **APPLIED 2026-04-13** inline.
- `BUILD_PLAN.md` (Phase 4) · Hard ordering constraint: Brand DNA Assessment SuperBad-self slice + gate middleware build before Intro Funnel synthesis (§13.3 + §13.4), Lead Gen draft generation, Outreach reply intelligence, brand-voice drift checks (FOUNDATIONS §11.5), Cockpit briefs referencing perpetual voice. Full client-facing Brand DNA Assessment surface can ship later. · F2.b resolution · 2026-04-13 · **gate: Phase 4 Build Plan**
- `INCIDENT_PLAYBOOK.md` (Phase 6) · Document `BRAND_DNA_GATE_BYPASS=true` env var as the recovery path if the First-Login Brand DNA Gate misfires and locks Andy out. · F2.b resolution · 2026-04-13 · **gate: Phase 6 launch (INCIDENT_PLAYBOOK.md is created in Phase 6)**
- `docs/specs/intro-funnel.md` §24 (content mini-session) · Drop the "Brand DNA stub markdown content" line if it ended up in the §24 list (it didn't — F2.b was resolved before §24 was extended for it). Confirmed: no patch needed. · F2.b resolution · 2026-04-13 · **n/a — no action required.**
- `docs/specs/brand-dna-assessment.md` (new content task) · Onboarding-route copy ("Lite needs to know who you are…"), single-paragraph framing, motion-treated reveal direction. Belongs in the Brand DNA Assessment content mini-session. · F2.b resolution · 2026-04-13 · **gate: Brand DNA content mini-session**

### Phase 3.5 Step 11 Stage 2 — F2.e (settings-registry: reflection delay)

- ~~`docs/settings-registry.md` · Add `intro_funnel.reflection_delay_hours_after_deliverables` (default 24, integer). Add new "Intro Funnel" section + note that broader Intro Funnel registry sweep is owed.~~ · F2.e resolution · 2026-04-13 · **APPLIED 2026-04-13** inline.
- ~~`docs/specs/intro-funnel.md` §4.1 (`intro_funnel_config`) · Mark `reflection_delay_hours_after_deliverables` column as deprecated; Phase 5 Intro Funnel build session drops it from the migration and consumes from `settings` instead.~~ · F2.e resolution · 2026-04-13 · **APPLIED 2026-04-13** inline.
- `docs/specs/intro-funnel.md` Batch C step 15 sweep · Register remaining Intro Funnel autonomy thresholds in `docs/settings-registry.md`: abandon cadence (15 min / 24 h / 3 d), advance notice (5 business days), per-week cap (3), reschedule limit (2), refund window (48 h), SMS quiet hours (8 a.m.–9 p.m. local), email quiet hours (7 a.m.–10 p.m. local), shoot duration (60 min). Convert spec literals + helper code to `settings.get()`. · F2.e (broader sweep owed) · 2026-04-13 · **gate: Phase 3.5 Batch C step 15 (literal grep)**

### Phase 3.5 Step 11 Stage 2 — F2.a (bundled deliverables release)

- ~~`docs/specs/intro-funnel.md` §15.1 · Bundled `deliverables_ready` rule: gate fires only when both gallery URL pasted AND Six-Week Plan approved; whichever is second triggers the unified state + single bundled announcement email; idempotent on either handler. Added cockpit `intro_funnel_awaiting_bundle` quiet entry showing the waiting side.~~ · F2.a resolution · 2026-04-13 · **APPLIED 2026-04-13** inline.
- ~~`docs/specs/intro-funnel.md` §13.1 · Reflection clock starts from the bundled `deliverables_ready` transition; settings key reference confirmed (`intro_funnel.reflection_delay_hours_after_deliverables`).~~ · F2.a resolution · 2026-04-13 · **APPLIED 2026-04-13** inline.
- ~~`docs/specs/intro-funnel.md` §3 step 8 · Journey narration rewritten as bundled reveal (gallery + plan); references upfront timeframe signposting.~~ · F2.a resolution · 2026-04-13 · **APPLIED 2026-04-13** inline.
- ~~`docs/specs/intro-funnel.md` §5.1 · State-machine annotation: `deliverables_ready` triggered by bundle gate (both pre-conditions), not by Pixieset URL paste alone.~~ · F2.a resolution · 2026-04-13 · **APPLIED 2026-04-13** inline.
- ~~`docs/specs/intro-funnel.md` §4.1 · Added `gallery_ready_at`, `plan_ready_at`, `deliverables_ready_at` columns on `intro_funnel_submissions`.~~ · F2.a resolution · 2026-04-13 · **APPLIED 2026-04-13** inline.
- ~~`docs/specs/intro-funnel.md` §17.2 · Notifications: added `intro_funnel_awaiting_bundle`, `six_week_plan_viewed`, bundled `deliverables_ready` quiet entries.~~ · F2.a resolution · 2026-04-13 · **APPLIED 2026-04-13** inline.
- ~~`docs/specs/intro-funnel.md` §24 · Content mini-session must produce upfront timeframe signposting copy + bundled announcement email body (replacing photos-only wording).~~ · F2.a resolution · 2026-04-13 · **APPLIED 2026-04-13** inline.
- ~~`docs/specs/six-week-plan-generator.md` §2.6 · Plan-released wording rewritten as bundled gate; this spec stops emitting its own release email; cockpit `intro_funnel_awaiting_bundle` referenced for the plan-ready-but-no-gallery-yet state.~~ · F2.a resolution · 2026-04-13 · **APPLIED 2026-04-13** inline.
- `FOUNDATIONS.md` §11.2 `sendEmail()` classification enum · Add `deliverables_ready_announcement` (single bundled gallery+plan email per Intro Funnel §15.1). · F2.a resolution · 2026-04-13 · **gate: Phase 5 Intro Funnel build session (or Phase 4 foundation session if added to the §11.2 enum patch list)**
- `docs/specs/six-week-plan-generator.md` §10.5 · `sendEmail()` classifications: confirm bundled-release path no longer needs a `six_week_plan_release` value (or mark as unused/superseded). · F2.a resolution · 2026-04-13 · **gate: Phase 5 Six-Week Plan build session**
- `docs/specs/sales-pipeline.md` activity_log.kind enum · Add `gallery_attached`, `intro_funnel_awaiting_bundle`, `six_week_plan_viewed` (or confirm existing values cover them). · F2.a resolution · 2026-04-13 · **gate: Phase 5 Sales Pipeline build session (or rolled into Intro Funnel build session)**
- `docs/specs/daily-cockpit.md` `getWaitingItems()` source-spec stubs · Acknowledge `intro_funnel_awaiting_bundle { waiting_on: 'gallery' | 'plan' }` as a quiet feed entry from Intro Funnel; refreshes (clear + re-emit) on each side completing. · F2.a resolution · 2026-04-13 · **gate: Phase 5 Daily Cockpit build session**

### Phase 3.5 Step 11 Stage 3 follow-up — Admin first-login sequencing lock (Brand DNA → Setup Wizards critical flight)

Andy's verification question after Stage 3 surfaced a handoff gap: each gate was individually specced but the bridge beat ("after Brand DNA completes, Setup Wizards critical flight auto-triggers as one continuous arc") was implicit. Locked now.

- ~~`docs/specs/brand-dna-assessment.md` §11.1 · Added "Post-completion handoff into Setup Wizards critical flight" block: reveal settles → direct transition into `stripe-admin` → Resend → Graph API → capstone → cockpit, no cockpit detour between gates. `hasCompletedCriticalFlight(user)` detection runs in same middleware layer as the Brand DNA gate; if Brand DNA clears but critical flight incomplete, admin routes 302 to `/lite/setup/critical-flight/[nextWizardKey]`. Middleware check self-terminates per user once all three critical-flight `wizard_completions` rows exist.~~ · Admin first-login sequencing lock · 2026-04-13 · **APPLIED 2026-04-13** inline.
- ~~`docs/specs/setup-wizards.md` §8.1 · Added mirror sequencing lock: critical-flight middleware check runs after Brand DNA gate clears; lazy-surfacing (§8.4) only begins after flight completes.~~ · Admin first-login sequencing lock · 2026-04-13 · **APPLIED 2026-04-13** inline.
- `FOUNDATIONS.md` §11.8 (First-Login Brand DNA Gate primitive) · Extend primitive definition to include the critical-flight middleware layering — same middleware stack, Brand DNA gate runs first, critical-flight check runs second, both self-terminate per user on completion. · Admin first-login sequencing lock · 2026-04-13 · **gate: Phase 4 foundation session**
- `INCIDENT_PLAYBOOK.md` (Phase 6) · Document the critical-flight bypass path alongside `BRAND_DNA_GATE_BYPASS=true` — if a bug in the critical-flight middleware ever locks Andy out mid-flight, a similar env var or manual `wizard_completions` row insert is the recovery path. · Admin first-login sequencing lock · 2026-04-13 · **gate: Phase 6 launch**

### Phase 3.5 Step 11 Stage 3 — F3.f (Daily Cockpit contract verification)

- ~~`docs/specs/six-week-plan-generator.md` §12.2 · Fixed count typo: "3 new source kinds" → "4 new source kinds" (list already had 4; only the intro line was wrong).~~ · F3.f resolution · 2026-04-13 · **APPLIED 2026-04-13** inline.
- ~~`docs/specs/daily-cockpit.md` §363 (`getWaitingItems()` spec roster) · Added Six-Week Plan Generator to the spec list with its 4 emitted kinds named inline.~~ · F3.f resolution · 2026-04-13 · **APPLIED 2026-04-13** inline.
- ~~`docs/specs/daily-cockpit.md` §398 (Health banner source contracts) · Added Six-Week Plan Generator to the banner-contributing spec roster with its emitted `six_week_plan_retainer_payment_without_refresh_review` kind named inline.~~ · F3.f resolution · 2026-04-13 · **APPLIED 2026-04-13** inline.

### Phase 3.5 Step 11 Stage 3 — F3.e (Plan view during retainer migration window)

- ~~`docs/specs/six-week-plan-generator.md` §6 intro · Added "Single surface, evolving source" lock block — `/portal/[token]/plan` is the one plan surface across lifecycle; read-source evolves (`six_week_plans` pre-retainer → Client Context `active_strategy` on Won); retainer mode does not add a second surface; label stays "Your plan" through pending-refresh-review, flips to "Your strategy" on live.~~ · F3.e resolution · 2026-04-13 · **APPLIED 2026-04-13** inline.
- ~~`docs/specs/six-week-plan-generator.md` §8.1 · Added step 5: portal read-source swap on Deal → Won; surface reads migrated Client Context `active_strategy`; pending band renders above intro; "Start Week 1" suppressed; activation driven by first retainer payment (§8.3), not prospect click.~~ · F3.e resolution · 2026-04-13 · **APPLIED 2026-04-13** inline.
- ~~`docs/specs/six-week-plan-generator.md` §8.3 · Week 1 trigger also clears pending-band, activates tracker mode, and flips navigation label "Your plan" → "Your strategy". If `pending_refresh_review` still true, band remains until Andy completes refresh-review.~~ · F3.e resolution · 2026-04-13 · **APPLIED 2026-04-13** inline.
- ~~`docs/specs/six-week-plan-generator.md` §17 (content mini-session) · Added pending-refresh-review band copy item with placeholder seed.~~ · F3.e resolution · 2026-04-13 · **APPLIED 2026-04-13** inline.
- `docs/specs/client-management.md` §10 retainer mode · Add lock: retainer mode does NOT render its own plan/strategy surface; consumes `/portal/[token]/plan` owned by `docs/specs/six-week-plan-generator.md` §6 (which is now canonical across both pre-retainer and retainer modes). Navigation label swap ("Your plan" → "Your strategy") on `active_strategy.status = live` AND first retainer payment landed — both conditions required; either alone keeps "Your plan". · F3.e resolution · 2026-04-13 · **gate: Phase 5 Client Management build session**
- `docs/specs/client-context-engine.md` · Confirm `active_strategy` artefact carries a `status` enum (`pending_refresh_review` | `live`) and that read-paths from the portal plan surface can filter on it. Already listed as a patch owed (see §14 of Six-Week Plan Generator spec); F3.e tightens the spec — the `status` field is load-bearing for portal UX, not just Andy's cockpit surfacing. · F3.e resolution · 2026-04-13 · **gate: Client Context Engine build session / Phase 5**
- `docs/specs/daily-cockpit.md` `getWaitingItems()` source-spec stubs · Existing `six_week_plan_refresh_review_requested { clientId, planId }` waiting-item (per §8.1) is unchanged by F3.e; confirm on consuming side. The portal pending-band is a prospect-side surface, not a cockpit surface — no new cockpit entry needed. · F3.e confirmation · 2026-04-13 · **gate: Phase 5 Daily Cockpit build session**

### Phase 3.5 Step 11 Stage 3 — F3.d (Day-60 wind-down: two-beat expiry with soft CTA)

- ~~`docs/specs/six-week-plan-generator.md` §8.4 · Rewrote day-60 lights-out as two scheduled beats: day-53 wind-down expiry email (PDF attached, soft CTA mailto with prefilled subject `"Coming back about my plan — {business_name}"`, proportional earned tone — no pitch, no form) and day-60 archive-only job. Warm sign-off framing; closing beat of the 60-day arc.~~ · F3.d resolution · 2026-04-13 · **APPLIED 2026-04-13** inline.
- ~~`docs/specs/six-week-plan-generator.md` §9 · Added `plan.expiry_email_days_before_archive` settings key (default 7 → email lands day 53).~~ · F3.d resolution · 2026-04-13 · **APPLIED 2026-04-13** inline.
- ~~`docs/specs/six-week-plan-generator.md` §10.1 · `six_week_plans` table gains `portal_expiry_email_sent_at` column.~~ · F3.d resolution · 2026-04-13 · **APPLIED 2026-04-13** inline.
- ~~`docs/specs/six-week-plan-generator.md` §10.3 · Added `six_week_plan_expiry_email_sent` activity_log entry.~~ · F3.d resolution · 2026-04-13 · **APPLIED 2026-04-13** inline.
- ~~`docs/specs/six-week-plan-generator.md` §10.4 · Split scheduled_tasks: added `six_week_plan_expiry_email` (day-53 sender); reduced `six_week_plan_non_converter_expiry` scope to day-60 archive-only.~~ · F3.d resolution · 2026-04-13 · **APPLIED 2026-04-13** inline.
- ~~`docs/specs/six-week-plan-generator.md` §17 (content mini-session) · Added expiry email body direction (warm sign-off + soft mailto CTA) and archived-portal microcopy items.~~ · F3.d resolution · 2026-04-13 · **APPLIED 2026-04-13** inline.
- ~~`docs/settings-registry.md` Plan section · Added `plan.expiry_email_days_before_archive` (default 7); Plan total bumped 8 → 9; overall total bumped 56 → 57.~~ · F3.d resolution · 2026-04-13 · **APPLIED 2026-04-13** inline.
- `FOUNDATIONS.md` §11.2 `sendEmail()` classification enum · Add `six_week_plan_expiry_email` (day-53 wind-down, soft CTA mailto). Confirm `six_week_plan_non_converter_expiry` remains or is retired — the archive beat has no outbound send, so the existing classification (if it existed for release comms) should be removed or repurposed. · F3.d resolution · 2026-04-13 · **gate: Phase 5 Six-Week Plan build session (or Phase 4 foundation session if rolled into the §11.2 enum patch list)**
- `docs/specs/daily-cockpit.md` pipeline panel · Andy's manual "extend portal" action (per §11 settings, `plan.extend_portal_days_on_manual_override`) must be surfaceable when the day-53 expiry email fires — confirm the pipeline panel shows a quiet "wind-down sent" signal on the prospect deal card so Andy has a one-click extend before day 60. Low priority; optional. · F3.d follow-up · 2026-04-13 · **gate: Phase 5 Daily Cockpit build session — optional, confirm or drop**
- `docs/specs/client-management.md` §10 archived-portal state · Confirm the day-60 archive UX — portal shows read-only plan page + re-download PDF + mailto door back — is already covered by the existing archived-mode spec. If not, add a one-liner that archived portals for non-converters must still expose the plan PDF re-download and the soft mailto. · F3.d follow-up · 2026-04-13 · **gate: Phase 5 Client Management build session**

### Phase 3.5 Step 11 Stage 3 — F3.c (Revision-resolution prospect-side UX)

- ~~`docs/specs/six-week-plan-generator.md` §7.2 · Clarified Andy's three revision-review actions with review-before-send lock on all paths; explain + hand-reject share a single review screen (Haiku draft vs empty textarea), single "Send reply" action.~~ · F3.c resolution · 2026-04-13 · **APPLIED 2026-04-13** inline.
- ~~`docs/specs/six-week-plan-generator.md` §7.3 · New section defining prospect-side notification: email-first + quiet portal echo. Regenerate fires `six_week_plan_revision_regenerated` email on new-plan approval + inline card. Explain/hand-reject fires `six_week_plan_revision_explained` email (Andy's reply IS the body) + inline card that expands to show reply text. Bartender reads revision resolution as chat context.~~ · F3.c resolution · 2026-04-13 · **APPLIED 2026-04-13** inline.
- ~~`docs/specs/six-week-plan-generator.md` §7.4 / §7.5 (renumbered) · One-revision enforcement retained; activity log entries clarified (both explain + hand-reject fire `six_week_plan_revision_explained` with `source` payload).~~ · F3.c resolution · 2026-04-13 · **APPLIED 2026-04-13** inline.
- ~~`docs/specs/six-week-plan-generator.md` §10.1 · `six_week_plans` table gains `revision_reply_sent_at`, `revision_reply_body`, `revision_reply_dismissed_at` columns.~~ · F3.c resolution · 2026-04-13 · **APPLIED 2026-04-13** inline.
- ~~`docs/specs/six-week-plan-generator.md` §10.5 · `sendEmail()` classifications replaced: `six_week_plan_revision_resolved` renamed/split into `six_week_plan_revision_regenerated` + `six_week_plan_revision_explained` (1:1 with the two revision paths).~~ · F3.c resolution · 2026-04-13 · **APPLIED 2026-04-13** inline.
- ~~`docs/specs/six-week-plan-generator.md` §6.1 · Added revision reply inline card UI state (two variants: regenerate, explain/hand-reject) above the intro block. Quiet inline card, Tier-1 house spring entrance, dismiss persists per prospect.~~ · F3.c resolution · 2026-04-13 · **APPLIED 2026-04-13** inline.
- ~~`docs/specs/six-week-plan-generator.md` §17 (content mini-session) · Added revision reply inline card copy (both variants + dismiss microcopy) and revision-resolution email bodies (`six_week_plan_revision_regenerated` full, `six_week_plan_revision_explained` envelope frame around Andy's reply).~~ · F3.c resolution · 2026-04-13 · **APPLIED 2026-04-13** inline.
- `FOUNDATIONS.md` §11.2 `sendEmail()` classification enum · Replace `six_week_plan_revision_resolved` with the two new values `six_week_plan_revision_regenerated` + `six_week_plan_revision_explained`. · F3.c resolution · 2026-04-13 · **gate: Phase 5 Six-Week Plan build session (or Phase 4 foundation session if rolled into the §11.2 enum patch list)**
- `docs/specs/client-management.md` §10.3 (bartender safe actions) · Add "surface the revision reply" / "point the prospect to the plan page when asked about the reply" capability. Reads `six_week_plans.revision_reply_body` + `revision_reply_sent_at` as chat context. Extends the existing "navigate to a specific invoice or quote" pattern. · F3.c resolution · 2026-04-13 · **gate: Phase 5 Client Management build session**
- `docs/specs/daily-cockpit.md` `getWaitingItems()` source-spec stubs · Acknowledge `six_week_plan_revision_request { planId, prospect_name, note_preview }` as a waiting item from Six-Week Plan Generator §7.2 (already listed in §12.2, but the consuming cockpit spec must have it in its `emits:` block post-Phase 3.5 step 7). · F3.c follow-up · 2026-04-13 · **gate: Phase 5 Daily Cockpit build session**
- `lib/ai/prompts/six-week-plan-revision-reply.ts` content authoring · Prompt for the Haiku-drafted explain-reply. Must read the plan + prospect's revision note + Brand DNA + Client Context. Voice: Andy's register (direct, personal — NOT bartender). Never defensive. Acknowledge the note, explain the reasoning, offer the retainer conversation as the place for deeper discussion. Belongs in Six-Week Plan Generator content mini-session. · F3.c resolution · 2026-04-13 · **gate: Six-Week Plan Generator content mini-session**

### Phase 3.5 Step 11 Stage 3 — F3.b (PDF as brand-forward take-away artefact)

- ~~`docs/specs/six-week-plan-generator.md` §6.5 · Rewrote PDF takeaway with brand-forward stance: filename `SuperBad-Six-Week-Plan-[business-slug]-[YYYY-MM-DD].pdf`, cover page (SuperBad mark + business name + plan date + subtitle), intermediate-page minimal footer (logo + page number), closing sign-off spread (sprinkle line in larger type + logo beneath), branded render overlay on first download, supersede-prompt modal when prospect holds a lower `generation_version`, day-60 email attachment always fresh-rendered.~~ · F3.b resolution · 2026-04-13 · **APPLIED 2026-04-13** inline.
- ~~`docs/specs/six-week-plan-generator.md` §13.3 · Sprinkle-claim placement clarified: closing sign-off page only, larger typography, not repeated in per-page footers; rationale (preserves single-beat weight) + cross-reference to F3.b.~~ · F3.b resolution · 2026-04-13 · **APPLIED 2026-04-13** inline.
- ~~`docs/specs/six-week-plan-generator.md` §10.3 · `six_week_plan_pdf_downloaded` activity_log entry carries `{ plan_id, generation_version }` payload so the portal can detect stale-PDF state.~~ · F3.b resolution · 2026-04-13 · **APPLIED 2026-04-13** inline.
- ~~`docs/specs/six-week-plan-generator.md` §17 (content mini-session) · Added PDF layout direction item — cover page composition, intermediate footer spec, closing sign-off spread, render overlay visual direction, sprinkle wording.~~ · F3.b resolution · 2026-04-13 · **APPLIED 2026-04-13** inline.
- `docs/specs/surprise-and-delight.md` sprinkle bank · Update the Six-Week Plan Generator sprinkle claim row to reflect "closing sign-off page, larger typography, single occurrence" rather than "footer once". · F3.b resolution · 2026-04-13 · **gate: next S&D spec touch / Phase 3.5 cross-spec cleanup**
- `docs/specs/design-system-baseline.md` revisit queue · Add `motion:pdf_render_overlay` (or lightweight token: "branded full-screen progress overlay with auto-dismiss on completion") as a Tier-1 motion token so the same pattern can be reused across other synchronous renders (future PDF exports, branded report generation, etc.). Optional — can be absorbed as a generic token during design-system-baseline revisit. · F3.b resolution · 2026-04-13 · **gate: design-system-baseline revisit session**
- `docs/specs/client-management.md` §10.3 (bartender safe actions) · The bartender may be asked "can I get the PDF again?" — confirm the chat can surface the portal plan page's download link (or inline the download trigger) without bypassing the supersede-prompt. No new capability; existing "navigate to a specific invoice or quote" pattern extends cleanly. · F3.b follow-up · 2026-04-13 · **gate: Phase 5 Client Management build session**
- `BUILD_PLAN.md` / Phase 5 Session C preconditions · Puppeteer setup must be in place before Session C lands. Branded Invoicing or Quote Builder's Phase 5 session — whichever ships first — lands the Puppeteer dependency; Six-Week Plan Session C consumes it. Already named in §18 preconditions; F3.b confirms no new dependency emerges. · F3.b confirmation · 2026-04-13 · **gate: Phase 4 Build Plan sequencing**

### Phase 3.5 Step 11 Stage 3 — F3.a (First-visit-after-bundle deliverables hub)

- ~~`docs/specs/client-management.md` §10.2 + §10.2.1 + §10.2.2 · Added one-shot deliverables hub as portal home override for first visit after bundled `deliverables_ready`. Replaces generic 3-step first-visit tour for bundled-release portals. Tiles: gallery + plan, equal weight, Tier-2 `motion:bundle_reveal`. Dismiss-to-tile routes to `/portal/[token]/gallery` or `/portal/[token]/plan` and queues bartender first-visit opening line for next chat-home navigation. Sticky server-side via `intro_funnel_submissions.bundled_hub_seen_at`.~~ · F3.a resolution · 2026-04-13 · **APPLIED 2026-04-13** inline.
- ~~`docs/specs/intro-funnel.md` §4.1 (`intro_funnel_submissions` schema) · Added `bundled_hub_seen_at timestamp (nullable)` column.~~ · F3.a resolution · 2026-04-13 · **APPLIED 2026-04-13** inline.
- ~~`docs/specs/intro-funnel.md` §15.1 · Added hub handoff block (bundled email CTA → `/portal/[token]` → hub render → chat-home handoff) with cross-reference to Client Management §10.2.1.~~ · F3.a resolution · 2026-04-13 · **APPLIED 2026-04-13** inline.
- ~~`docs/specs/intro-funnel.md` §24 (content mini-session) · Added "First-visit-after-bundle deliverables hub copy" item — hub header, tile microcopy, post-hub bartender first-visit opening line.~~ · F3.a resolution · 2026-04-13 · **APPLIED 2026-04-13** inline.
- ~~`docs/specs/six-week-plan-generator.md` §6.1 · Added explanatory block that the plan page is not the first-visit entry state after bundled release; first-visit arrival is Client Management §10.2.1's hub.~~ · F3.a resolution · 2026-04-13 · **APPLIED 2026-04-13** inline.
- ~~`docs/specs/six-week-plan-generator.md` §6.6 + §13.4 (motion candidates) · Rescoped `motion:plan_reveal` from "first portal visit" to "plan section's internal first-open from the hub's plan tile"; added new `motion:bundle_reveal` candidate (owned by Client Management §10.2.1) for the hub arrival.~~ · F3.a resolution · 2026-04-13 · **APPLIED 2026-04-13** inline.
- `docs/specs/client-management.md` §10.4 Prompt 1 (bartender opening line) content authoring · When rendering chat-home immediately after a hub dismiss (detectable via `bundled_hub_seen_at` just-set + no prior chat messages), the opening line must acknowledge both deliverables and offer navigation — never summarise the plan, never explain the gallery. Belongs in the Client Management content mini-session. · F3.a resolution · 2026-04-13 · **gate: Client Management content mini-session**
- `docs/specs/design-system-baseline.md` revisit queue · Add Tier-2 candidate `motion:bundle_reveal` — gallery tile + plan tile surfacing together as a single orchestrated beat on first-visit-after-bundle hub. · F3.a resolution · 2026-04-13 · **gate: design-system-baseline revisit session**
- `docs/specs/daily-cockpit.md` `getWaitingItems()` or attention-rail · Consider a quiet feed entry `intro_funnel_hub_seen { deal_id, prospect_name, picked: 'gallery' | 'plan' }` so Andy gets a light signal that the prospect has opened the bundled deliverables and which tile they chose first (ambient intel, not actionable). Low priority; optional. · F3.a follow-up · 2026-04-13 · **gate: Phase 5 Daily Cockpit build session — optional, confirm or drop**

### Phase 3.5 Batch C — Step 13 legal / compliance sweep (2026-04-13)

Coverage audit across 21 specs + FOUNDATIONS found strong coverage on Spam Act 2003 (Lead Gen §§4/6/12.L, Content Engine §§168/173-175/195, FOUNDATIONS §14), ATO tax invoice compliance (Branded Invoicing §§3/5/§PDF-Q6 — ABN, "Tax Invoice" title, itemised GST, globally unique number), GST canonicalisation (Quote Builder Q6, GST-inclusive stored), SMS consent (Intro Funnel §4 `sms_consent_at` + DNC primitive §51), and OAuth consent (Setup Wizards + Unified Inbox Graph API). Five orphan concerns surfaced with no spec owner — patches owed below.

- `FOUNDATIONS.md` (new §16 or append to §11) + new micro-spec or Content Engine extension · **Static legal pages owner.** No spec owns `/lite/legal/terms`, `/lite/legal/privacy`, `/lite/legal/cookie-policy`, or the `/legal` landing. The Quote Builder PDF §Q14 references a "terms link" with no target; SaaS signup needs ToS + Privacy acceptance surfaces with no page to link to. Recommend: single mini-spec `docs/specs/legal-pages.md` or append §19 to `content-engine.md` (already owns Lite-hosted blog + embeddable form = public marketing-shell surface owner). Authoring is LLM-drafted from template + Andy approve-once (memory: `feedback_no_content_authoring`). Rendering: static MDX pages under `app/lite/legal/*`. · Phase 3.5 Batch C step 13 · 2026-04-13 · **gate: Phase 4 Build Plan (assign owner) → Phase 5 dedicated session**
- `docs/specs/saas-subscription-billing.md` signup schema + flow · **ToS + Privacy acceptance at SaaS signup.** Current spec does not require an explicit acceptance tickbox. Patch: add `tos_accepted_at` + `privacy_accepted_at` columns on `subscribers` (or equivalent); signup form gates CTA on both ticked; ticks stamp timestamps + record version hash of the accepted documents (`legal_doc_versions` reference table). Quote Builder `/lite/quotes/[token]/accept` already has tickbox gating per §11 — mirror that pattern. · Phase 3.5 Batch C step 13 · 2026-04-13 · **gate: Phase 5 SaaS Subscription Billing build session**
- `FOUNDATIONS.md` §5 (SQLite + Litestream → R2 backup) · **Tax record retention policy.** ATO requires a minimum 5 years of transactional record retention. Current §5 covers backup mechanics but not retention duration. Patch: add "R2 backup objects retained ≥ 7 years (exceeds ATO 5-year floor; no aged-out deletes in v1); no delete tooling for `invoices`, `deals`, `subscriptions`, `payments` in v1 — tombstone-only if a deletion flow ever ships." · Phase 3.5 Batch C step 13 · 2026-04-13 · **gate: Phase 4 foundation session**
- `FOUNDATIONS.md` §11 (cross-cutting primitives) · **Credential vault primitive.** Setup Wizards §5.2 + Unified Inbox §graph-api reference "encrypted storage" for OAuth tokens / Stripe secret keys without a named primitive. Patch: add `lib/crypto/vault.ts` with `vault.encrypt(plaintext, context)` / `vault.decrypt(ciphertext, context)` — AES-256-GCM, key from `CREDENTIAL_VAULT_KEY` env var, authenticated additional data = context scope. Single point of encryption for all third-party credentials. · Phase 3.5 Batch C step 13 · 2026-04-13 · **gate: Phase 4 foundation session**
- New spec or Content Engine extension · **Privacy Act 1988 (APP) data-subject-request flow.** No spec owns access/correction/erasure requests from clients, prospects, subscribers, or unsubscribed leads. v1 minimum: email-based handling — legal pages display `privacy@superbadmedia.com.au` + 30-day response commitment. Patch owed lives under the same legal-pages owner decision as L1 above. Self-service request portal is v1.1 (aligned with `feedback_primary_action_focus` — push edge cases to support email). · Phase 3.5 Batch C step 13 · 2026-04-13 · **gate: rolled into legal-pages spec**
- `docs/specs/quote-builder.md` §Q14 PDF shape + §11 web-page acceptance · **Quote "terms link" target.** Currently unspecified. Patch once L1 resolves: link points to `/lite/legal/terms#retainer-and-project-work` anchor. Tickbox text on acceptance reads "I accept the [terms](…) and [privacy policy](…)." · Phase 3.5 Batch C step 13 · 2026-04-13 · **gate: Phase 5 Quote Builder build session (blocked on L1)**

### Phase 3.5 Stop 14 — product-judgement resolutions (2026-04-13)

Four legal/product questions surfaced by Batch C step 13 and answered by Andy in Stop 14. Resolutions below are authoritative — they override any ambiguity in the step 13 rows above.

- **L1 (legal pages owner) RESOLVED: standalone mini-spec.** A new spec `docs/specs/legal-pages.md` owns `/lite/legal/privacy`, `/lite/legal/terms`, `/lite/legal/acceptable-use`, and the `/lite/legal` landing. Content is LLM-drafted from template + Andy approve-once (memory: `feedback_no_content_authoring`), rendered as static MDX under `app/lite/legal/*`, versioned with `effective_date` and a `legal_doc_versions` reference table. Row 199 above now reads as: owner = **new standalone spec**, not Content Engine §19. · Stop 14 Q1 · 2026-04-13 · **gate: new spec drafting owed before Phase 5 legal-pages build session; spec drafting itself is a tiny Phase 3 backfill session (<30 min) or can be folded into Phase 4 foundation session's docs pass — Phase 4 to decide.**
- **L2 (DSR surface at v1.0) RESOLVED: email-only.** No DSR UI at v1.0. Privacy Policy page displays `privacy@superbadmedia.com.au` + 30-day response commitment. Self-service "Request my data" button is explicitly **v1.1**, not v1.0. Row 203 above is rolled into the legal-pages spec. · Stop 14 Q2 · 2026-04-13 · **gate: legal-pages spec (L1).**
- **L3 (cookie consent banner) NEW PATCH: geo-gated GDPR banner for EU IPs + universal footer cookie link.** `docs/specs/legal-pages.md` (L1) owns a third surface: a cookie consent component that (i) renders full GDPR-style banner (reject-all / accept-all / manage categories) only for EU IPs via MaxMind geo-lookup, (ii) renders a permanent "We use cookies — [details]" link in the global site footer for everyone else linking to `/lite/legal/cookie-policy`. Cookie policy is a sub-page of legal-pages. Consent state persisted in localStorage + echoed to a `cookie_consents` audit table for EU traffic. Implementation: evaluate Klaro vs rolled-your-own during Phase 5 legal-pages build session. · Stop 14 Q3 · 2026-04-13 · **gate: Phase 5 legal-pages build session (rolled into L1).**
- **L4 (SaaS signup acceptance pattern) LOCKED: single tickbox, inline links, both timestamps stamped.** Confirms row 200 above. Signup form shows one unticked checkbox: "I accept the [Terms of Service](…) and [Privacy Policy](…)." Submit button disabled until ticked. On submit, stamp **both** `tos_accepted_at` and `privacy_accepted_at` on the subscriber row + record `legal_doc_versions.id` hash for each document version accepted. Quote Builder acceptance (row 204 above) mirrors the same pattern — single tickbox covering terms + privacy, both timestamps stamped on `deals` (or quote acceptance table). · Stop 14 Q4 · 2026-04-13 · **gate: Phase 5 SaaS Subscription Billing build session + Phase 5 Quote Builder build session.**

### Phase 3.5 Batch C — Step 15 literal grep + success metrics (2026-04-13)

- `docs/specs/intro-funnel.md` literal sweep + `docs/settings-registry.md` · **Intro Funnel autonomy threshold sweep** (carried forward from F2.e row 103 above). Literal-grep in Intro Funnel shows 36 remaining numeric literals for abandon cadence, advance notice, per-week cap, reschedule limit, refund window, SMS/email quiet hours, shoot duration. All 8 keys already enumerated on row 103. No re-architecture in-flight — patch applied in Phase 5 Intro Funnel build session alongside the already-deferred `settings.get()` migration. · Phase 3.5 Batch C step 15 · 2026-04-13 · **gate: Phase 5 Intro Funnel build session (see row 103 above — unchanged)**
- All specs · **Per-spec success metrics section.** 12 of 21 specs have no explicit "Success metrics" section (intro-funnel, brand-dna-assessment, client-management, saas-subscription-billing, quote-builder, branded-invoicing, surprise-and-delight, client-context-engine, design-system-baseline, client-management, onboarding-and-segmentation, six-week-plan-generator). Intentional deferral: feature-level KPIs belong to the v1.1 Strategic Planning feature (memory: `project_strategic_planning_postlaunch.md`) which consumes observatory + operational data once real usage exists. Cost & Usage Observatory §3 + §4.2 already cover operational/economic metrics for v1. No patches owed — recorded here to close the audit loop. · Phase 3.5 Batch C step 15 · 2026-04-13 · **n/a — acknowledged deferral, no action required.**
- All specs · **Final literal-grep for autonomy-sensitive paths.** Grep for `setTimeout|setInterval|process.env (non-NEXT_PUBLIC)|Math.random|hardcoded|TODO|FIXME|XXX` across all 21 specs returned zero matches. Step 7a (settings-registry sweep) already caught spec-level literals for autonomy thresholds. Clean. · Phase 3.5 Batch C step 15 · 2026-04-13 · **n/a — clean, no action required.**

### Phase 5 Wave 1 A1 — project-initialisation follow-ups (2026-04-13)

- ~~`package.json` + `vitest.config.ts` + `tests/*.test.ts` · **Test harness (Vitest) + first smoke test.**~~ — **APPLIED 2026-04-13** by A2: Vitest installed, `vitest.config.ts` added, `npm test` script wired, `tests/tokens.test.ts` asserts CSS-vs-TS token parity (6 tests). `npm test` is now a real assertion, not a no-op.
- `public/` assets + `app/layout.tsx` favicon · **Replace Next.js default SVGs.** `public/next.svg`, `public/vercel.svg`, etc. are scaffolder defaults. The favicon + logo set should be dropped in during A2 theming or the first marketing-site build session — whichever fires first. Non-blocking for Wave 1 but must be clean before `LAUNCH_READY.md` ships. · A1 handoff · 2026-04-13 · **gate: Phase 5 Wave 1 A2 (preferred)** — *A2 declined: branded favicon/logo belongs with the marketing-site spec, not the token baseline; row rolled forward to the marketing-site / LAUNCH_READY sweep.*

### Phase 5 Wave 1 A2 — design-baseline follow-ups (2026-04-13)

- `docs/specs/design-system-baseline.md` "Data model → `users` table — preference fields" + Drizzle schema at first migration · **Add 6 user-preference columns + `first_signed_in_at`.** A2 defined the enums and defaults in `lib/design-tokens.ts` (`MOTION_PREFERENCES`, `DENSITY_PREFERENCES`, `TEXT_SIZE_PREFERENCES`, `DEFAULT_*`) so any later session can reference them without re-inventing. BUILD_PLAN A2 `Owns` line claims these columns land in A2; the claim was impossible (no Drizzle, no SQLite, no user table until A5). Moving ownership to **A5** (the session that first creates the schema) so the columns ship atomically with the user table. · A2 handoff · 2026-04-13 · **gate: Phase 5 Wave 1 A5 build session**
- Settings → Display UI (`app/lite/settings/display/page.tsx` + server actions) · **Build the 6-control preferences panel.** A2 ships cookie-driven presets on `/lite/design` that exercise every preset server-side, but the real per-user Settings → Display UI needs (a) auth (A8), (b) the user table columns (see row above), (c) A3 primitives (Button, Switch, RadioGroup, Form) to render cleanly. Spin up after A8 in a dedicated UI session. Until then cookies cover the design-verification path. · A2 handoff · 2026-04-13 · **gate: post-A8 UI session (new — add to BUILD_PLAN Wave 2 or Wave 3 queue)**
- `BUILD_PLAN.md` §R URL prefixes + A2 block · **Rename `/lite/_design` → `/lite/design`.** Next.js App Router treats `_foo` folders as private (404 on the URL), so the original path as written in BUILD_PLAN wasn't routable. Renamed in code already; BUILD_PLAN §R line `/lite/_design → A2` and the A2 "Owns" line need the underscore dropped for consistency. · A2 handoff · 2026-04-13 · **gate: Phase 5 Wave 2 housekeeping or next BUILD_PLAN maintenance touch**
- `lib/fonts.ts` typeface bundle loading · **Lazy-load alternate typeface presets per cookie.** A2 loads all three typeface presets (9 font families total) into every page because `next/font/google` requires module-scope instantiation and we need all three available for `/lite/design` to exercise every variant. Acceptable for v1 dev but ~200kb of font weight per request in production. Proper fix = layout-per-preset or dynamic subset loading; complex enough to defer until real production load shows the cost matters. · A2 handoff · 2026-04-13 · **gate: post-launch optimisation (Phase 6)**
- `lib/fonts.ts` `dispatchBodyFont` · **Swap Outfit for Fontshare-hosted General Sans.** Spec's Dispatch preset calls for General Sans (IT Foundry, Fontshare-only). Google Fonts substitute Outfit is character-adjacent but not identical. If Dispatch preset ends up being a popular pick and the visual gap matters, wire Fontshare via `@font-face` in `globals.css` for General Sans specifically. · A2 handoff · 2026-04-13 · **gate: post-launch / on first real user complaint (Phase 6)**
- ~~`app/lite/design/page.tsx` + real admin gate · **Wire Brand DNA Gate + role check in front of `/lite/design`.** Route is currently publicly accessible (robots: noindex but not auth-gated). A8 (portal-guard primitive + middleware) must add `/lite/design` to the admin-only path set.~~ — **APPLIED 2026-04-13** by A8: `middleware.ts` `isAdminPath()` classifies all `/lite/` paths (except `/lite/portal/`, `/lite/onboarding`, `/lite/login`) as admin-only; unauthenticated requests redirect to `/lite/login?callbackUrl=…`. · A2 handoff · 2026-04-13

### Phase 5 Wave 1 A3 — design-primitives follow-ups (2026-04-14)

- `components/ui/form.tsx` · **Replace the local `Slot` with a library `Slot`** once a consumer needs richer composition semantics (ref forwarding across complex children, Radix-style `asChild` patterns). A3's hand-rolled 20-line `Slot` clones a single element and merges props — fine for every current form consumer, but brittle if a feature spec nests children or needs `forwardRef` chaining. Non-blocking. · A3 handoff · 2026-04-14 · **gate: first feature session that hits the limit, or `LAUNCH_READY` dep audit**
- `package.json` · **Audit `next-themes` removal.** Pulled transitively by `npx shadcn add sonner`; unused in Lite (dark-only app sets the `dark` class via `getActivePresets()`). Candidate for deletion during `LAUNCH_READY` dep-weight sweep. · A3 handoff · 2026-04-14 · **gate: Phase 6 LAUNCH_READY dep audit**
- `lib/copy/empty-states.ts` (new file) · **Stand up the empty-state copy bank** once any feature spec starts calling `<EmptyState>`. Spec rule 12 ("voice is part of the design system") requires bank-sourced copy, never inlined strings; A3 accepts inline strings only because no feature surface consumes `EmptyState` yet. First consumer session creates the bank and migrates the A3 demo strings into it. · A3 handoff · 2026-04-14 · **gate: first feature session that calls `<EmptyState>`**

### Phase 4 mop-up — pre-compiled session briefs backfill (2026-04-14)

- ~~AUTONOMY_PROTOCOL.md §G0 + §"Pre-compiled session briefs" required `sessions/<id>-brief.md` per Phase 5 session, written by Phase 4. Phase 4 closed without producing any. Wave 0 P0 + Wave 1 A1–A4 ran from inline BUILD_PLAN blocks instead.~~ — **APPLIED 2026-04-14** by Phase 4 mop-up: `sessions/_brief-template.md` created; `sessions/a5-brief.md`, `sessions/a6-brief.md`, `sessions/a7-brief.md`, `sessions/a8-brief.md` written against current repo state; AUTONOMY_PROTOCOL.md G11 extension added so each wave's closing session writes the next wave's briefs (with split escape hatch). Wave 2 (B1–B3) briefs land at A8 closure under the new rule, not now. Retro parity check on A1–A4 came back clean against repo (every artefact a brief would have required is in the tree); no scope drift to back-fill. · Phase 4 mop-up session · 2026-04-14
- `BUILD_PLAN.md` Wave 1 A4 skill list · **Reconcile triple motion-skill listing.** A4 block names `framer-motion`, `motion-design-principles`, `design-motion-principles` — the latter two overlap (`motion-design-principles` is the project skill at `.claude/skills/motion-design-principles/`; `design-motion-principles` is the global skill at `~/.claude/skills/design-motion-principles/`). A4 didn't load either (motion choreographies were spec-locked, not designed). Decide whether to dedupe the BUILD_PLAN line or keep both as a belt-and-braces signal. Non-blocking — flagged so a future BUILD_PLAN housekeeping pass picks it up. · Phase 4 mop-up retro · 2026-04-14 · **gate: next BUILD_PLAN housekeeping pass**

### Phase 5 Wave 1 A4 — motion + sound registry follow-ups (2026-04-14)

- `components/lite/sound-provider.tsx` · **Swap `onloaderror` no-op for a real warning** once the first sound file lands in `/public/sounds/approved/`. Today a 404 is the expected state (files sourced in a later admin session); after the first file lands, a missing file would be a regression and should log. · A4 handoff · 2026-04-14 · **gate: first sound file committed to `/public/sounds/approved/`**
- `docs/sound-attributions.md` (new file) · **Create sound licensing log** when the first sound file lands. Per design-system-baseline §Sound implementation: every sourced file gets an attribution row (source, licence, date, Andy-approved variant ID). · A4 handoff · 2026-04-14 · **gate: first sound file committed**
- `BUILD_PLAN.md` Wave 1 A4 block · **Update wording** from "`use-sound` (Howler.js) integration" to "`howler` direct integration". A4 installed `howler` directly because the provider/`play(key)` pattern doesn't map to `use-sound`'s hook-per-sound API. Descriptive-not-prescriptive update; non-blocking. · A4 handoff · 2026-04-14 · **gate: next BUILD_PLAN housekeeping pass**
- `components/lite/motion-provider.tsx` + Dialog primitive · **Verify the 100ms-modal-fade-at-motion=off carve-out.** Spec §Reduced motion behaviour requires Dialog to keep a 100ms fade even when `motion === 'off'`. MotionProvider currently sets `{ duration: 0 }` globally; base-nova's Dialog has its own transition, which should survive — but unverified until a feature session exercises a Dialog at `motion === 'off'`. Add a targeted test or wire a local Dialog override at that point. · A4 handoff · 2026-04-14 · **gate: first feature session that ships a Dialog surface**

### Phase 5 Wave 1 A8 — Portal-guard + Brand DNA Gate middleware follow-ups (2026-04-13)

- `middleware.ts` · **Rename to `proxy.ts` (Next.js 16 deprecation).** Next.js 16 deprecates `middleware.ts` in favour of `proxy.ts`; the warning is present but the app functions correctly for now. Rename + update matcher config in a housekeeping pass. Non-blocking in dev; must land before `LAUNCH_READY.md` ships. · A8 handoff · 2026-04-13 · **gate: next middleware-touch build session or Phase 6 LAUNCH_READY sweep**
- `lib/auth/auth.ts` + `lib/crypto/vault.ts` (B2) · **`@auth/drizzle-adapter` installed but not wired.** Installed alongside `next-auth@beta` for future OAuth/DB-session needs but v1.0 uses JWT-only strategy against the `user` table; the adapter's session-table wiring is unused. Wire when a future session requires OAuth sign-in or server-side session storage. Until then the package is dead weight — remove in the dep-audit sweep if no consumer lands by launch. · A8 handoff · 2026-04-13 · **gate: first OAuth consumer session, or Phase 6 dep-audit sweep**
- `lib/db/schema/portal-magic-links.ts` · **Deferred FK constraints.** Three columns have no DB-level FK: `contact_id` (references `contacts`, lands in A6 but as activity-log contact not a contacts table — gate: first session that creates `contacts`), `submission_id` (references `intro_funnel_submissions`, gate: IF-1), `client_id` (references `clients`, gate: CM-1). All three are intentional v1.0 deferrals — gate sessions enforce integrity at application layer until the referenced tables exist. · A8 handoff · 2026-04-13 · **gate: IF-1 (submission_id), CM-1 (client_id), SP-1 or IF-1 (contact_id — whichever creates the `contacts` table first)**
- `lib/db/schema/brand-dna-profiles.ts` · **Minimal stub schema only.** A8 ships 5 columns (`id`, `subject_type`, `subject_id`, `status`, `created_at_ms`, `updated_at_ms`) sufficient for the gate query. Full BDA schema (answers, blends, invites, prose-portrait, etc.) lands in BDA-1. A8's migration `0004_a8_portal.sql` creates both `brand_dna_profiles` and `portal_magic_links`; BDA-1 must ALTER or re-create `brand_dna_profiles` as part of its own migration. · A8 handoff · 2026-04-13 · **gate: BDA-1 (Wave 3)**

### Phase 5 Wave 1 A5 — settings + permissions + kill-switches + ESLint rules follow-ups (2026-04-14)

- `lib/auth/permissions.ts` · **Phase 3.5 access matrix was never consolidated.** Only `docs/specs/finance-dashboard.md` §13 contributed rules. Every other spec owes its own "Permissions matrix contribution" section, but compiling that retroactively is a Phase 3.5-scale task. Instead, each subsequent wave's session extends `lib/auth/permissions.ts` for its own routes + cron jobs, and every brief from A9 onward inherits the precondition "if this session adds a route or cron job, append a rule". First wave session with new routes is the first consumer. · A5 handoff · 2026-04-14 · **gate: every Phase 5 session that adds a route or cron job**
- `lib/db/schema/*.ts` + consolidated 166-value enum source file · **`activity_log.kind` enum source file missing.** Phase 3.5 Batch A step 2a was meant to consolidate the 166 values across all 21 specs; the artefact was never produced. A6's brief now flags this explicitly: A6 must either author the consolidation itself or pause-and-escalate. Do not invent values. · A5 handoff · 2026-04-14 · **gate: A6**
- `lib/db/migrate.ts` · **A5 created a file not on the brief whitelist.** Rationale: Drizzle's built-in `migrate()` only applies journal-tracked schema migrations; seed SQL files need a sibling runner. The file is a small utility (~50 LOC) with `runMigrations()` + `runSeeds()`. Noted for transparency; A6 is free to extend or replace. · A5 handoff · 2026-04-14 · **gate: A6 review of migration pattern**
- `docs/settings-registry.md` Totals line · **Pre-A5 arithmetic was off by one** — enumerated 61 rows but claimed "Total: 60". A5 corrected to 68 (post-A5). Audit note only; no action owed. · A5 handoff · 2026-04-14 · **gate: none (historical)**

### Phase 5 Wave 1 A8 — Portal-guard + Brand DNA Gate + Auth.js v5 (2026-04-13)

- `lib/auth/auth.ts` + `lib/db/schema/user.ts` · **Credentials `authorize` validates email existence only — no password check.** No `password_hash` column on the `user` table in A8. Admin login UI + password seeding owed to Wave 2. Until then the admin auth route is non-functional. Token: `a8_credentials_provider_no_password`. · A8 handoff · 2026-04-13 · **gate: Wave 2 admin login session**
- `lib/portal/guard.ts` · **Portal session cookie is base64url-encoded JSON, unsigned.** No HMAC or JWE over the cookie payload. A malicious actor who can set cookies can spoof a portal session. Acceptable for v1.0 (portals show non-PII photos + plans), but should be hardened with iron-session or JWE before confidential data lands in the portal. Token: `a8_portal_cookie_unsigned`. · A8 handoff · 2026-04-13 · **gate: pre-launch or when portal carries confidential data**
- `app/lite/portal/recover/actions.ts` · **Recovery form `requestPortalLink()` is a no-op stub.** Does not look up `intro_funnel_submissions` or call `sendEmail()` because the contacts/submissions tables don't exist yet. IF-4 wires the real lookup + send. Token: `a8_recovery_form_contacts_lookup_stub`. · A8 handoff · 2026-04-13 · **gate: IF-4**
- `INCIDENT_PLAYBOOK.md` · **G6 rollback for A8 not yet documented** — INCIDENT_PLAYBOOK.md is owed in Phase 6. The rollback for A8 is: set `BRAND_DNA_GATE_BYPASS=true` in Coolify env to bypass the gate; `portal_magic_links` + `brand_dna_profiles` tables are migration-reversible via `drizzle-kit drop`. Token: `a8_incident_playbook_rollback`. · A8 handoff · 2026-04-13 · **gate: Phase 6 INCIDENT_PLAYBOOK.md**
- `proxy.ts` · **Next.js 16 renamed `middleware.ts` → `proxy.ts`.** A8 brief was written against `middleware.ts`; A8 correctly created `proxy.ts` instead. All future sessions should use `proxy.ts` at the project root. The brief template and any session briefs that reference `middleware.ts` should be updated to `proxy.ts`. Token: `a8_middleware_renamed_proxy`. · A8 handoff · 2026-04-13 · **gate: next session that touches proxy/middleware**
- `lib/auth/auth.config.ts` · **Undocumented sub-artefact** — needed for Next.js Edge/Node split config pattern (middleware imports `auth.config.ts`, Node.js code imports `auth.ts`). Not in A8 brief whitelist. Adding here for transparency; B1+ may reference it for Sentry user-context wiring. · A8 handoff · 2026-04-13 · **gate: none (low risk, small file)**

### Phase 5 Wave 2 B1 — Sentry + reportIssue + cost alerts (2026-04-13)

- `middleware.ts` · **Deleted stale legacy file** — A8 created `proxy.ts` (Next.js 16 middleware convention) but left `middleware.ts` in place. `middleware.ts` had broken imports (`applyBrandDnaGate`, `isAdminPath` not exported from `lib/auth/auth`) causing G8 typecheck failures. B1 deleted `middleware.ts`; `proxy.ts` is the sole middleware entry point. Token: `b1_middleware_ts_deleted`. · B1 · 2026-04-13 · **gate: applied in B1 (no further action needed)**
- `@sentry/nextjs` · **New production dependency installed** — v10.48.0. Required for Sentry SDK init (client + server + edge). Kill-switch-gated; `sentry_enabled=false` by default so no events fire until Phase 6 enablement. Token: `b1_sentry_dep`. · B1 · 2026-04-13 · **gate: Phase 6 — enable via `sentry_enabled` kill-switch flip**
- `lib/support/reportIssue.ts` · **Sentry session replay URL not yet populated** — `session_replay_url` column in `support_tickets` is nullable; value is always `null` in B1 because the replay SDK integration requires a separate Sentry project configuration. Populate when replay SDK is set up. Token: `b1_replay_url_unpopulated`. · B1 · 2026-04-13 · **gate: Phase 6 or a dedicated Sentry replay session**
- `app/lite/admin/errors/page.tsx` · **No mark-as-resolved action** — triage dashboard lists open tickets but offers no inline status-change action. B1 scope is read-only. Add a Server Action to mark tickets resolved when the admin shell UI wave lands. Token: `b1_errors_no_resolve_action`. · B1 · 2026-04-13 · **gate: admin shell UI wave**
- `lib/support/cost-alerts.ts` · **Only Anthropic cap check wired** — `alerts.stripe_fee_anomaly_multiplier` + `alerts.resend_bounce_rate_threshold` keys are seeded by A5 and consumed by B1's settings reads, but the Stripe fee anomaly check and Resend bounce-rate check are not yet implemented (no baseline/median available yet). Wire in the Observatory wave when `external_call_log` has sufficient history. Token: `b1_stripe_resend_alerts_deferred`. · B1 · 2026-04-13 · **gate: Wave 21 Observatory**
- `lib/kill-switches.ts` + `lib/db/schema/activity-log.ts` · **Edited outside B1 file whitelist** — brief whitelist omitted these files but both are required by B1 DoD (`sentry_enabled` kill-switch, `support_ticket_created` activity kind). Logged for transparency per G2. Token: `b1_out_of_whitelist_edits`. · B1 · 2026-04-13 · **gate: none (correct and intentional)**
- `sessions/sw-1-brief.md` vs `docs/settings-registry.md` · **Brief §6 settings-key names drift from registry/seed** — SW-1 brief lists `wizards.max_resume_count` and `wizards.admin_idle_banner_days` as consumed keys. Neither exists in `docs/settings-registry.md` or `0001_seed_settings.sql`; the actual seeded registry uses `wizards.admin_cockpit_banner_days`, `wizards.help_escalation_failure_count`, `wizards.step_retry_max`, `wizards.critical_flight_wizards`. SW-1's actual code only reads `wizards.expiry_days` (confirmed correct vs registry), so this did not block the session, but SW-6 (resume/expiry worker) must not blindly reach for `max_resume_count` — it has to use `step_retry_max` or extend the registry. Token: `sw1_brief_settings_key_drift`. · SW-1 · 2026-04-14 · **gate: SW-6 brief-writing session (reconcile names against the seeded registry before SW-6 builds)**
- `lib/wizards/shell-config.ts` · **Added outside SW-1 file whitelist** — brief whitelist omitted a server-side settings read helper, but `WizardShell` is a client component and therefore can't `await settings.get('wizards.expiry_days')` directly. Added a minimal server helper (`getWizardShellConfig()`) so a Server Component parent can read the key once and pass the value as a prop, keeping the G4 literal-grep gate clean. Narrowest addition that preserved the brief's intent. Token: `sw1_shell_config_helper_added`. · SW-1 · 2026-04-14 · **gate: none (intentional; document the pattern for SW-2 shell wire-up)**

### Phase 5 Wave 6 qb-subs — G11.b miss (2026-04-14)

- `sessions/qb-6-brief.md` · **G11.b violated — next-session brief not authored by closing session** — qb-subs closed Wave 6's subscription-lifecycle webhook slice without writing the in-wave successor brief. AUTONOMY_PROTOCOL.md §G11.b requires every Phase 5 session's handoff to also commit `sessions/<next-id>-brief.md` in the same commit. qb-6-brief.md authored at the top of the QB-6 session instead (correct outcome; wrong lane). No functional regression — qb-subs's own work shipped 585/585 green — but the gate exists specifically so the next session doesn't have to re-decide its own scope at start. Fix: brief authored now; qb-subs handoff stands as closed (no retro-amend). Forward-looking: every Wave 6 closing session from QB-6 onward must commit the next brief alongside its handoff; mid-wave skips cost context on the next cold-start. Token: `qb_subs_g11b_brief_miss`. · QB-6 kickoff · 2026-04-14 · **gate: applied in QB-6 (brief landed); flag recurrence risk on QB-7/QB-8 closers** · **CLOSED 2026-04-14 (QB-6 landed the brief + wrote qb-7-brief.md same-commit; pattern restored)**

### Phase 5 Wave 6 QB-6 — reviewer notes deferred (2026-04-14)

- `lib/quote-builder/handle-quote-expire.ts` · **`expires_at_ms` semantics drift** — handler patches `expires_at_ms: Date.now()` on terminal transition, but that column is the scheduled expiry. QB-7 touches public quote state surfaces — resolve by adding a separate `expired_at_ms` column (recommended) or documenting semantics shift. Token: `qb6_expires_at_ms_semantics`. · QB-6 · 2026-04-14 · **gate: QB-7 (brief §10 carries this)** · **CLOSED 2026-04-14 (QB-7 migration 0019 added `expired_at_ms`; handler now patches the new column; `expires_at_ms` retains scheduled-expiry semantics)**
- `lib/quote-builder/handle-quote-reminder-3d.ts` · **`fail_redraft` drift-slug logged but not acted on** — meta.drift_check_result emits `fail_redraft` but handler dispatches regardless. Either rename `fail_observed` (truthful, cheap) or add single-redraft retry. Token: `qb6_drift_fail_redraft_unacted`. · QB-6 · 2026-04-14 · **gate: QB-7 or v1.1** · **CLOSED 2026-04-14 (QB-7 renamed slug to `fail_observed`; redraft loop remains a v1.1 option)**

### Phase 5 Wave 6 QB-7 — deferred notes (2026-04-14)

- ~~`lib/quote-builder/compose-send-email.ts` (or a new `compose-supersede-send-email.ts`) · **Supersede outbound email doesn't explain the replacement**~~ **CLOSED 2026-04-15 by qb-polish-1.** — Spec §Q20 says the supersede outbound email "explains the replacement" to the client. QB-7's `sendQuoteAction` supersede branch reuses the standard compose pipeline, so the intro paragraph doesn't acknowledge that this quote replaces an earlier one. Functional (Andy edits in modal) but not on-message by default. Fix: detect `supersedes_quote_id` on the draft in the composer and swap the §1 intro with a short "here's the updated version — this replaces quote SB-YYYY-NNNN" variant. Token: `qb7_supersede_email_variant`. · QB-7 · 2026-04-14 · **gate: QB-E2E or a small QB polish mop-up**
- `app/lite/admin/deals/[id]/quotes/[quote_id]/edit/page.tsx` (and/or deal drawer) · **No admin UI buttons for Edit (fork) / Withdraw** — Server actions `forkDraftFromSentAction` and `withdrawQuoteAction` exist and are tested but no admin surface renders a button to invoke them. Admin can call via console or follow-up UI pass. Fix: add an admin drawer / banner on the edit page for live-status quotes surfacing both actions with a confirm step. Token: `qb7_admin_drawer_buttons`. · QB-7 · 2026-04-14 · **gate: admin cockpit UI wave**
- ~~`lib/stripe/client.ts` · **Eager Stripe instantiation breaks `npm run build`**~~ — `new Stripe(process.env.STRIPE_SECRET_KEY ?? "")` evaluates at module-top in the Stripe 2026 SDK, which throws `Neither apiKey nor config.authenticator provided` during Next 16 page-data collection when the env is unset (dev / CI build). SW-5b already proved the lazy `getStripe()` pattern works for this. Fix: convert the singleton to a lazy `getStripe()` that returns `new Stripe(key || "sk_test_placeholder", …)` per call (or cached after first call). Not a QB-7 regression — QB-4c introduced the Stripe import path; QB-7 just noticed it during the build gate. Token: `qb4c_stripe_client_eager_build_break`. · QB-7 (found) · 2026-04-14 · **gate: QB-E2E preflight (E2E needs a clean build to run against)**

### Phase 5 Wave 6 QB-E2E — dev-server write-persistence blocker (2026-04-14)

- ~~`playwright.config.ts` / `next.config.ts` · **Next 16 Turbopack dev-server sqlite writes do not persist to disk**~~ — **APPLIED 2026-04-14** by QB-E2E: switched webServer to `next build && next start` (production mode) with `AUTH_TRUST_HOST=true`. Writes persist normally under production Node runtime. Root cause (Turbopack RSC sandbox vs better-sqlite3 native addon) not investigated further — production-mode E2E is the correct shape anyway. Original note: — Blocker for QB-E2E. Reproduced deterministically: inside `next dev` + Turbopack, `drizzle.update(...).set(...).where(...).returning()` returns the new row (per `.returning()`) and `sqliteConnection.prepare('UPDATE …').run()` reports `changes: 1`, but a second `new Database(path, { readonly: true })` in the SAME process reads the pre-update value, AND an out-of-process reader after the request sees no change. The same drizzle + better-sqlite3 code outside the dev server (plain `tsx`) persists writes correctly. Tried: moving DB out of iCloud-synced Desktop to `$TMPDIR` (same symptom), adding `serverExternalPackages: ["better-sqlite3"]` to `next.config.ts` (no change), wiping `.next` between runs. Suspected cause: Turbopack RSC sandbox / module-graph isolation discarding better-sqlite3 write side effects, or per-request module re-instantiation opening rival connections that contend and silently drop commits (4 `new Database()` opens per request observed). Consequence: all E2E specs that depend on the dev server performing writes (QB-E2E's `sent → viewed` and `viewed → accepted` transitions) cannot pass; only precondition test is green. Fix candidates to explore: (a) run Playwright against `next build && next start` instead of `next dev`; (b) investigate Turbopack `turbo.externalPackages` or alternative externals config; (c) switch to a sqlite driver that doesn't rely on native file-handle sharing (e.g. libsql / sqld via HTTP); (d) rework E2E spec to assert via UI state only, not DB reads (weaker but unblocks). Token: `qbe2e_dev_server_write_persistence`. · QB-E2E · 2026-04-14 · **gate: dedicated investigation session — blocks QB-E2E completion**
- ~~`docs/specs/quote-builder.md` §5.1 · **Manual-billed accept emits no `quote_settled` activity row**~~ **CLOSED 2026-04-15 by qb-polish-1.** — Spec says both manual and Stripe paths log `quote_settled` on accept; code only emits the row from `payment_intent.succeeded` (Stripe path). Found while authoring QB-E2E assertions. Either the spec should be narrowed ("manual-billed accept settles at invoice-paid time"), or the manual accept action in `lib/quote-builder/accept.ts` should log a `quote_settled` row. Token: `qbe2e_manual_quote_settled_missing`. · QB-E2E · 2026-04-14 · **gate: QB polish wave or BI-1 build session (BI-1 is closest adjacent concern)**

### Phase 5 Wave 5 SP-4 cleanup pass — admin login placeholder (2026-04-14)

- `app/lite/login/page.tsx` · **Dev-only placeholder page is live but uncommitted** — Untracked file surfaced during SP-4 cleanup. Working `signIn("credentials", …)` form with raw inline styles, no design-system use; self-describes as "Dev-only placeholder. Real login page lands in Wave 2 B-series." Wave 2 B-series closed without replacing it, so admin sign-in in dev currently runs through this page. Needs a proper login surface on house typography + primitives + motion + sound, correct error states, and (per `feedback_setup_is_hand_held` + `feedback_individual_feel`) a branded feel rather than a raw form. File is intentionally left untracked until a dedicated session lands the real page — committing the placeholder would risk it calcifying. Token: `sp4_login_placeholder_owed`. · SP-4 cleanup · 2026-04-14 · **gate: dedicated login-page session (probably sequenced near admin shell UI wave)**

### SB-5 — public checkout page (2026-04-15)

- `/get-started/pricing` → `/get-started/checkout` → Stripe Payment Element (test card `4242 4242 4242 4242`) → `/get-started/welcome` · Andy to walk the full conversion flow end-to-end in the browser. Unit + Playwright coverage is hermetic (fake Stripe); this is the first live Stripe Payment Element surface on a public money path, so manual confirmation matters. Token: `sb5_manual_browser_verify`. · SB-5 · 2026-04-15 · **gate: non-blocking; before launch or next SB wave close**

### Post-v1.0 braindump — future-proofing hooks (2026-04-15)

Cheap v1.0 moves that keep future features (three-mode outreach, platform-data audience seeds, SuperBad-as-a-service) from being expensive retrofits. Each row is a small, zero-to-low-cost insert into an existing Phase 5 build session — not a new feature.

- `docs/specs/outreach-engine.md` + outreach/prospects schema · Add `mode` enum column to `prospects` table (`trial_shoot` | `saas` | `unified`), default `trial_shoot` for v1.0. ICP scoring remains single-track in v1.0, but the column lets post-v1.0 three-mode outreach (per `project_outreach_three_modes`) land without a backfill migration over a populated table. · Braindump 2026-04-15 · 2026-04-15 · **gate: Phase 5 Outreach build session**
- `FOUNDATIONS.md` §11 (or nearest event-log primitive) + relevant spec schemas · Ensure SaaS subscription lifecycle events, prospect reply events, and retainer conversion events are written to a durable event log with at minimum `{email, timestamp, source, outcome}` — enough signal to later export to Meta/Google Conversions API for lookalike + retarget audience seeding (per `project_lookalike_retarget_from_platform_data`). No sync to build in v1.0; requirement is "don't lose the data." Audit existing tables first — most fields likely already present; patch is confirming + adding any gaps. · Braindump 2026-04-15 · 2026-04-15 · **gate: Phase 5 SaaS billing + Outreach + Sales Pipeline build sessions (cross-cut audit)**
- Core tables (`clients`, `brand_dna_profiles`, `settings`, and any other top-level tenant-scoped entity) · **Only if zero-cost**, add nullable `tenant_id` column at table-creation time to preserve optionality for the v3+ SuperBad-as-a-service idea (per `project_superbad_as_a_service`). Skip anywhere it adds real complexity to queries, RLS, or joins — this is a "take it if free" patch, not a multi-tenancy build. Default `NULL` = Andy's own tenant in v1.0. · Braindump 2026-04-15 · 2026-04-15 · **gate: Phase 5 foundation + each core-entity build session (zero-cost check only)**

### v1.1 — Companies list index (2026-04-16)

- `app/lite/admin/companies/page.tsx` (new) · **Build a proper companies list index surface.** Current state: `/lite/admin/companies/[id]` detail exists, Pipeline links through to it (polish-4 carve-out), but there's no list route — `/lite/admin/companies` 404s. v1.0 acceptable because Pipeline covers all companies with active deals; archived / prospect-without-deal / past-non-converter companies are reachable only via direct URL. v1.1 index should mirror the polish-3 invoices index pattern: §3 header, filter tabs (all / active / prospect / paused / archived), native search, §7 table (name / vertical / size / primary contact / last touch / status chip), voiced empty states, §11 stale banner if >30d untouched clients exceed threshold. Drill-through from each row opens the detail page. · admin-polish-4 G1 preflight · 2026-04-16 · **gate: v1.1**

## Applied

All rows below applied by `phase-3.5-backward-reconciliation` on 2026-04-13 unless noted.

### FOUNDATIONS

- `FOUNDATIONS.md` · Add LLM model registry + external-call observability section per memory `project_llm_model_registry.md` — added §11.6
- `FOUNDATIONS.md` §11.2 · Add `classification: 'transactional' | 'outreach'` parameter to `sendEmail()` — documented; classification enum extended to include 6 hiring values
- `FOUNDATIONS.md` §11.2 `sendEmail()` classification enum · Add 6 new values: `hiring_invite`, `hiring_followup_question`, `hiring_trial_send`, `hiring_archive_notice`, `hiring_contractor_auth`, `hiring_bench_assignment`

### Setup Wizards wiring

- `docs/specs/saas-subscription-billing.md` Q18 · `WizardDefinition` for `saas-product-setup` — added §1.1
- `docs/specs/content-engine.md` · `WizardDefinition` for `content-engine-onboarding` — added §1.1
- `docs/specs/unified-inbox.md` · Split Graph API consent into `graph-api-admin` + `graph-api-client` — added §13 preamble
- `docs/specs/onboarding-and-segmentation.md` · `WizardDefinition` for `onboarding-segmentation` — added §1.1
- `docs/specs/brand-dna-assessment.md` · `WizardDefinition` for `brand-dna` — added §1.1
- `docs/specs/intro-funnel.md` · `WizardDefinition` for `intro-funnel-questionnaire` — folded into §0 retroactive patch block
- `docs/specs/setup-wizards.md` · Add `finance-tax-rates` step — added §5.2

### Daily Cockpit contract additions

- `docs/specs/daily-cockpit.md` · `getHealthBanners()` contract: `in_flight_admin_wizard` + 3 finance kinds + 3 hiring kinds; attention-rail `wizard_completion` + 7 hiring source kinds

### Cost & Usage Observatory registry additions

- `docs/specs/cost-usage-observatory.md` · `admin-setup-assistant`, `finance-narrative`, `stripe-balance-read`, `stripe-balance-transactions-read`, 8 hiring jobs — consolidated into Registered jobs inventory

### Branded Invoicing

- `docs/specs/branded-invoicing.md` §4.1 · `?filter=overdue` query param
- `docs/specs/branded-invoicing.md` §4.5 · `renderBundle()` multi-document export variant

### Cross-spec enum additions

- `activity_log.kind` enum · 10 Observatory + 8 Finance + 16 Hiring values — consolidated in sales-pipeline.md

### Content Engine claimable backlog

- `docs/specs/content-engine.md` §14.0 · `listClaimableContentItems` / `claimInternalContentItem` / `releaseContentItem` + new `content_items` columns

### Finance Dashboard

- `docs/specs/finance-dashboard.md` §4.2 · `expense_line.candidate_id` FK + "Contractor payments" rollup

### Task Manager

- `docs/specs/task-manager.md` · `getAvailableBenchMembers(role, hours_needed, options?)` documented as cross-spec consumer contract

### Lead Generation

- `docs/specs/lead-generation.md` §13.0 · Reply-intelligence primitive formalised (`classifyReply` + `registerReplyDispatch`)

### Unified Inbox

- `docs/specs/unified-inbox.md` §11.2 · `hiring_invite` reply dispatch table (5 intents)

### Intro Funnel / Client Management lifecycle

- `docs/specs/intro-funnel.md` · §0 retroactive patches P1–P4 + §2 lock 14 amendment (trial shoot facts, 60-day portal lifecycle, questionnaire extension, portal migration to Client Management)
- `docs/specs/client-management.md` §10.0 · Pre-retainer rendering mode table (pre-retainer / retainer / archived) + settings keys
- `docs/specs/client-management.md` §10.3 · Bartender gains Six-Week Plan awareness ("explain a week/task" limited safe action)

### Sub-agent-surfaced candidates (applied 2026-04-13)

- `docs/specs/sales-pipeline.md` §11A · Voice & Delight treatment (empty states, toasts, Tier 2 budget=0, S&D hooks)
- `docs/specs/client-context-engine.md` §14.5 · Draft drawer motion spec (slide-from-right Tier 2, unsent-draft pulse, reduced-motion parity)
- `docs/specs/design-system-baseline.md` · Discipline #12 "Voice is part of the design system" — central banks pointer + motion-universal reference
- `docs/specs/quote-builder.md` §4.1 · Two-pane live preview motion spec (mount, debounced crossfade, mobile/desktop frame toggle)
- `docs/specs/surprise-and-delight.md` · Data-access audit checklist — JSDoc schema, 4 invariants, Phase 4 AUTONOMY_PROTOCOL CI check

### Broad backward reconciliation pass

- All pre-2026-04-13 locked specs · Retroactive audit against memories added after each spec's lock date — pass completed; 5 new candidates surfaced by Explore sub-agent and all applied above

### Phase 3.5 step 2 — cross-spec flag reconciliation (2026-04-13)

- `docs/specs/client-context-engine.md` §11.1, §11.2, §11.3, §12.5 · Define `active_strategy` artefact (origin, lifecycle, table, API surface) — was raised by Six-Week Plan Generator §12.4, never acknowledged in Context Engine
- `docs/specs/sales-pipeline.md` §4.1 `activity_log.kind` enum · 4 new values: `active_strategy_created`, `active_strategy_reviewed`, `active_strategy_updated`, `active_strategy_archived`
- `docs/specs/brand-dna-assessment.md` §3.2 · Existing-profile skip note — was self-flagged as cross-spec gap in onboarding-and-segmentation.md §15.4

### Phase 3.5 step 2a — spec self-containment pass (2026-04-13)

- `docs/specs/sales-pipeline.md` §4.1 `activity_log.kind` enum · Consolidated 17 Quote Builder + 8 Branded Invoicing + 8 Brand DNA + 21 Intro Funnel + 10 Lead Generation + 15 Content Engine + 11 Client Context Engine (non-active_strategy) + 8 Client Management + 8 Daily Cockpit + 8 Onboarding + 11 SaaS Billing + 6 Setup Wizards + 18 Unified Inbox + 17 Six-Week Plan = 166 values in labelled blocks. Authoritative-receiver note added at top of §4.1.
- `docs/specs/quote-builder.md` §5.4 `scheduled_tasks.task_type` enum · Consolidated 31 values across 10 specs (Quote Builder + Branded Invoicing + Client Context Engine + Content Engine + Client Management + Daily Cockpit + Unified Inbox + SaaS Billing + Observatory + Finance Dashboard) in labelled blocks, with authoritative-union note. Includes `manual_invoice_generate` per Branded Invoicing refinement.
- `docs/specs/quote-builder.md` §8.2 handler-map dispatch table · Expanded from 5 entries to 32 to list every owner-spec handler that must register against the shared worker. Each entry flagged with its owner-spec section reference.
- `docs/specs/quote-builder.md` §3.2 (step 7 post-accept side effects) · First-cycle manual-billed enqueue switched from `manual_invoice_send` to `manual_invoice_generate` with `run_at = first_invoice_date - 3 days` per Branded Invoicing's two-step review-window refinement.

### Phase 4 AUTONOMY_PROTOCOL — token/usage optimisation inputs (2026-04-13)

Inputs to fold into `AUTONOMY_PROTOCOL.md` when Phase 4 runs. Goal: minimise token burn during autonomous Phase 5 build without putting quality at risk.

- **Already slated (confirm these carry through):** 70% context checkpoint → interim handoff + fresh session; tight per-session scope caps; mandatory compaction between sessions; last-2 handoff notes loaded at session start (not full history); token budget kill-switch (spend ceiling pauses the loop); `external_call_log` feeds Observatory so runaway jobs surface.
- **Build-session model tiering.** Mirror the runtime LLM registry principle: Phase 4 tags each build session with a model tier — Haiku for mechanical work (migrations, scaffolds, renames, test stubs, enum expansions), Sonnet default for feature work, Opus reserved for architecture + hard debugging. Cheaper tiers run first so context is fresh when Opus sessions land. **Important caveat:** Claude Code does NOT auto-switch between models mid-run. Tiering is prescribed in the session brief and executed via explicit `/quick`, `/normal`, or `/deep` at session start, OR by spawning work inside subagents with `model` overrides.
- **Plan-level fallback behaviour.** Claude Code on Max plans degrades Opus → Sonnet once the Opus quota for the 5-hour/weekly window depletes. AUTONOMY_PROTOCOL must detect this (session logs will show the switch) and either pause the loop until reset, or explicitly continue on Sonnet for tagged-safe sessions only. Default: pause and resume at reset rather than silently degrade.
- **Cache-aware session batching.** Prompt cache has ~5-minute TTL. Phase 4 sequences related sessions back-to-back so CLAUDE.md + skills stay cached across the handoff. Long natural gaps (design → build, infra → feature) get treated as cold starts intentionally rather than burning cache midway.
- **Pre-compiled session briefs.** Phase 4 writes a terse `sessions/<id>-brief.md` per session containing: spec pointer(s), acceptance criteria, file whitelist, skill whitelist, model tier, preconditions. Claude reads the brief at session start, not the full 21 specs.
- **Per-session skill whitelist.** Stop auto-loading the full skill library on every session; the brief names the 2–5 skills that session needs.
- **Sub-agent offload for discovery.** Codebase exploration ("where does X live", "find all callers of Y") goes through Explore/general-purpose subagents; their tool-call noise dies with them, main context stays lean.
- **Verification-gate discipline.** Typecheck + tests run as single short commands at the end, not iterative poking. Dev server output is snapshotted into the handoff, not streamed into main context.
- **Anti-patterns (build risk > token savings):** don't aggressively downgrade to Haiku for feature work (rework costs more than saves); don't mid-session compact on hard debugging (finish-and-handoff is safer); don't over-tighten scope below ~1 feature slice (more session boundaries = more cache misses = net loss).

· Andy question 2026-04-13 (autonomous build token/usage optimisation + model-switching behaviour) · 2026-04-13 · **gate: Phase 4 AUTONOMY_PROTOCOL.md drafting**
- `docs/specs/quote-builder.md` §3.3 (long-tail monthly invoicing) · Rewritten to describe the two-task per-cycle chain (`manual_invoice_generate` → `manual_invoice_send`), with draft-status branching on void/sent/draft and chain-stop conditions.
- `docs/specs/quote-builder.md` §8.3 handlers · Narrowed existing `handleManualInvoiceSend` to send-only behaviour (check status, dispatch, chain-forward). Added full `handleManualInvoiceGenerate` signature (create draft, cockpit notification, enqueue matching send task).

### Phase 3.5 Batch A — steps 3, 3a, 3b, 4, 5 (2026-04-13)

- **Step 3 — deferred-task inventory:** audited all specs for deferred/parked items; confirmed nothing quietly dropped. Every deferral has a home (v1.1 roadmap, Phase 5 content mini-session, content mini-session scope sections, or open-questions lists). No new patches owed.
- **Step 3a — content-authoring output home:** created `docs/content/README.md` establishing `docs/content/<spec-name>.md` convention for content mini-session output. No pre-existing content required re-homing (all prior mini-sessions produced spec-inline locks).
- **Step 3b — prompt-file extraction:** created `lib/ai/prompts/README.md` + `INDEX.md` (47 prompts across 14 specs) + 14 per-spec stub files (`quote-builder.md`, `branded-invoicing.md`, `intro-funnel.md`, `client-context-engine.md`, `brand-dna-assessment.md`, `content-engine.md`, `six-week-plan-generator.md`, `cost-usage-observatory.md`, `finance-dashboard.md`, `daily-cockpit.md`, `lead-generation.md`, `client-management.md`, `task-manager.md`, `unified-inbox.md`). Each of the 14 prompt-heavy specs now carries a `> **Prompt files:**` cross-reference at the top pointing at its stub file. Phase 4 foundation splits stubs into per-prompt `.ts` files.
- **Step 4 — SCOPE.md vs specs alignment:** audit complete. Four product-judgement items surfaced to Andy at batch-A close-out (now listed in Pending → SCOPE.md section above): Branded Invoicing, Intro Funnel, Six-Week Plan Generator, Hiring Pipeline — none are in SCOPE.md as first-class features despite having full specs. Awaiting Andy's call on promote-to-v1.0 vs sub-heading vs defer-to-v1.1.
- **Step 5 — FOUNDATIONS patch-list consolidation:** audit complete. Three patches already applied (§11.6 model registry, §11.2 classification base, §11.2 6 hiring classification values). Four Phase-5-gated pending patches recorded in Pending → FOUNDATIONS.md section above (six-week-plan classification extensions, Task Manager + S&D disciplines 24–29, `scheduled_tasks` §11-tier pointer, Observatory unified-send-gate cross-ref). No blockers for Phase 4 Build Plan sequencing.

### Phase 3.5 Batch B — steps 6, 7, 7a, 10 (2026-04-13)

- **Step 6 — cross-spec enum audit:** `activity_log.kind` (166 values) and `scheduled_tasks.task_type` (31 values) consolidation in place from Batch A step 2a. Remaining audit: `deals.won_outcome` enum extended from `['retainer','saas']` to `['retainer','saas','project']` per Quote Builder §5.6 one-off project outcome — applied to `docs/specs/sales-pipeline.md` line 156. No other enum gaps found.
- **Step 7 — Phase 4/5 coordination items:** three cross-cutting items recorded in Pending → FOUNDATIONS.md and Specs sections above for downstream sequencing — (1) Observatory prompt-cardinality reconciliation against `lib/ai/prompts/INDEX.md`, (2) runtime definitions for `canSendTo`/`renderToPdf`/`checkBrandVoiceDrift` (referenced across ≥5 specs with only interface hints), (3) Daily Cockpit `getWaitingItems()`/`getHealthBanners()` source-spec enumeration gap. All gated to their respective Phase 4/5 build sessions; none block Phase 4 Build Plan.
- **Step 7a — settings registry:** created `docs/settings-registry.md` with full 56-key table at v1.0 seed (Finance 11, Wizards 6, Plan 8, Portal 3, Hiring 28). Consumers read via `settings.get(key)` only; Phase 5 Session A emits the seed migration from this file. Every spec with autonomy thresholds now expresses them as settings keys instead of inline literals.
- **Step 10 — canonical subscription state machine:** added as `FOUNDATIONS.md` §12 (13 states spanning Stripe-billed + manual-billed paths; canonical persistence on `deals.subscription_state`; transition owner map; cross-cutting `activity_log` rule). Three reference patches applied: (a) `docs/specs/quote-builder.md` §7.1 `invoice.payment_failed` transitions `active_current → past_due` with retainer parity note; (b) `docs/specs/branded-invoicing.md` §3.1 step 11 chain-stop on subscription exit; (c) `docs/specs/client-management.md` §2.3 Pause action visibility gated on `subscription_state = 'active_current'` + commitment window + `pause_used_this_commitment = false` + SaaS-only (retainers cannot pause).

### Phase 5 Wave 4 SW-5 (2026-04-14)

- ~~**`sw5_stripe_webhook_receiver_owed`**~~ · `/api/stripe/webhook` Route Handler — signature verify via `stripe.webhooks.constructEvent` + `STRIPE_WEBHOOK_SECRET` env; emits `external_call_log` row `job="stripe.webhook.receive"` on valid events. · **CLOSED 2026-04-14 by SW-5b** — `app/api/stripe/webhook/route.ts` ships; lazy per-request `new Stripe()` to dodge singleton-at-build failure; 5 unit tests (320/320 green).
- ~~**`sw5_wizard_defs_barrel_owed`** · `lib/wizards/defs/index.ts` barrel that eagerly imports every WizardDefinition module so registration side-effects fire in one place (currently the route page imports `stripe-admin` directly). Low priority; fold in with the second def. · SW-5 handoff · 2026-04-14 · **gate: SW-5b or SW-6**~~ — **APPLIED 2026-04-14** by SW-6: `lib/wizards/defs/index.ts` barrel ships with `./stripe-admin` + `./resend`; `app/lite/setup/critical-flight/[key]/page.tsx` now imports `@/lib/wizards/defs` for side-effect registration.
- **`sw5_integration_rollback_on_insert_failure`** · If `registerIntegration` succeeds but the `wizard_completions` insert throws, the `integration_connections` row stays `status="active"` orphaned from any completion. Retake flow should detect + update-in-place rather than insert a second active row. Decide during Observatory-facing work. · SW-5 handoff · 2026-04-14 · **gate: Observatory wave**
- **`sw7b_graph_oauth_callback_hardening`** · `/api/oauth/graph-api/callback` ships as an SW-7-a skeleton that just redirects back to the wizard with a `?oauth=pending`/`?oauth=error` note. SW-7-b replaces it with a real `code → access_token` exchange against `login.microsoftonline.com/<tenant>/oauth2/v2.0/token` plus a signed-cookie handoff to the client (consumed by a new `claimGraphOAuthTokenAction()` Server Action). Pairs with Andy registering an Azure app (client id + tenant + secret + redirect whitelist). Until then, the graph-api-admin E2E runs via `?testToken=…` direct injection. · SW-7 handoff · 2026-04-14 · **gate: SW-7-b (pairs with Azure app registration)**
- **`sw10b_meta_ads_oauth_callback_hardening`** · `/api/oauth/meta-ads/callback` ships as an SW-10-a skeleton that redirects back to the wizard with `?oauth=pending`/`?oauth=error`. SW-10-b replaces it with a real `code → access_token` exchange against `graph.facebook.com/<version>/oauth/access_token` (requires `META_ADS_CLIENT_SECRET`) plus a signed-cookie handoff to the client (consumed by a new `claimMetaOAuthTokenAction()` Server Action). Pairs with Andy registering a Meta app (App ID + App Secret + redirect URI whitelist). Until then, the meta-ads E2E runs via `?testToken=…` direct injection. · SW-10 handoff · 2026-04-14 · **gate: SW-10-b (pairs with Meta app registration)**
- **`sw11b_google_ads_oauth_callback_hardening`** · `/api/oauth/google-ads/callback` ships as an SW-11-a skeleton that redirects back to the wizard with `?oauth=pending`/`?oauth=error`. SW-11-b replaces it with a real `code → access_token` exchange against `oauth2.googleapis.com/token` (requires `GOOGLE_ADS_CLIENT_SECRET`) plus a signed-cookie handoff to the client (consumed by a new `claimGoogleOAuthTokenAction()` Server Action). Pairs with Andy registering a Google Cloud app (OAuth client ID + secret + redirect URI whitelist + consent screen config + Ads API enabled). NOTE: actual Ads API feature sessions additionally require a Google-issued `developer-token` HTTP header on each call — not needed for the wizard's `/userinfo` ping but flagged here so ad-campaign-builder doesn't rediscover it. Until then, the google-ads E2E runs via `?testToken=…` direct injection. · SW-11 handoff · 2026-04-14 · **gate: SW-11-b (pairs with Google Cloud app registration)**


### Phase 5 Wave 5 SP-7 (2026-04-14)

- **`sp7_if_stripe_metadata_contract`** · Intro Funnel's Stripe Checkout Session creation must stamp `metadata.deal_id` (string, required) + `metadata.product_type='intro'` (required). SP-7's webhook dispatcher rejects sessions missing either field — the deal does not advance and a `webhook_events.result='error'` row is written. **Reframed by QB-5 (2026-04-14):** Intro Funnel currently uses PaymentIntent (per spec §11.2), not Checkout Session — no code owes this patch yet. Contract is forward-declared against future SaaS/Checkout Session creation code. · SP-7 handoff · 2026-04-14 · **gate: any build session that introduces `stripe.checkout.sessions.create()` for intro flows**
- **`sp7_qb_stripe_metadata_contract`** · Quote Builder's Stripe Checkout Session creation (retainer + SaaS) must stamp `metadata.deal_id` (string, required) + `metadata.product_type` ∈ `retainer|saas` (required). Same rejection rule as the intro contract. **Reframed by QB-5 (2026-04-14):** QB-4c shipped PaymentIntent-based accept (not Checkout Session) with `metadata.product_type="quote"` + `metadata.quote_id` + `metadata.deal_id` + `metadata.company_id` — PaymentIntent contract satisfied. Checkout Session contract carries forward for any future retainer/saas flow that uses Checkout instead of PaymentIntent. · SP-7 handoff · 2026-04-14 · **gate: SaaS Subscription Billing build session (if it uses Checkout Session) OR any other QB code path that introduces `stripe.checkout.sessions.create()`**


### Phase 5 Wave 5 SP-9 (2026-04-14)

- **`sp9_three_wons_migrate_to_hidden_egg_fires`** · `maybeFireThreeWonsEgg()` currently reads/writes `pipeline.sd_three_wons_last_fired_ms` as the ≤ once/30-day gate. When the `hidden_egg_fires` table lands (S&D spec Phase 5 build), migrate the egg to write a row there (`egg_id='three_wons'`) and retire the settings key. The S&D spec's 30-day cooldown evaluator over `hidden_egg_fires` becomes the authoritative gate. · SP-9 handoff · 2026-04-14 · **gate: S&D spec Phase 5 build (hidden_egg_fires table)**
- **`sp9_bounce_rollback_toast`** · Sales-pipeline §11A.2 specifies *"{Contact} bounced. Deal rolled back to Lead."* + `urgent-thud` as an admin toast fired by the Resend webhook (SP-8). SP-8 performs the rollback silently server-side (no real-time channel to the admin UI yet). The toast requires a server-sent-events or similar push mechanism to Andy's open `/lite/admin/pipeline` tab. Land with the realtime-nextjs session when the pipeline subscribes to deal-stage-change events. Until then, the activity_log row is the audit trail. · SP-9 handoff · 2026-04-14 · **gate: realtime channel build session**
- **`sp9_payment_failed_urgent_toast`** · Same shape as bounce-rollback: §11 sound table calls for `urgent-thud` on `invoice.payment_failed`. Needs the same realtime channel. Land alongside `sp9_bounce_rollback_toast`. · SP-9 handoff · 2026-04-14 · **gate: realtime channel build session**
- **`sp9_sound_registry_name_map`** · Sales-pipeline §11A.2 + §11 refer to sound slots `chime-bright`, `tick-warm`, `urgent-thud` that don't exist in the locked 8-sound registry (`lib/sounds.ts`). SP-9 maps them via the code comments: `chime-bright` → `quote-accepted` (retainer/project) + `subscription-activated` (saas); `tick-warm` → `kanban-drop`; `urgent-thud` → `error`. Future design-system revisit should either (a) rename the spec slots to match the registry, or (b) add distinct registry entries (triggers the 8→N gate). Cheaper option is (a). · SP-9 handoff · 2026-04-14 · **gate: design-system-baseline revisit**


### Phase 5 Wave 3 BDA-POLISH-1 (2026-04-14)

- ~~**`bdapolish1_question_selection_regression_e2e`**~~ **CLOSED 2026-04-15 by qb-polish-1** (Playwright spec at `tests/e2e/brand-dna-question-selection.spec.ts`). · BDA-POLISH-1 fixed the "option pre-selected on next question" defect by resetting `selected`/`pending` in a `useEffect([question.id])` inside `app/lite/brand-dna/section/[n]/question-card-client.tsx`. Brief asked for a regression test that navigates two questions and asserts the second renders with no option selected. Project has no React Testing Library (would require a new dependency — flagged; not landed in this session) and the BDA flow has no E2E seed yet. Land as a Playwright spec under `tests/e2e/brand-dna-question-flow.spec.ts` once the BDA E2E seed exists, or add RTL + a vitest component test in a separate dependency-flagged session. Until then the fix is covered by code review of the `useEffect` reset. · BDA-POLISH-1 handoff · 2026-04-14 · **gate: BDA E2E seed session OR RTL adoption decision**
- **`bdapolish1_visual_remediation_backlog`** · Per BDA-POLISH-1 brief §10: other Wave-3+ surfaces with `mockup-*.html` files (Portal, Intro Funnel, Cockpit) were also built before the visual-references-binding rule landed. They likely need the same parity remediation BDA just received. Audit each surface against its mockup at the start of its respective remediation session; spawn a BDA-POLISH-style brief when drift is confirmed. · BDA-POLISH-1 handoff · 2026-04-14 · **gate: per-surface visual audit at end of Wave 6 / Wave 7**


### Phase 5 Wave 6 QB-4c (2026-04-14)

- **`qb4c_subscription_lifecycle_webhooks`** · ✅ **CLOSED by qb-subs (2026-04-14).** Four new handlers landed: `subscription-updated.ts` (Stripe status → our state mapping; `active`/`past_due`/`unpaid`/`paused` mapped, `canceled`/`trialing`/`incomplete*` skipped), `subscription-deleted.ts` (handshake log only — our cancel flow owns terminal state), `invoice-payment-failed.ts` (`active → past_due` + audit meta), `invoice-payment-succeeded.ts` (recovery-only: `past_due → active`; ongoing-cycle payments are noise-free per Branded Invoicing ownership). All registered in `lib/stripe/webhook-handlers/index.ts`. `DEAL_SUBSCRIPTION_STATES` enum grew from 7 → 8 to add `past_due`. 23 new unit tests; 585/585 green. **Deferred / next:** `customer.subscription.paused` + `customer.subscription.resumed` (SaaS-only, owned by SaaS Subscription Billing build); `customer.subscription.created` (we create the sub server-side, so the receive-side is informational only — punted unless we need audit redundancy).
- **`qb_subs_subscription_state_naming_drift`** · `DEAL_SUBSCRIPTION_STATES` in `lib/db/schema/deals.ts` uses `active` / `pending_early_exit`; FOUNDATIONS §12 canonical machine names them `active_current` / `cancel_scheduled_preterm`. The code has shipped against the short names since SP-1 + QB-4c. Reconciliation is an enum-rename + cascade through PI-succeeded writer + cancel flow — own session, not bundled. · qb-subs handoff · 2026-04-14 · **gate: pre-Wave 7 cleanup or as part of cancel-flow build**
- **`qb4c_sound_quote_accepted_emit`** · Spec §4.4 calls for a `quote_accepted` sound on the client quote page at confirmation. QB-4c ships the confirmation screen but not the sound trigger (depends on the shared realtime/sound channel — see `sp9_bounce_rollback_toast`). Wire `useSound('quote-accepted').play()` inside `ConfirmationScreen` once sound registry client hook is available on public routes. · QB-4c handoff · 2026-04-14 · **gate: sound hook on public routes**
- ~~**`qb4c_manual_invoice_generate_handler`**~~ · ✅ **CLOSED by BI-1a (2026-04-15).** Real handlers landed in `lib/invoicing/handlers.ts` (`manual_invoice_generate` / `manual_invoice_send` / `invoice_overdue_reminder`), registered via `INVOICING_HANDLERS` in `lib/scheduled-tasks/handlers/index.ts`; stub slots removed from `quote-builder.ts` (`QUOTE_BUILDER_STUB_TASK_TYPES` 4 → 2). Kill-switch `invoicing_manual_cycle_enqueue_enabled` **stays false** until BI-1b lands the PDF + token-authed web view; `accept.ts` gate unchanged.
- **`qb4c_payment_failed_handler`** · ✅ **CLOSED by QB-5 (2026-04-14).** `lib/stripe/webhook-handlers/payment-intent-failed.ts` landed; registered in dispatcher `index.ts`. Handler logs `kind:"note" + meta.kind:"quote_payment_failed"` with Stripe failure code + message; quote status unchanged per spec §7.1. Non-quote PIs skipped (`covered_by_checkout_session`). 6 unit tests in `tests/stripe/dispatch-payment-intent-failed.test.ts`. `quote_payment_failed` kept off the `ACTIVITY_LOG_KINDS` enum for now (noted in spec §7.1 line 614 as "confirm in Build" — surfaced via `meta.kind` per the SP-9 precedent; enum migration owed if/when downstream surfaces need to filter on it as a first-class kind).

### Phase 5 Wave 8 SB-2a (2026-04-15)

- **`sb2a_manual_browser_verify`** · Andy to run `/lite/admin/products` → "New product" → complete steps 1–2 → refresh the index and confirm the new draft card appears. Non-blocking — the Server Action transactional path is unit-covered (`tests/saas-products/sb2a-persist-draft.test.ts`), the index page is server-only + query-tested, the wizard shell + dimensions step are exercised structurally. · SB-2a handoff · 2026-04-15 · **gate: Andy has local dev time**
- **`sb2a_setup_wizards_kill_switch_route_gate`** · Brief specified `killSwitchKey: "setup_wizards_enabled"` on the `saas-product-setup` wizard def, but `WizardDefinition` has no root-level kill-switch slot (only `vendorManifest.killSwitchKey` exists, and this wizard has no vendor manifest). If/when we need to freeze the saas-product-setup wizard pre-SB-2b, add a route-level check in `app/lite/setup/admin/[key]/page.tsx` reading `settings.get("setup_wizards_enabled")` and redirecting/404ing when disabled. · SB-2a handoff · 2026-04-15 · **gate: real freeze need surfaces**


### Phase 5 Wave 8 SB-10 (2026-04-15)

- **`sb10_manual_browser_verify`** · Andy to run `/lite/admin/products` and `/lite/admin/products/[id]` with the `saas_headlines_enabled` kill switch on + a seeded active deal, and confirm the HeadlineStrip renders the 4 primary + 3 secondary tiles with honest numbers and the house-spring fade-in. Unit-covered by `tests/saas-products/sb10-headline-signals.test.ts` (12 tests), but visual parity (tone colours, spacing, motion cadence) needs a human eye. · SB-10 handoff · 2026-04-15 · **gate: Andy has local dev time**
- **`sb10_churn_precision_upgrade`** · `computeChurn()` in `lib/saas-products/headline-signals.ts` proxies the churn timestamp via `deals.updated_at_ms` because there is no `saas_subscription_cancelled` activity-log kind yet. The proxy is correct when a terminal-state transition is the most recent write to the deal, but drifts if a later unrelated write bumps `updated_at_ms` after cancellation. Add `saas_subscription_cancelled` to `ACTIVITY_LOG_KINDS`, emit it from the SaaS cancel flow (SB-11), and switch `computeChurn()` to read the activity-log timestamp. · SB-10 handoff · 2026-04-15 · **gate: SaaS cancel flow build (SB-11)**
- **`sb10_external_reviewer_pass`** · G10.5 reviewer gate owed per brief (aggregation queries + financial signal). Reviewer checks: (a) MRR cadence normalisation is correct across all three cadences; (b) new/churn window doesn't double-count on re-subscribe; (c) `past_due` counts toward active MRR matches spec §4.6 lockout semantics; (d) near-cap query uses threshold setting not literal; (e) numeric formatting reads honest (no rounded-to-zero vanity). · SB-10 handoff · 2026-04-15 · **gate: next session that can spawn a reviewer subagent**
- **`sb10_health_banner_wiring`** · `getSaasHealthBanners(userId)` is implemented and returns the canonical cockpit-spec shape `{id, severity, summary, href, source, first_fired_at}`, but nothing reads it yet. Daily Cockpit's HealthBanner surface needs to call it and merge into its existing banner list. Covered by unit tests (emit + kill-switch-off), but no page render yet. · SB-10 handoff · 2026-04-15 · **gate: Daily Cockpit HealthBanner aggregation session**


### Phase 5 Wave 8 SB-11 (2026-04-15)

- ~~**`sb10_churn_precision_upgrade`**~~ · ✅ **CLOSED by SB-11 (2026-04-15).** `computeChurn()` in `lib/saas-products/headline-signals.ts` now reads the `saas_subscription_cancelled` activity-log kind (emitted from every terminal branch of the SaaS cancel flow) via the existing `countActivityKindSince` deal-join. SB-10's `updated_at_ms` proxy retired; reconcile comment #2 rewritten. SB-10 churn fixture updated to seed the activity kind.
- **`sb11_retainer_route_lift`** · `/lite/portal/subscription` currently returns a "reach out" stub for non-SaaS deals (retainer cancel primitive wasn't wired into the shared surface when SB-11 landed). When retainer cancel domain is next touched, lift the existing retainer cancel primitives through this route and remove the stub branch in `page.tsx`. · SB-11 handoff · 2026-04-15 · **gate: next retainer-cancel session**
- **`sb11_manual_browser_verify`** · Andy to walk each branch: (a) paused deal → resume + cancel; (b) pre-term → pay-remainder confirm with remainder label, buyout confirm with buyout label, product-switch soft step, pause entry, continue; (c) post-term → upgrade/downgrade tier cards + cancel confirm; (d) CNOF — remove default PM on a test customer, confirm paid paths disable + banner shows. Visual parity vs mockup-portal. · SB-11 handoff · 2026-04-15 · **gate: Andy has local dev time**
- **`sb11_g105_external_reviewer`** · G10.5 external reviewer pass owed. State-change moment + subscriber-visible destructive copy + idempotent PI key shape + dual-log emission contract. · SB-11 handoff · 2026-04-15 · **gate: next session that can spawn a reviewer subagent**
- **`sb11_action_integration_tests`** · Cancel-math unit tests shipped (5 tests, `tests/saas-products/sb11-cancel-flow.test.ts`). Server Action end-to-end owed: auth gate, Stripe-mocked `createOffSessionPaymentIntent` / `scheduleSubscriptionCancel` / `cancelSubscriptionImmediately`, idempotent replay → `{ ok: true, already: true }`, CNOF short-circuit → `no_default_payment_method`, state flip + dual activity-row assertions per branch, post-term boundary guard. Pattern exists in `sb9-payment-failure.test.ts`. · SB-11 handoff · 2026-04-15 · **gate: dedicated test-fill session**
- **`sb11_sb12_brief_precompile`** · G11.b owed — next-session brief at `sessions/sb-12-brief.md` was deferred under context pressure. Pre-compile it at the start of the SB-12 session before build begins. · SB-11 handoff · 2026-04-15 · **gate: start of SB-12 session**
- **`sb11_copy_mini_session`** · Four COPY_OWED sentinels in `lib/portal/cancel-copy.ts` need authoring: `motivationalRealityCheck` (honest reality-check atop every pre-term + post-term branch, no guilt, no discounts), `productSwitchHeading`, `pausedHeading`, `postTermCancelConfirmHeading`. Placeholders ship-safe but not final SuperBad voice. · SB-11 handoff · 2026-04-15 · **gate: content mini-session with `superbad-brand-voice` loaded**


### Phase 5 Wave 8 SB-E2E (2026-04-15)

- **`sbe2e_ci_wire`** · Brief §4 asked to wire `npm run test:e2e` into pre-commit or nightly flow, but `.github/workflows/` is empty and no husky hook exists — nothing to attach to. When CI infrastructure ships (Phase 6 adjacent), add `STRIPE_TEST_KEY` + `STRIPE_TEST_PUBLISHABLE_KEY` as CI secrets and run `npm run test:e2e -- tests/e2e/saas-signup.spec.ts` nightly so this spec exercises rather than forever-skipping. · SB-E2E handoff · 2026-04-15 · **gate: CI infra setup**
- **`sbe2e_stripe_cleanup`** · Each spec run creates a fresh Stripe test-mode product + price (`SB-E2E <ISO-timestamp>`) in `beforeAll` and never deletes them. Stripe test mode has no ceiling so it's harmless, but a weekly cron that prunes `SB-E2E *` products would keep the dashboard tidy. Pure housekeeping. · SB-E2E handoff · 2026-04-15 · **gate: housekeeping pass**


### Admin chrome gap (2026-04-15)

- ~~**`admin_chrome_apply_shell`**~~ · CLOSED 2026-04-15 in session `admin-chrome-1`. `app/lite/admin/layout.tsx` now wraps every admin surface in `AdminShellWithNav` (sidebar nav + Pacifico wordmark + active-state motion + profile chip). Seven admin pages unwrapped from `<main>` → `<div>` so the shell owns the single landmark. G10.5 external review `PASS_WITH_NOTES`. See `sessions/admin-chrome-1-handoff.md`.

### Admin interior visual debt — all admin surfaces (2026-04-15)

- **`admin_interior_visual_debt`** · Caught 2026-04-15 in Andy's post-`admin-chrome-1` browser walkthrough. With the sidebar chrome applied, every `/lite/admin/**` *interior* reads as generic styling: flat-black surfaces with no `surface-1`/`surface-2` warmth, thin outlined cards with no lift/hierarchy, DM Sans body only (no Pacifico wordmark cameos, no Righteous labels), no BHS moments, no stage/status temperature. Affected surfaces: `pipeline`, `products` + `[id]` + `[id]/clients`, `invoices`, `errors`, `companies/[id]`, `deals/[id]/quotes/new` + `[quote_id]/edit`, `settings/catalogue`, `settings/quote-templates`. Root cause: **no `mockup-admin-*.html` ever existed** — admin surfaces were never mocked, so every admin-build session (SP-3/4/6/9, products sessions, invoices/errors, quote-builder create/edit, settings surfaces) cited `docs/superbad_brand_guidelines.html` + `/lite/design` tokens but had no *compositional* reference. Specs describe what; mockups encode feel. The memory `feedback_visual_references_binding` captures the principle but assumes a per-surface mockup exists; for admin the fix is a **shared reference mockup** covering admin primitives (chips, cards, tables, headers, toolbars, empty states, BHS moments). Scope is a sequence, not one session:
  - **`admin-polish-0`** — cut `mockup-admin-interior.html` at repo root as the shared admin-interior reference. Reference-only; zero code. Also updates AUTONOMY_PROTOCOL.md §G0/§2a to require admin-interior UI sessions cite this mockup, and extends `feedback_visual_references_binding` memory to cover the admin case. G10.5 reviewer runs against the mockup itself.
  - **`admin-polish-1..N`** — per-surface rebuilds against the reference mockup, slicing finalised at `admin-polish-0` close. Anticipated slicing (subject to revision): `-1` pipeline, `-2` products + detail + clients, `-3` invoices + errors, `-4` companies detail, `-5` quote-builder create/edit, `-6` settings surfaces. Each session: G10 parity against `mockup-admin-interior.html`, G10.5 binding, no `KanbanBoard`/`AdminShell` primitive mutations (consumer-only polish).
  - **Durable discipline (moving forward):** every net-new admin-interior UI session must cite `mockup-admin-interior.html` in brief §2a, or cut a surface-specific mockup *first* if the shared reference doesn't cover the primitive it introduces. Enforced at G0 precondition + G10 parity gate.
  - Closes-when: `mockup-admin-interior.html` exists + every admin interior passes G10 parity + G10.5 PASS + AUTONOMY_PROTOCOL patched + `ui-1` (Unified Inbox) ships citing the reference mockup in its §2a.
  - · source: Andy walkthrough 2026-04-15 · **gate: `admin-polish-0` first, then `admin-polish-1..N`, inserted ahead of `ui-1`**


### Phase 5 Wave 9 admin-polish-1 (2026-04-15)

- **`admin_polish_1_manual_browser_verify`** · Andy to walk `/lite/admin/pipeline` and eyeball G10 parity vs `mockup-admin-interior.html`: (a) header eyebrow (Righteous 10px 2px-tracking) → display H1 (Black Han Sans 40px, -0.4px) → body-pink mutter → meta row; (b) eight column headers with surface-highlight inset, Won column gradient + red hairline + cream label, Lost column neutral-500 text, others charcoal on tintVar; (c) deal cards — surface-2 fill, 12px radius, hover lift + pink hairline, Won badge on Won cards only (Righteous not BHS), value tabular-nums, next-action neutral-500 on the foot line, Righteous when-stamp right-aligned; (d) stale cards — dashed border + halo pulse (3.2s spring); (e) Won drop → confirm modal → retainer / saas sound pairing; (f) Lost drop → reason modal → silent. Visual-only, zero behavioural regressions expected. · admin-polish-1 handoff · 2026-04-15 · **gate: Andy has local dev time**
- **`admin_polish_1_stale_halo_pseudo_swap`** · Stale halo animation runs on an absolutely-positioned child `<span>` because Tailwind's arbitrary-selector syntax for `::after` was awkward with the conditional stale branching. Functionally identical to `::after` — same size, same stacking context via `pointer-events-none absolute inset-0`, same border-radius, same 3.2s spring keyframe — but a pure-CSS pseudo-element would be one fewer DOM node per card. Low-priority cosmetic; swap when a later admin-polish session needs the same pattern and can formalise a utility. · admin-polish-1 handoff · 2026-04-15 · **gate: next session that revisits pipeline cards**
- **`admin_polish_1_g105_rereview`** · G10.5 external reviewer gave `PASS_WITH_NOTES` with six defects; all six fixes landed in-session (rule 05 BHS proliferation stripped; rule 07 mutter density dropped Playfair from `.sub` + `next_action_text`; rule 09 card hover wired to `-translate-y-px` + pink hairline; token drift → `var(--surface-highlight)`; stale halo isolated to child span; Lost column contrast → neutral-500). Re-review is optional — every defect is a localised swap verifiable by grep — but a second PASS run would close the gate cleanly for audit. · admin-polish-1 handoff · 2026-04-15 · **gate: opportunistic; not blocking admin-polish-2**
- **`admin_polish_inherit_patterns_across_wave`** · Admin-polish-1 locked five patterns that admin-polish-2..N should inherit verbatim unless the mockup prescribes otherwise: (1) header stack = Righteous eyebrow + BHS H1 + body-pink narrative mutter + meta row; (2) `var(--surface-highlight)` on every elevated surface (header chips, cards, column rails, toolbars) — never inline the literal; (3) entity-card recipe = surface-2 / 12px radius / 18px 20px padding / `hover:-translate-y-px hover:border-[color:rgba(244,160,176,0.18)]` / stale = dashed + rgba(34,34,31,0.5); (4) stale halo animation reusable as-is via `stale-halo` keyframe in `globals.css`; (5) Righteous for all chips/badges/eyebrows at 10–11px, 1.5–1.8px tracking — BHS reserved for display H1 + hero gradient moments only. · admin-polish-1 handoff · 2026-04-15 · **gate: admin-polish-2 brief compilation**

### Phase 5 Wave 9 admin-polish-2 brief reroute (2026-04-16)

- **`admin_polish_2_brief_clients_route_miss`** · `admin-polish-2-brief.md` §5 lists `app/lite/admin/products/[id]/clients/page.tsx` for a §7 table + §8 voiced-empty rebuild, and §7 preconditions claim it exists. It does not — `products/[id]/clients/` is a client-component directory holding `archive-button-client.tsx` + `status-pill-client.tsx` (helpers imported by `[id]/page.tsx:17-18`). No subscribers-per-product surface exists anywhere in `app/lite/admin/**` (confirmed via grep). Brief §5 scope item dropped from admin-polish-2 at Andy's direction — polish sessions do not grow new routes. Brief §2 spec ref `saas-products.md` also corrected to the actual spec file `saas-subscription-billing.md` (the path `[id]/page.tsx:3` cites). · admin-polish-2 session · 2026-04-16 · **gate: closed — recorded for audit**
- **`subscribers_per_product_admin_surface`** · **Candidate future session, not a patch.** A `/lite/admin/products/[id]/subscribers` (or similar) list view — subscribers on a given SaaS product, stale flag for dormant subs, running MRR, link-through to the company/client — would be useful operationally and hit §7 table + §8 voiced empty naturally. Not in any admin-polish slice; out of scope for the polish wave entirely (polish sessions rebuild existing surfaces, not grow new ones). Brainstorm + brief before building. · admin-polish-2 session · 2026-04-16 · **gate: post-Wave-9 backlog candidate**
- **`admin_polish_2_manual_browser_verify`** · G10 side-by-side parity screenshots (`/lite/admin/products` index + `[id]` detail + archived-state banner + HeadlineStrip warn/watch tones) owed vs `mockup-admin-interior.html` §3 + §6 + §8 + §9 + §11. Deferred at admin-polish-2 close per standard admin-auth-seed friction. · admin-polish-2 handoff · 2026-04-16 · **gate: opportunistic; not blocking admin-polish-3**
- **`admin_polish_2_g105_rereview`** · G10.5 external reviewer gave `PASS_WITH_NOTES` with three defects; all three fixes landed in-session (rule-09 `duration-[180ms] ease-[cubic-bezier(0.16,1,0.3,1)]` on 8 interactive surfaces; rule-02 tracking bump from 1.2→1.5px on MetricCell label + tier Setup line + feature-flag pills; archived banner body cream→neutral-300). Re-review optional — every defect grep-verifiable — but a second PASS run would close the gate cleanly for audit. · admin-polish-2 handoff · 2026-04-16 · **gate: opportunistic; not blocking admin-polish-3**

### Phase 5 Wave 9 admin-polish-3 (2026-04-16)

- **`admin_polish_3_manual_browser_verify`** · G10 side-by-side parity screenshots owed vs `mockup-admin-interior.html` — `/lite/admin/invoices` (header + overdue-count banner + §7 table + tfoot totals with §9 BHS paid moment + voiced empty) and `/lite/admin/errors` (header + open-count banner + §7 table + resolved-dim variant + voiced empty). Deferred at admin-polish-3 close per standard admin-auth-seed friction. · admin-polish-3 handoff · 2026-04-16 · **gate: opportunistic; not blocking admin-polish-4**
- **`admin_polish_3_errors_severity_schema_gap`** · Brief §3 speculated a `severity: fatal|warn|info` chip on the errors table, but `support_tickets` schema only has `status: open|resolved` + optional `sentry_issue_id`. Rebuild substituted status chips (open=orange, resolved=success) and dimmed resolved rows to satisfy the ladder discipline, which reads honestly against the current data. If a future session wants real severity triage, options are: (a) extend the schema with a `severity` column + let Sentry/`reportIssue()` populate it, (b) derive severity from `sentry_issue_id` presence + a lightweight classifier. Not a session on its own — candidate for Observatory or Support backlog. · admin-polish-3 session · 2026-04-16 · **gate: post-Wave-9 backlog candidate; schema change required**
- **`admin_polish_3_invoice_stale_halo_row_variant`** · G10.5 reviewer flagged (non-blocking) that overdue-dormant invoice rows use a color-shift + dashed left-border + pink leading dot instead of the `@keyframes stale-halo` pulse defined for cards in `admin-polish-1`. The halo was originally designed for card-level signals; on tbody rows it competes with the §7 borderless table rhythm and the pulse would draw eye continuously across a table that may have dozens of rows. If a future polish session finds the `is-stale` row needs more weight, the halo can be applied via an absolutely-positioned `<span>` on the first cell (same pseudo-pattern as deal-card), or reframed as a one-shot fade on load. Low-priority; deliberate call, not a regression. · admin-polish-3 session · 2026-04-16 · **gate: opportunistic; reconsider if rows need more stale weight**
- **`admin_polish_3_stale_days_to_settings`** · The "overdue-dormant" threshold on invoice rows is hard-coded to 14 days (`STALE_DAYS` in `components/lite/invoices/invoice-index-client.tsx`). Per `project_settings_table_v1_architecture` and the Phase 3.5 settings-key registry discipline, autonomy thresholds should live in `settings` with a registry row. Candidate key: `invoices.stale_days_overdue`. Out of scope for the visual polish wave (UI-only diff; settings are data-layer); log for the next invoices-functional session or the Settings Audit Pass. · admin-polish-3 session · 2026-04-16 · **gate: Phase 4/5 Settings Audit Pass or next invoices-functional session**
- **`admin_polish_3_resolved_window_to_settings`** · Similarly, the errors page displays resolved tickets from the last 30 days (`RESOLVED_WINDOW_DAYS` in `app/lite/admin/errors/page.tsx`). Candidate settings key: `errors.resolved_window_days`. Same rationale as `admin_polish_3_stale_days_to_settings`. · admin-polish-3 session · 2026-04-16 · **gate: Phase 4/5 Settings Audit Pass or next errors-functional session**
- **`admin_polish_3_g105_rereview`** · G10.5 external reviewer gave `PASS_WITH_NOTES` with two non-blocking defects (empty-state mutter placement — judged against §8 polish-2 precedent, no change warranted; stale-halo-on-rows — non-blocking, logged as `admin_polish_3_invoice_stale_halo_row_variant`). No in-session fixes applied because neither defect is blocking per the reviewer and the empty-state mutter follows accepted polish-2 pattern. Re-review optional for audit. · admin-polish-3 handoff · 2026-04-16 · **gate: opportunistic; not blocking admin-polish-4**

### Phase 5 Wave 9 admin-polish-4 (2026-04-16)

- **`admin_polish_4_manual_browser_verify`** · G10 side-by-side parity screenshots owed vs `mockup-admin-interior.html` — `/lite/admin/companies/[id]` Overview tab (§3 entity-detail header with BHS company name + Righteous crumbs + voiced deck + Righteous meta row; §6 hero summary card with optional §9 BHS moment on LTV or Deal-Won count; three §6 linked panels — deals / invoices / contacts — each wrapping a §7 table with §8 voiced empty; conditional §11 cool archived banner on hero + §11 warm overdue banner on the invoices panel) + tab strip chrome (Overview · Trial Shoot · Billing with `layoutId="company-tab-active"` spring underline) + reachability (click a company name on `/lite/admin/pipeline` → lands on Overview tab). Deferred at admin-polish-4 close per standard admin-auth-seed friction. · admin-polish-4 handoff · 2026-04-16 · **gate: opportunistic; not blocking admin-polish-5**
- **`admin_polish_4_company_status_column`** · Companies have no `status` column; `CompanyStatusBadge` receives a derived `prospect | active | archived` computed at the page level (`do_not_contact → archived`, `wonCount>0 → active`, else `prospect`). Works for v1.0 but couples the status chip to page-level query context — a first-class `companies.status` enum would let other surfaces (future companies index, pipeline hover cards, portal header) render the same chip without re-deriving. Candidate future migration (small: add column + backfill via derivation rule + swap the derive() call sites). Not urgent; deliberate v1.0 call. · admin-polish-4 session · 2026-04-16 · **gate: future client-management functional session or v1.1 companies index**
- **`admin_polish_4_company_stale_days_to_settings`** · The "quiet since {month}" hero-deck mutter fires when `nowMs - lastTouchMs > 14 days` (hard-coded `COMPANY_STALE_DAYS=14` in `app/lite/admin/companies/[id]/page.tsx`). Per `project_settings_table_v1_architecture`, autonomy thresholds belong in `settings` with a registry row. Candidate key: `companies.stale_days`. Polish-session scope is visual-only; migrate during the next client-management-functional session or the Settings Audit Pass. Mirrors polish-3 precedent (`admin_polish_3_stale_days_to_settings`). · admin-polish-4 session · 2026-04-16 · **gate: Phase 4/5 Settings Audit Pass or next client-management-functional session**
- **`admin_polish_4_linked_panel_row_hover`** · Inherit rule 8 (polish-3 locked) specifies `motion.tr whileHover rgba(253,245,230,0.025)` for §7 tables. The linked-panel tables on the companies Overview tab use plain `<tr>` (only the invoice-number column is clickable — most cells are display-only). Reader-of-rules strict interpretation is a drift; judgment call is that row-hover on a mostly-static detail panel adds visual noise without affordance. If a future session drills more row-level interactions (e.g. the whole deal row becomes a link), re-introduce the motion.tr hover. Low-priority; deliberate call. · admin-polish-4 session · 2026-04-16 · **gate: opportunistic; reconsider when rows gain click affordance**
- **`admin_polish_4_g105_rereview`** · G10.5 external reviewer self-assessment `PASS_WITH_NOTES` with two non-blocking notes (manual browser screenshots owed; linked-panel row-hover intentionally absent — both logged above). No in-session fixes required — every binding rule holds, memory anchors align, zero plain `transition` classes, zero `min-h-screen bg-background` regressions, inherit rules 1–9 satisfied. Re-review optional; a second PASS run would close the gate cleanly for audit. · admin-polish-4 handoff · 2026-04-16 · **gate: opportunistic; not blocking admin-polish-5**
- **`admin_polish_4_drill_through_deal_card_pointer_stop`** · The drill-through `<Link>` on `components/lite/sales-pipeline/deal-card.tsx` adds `onPointerDown` + `onClick` `stopPropagation` so drag-to-stage still initiates from the card body while the company name navigates. This was silent-added beyond the brief's one-line wrap (brief specified `<Link>` only). Silent call per `feedback_technical_decisions_claude_calls` — without the stop, mouse-down on the company name would start a drag *and* navigate, a UX collision. If dnd-kit is swapped for a library that handles this via activation constraint, the handlers become redundant and should be removed. · admin-polish-4 session · 2026-04-16 · **gate: opportunistic; tied to dnd-kit rewrites**

### Phase 5 Wave 9 admin-polish-5 (2026-04-16)

- **`admin_polish_5_manual_browser_verify`** · Andy to walk `/lite/admin/deals/[id]/quotes/[quote_id]/edit` editor chrome in browser; non-blocking. · admin-polish-5 handoff · 2026-04-16 · **gate: Andy has local dev time**
- **`admin_polish_5_g10_screenshots`** · Parity screenshots vs mockup §3/§6/§7/§11; non-blocking. · admin-polish-5 handoff · 2026-04-16 · **gate: opportunistic**
- **`admin_polish_5_g105_rereview`** · Re-run G10.5 once screenshots addressed; non-blocking. · admin-polish-5 handoff · 2026-04-16 · **gate: opportunistic**
- **`admin_polish_5_send_button_layoutid_absent`** · Admin-side Send button does not share a `layoutId` with the Send modal. Brief claimed it did; grep showed `layoutId="quote-primary-action"` lives only on the public `quote-accept-block.tsx`. Optional admin-side morph. · admin-polish-5 handoff · 2026-04-16 · **gate: opportunistic**
- **`admin_polish_5_quote_status_badge_export`** · `QuoteStatusBadge` is admin-owned in `components/lite/quote-builder/`. Lift to shared barrel if portal/PDF/email want the same chip. · admin-polish-5 handoff · 2026-04-16 · **gate: next consumer**
- **`admin_polish_5_template_select_reset`** · `applyQuoteTemplateAction` success toast doesn't reset the template Select; trigger keeps showing the picked name briefly. Cosmetic. · admin-polish-5 handoff · 2026-04-16 · **gate: opportunistic**

### Phase 5 Wave 9 admin-polish-6 (2026-04-16)

- **`admin_polish_6_manual_browser_verify`** · Andy to walk `/lite/admin/settings/catalogue` + `/lite/admin/settings/quote-templates` in browser — §3 header, §7 table with row-hover, §5 category/structure chips, §8 voiced empty, §6 dialog chrome, brand-red CTAs. Non-blocking; standard admin-auth-seed friction. · admin-polish-6 handoff · 2026-04-16 · **gate: Andy has local dev time**
- **`admin_polish_6_g105_rereview`** · G10.5 gave PASS_WITH_NOTES. One non-blocking cosmetic note (row `transition-colors` 160ms vs 180ms — consistent with polish-3 invoice-index-client anchor). Optional re-review. · admin-polish-6 handoff · 2026-04-16 · **gate: opportunistic**


### Phase 5 Wave 9 UI-1 (2026-04-16)

- **`ui_1_brief_missing`** · UI-1 brief was not pre-compiled by admin-polish-6 (wave closer). Self-written per G11.b mop-up rule. No action needed — resolved in-session. · UI-1 handoff · 2026-04-16 · **gate: resolved**
- **`ui_1_webhook_client_state_verification`** · `app/api/webhooks/graph/route.ts` does not verify `clientState` from Graph notifications against stored value. Low risk (handler is idempotent — duplicate messages prevented by `graph_message_id` check) but should be hardened before Phase 6 launch. · G10.5 reviewer note · 2026-04-16 · **gate: Phase 6 launch prep**
- **`ui_1_sync_send_integration_tests`** · `tests/graph-sync.test.ts` and `tests/graph-send.test.ts` test Zod schemas and kill-switch state but not actual sync/send logic with mocked DB/client. Acceptable while `inbox_sync_enabled` ships disabled; integration tests should be added before the switch flips on. · G10.5 reviewer note · 2026-04-16 · **gate: before `inbox_sync_enabled` flip**
- **`ui_1_settings_registry_total_drift`** · `docs/settings-registry.md` totals section says 92 but TypeScript registry has 99 keys. Pre-existing drift, not caused by UI-1. · G10.5 reviewer note · 2026-04-16 · **gate: opportunistic**


### Phase 5 Wave 10 UI-3 (2026-04-16)

- **`ui_3_fired_transport_spec_reconciliation`** · `docs/specs/unified-inbox.md` §5.1 describes `notifications.fired_transport` as required, but UI-3 implemented it nullable to represent the three-state lifecycle (`'none'` = silent, `NULL` = queued for wave-E dispatcher, real values = fired). Wave E (dispatcher + web/pwa push transports) either (a) accepts the nullable contract or (b) requires a default value + UI-3 adjusts forward. Not blocking UI-4. · UI-3 handoff · 2026-04-16 · **gate: wave E dispatcher session**

### Phase 5 Wave 10 UI-5 (2026-04-16)

- **`ui_5_spec_col_reconciliation`** · `docs/specs/unified-inbox.md` §5.1 threads column list — explicitly list `cached_draft_low_confidence_flags` (nullable JSON). Currently inferred from §7.4 output shape; UI-5 added the column via migration 0035 but the spec prose still doesn't name it. Non-blocking. · UI-5 handoff · 2026-04-16 · **gate: Phase 3.5 reconciliation or next unified-inbox spec edit**
- **`ui_5_task_type_rename_reconciliation`** · `docs/specs/unified-inbox.md` §11.4 names `inbox_draft_reply` as the task type for reply generation. UI-5 introduced `inbox_draft_generate` for generation and left `inbox_draft_reply` reserved for the future send path. Reconcile spec to either (a) list both task types with their distinct roles or (b) rename to match the build. · UI-5 handoff · 2026-04-16 · **gate: Phase 3.5 reconciliation or next unified-inbox spec edit**
- **`ui_5_structural_constants_to_settings`** · Drafter lexical constants (`DRAFT_DEBOUNCE_MS=60_000`, `MAX_THREAD_MESSAGES=20`, `MAX_FEW_SHOTS=10`, `MAX_BODY_CHARS=2000`, `MAX_OUTPUT_TOKENS=1200`, `MAX_CCE_ACTIVITY_ROWS=10`) live inline in `lib/graph/draft-reply.ts` + `draft-reply-prompt.ts` + `sync.ts`. Phase 3.5 may want these in the `settings` table. Mirrors UI-3/UI-4 precedent. · UI-5 handoff · 2026-04-16 · **gate: opportunistic / Phase 3.5 reconciliation**
- **`ui_5_invoke_system_role_plumbing`** · `lib/ai/invoke.ts` `invokeLlmText()` accepts only a single `prompt` string. The two-perpetual-contexts discipline is honoured at the composition layer (Brand DNA into `buildDraftReplySystemPrompt`, CCE into `buildDraftReplyUserPrompt`) but both are folded into one labelled-preamble string at the SDK wire — there is no Anthropic `system` field in the API call. Retrofit: add optional `system` parameter to `invokeLlmText`, route via the SDK's `system` field; update all four callers (router, notifier, signal-noise, draft-reply). Latent correctness gap: the composition comments assert a split that the boundary doesn't deliver. · UI-5 G10.5 reviewer · 2026-04-16 · **gate: before Wave 16 CCE-1 lands (CCE payload will be richer and the split becomes more impactful)**
- **`ui_5_stale_flag_decouple_from_kill_switch`** · `maybeEnqueueDraftGeneration` in `lib/graph/sync.ts` flips `cached_draft_stale=true` AND enqueues behind the same kill-switch gate. If either switch flips off, the stale flag also skips — on recovery, any inbound that arrived during outage won't have invalidated its thread's cached draft. Low severity (drafter is fully disabled during outage; no user-facing "refreshing…" state is meaningful either). Reconsider: flip the stale flag before the kill-switch check, or document the coupling as intentional. · UI-5 G10.5 reviewer · 2026-04-16 · **gate: opportunistic**
- **`ui_5_cce_wave16_contract`** · Wave 16 CCE-1's `loadClientContextForDrafter(contactId)` export must return a superset of UI-5's stub shape: `{ relationship_type, display_name, recent_activity: Array<{ kind, body, created_at_ms }>, summary, open_action_items, source }`. The stub sets `source: "stub"`; Wave 16 sets `source: "cce"`. If Wave 16 narrows the contract, the drafter's `buildDraftReplyUserPrompt` breaks. · UI-5 handoff · 2026-04-16 · **gate: Wave 16 CCE-1 build session**
- **`ui_5_few_shot_wave11_contract`** · Wave 11's `loadDraftReplyFewShots(limit)` export must return `FewShotExample[]`: `{ inbound_summary, andy_reply }[]`. Until Wave 11 ships, UI-5's dynamic-specifier `import()` returns empty and the prompt runs with no few-shots (deliberate — fewer correct examples beats fabricated ones per discipline #63). · UI-5 handoff · 2026-04-16 · **gate: Wave 11 few-shot store build session**
- **`ui_5_ui4_handoff_incorrect_claim`** · `sessions/ui-4-handoff.md` claims `inbox-draft-reply` was already registered in the MODELS map from A6. Not true — UI-5 registered it. Future readers of the UI-4 handoff should read the claim as "will be registered in UI-5." No retroactive edit needed; documented here for audit. · UI-5 handoff · 2026-04-16 · **gate: none — documentation only**

### Phase 5 Wave 10 UI-6 (2026-04-16)

- **`ui_6_autosave_for_compose_drafts`** · `compose_drafts` table landed with explicit Save button only — no autosave. Spec §4.4 action row lists "Save to drafts" as a button (Option B locked); autosave (debounced write on keystroke pause) deferred to a compose-polish session. No data-loss risk today (Save is always-visible per spec) but flagged as UX gap. · UI-6 handoff · 2026-04-16 · **gate: later compose-polish session (post-UI-8 UI surface lands)**
- **`ui_6_subject_cap_to_settings`** · `COMPOSE_SUBJECT_MAX_LEN=60` is an inline constant in `lib/graph/compose-draft.ts`. Phase 3.5 may want it in `settings` alongside UI-3/UI-4/UI-5 structural constants (retention windows, debounce windows, history caps). · UI-6 handoff · 2026-04-16 · **gate: opportunistic / Phase 3.5 reconciliation**
- **`ui_6_invoke_system_role_plumbing_still_owed`** · `lib/ai/invoke.ts` still accepts only a single `prompt` string (see `ui_5_invoke_system_role_plumbing`). UI-6 inherits the labelled-preamble pattern for the new `inbox-compose-draft` slug and carries the same composition-layer-vs-wire-layer gap UI-5 logged. Fix is the same cross-cutting refactor (add optional `system` param, route via Anthropic `system` field); not done in UI-6 because scope stays compose-only. · UI-6 handoff · 2026-04-16 · **gate: before Wave 16 CCE-1 (batched with `ui_5_invoke_system_role_plumbing` fix)**
- **`ui_6_action_e2e_coverage`** · `tests/inbox-compose-actions.test.ts` targets the library (`lib/graph/compose-send.ts`) not the server action (`app/lite/inbox/compose/actions.ts`). Decision was taken because NextAuth `auth()` + `revalidatePath` + active Graph state are painful to mock and the library is where all the real wiring lives. A later E2E wave should exercise the action end-to-end (auth boundary + Zod validation + revalidation + real DB). · UI-6 handoff · 2026-04-16 · **gate: E2E coverage wave (Phase 5 later, or Phase 6 pre-launch)**
- **`ui_6_send_at_schedule_still_reserved`** · `inbox_draft_reply` scheduled-task slug stays reserved in `SCHEDULED_TASK_TYPES` with no handler through UI-5 and UI-6. Schedule-send feature (compose surface + date picker + task handler that calls `sendComposeMessage` from a task row at the scheduled time) will own the wiring. · UI-6 handoff · 2026-04-16 · **gate: future schedule-send feature session (post-UI-8)**
- **`ui_6_text_to_html_richness_deferred`** · `textToSimpleHtml(text)` in `lib/graph/compose-send.ts` is a minimal 5-char escape + `\n→<br>` wrap. Rich-text compose (Markdown paste, inline links, attachments, signature insertion) is a later content mini-session / compose-polish scope. Sending plain-as-HTML renders fine through Graph today. · UI-6 handoff · 2026-04-16 · **gate: compose-polish / content mini-session**

### Phase 5 Wave 10 UI-7 (2026-04-16)

- **`ui_7_brief_spec_citation_fix`** · UI-7 brief §2 (Spec references) and §10 (Notes for next-session brief writer) attributed the "preserve prior draft on failure" posture as a spec §7.4 rule. Resolved inline during UI-7 authoring: the posture is a builder-level discipline derived from §7.4's "ephemeral in-memory session" intent — the draft-family fallback asymmetry convention (UI-5 no-write / UI-6 empty body / UI-7 preserve prior) is a SuperBad-internal pattern, not a numbered spec rule. Brief now cites it as such. Resolved — close-as-built. Possible future codification: lift the asymmetry convention into `docs/specs/unified-inbox.md` §16 disciplines so later waves inherit it explicitly. · UI-7 G0 brief audit · 2026-04-16 · **gate: resolved (optional spec codification on next unified-inbox edit)**
- **`ui_7_server_action_tests_owed`** · `tests/inbox-refine-actions.test.ts` not built. `refineDraft` server action at `app/lite/inbox/compose/actions.ts:232-289` is covered by library-surface tests only (`tests/graph-refine-draft.test.ts` targets `generateRefinedDraft` directly). Decision mirrors UI-6 precedent: NextAuth `auth()` + `revalidatePath` + admin-role gate painful to mock, real wiring lives in the library. A later E2E wave should exercise auth boundary + Zod validation + activity-log write + revalidation against a real DB. · UI-7 G10.5 reviewer · 2026-04-16 · **gate: E2E coverage wave (Phase 5 later, or Phase 6 pre-launch) — batch with `ui_6_action_e2e_coverage`**
- **`ui_7_constants_to_settings`** · Inline module constants in `lib/graph/refine-draft.ts`: `MAX_REFINE_INSTRUCTION_CHARS=500`, `MAX_REFINE_TURNS=6`, `MAX_THREAD_MESSAGES=20`, `MAX_BODY_CHARS=2000`, `REFINE_MAX_OUTPUT_TOKENS=1500`. Phase 3.5 reconciliation may want these in the `settings` table alongside UI-3/UI-4/UI-5/UI-6 structural constants (retention windows, subject caps, debounce windows, history caps). · UI-7 handoff · 2026-04-16 · **gate: opportunistic / Phase 3.5 reconciliation**
- **`ui_7_invoke_system_role_plumbing_still_owed`** · `lib/ai/invoke.ts` still accepts only a single `prompt` string (see `ui_5_invoke_system_role_plumbing` + `ui_6_invoke_system_role_plumbing_still_owed`). UI-7's new `inbox-draft-refine` slug inherits the labelled-preamble pattern and folds `${systemPrompt}\n\n---\n\n${userPrompt}` into one string at the SDK wire — same composition-layer-vs-wire-layer gap. All three draft-family feature files (`draft-reply.ts`, `compose-draft.ts`, `refine-draft.ts`) migrate in one patch when the refactor lands. · UI-7 handoff · 2026-04-16 · **gate: before Wave 16 CCE-1 (batched with the two prior `invoke_system_role_plumbing` rows)**
- **`ui_7_refine_turn_persistence_if_learning_needed`** · v1 locks ephemeral turns (client React state only, replayed per call, no `refine_turns` table) per spec §7.4 "no persistence unless useful for future learning." If a future-learning use case emerges (training few-shots from successful refine iterations, or debugging cost attribution on long refine chains), persistence can be added as a non-breaking migration. Not in v1 scope. · UI-7 handoff · 2026-04-16 · **gate: future product decision (not a v1 build blocker)**
