# SuperBad Lite — Session Tracker

## 🧭 Next Action

**Phase:** 5 — Build Execution. Phase 4 CLOSED 2026-04-13. Wave 0 P0 spike CLOSED 2026-04-13. Wave 1 A1 CLOSED 2026-04-13. Wave 1 A2 CLOSED 2026-04-13. Wave 1 A3 CLOSED 2026-04-14. Wave 1 A4 CLOSED 2026-04-14. Wave 1 A5 CLOSED 2026-04-14. Wave 1 A6 CLOSED 2026-04-14. Phase 4 mop-up (pre-compiled session briefs backfill) CLOSED 2026-04-14 — `sessions/_brief-template.md` + briefs for A5/A6/A7/A8 written; AUTONOMY_PROTOCOL.md gained §G11.b rolling-cadence rule (each wave's closing session writes the next wave's briefs); A1–A4 retro parity check came back clean against repo (all artefacts a brief would have required ship in the tree); BUILD_PLAN A4 motion-skill triple flagged as a housekeeping nit. Anti-cycle: no further mop-up spawned. Full recap in `sessions/phase-4-mopup-briefs-handoff.md`.
**Protocol:** `START_HERE.md` § Phase 5 + every per-session gate in `AUTONOMY_PROTOCOL.md` §1 (G0–G12 + new G11.b rolling-cadence rule).
**Next session:** **Wave 1 A7 — Email adapter + canSendTo + quiet window + drift check + Stripe helper + renderToPdf stub** per `sessions/a7-brief.md` (pre-compiled by 2026-04-14 mop-up; drift-corrected by A6's closing handoff — migration numbering now `0003_*.sql` since A6 used 0002; `lib/ai/models.ts` currently has no drift-grader slug so A7 probably adds `drift-check-grader`/haiku to both `MODELS` and `lib/ai/prompts/INDEX.md`; `ACTIVITY_LOG_KINDS` is 217 values and may need drift/magic-link-specific additions A7 lands inline). Ships Resend email adapter + classification enum (16 values) + `canSendTo()` suppression gate + `isWithinQuietWindow()` + `checkBrandVoiceDrift()` Haiku grader + `ensureStripeCustomer()` + `renderToPdf()` stub (Puppeteer arrives at QB-3) + `/data/au-holidays.json` + `legal_doc_versions` + `email_suppressions` tables. Rollback: feature-flag-gated via `outreach_send_enabled` + `drift_check_enabled` (default OFF). Settings reads: `email.quiet_window_*`, `email.drift_*`, 3 kill-switches (all A5-seeded). Model tier: `/normal` (Sonnet).

**A6 outcome (2026-04-14):** 6 new tables landed (`activity_log`, `scheduled_tasks`, `worker_heartbeats`, `external_call_log`, `threads`, `messages`). Consolidated 217-value `activity_log.kind` enum + 37-value `task_type` enum — A5's PATCHES_OWED row about the missing consolidated source was inaccurate (the consolidation lives in `docs/specs/sales-pipeline.md` §4.1 since Phase 3.5 step 2a). `logActivity()`, `enqueueTask()`, scheduled-tasks worker (gated on `scheduled_tasks_enabled`, 15-min stale reclaim, 3-attempt retry, 5min×6^n backoff, 50-row batch, 60s tick). `formatTimestamp(date, tz)` with 5 format modes. `lib/ai/models.ts` maps all 53 `lib/ai/prompts/INDEX.md` slugs to tiers (opus/sonnet/haiku); `modelFor(slug)` resolves to Claude 4.6 model IDs. Migration `0002_a6_activity_scheduled_inbox.sql`. Typecheck clean, 68/68 tests green (26 new: activity-log 3, scheduled-tasks 7, format-timestamp 8, llm-models 5, messages-schema 3), `npm run build` clean, `npm run lint` clean. 6 new PATCHES_OWED rows (deferred FKs; spec counts drift from consolidated reality; worker runtime parameters live as code-constants). Full recap in `sessions/a6-handoff.md`.

**Read at the start of A7:** `sessions/a7-brief.md` (primary — drives G0 → G12), `CLAUDE.md`, `START_HERE.md` § Phase 5, `AUTONOMY_PROTOCOL.md` (full), `sessions/a6-handoff.md` + `sessions/a5-handoff.md` (last 2 handoffs per CLAUDE.md), `FOUNDATIONS.md` §11.2/§11.4/§11.5/§11.7, `PATCHES_OWED.md` row re: `canSendTo` / `renderToPdf` / `checkBrandVoiceDrift` runtime sigs. Skills whitelist (per A7 brief §4): `email-nodejs`, `claude-api`, `stripe`.

**A5 outcome (2026-04-14):** Drizzle client singleton at `lib/db/index.ts` (better-sqlite3 + WAL + FK pragma). `settings` table (schema `lib/db/schema/settings.ts`; schema migration `0000_init.sql`; seed migration `0001_seed_settings.sql` — 68 rows across Finance/Wizards/Plan/Portal/Intro Funnel/Hiring/Email/Alerts; all INSERTs `INSERT OR IGNORE` so re-runs are idempotent). Migration runner at `lib/db/migrate.ts` applies Drizzle's journal migrations then walks untracked `.sql` seeds. `settings.get(key)` helper at `lib/settings.ts` — typed key map, per-key zod parser, in-memory cache invalidated on write. `user` table at `lib/db/schema/user.ts` with `role` + `timezone` + the six display-preference columns + `first_signed_in_at_ms` (Auth.js-compatible; A8 wires NextAuth against it). `lib/auth/permissions.ts` — `Role` union + `can(role, action, resource)` default-deny helper, seeded with Finance Dashboard §13 matrix only (each subsequent wave extends). `lib/kill-switches.ts` — 4 flags, all default-disabled. `lib/types/glossary.ts` — branded TS aliases for 11 entity types + onboarding entry paths. Three custom ESLint rules at `lib/eslint-rules/*.ts` loaded via jiti from `eslint.config.mjs` — `no-direct-anthropic-import`, `no-direct-stripe-customer-create`, `no-direct-resend-send`. `DATABASE_URL` added to `lib/env.ts` + `.env.example`. Registry doc extended with Email + Alerts sections; Totals reconciled to 68 (pre-A5 was 61; the historical "60" was an arithmetic mistake). Typecheck clean, `npm test` 42/42 green (25 pre-existing + 17 new), `npm run build` clean, `npm run lint` clean. Full recap in `sessions/a5-handoff.md`. Four new PATCHES_OWED rows (access matrix never consolidated → every wave owes its own rules; `activity_log.kind` enum source file missing → A6 escalation risk; `lib/db/migrate.ts` created off-whitelist with rationale; registry Totals arithmetic historical note).

**A4 outcome (2026-04-14):** `houseSpring` wired through Framer `MotionConfig` at `components/lite/motion-provider.tsx` with preference-derived transition (full → spring, reduced → 180ms linear, off → instant). 7 locked Tier 2 choreographies in `lib/motion/choreographies.ts` (`dashboard-first-load`, `morning-brief-open`, `quote-accepted`, `subscription-activated`, `wizard-complete`, `portal-first-load`, `inbox-zero`) — each with variants + transition + reduced fallback; `pdfRenderOverlay` Tier 1 token ready for QB-3/BI-2/SWP-8. 7-item sound registry in `lib/sounds.ts` with Howler-backed lazy-instanced `play()` at `components/lite/sound-provider.tsx`; hard-muted when `!soundsEnabled || motion !== 'full'`. `Tier2Reveal` rewritten on Framer with a required typed `choreography` prop. `/lite/design` extended with a picker-driven Tier 2 demo + 7 sound triggers. `SoundKey` union fixed to spec's canonical kebab-cased keys (A3 had speculative names — A4's handoff explains). `framer-motion` + `howler` + `@types/howler` installed; `use-sound` explicitly NOT installed (Howler directly fits provider API — see A4 handoff decision #1). Typecheck clean, `npm test` 25 / 25 green (+ 8 new tests), `npm run build` clean, `/lite/design` HTTP 200. Full recap in `sessions/a4-handoff.md`. Four new PATCHES_OWED rows (loaderror warning swap, sound-attributions file, BUILD_PLAN wording update, Dialog modal-fade carve-out verification).

**A3 outcome (2026-04-14):** 35 primitives in `components/ui/` (32 via `npx shadcn add`, plus hand-rolled `form.tsx` and `date-picker.tsx` because base-nova doesn't ship either from the registry). 8 custom Lite wrappers in `components/lite/` (`ThemeProvider`, `MotionProvider`, `SoundProvider`, `AdminShell`, `BrandHero`, `SkeletonTile`, `EmptyState`, `Tier2Reveal`) — each with JSDoc listing consumed tokens per spec rule 11. `BrandHero` enforces the BHS closed list at type level via `location: BhsLocation`. `/lite/design` extended with a primitives gallery + 6-axis accessibility panel (10-axis coverage via toggles + documented axes). Layout now wraps in `ThemeProvider → MotionProvider → SoundProvider` with `<Toaster />` mounted globally. Typecheck clean, `npm test` green (17 tests / 2 files), `npm run build` clean, `/lite/design` HTTP 200. **Provider stubs for A4:** `MotionProvider` is CSS-attribute-only; `SoundProvider.play()` is a no-op; `Tier2Reveal` uses CSS keyframes. A4 replaces each under the same public APIs. Three new PATCHES_OWED rows (Slot library upgrade, `next-themes` removal audit, `lib/copy/empty-states.ts` bank). Full recap in `sessions/a3-handoff.md`.

**A2 outcome (2026-04-13) [superseded by A3 note above]:** Full SuperBad token set in `app/globals.css` + typed mirror at `lib/design-tokens.ts`; 3 theme presets + 3 typeface presets (cookie-selected via `lib/presets.ts`); all nine fonts wired through `lib/fonts.ts`; `/lite/design` reference route live (note: renamed from `/lite/_design` because Next.js treats underscore-prefixed folders as private); Vitest harness + `tests/tokens.test.ts` (6 green) asserts CSS↔TS parity. Typecheck + build clean. **Scope descope:** BUILD_PLAN-A2 claims re: `user`-table preference columns (moved to A5) and Settings → Display UI (moved to post-A8 session) — neither buildable without preceding infra. Full recap in `sessions/a2-handoff.md`. Five new PATCHES_OWED rows (user columns → A5, Settings UI → post-A8, BUILD_PLAN `/lite/_design` rename, lazy-font optimisation, General Sans swap, admin gate → A8); one closed (Vitest harness satisfied).

**A1 outcome (2026-04-13):** Next.js 16.2.3 + React 19.2.4 + Tailwind v4 + shadcn (`base-nova` default preset, neutral base colour) scaffolded. Dev server pinned to `:3001`. Env validator via `instrumentation.ts` → `lib/env.ts` (zod) fires loud on boot. Typecheck + `npm run build` both clean. Full recap in `sessions/a1-handoff.md`.

**P0 outcome (2026-04-13):** **B — no public Pixieset API, no webhook, no iframe support.** Path B (on-brand link-out launch card + manual URL-paste trigger for `gallery_ready_at`) locked for v1.0. Alternatives mop-up declined (Pic-Time, Cloudspot, ShootProof share the same closed posture — switching wouldn't unlock Path A). IF-2 (Wave 14) builds Path B only. Follow-on rows in `PATCHES_OWED.md` under F2.c section for IF-2 + SW-5. Full recap in `sessions/p0-pixieset-spike-handoff.md`.

**After A1:** Wave 1 Foundation A continues A2 → A3 → ... → A8 per `BUILD_PLAN.md` Wave 1. Foundation A must complete before Foundation B (Wave 2); Foundation B must complete before Wave 3.

**Phase 4 closed 2026-04-13.** See `sessions/phase-4-handoff.md` for full recap (final handoff) and `sessions/phase-4-partial-handoff.md` (BUILD_PLAN.md landing).

**Phase 3.5 closed 2026-04-13.** Full recap in `sessions/phase-3.5-exit-handoff.md`.

**Step 11 is COMPLETE (all 4 stages done 2026-04-13).** Stage 1 (Entry → Booking) — `sessions/phase-3.5-step-11-stage-1-handoff.md`. Stage 2 (Trial Shoot → Reflection → Retainer-Fit) — `sessions/phase-3.5-step-11-stage-2-handoff.md`. Stage 3 (Six-Week Plan flow) — `sessions/phase-3.5-step-11-stage-3-handoff.md`. Stage 4 (Retainer conversion + F1.a closure) — `sessions/phase-3.5-step-11-stage-4-handoff.md`. Stage 1 flags: F1.a (portal session durability — closed at Stage 4), F1.b, F1.c, F1.d. Stage 2 flags: F2.a, F2.b, F2.c, F2.d, F2.e. Stage 3 flags: F3.a, F3.b, F3.c, F3.d, F3.e, F3.f. Stage 4 flags: F4.a (retainer-payment-before-refresh-review queue + retroactive Week 1 fire; new `plan.refresh_review_block_escalation_hours` settings key), F4.b (Brand DNA gate-first → bartender-led kickoff on gate-clear; new `retainer_kickoff_bartender_said_at` column + 2 activity_log kinds), F4.c (Onboarding §8.1 welcome screen branches: trial-shoot graduates bypass, direct/referral render; new `onboarding_welcome_seen_at` column), F1.a closure (portal-guard primitive + magic-link-in-every-email inline at intro-funnel §10.1 + client-management §10.1; new `portal_magic_links` table; new `portal.magic_link_ttl_hours` + `portal.session_cookie_ttl_days` keys).

Settings registry now at **60 keys** (Plan 10, Portal 5, others unchanged).

**Batch A DONE 2026-04-13** (steps 3, 3a, 3b, 4, 5). See `sessions/phase-3.5-batch-a-handoff.md`.
**Batch B DONE 2026-04-13** (steps 6, 7, 7a, 10). See `sessions/phase-3.5-batch-b-handoff.md`.
- Step 6 (enum audit): `deals.won_outcome` extended to `['retainer','saas','project']`. No other enum gaps.
- Step 7 (Phase 4/5 coordination items): 3 items recorded in PATCHES_OWED → Pending (Observatory prompt-cardinality, `canSendTo`/`renderToPdf`/`checkBrandVoiceDrift` runtime sigs, cockpit source-spec enumeration). None block Phase 4.
- Step 7a (settings registry): `docs/settings-registry.md` created with 56 keys across Finance/Wizards/Plan/Portal/Hiring. Phase 5 Session A seeds from this file.
- Step 10 (subscription state machine): `FOUNDATIONS.md` §12 canonicalised (13 states, 2 billing paths, transition owners, activity_log rule). 3 reference patches applied (Quote Builder §7.1 past_due transition, Branded Invoicing §3.1 step 11 chain-stop, Client Management §2.3 pause-action visibility gate).

**SCOPE stop-point RESOLVED 2026-04-13.** All four items promoted to v1.0: Branded Invoicing §17, Intro Funnel §18, Six-Week Plan Generator §19, Hiring Pipeline §20.

**Next actions (in order):**
1. ~~**Step 11 walkthrough Stage 1 (Entry → Booking)** — DONE 2026-04-13.~~ See `sessions/phase-3.5-step-11-stage-1-handoff.md`.
2. ~~**Step 11 walkthrough Stage 2 (Trial Shoot → Reflection → Retainer-Fit)** — DONE 2026-04-13.~~ See `sessions/phase-3.5-step-11-stage-2-handoff.md`.
3. ~~**Step 11 walkthrough Stage 3 (Six-Week Plan flow)** — DONE 2026-04-13.~~ See `sessions/phase-3.5-step-11-stage-3-handoff.md`.
4. ~~**Step 11 walkthrough Stage 4 (Retainer conversion + F1.a portal-guard/magic-link closure)** — DONE 2026-04-13.~~ See `sessions/phase-3.5-step-11-stage-4-handoff.md`.
5. ~~**Batch C (steps 8, 9, 12, 13, 15)** — autonomous.~~ **DONE 2026-04-13.** See `sessions/phase-3.5-batch-c-handoff.md`. Step 8 added FOUNDATIONS §13 Glossary + 22 literal replacements across 6 specs (F1.c `stripe.Customer` ≠ Client terminology lock APPLIED). Step 9 rewrote Observatory §4.2 registered-jobs inventory against `lib/ai/prompts/INDEX.md` slugs + added F4.b kickoff-variant to `client-mgmt-bartender-opening-line`. Step 12 confirmed GHL cutover assumption consistent across 21 specs, no patches. Step 13 legal/compliance sweep surfaced 6 orphan patches owed (static legal pages, ToS/Privacy acceptance at SaaS signup, tax record retention, credential vault primitive, DSR flow, Quote terms link target) — logged to PATCHES_OWED Pending. Step 15 final literal-grep clean; per-spec success metrics deferred to v1.1 Strategic Planning (acknowledged, no patches).
6. ~~**Stop 14** — product-judgement (4 legal/compliance Qs from Batch C step 13) — DONE 2026-04-13.~~ See `sessions/phase-3.5-stop-14-handoff.md`. All 4 resolved (option A on each): L1 legal-pages = standalone mini-spec `docs/specs/legal-pages.md`; L2 DSR = email-only at v1.0; L3 cookie banner = geo-gated EU + universal footer link, rolled into legal-pages spec; L4 signup acceptance = single tickbox + inline links + two timestamps. 4 resolution rows added to PATCHES_OWED Stop 14 section.
7. ~~**Stop 16** — Phase 3.5 exit approval from Andy — DONE 2026-04-13.~~ Andy acked Phase 3.5 closed; tracker flipped to Phase 4.

---

---

**Phase 3 spec backlog (updated 2026-04-13 — 18 specs total, 15 locked + 1 pre-written, 3 remaining; Daily Cockpit at #12 newly locked — 9 questions, narrative brief + attention rail + banner strip + calendar preview + switchable kanban/list, three daily briefs with chained continuity + event trail + material-event regen + quiet-slot skip, fleet-scoped chips folded into rail, no dismissal/snooze, mobile PWA with tabbed kanban, 1 new table (`cockpit_briefs`), 1 Opus prompt, 9 build-time disciplines (65–73), 3 cross-spec contract patches owed at Phase 3.5 (`getWaitingItems()` × 11 specs, `maybeRegenerateBrief()` × 8 specs, `getHealthBanners()` × 4 specs), 5 build sessions; Cost & Usage Observatory at #18 is the recommended next session — Cockpit's cost-anomaly banner source depends on Observatory's primitives; Observatory's own sequencing note places it immediately after Cockpit and before Setup Wizards):**

*Recommended ordering groups related specs and respects dependencies. Final order is Andy's call at each session start.*

1. ~~`docs/specs/lead-generation.md`~~ ✅ **LOCKED 2026-04-12** — daily run operations pipeline, two tracks (SaaS + retainer) with winner-takes-all scoring, 9-signal enrichment, single approval queue with earned autonomy per track, 4-week warmup ramp non-overrideable, 4-tier engagement-gated indefinite sequences, three-tier DNC with single enforcement function, single sender `Andy Robinson <hi@contact.superbadmedia.com.au>`, Candidate → Deal promotion at first-send time. See `docs/specs/lead-generation.md` + `sessions/phase-3-lead-generation-handoff.md`. Flagged for Pipeline spec update: `contacts.email_status` enum gains `'unsubscribed'`; `activity_log.kind` enum gains 10 Lead Gen values.
2. ~~`docs/specs/intro-funnel.md`~~ ✅ **LOCKED 2026-04-12** — customer-facing acquisition surface for the $297 trial shoot. Branded editorial landing page (not conversion funnel), fast-dopamine portal after section 1 contact submit (not after all 4 sections), shape-branched self-persuasion questionnaire (3 shape variants × 4 sections = 12 banks), Stripe Payment Element inline with dynamic amount, native calendar (not Cal.com) with 5-business-day advance notice + 3-per-week cap, Pixieset API native gallery for deliverables, 8-screen reflection questionnaire with safety-valve Q1 + Claude synthesis reveal, Claude-generated retainer-fit recommendation (Opus, structured JSON), aggressive abandon cadence 15m SMS→24h SMS+email→3d email→demote (Twilio integration — new stack addition), cancel/reschedule policy D + SuperBad-side always-refundable. Deal creation at section 1 submit (direct to Trial Shoot stage, not Lead). See `docs/specs/intro-funnel.md` + `sessions/phase-3-intro-funnel-handoff.md`. **Cross-spec flags:** Pipeline `deals` gains 6 nullable columns + `contacts` gains 3 nullable columns + `activity_log.kind` gains 21 values + `deals.lost_reason` closed list grows 7→14 + Trial Shoot sub-status gains 2 values + new Trial Shoot panel sub-surfaces. Design-system-baseline needs revisit to absorb 3 new Tier-2 motion candidates. FOUNDATIONS gains Twilio as new stack integration. **New memory:** `feedback_dont_undershoot_llm_capability.md`. **Follow-up mini-session owed:** content authoring for landing page + question banks + reflection + copy.
3. ~~`docs/specs/quote-builder.md`~~ ✅ **LOCKED 2026-04-12** — 21 questions resolved. Stage-gated proposal structure (5 named sections rendered as scroll-snap panels), three-mode drafting (Claude draft-from-context default + template load + blank), `structure` enum supporting retainer/project/mixed via Stripe `add_invoice_items`, per-quote term length picker, per-quote expiry picker, **GST-inclusive canonical storage** (surfaced the "felt experience wins" principle — flagged for memory promotion), 7-state state machine with view tracking via token-URL first-fetch, structured catalogue with category + unit + `tier_rank` for retainers, **two-pane split draft editor** with 40/60 live preview, **Claude-drafted send email** with drift check and Andy review in a send modal (link-only delivery, no PDF attachment), **deliberate second-artefact PDF** as invoice-style one-pager (not compressed web-page mirror), **same-page cinematic Payment Element reveal** via Framer `layoutId` inheriting Intro Funnel's Tier-2 motion slot (no new slot spent), **shared-shell billing-mode-specific confirmation screen**, **pyramid synthesis** for the "What you told us" intro paragraph applying the client doc source hierarchy memory, **supersede-via-new-row** for edit-after-send with hash-based proof-of-acceptance, **custom cancel flow at Lite UX layer** (Billing Portal cancel disabled) with three pre-term retainer options (*Let's chat* / pay remainder / 50% buyout), three pre-term SaaS options (pay remainder / 1-month pause / continue), and post-term retention page (upgrade / downgrade / "here's what you'd be losing" / cancel through — no discounts ever). **New shared primitive introduced: `scheduled_tasks` table + single Node cron worker on Coolify** — polymorphic `task_type` with handler map, idempotency keys, stale-row reclaim, exponential retry, heartbeat table. Every subsequent Phase 3 spec can schedule work via this. **New tables:** `quotes`, `catalogue_items`, `quote_templates`, `scheduled_tasks`, `worker_heartbeats`. **New Claude prompts:** `draft-quote-from-context.ts` (Opus), `draft-intro-paragraph.ts` (Opus), `draft-send-email.ts` (Opus), `draft-scope-summary.ts` (Haiku), `draft-pdf-cover-line.ts` (Opus or rotation — content session decides), `draft-settle-email.ts` (Opus), `draft-cancel-intercept-email.ts` (Opus). **Sound registry grows 7 → 8:** one new slot `sound:quote_accepted`, admin-cockpit-only, visibility-gated, idempotent per quote. **Sprinkle claimed:** §3 Quote PDF cover line. **Cross-spec flags:** Pipeline gains `won_outcome = 'project'` + `deals.committed_until_date` + `deals.subscription_state` (7 values) + `deals.pause_used_this_commitment` + `deals.stripe_subscription_id` + `deals.stripe_customer_id` + `companies.gst_applicable` + 18 new `activity_log.kind` values; Branded Invoicing must expose invoice-generation primitive callable by `handleManualInvoiceSend`; Client Management must surface manual-billed "my invoices" page + subscription state on profile; **Client Portal spec** (rolled into Client Management until split) owes the full `/lite/portal/subscription` cancel surface with pre-term + post-term branches; SaaS Subscription Billing must implement tier pricing with commitment-length discounts and close the "cancel-long-resubscribe-short" loophole; Daily Cockpit must subscribe to quote real-time events and render scheduled-tasks pane + worker heartbeat banner; Comms Inbox must thread quote emails via `In-Reply-To`/`References`. **Foundations §11.2 patch** (already owed from Task Manager) gains the `classification: 'transactional' | 'outreach'` parameter — Quote Builder relies on this for its ~13 outbound emails. **New memory owed:** `feedback_felt_experience_wins.md` (flagged for promotion — see handoff). **Follow-up content mini-session owed** — scope listed in deferred tasks above. **Phase 5 sizing note:** expect 3–4 build sessions minimum (Data+Stripe / Scheduled-tasks / Draft editor+send / Client web page+confirmation+cancel flow). Sessions A and B can run in parallel. | [phase-3-quote-builder-handoff.md](sessions/phase-3-quote-builder-handoff.md)
4. ~~`docs/specs/branded-invoicing.md`~~ ✅ **LOCKED 2026-04-12** — 25 questions resolved. Manual-billed-company invoice generation with ATO-compliant tax invoices, year-prefixed sequential numbering (`SB-INV-2026-0001`), two-step generate/send primitive, 3-day pre-send review window with cockpit notification for add-on line items, self-perpetuating monthly task chain (two tasks per cycle: `manual_invoice_generate` + `manual_invoice_send`), per-company payment terms (default 14 days), bank transfer primary + optional Stripe online payment via inline Payment Element (inherits Tier-2 slot, no new motion spent), full branded two-section scroll-snap web view at `/lite/invoices/[token]`, shared PDF renderer with Quote Builder, 5-state lifecycle (draft/sent/overdue/paid/void), automated 3-day-post-due reminder + manual follow-ups, standalone index at `/lite/invoices` with summary cards + company profile billing tab, void + reissue via supersede (no credit notes in v1), one-click mark-as-paid for bank transfers, full hidden-egg suppression on all invoice surfaces. **Sprinkle claimed:** §3 Invoice PDF footer — split treatment: dry line on unpaid PDF (content mini-session), *"paid in full. pleasure doing business."* on web view paid confirmation. **Refines Quote Builder's handler integration:** first cycle enqueue changes from `manual_invoice_send` to `manual_invoice_generate`; handler map gains 1 new entry; `manual_invoice_send` narrows to send-only. **New table:** `invoices`. **New column:** `companies.payment_terms_days`. **`activity_log.kind` gains 8 values.** `scheduled_tasks.task_type` gains 2 values (`manual_invoice_generate`, `invoice_overdue_reminder`). **3 Claude prompts:** `draft-invoice-email.ts` (Opus), `draft-invoice-reminder.ts` (Opus), `draft-supersede-notification.ts` (Haiku). **Cross-spec flags:** Quote Builder (handler refinement), Pipeline (companies column + activity log), Daily Cockpit (draft review + overdue + failed task cards), Client Management (billing tab), Client Portal (read-only invoice list), Comms Inbox (email threading), Foundations (transactional classification dependency). **No new motion slots. No new sound slots. No new specs spawned. No new memories.** **Content mini-session owed** (small — can fold into Quote Builder's). **Phase 5 sizing:** 2 sessions (Data+primitives+handlers+PDF / Web view+Payment Element+index+billing tab). Session A must land before Quote Builder Session C. | [phase-3-branded-invoicing-handoff.md](sessions/phase-3-branded-invoicing-handoff.md)
5. ~~`docs/specs/brand-dna-assessment.md`~~ ✅ **LOCKED 2026-04-12** — 30 questions resolved. Full psychological + aesthetic + communication profile. Three tracks routed by alignment gate (Founder / Business / Founder + Supplement). Shape-aware sectioned banks (5 sections × 3 shapes × 2 framings = 30 core banks + 1 supplement = 31 total). 4 MC options per question, 1–3 tags per option, unweighted. Two-level signal tag taxonomy (domain → tag, ~40–60 tags across 5 domains). Profile output: structured tags JSON + Opus prose portrait + first impression paragraph. Between-section Opus insights (×4) — must sound human, never AI. Cinematic reveal: first impression lead-in → beat → section-by-section profile build. Multi-stakeholder: individual profiles + Claude-blended company profile with divergence flags. Hard portal lock with visible preview for retainer/SaaS; optional for trial shoot. Tokenised invite links for prospects. Full retake from scratch with side-by-side Claude-highlighted comparison. Tiered downstream injection: full profile for Opus, tags-only for Haiku. Card-per-question UI with section-driven ambient visual evolution. ~20–30% visual options (autonomously generated). SuperBad's own profile as Phase 5 milestone (Andy takes assessment, cross-references brand skills). **New tables:** `brand_dna_profiles`, `brand_dna_answers`, `brand_dna_blends`, `brand_dna_invites`. **5 Opus prompts.** **activity_log.kind gains ~8 values.** **New Tier-2 motion slot:** cinematic reveal (list grows to 11). **New sound:** `sound:brand_dna_reveal` (registry grows to 9). **Sprinkle claimed:** §2 browser tab titles. **New memory:** `feedback_no_lite_on_client_facing.md` — client-facing surfaces say "SuperBad", never "SuperBad Lite." **Content mini-session owed** — largest in the project (31 banks + taxonomy + prompts + visual assets + section art direction). **Cross-spec flags:** Onboarding (#6 — composes Brand DNA), Client Context Engine (#7 — reads as perpetual context), Intro Funnel (LOCKED — trial shoot nudge), Client Management (#8 — profile status), S&D (LOCKED — silent build dependency), Content Engine (#10 — reads for voice), Daily Cockpit (#12 — morning brief tone), Sales Pipeline (LOCKED — `brand_dna_status` field), Design System Baseline (LOCKED — Tier-2 + sound additions). **Phase 5 sizing:** 3–4 sessions (A: data+invites, B: card UI+transitions+insights, C: reveal+generation+sound, D: blend+retake+portal gate). A and B parallel. | [phase-3-brand-dna-assessment-handoff.md](sessions/phase-3-brand-dna-assessment-handoff.md)
6. ~~`docs/specs/onboarding-and-segmentation.md`~~ ✅ **LOCKED 2026-04-12** — 34 questions resolved. Composition layer orchestrating Brand DNA + Revenue Segmentation + setup wizards + SaaS product config into audience-specific onboarding. Retainer: quote acceptance auto-triggers → welcome email (Claude-drafted, personalised) → welcome screen (with "what we already know" Claude summary) → Brand DNA (hard gate) → practical setup (return-when-ready: contacts, ad accounts, content archive; 24h/72h/weekly nudge). SaaS: payment → welcome email (graceful degradation) → welcome screen → Brand DNA (hard gate, resume screen on abandon) → Revenue Segmentation (5 MC questions, SaaS-only, ~2 min) → product config (interface contract: `productConfig` + `firstRunView` + metadata) → credentials (magic link only) → product unlocked. Revenue Segmentation stored as 5 enum columns on `companies` + `location` + `revenue_segmentation_completed_at`. Company auto-created for all SaaS signups. Two-tier upsell targeting: Warm (budget OR engagement) / Hot (budget AND engagement AND goal) with Victoria location gate (configurable). Cockpit alerts for Hot, pipeline filter for Warm+Hot. Brand DNA retake nudge (12-month threshold) replaces first-time-completion nudge. Onboarding progress derived from primitives — no dedicated state. Token-based portal access at `/portal/[token]`, credentials at end. All retainer clients enter via quote acceptance — no alternative trigger, no legacy migration. **IMPORTANT BUILD NOTE: Brand DNA is a hard gate for retainer AND SaaS. Optional for trial shoot only.** **Sprinkles claimed:** welcome email first line (§3), client portal footer (§7). **New columns on `companies`:** `revenue_range`, `team_size`, `biggest_constraint`, `twelve_month_goal`, `industry_vertical`, `industry_vertical_other`, `location`, `revenue_segmentation_completed_at`. **No new tables.** **`activity_log.kind` gains ~8 values.** **Content mini-session owed** (small — can fold into another spec's). **No new memories.** **Phase 5 sizing:** 2 sessions. | [phase-3-onboarding-and-segmentation-handoff.md](sessions/phase-3-onboarding-and-segmentation-handoff.md)
7. ~~`docs/specs/client-context-engine.md`~~ ✅ **LOCKED 2026-04-12** — 31 questions resolved. Per-contact always-on Claude engine — the "where you are" companion to Brand DNA's "who they are." Compose-at-read-time architecture (no dual-write obligation on other features), sectioned summary cache (narrative + signals computed on read + inline draft fields), explicit closed material event list mapped to regeneration sections, dedicated `action_items` table with `you`/`them` ownership and soft-status lifecycle, rule-based composite health score (`healthy`/`cooling`/`at_risk`/`stale` — no Claude call), background regeneration via `scheduled_tasks` with deduplication at enqueue time, draft prompt architecture with context as silent system knowledge (never recaps — reply targets latest message only), hybrid one-shot + nudge field reusing Content Engine's rejection-chat primitive, channel auto-picked from `preferred_channel` column with one-click switcher (email v1, SMS-ready via adapter interface), right-side draft drawer consistent with Task Manager pattern, auto-saved unsent drafts (one per contact, persist until sent/discarded), separate `private_notes` table with physical module boundary (engine has no import path — enforced at code review), cold prospect fallback using SuperBad's own Brand DNA + enrichment data, fixed typed signal set for cockpit consumption, exported function library in `lib/context-engine/` (no API routes for inter-feature calls), per-action token caps + `llm_usage_log` table for cost visibility, flat factual summary voice (working tool, not personality surface). **New tables:** `context_summaries`, `action_items`, `private_notes`, `llm_usage_log`. **New column:** `contacts.preferred_channel`. **5 Claude prompts:** `summarise-contact-context.ts` (Haiku), `extract-action-items.ts` (Haiku), `generate-draft.ts` (Opus), `regenerate-draft-with-nudge.ts` (Opus), `reformat-draft-for-channel.ts` (Haiku). **`activity_log.kind` gains 11 values.** `scheduled_tasks.task_type` gains 2 values (`context_summary_regenerate`, `context_action_item_extract`). **4 new build-time disciplines (30–33):** no private-notes import in engine module, material event list verification, `assembleContext()` as sole context path, dual-context requirement on draft prompts. **No new motion slots. No new sound slots. No new sprinkle claims.** **Content mini-session owed** (small — prompt calibration + empty/loading copy, can fold into another spec's session). **Cross-spec flags:** Client Management (#8 — embeds summary tile + action items + draft drawer + private notes timeline), Lead Gen (LOCKED — `generateDraft()` cold fallback), Daily Cockpit (#12 — `getSignalsForAllContacts()` + aggregated action items), Sales Pipeline (LOCKED — card hover summary + `preferred_channel` column), Unified Inbox (#11 — must provide `messages` table fulfilling abstract schema), Quote Builder (LOCKED — quote events in signals-only event list), Branded Invoicing (LOCKED — invoice events in signals-only event list), Brand DNA (LOCKED — profile as tiered draft input), Task Manager (LOCKED — deliverable events in signals-only event list, action items and tasks are separate tables), Onboarding (LOCKED — welcome email reads summary), Content Engine (#10 — aggregated client signals), S&D (LOCKED — suppression on draft drawer + action items). **Phase 5 sizing:** 3 sessions (A: data model + assembleContext + health score + handlers, B: prompts + regeneration pipeline + deduplication + usage logging, C: draft drawer + action items panel + profile tile + empty/loading states). Sequential — A before B before C. | [phase-3-client-context-engine-handoff.md](sessions/phase-3-client-context-engine-handoff.md)
8. ~~`docs/specs/client-management.md`~~ ✅ **LOCKED 2026-04-12** — 33 questions resolved. Dual-entry model (companies + contacts both first-class). Company profile: 7 tabs (Overview / Deliverables / Billing / Brand DNA / Comms / Portal Chat / Activity). Contact profile: 5 tabs (Overview / Comms / Brand DNA / Portal Chat / Activity). Contact Overview: two-column layout with Context Engine summary tile + action items + tasks + private notes + deal snapshot + draft drawer. Client portal: **chat-first design** — portal home is a full-page AI chat with bartender register (unnamed, warm, knows-your-name voice from S&D spec), menu bubble expands to full-page clean overlay menu, first-visit 3-step tour, fallback chain (chat → Pixieset gallery → SuperBad-branded error page). AI chat: Opus-powered read-only Q&A + escalation to Andy + limited safe actions (approve deliverable, trigger export, view invoice, Stripe Billing Portal link). Chat persistent per contact, escalations to comms inbox, full log viewable by Andy on admin profiles. Portal sections: Deliverables, Invoices, Brand DNA, Package, Messages, Gallery, Data Export. Real-time portal notifications: email + badge on load + WebSocket/SSE push. Individual portals isolated per contact. Clients index at `/lite/clients` with summary cards + compact list + health score dots + Active/Completed/Churned filters. Global search (`Cmd+K` + header) across entities + documents + tasks. One-click data export via `scheduled_tasks` (admin-triggered or client-triggered via chat). Intro Funnel fork-at-Won portal migration job. Deliverables via Task Manager (no fork). Cancel flow pages at `/lite/portal/subscription` consuming Quote Builder's defined branches. **New table:** `portal_chat_messages`. **New columns on `contacts`:** `portal_chat_last_seen_at`, `portal_last_visited_at`. **3 Claude prompts:** bartender opening line (Haiku), chat responses (Opus), escalation summary (Haiku). **`activity_log.kind` gains 8 values.** `scheduled_tasks.task_type` gains 2 values (`client_data_export`, `intro_funnel_portal_migration`). **4 new build-time disciplines (34–37).** **Sprinkle claimed:** §2 browser tab titles for admin profiles + portal pages. **No new motion/sound slots. No new specs spawned. No new memories.** **Content mini-session owed** (small — portal tour copy, error page, empty/loading states, notification email). **Cross-spec flags:** Unified Inbox (must support `source='portal_chat_escalation'`), Daily Cockpit (new client/churned/escalation alerts), Setup Wizards (external links in practical setup), SaaS Billing (cancel flow variant). **Phase 5 sizing:** 4 sessions (A: admin profiles + search, B: portal chat, C: portal sections + notifications, D: export + migration + cancel flow). A before B before C; D parallels B or C. | [phase-3-client-management-handoff.md](sessions/phase-3-client-management-handoff.md)
9. ~~`docs/specs/saas-subscription-billing.md`~~ ✅ **LOCKED 2026-04-12** — 29 questions resolved. Generic multi-product subscription infrastructure for Lite's self-serve SaaS arm. Identity-based pricing (same per-month rate everywhere; monthly pays one-time setup fee, annual waives it; three commitment cards: getting started / committed / all in). Popcorn at two levels: within products (small/medium/large tiers differentiated by usage limits) and across products (Full Suite priced to make stacking obviously worse). Multiple named usage dimensions per product + feature flags. Hard cap with one-click pro-rated upgrade + persistent usage sticky bar with evolving voice personality (calm→dry→dead-pan at limit). Two entry paths: warm traffic (interactive demo landing page, fully open no email gate, convention-break callout) and cold outreach (meta-demonstration email + pre-populated dashboard from enrichment data). Brand DNA hard gate after payment, pre-seeded from enrichment for cold prospects. SaaS cancel flow diverges from retainer: AI chatbot contact (not Andy's email, max escalation only), motivational reality check about entrepreneurship, pay-remainder retains access until anniversary, product-switch as soft first step. Fully native account management (no Stripe Billing Portal exposure). Multiple independent subscriptions per customer (one deal per product subscription). Setup fee per new monthly subscription naturally closes cancel-long-resubscribe-short loophole. Product admin at `/lite/products` with setup wizard, archive only (no duplicate — "that's how small details get missed"), three-level analytics zoom (cockpit → aggregate → per-product). Immediate payment failure lockout with data preservation, 3x failure → data-loss warning escalation. 9 billing email templates. 12 voice-treated surfaces. **New tables:** `saas_products`, `saas_tiers`, `saas_usage_dimensions`, `saas_tier_limits`, `usage_records`. **New columns on `deals`:** `saas_product_id`, `saas_tier_id`, `billing_cadence`. **`activity_log.kind` gains 11 values.** `scheduled_tasks.task_type` gains 3 values (`saas_data_loss_warning`, `saas_annual_renewal_reminder`, `saas_card_expiry_warning`). **5 new build-time disciplines (38–42).** **Sprinkles claimed:** §3 "Subscription cancelled" email, §2 browser tab titles for `/lite/products`. **No new motion/sound slots. No new specs spawned. No new memories.** **Content mini-session owed** (large — 12 voice surfaces + 9 emails + admin empty states). **Cross-spec flags:** Pipeline (3 new deal columns, multiple deals per contact), Lead Gen (meta-demo outreach, enrichment data access), Brand DNA (pre-seeding mechanism), Client Management (native account management, SaaS cancel branches, product menu items), Onboarding (Brand DNA pre-seeding trigger), Daily Cockpit (SaaS health signals), Finance Dashboard (SaaS revenue data), Quote Builder (shared cancel flow, shared scheduled_tasks). **Phase 5 sizing:** 5 sessions (A: data+Stripe / B: product admin / C: public signup / D: subscriber experience / E: cancel+failures). B and C parallel after A. | [phase-3-saas-subscription-billing-handoff.md](sessions/phase-3-saas-subscription-billing-handoff.md)
10. ~~`docs/specs/content-engine.md`~~ ✅ **LOCKED 2026-04-13** — 26 questions resolved. Dual-use from day one: SuperBad's own SEO content engine AND a SaaS product sold to subscribers (first product on the billing infrastructure). Six-stage pipeline: Research → Topic Queue (visible, passive, with outlines) → Generation (Haiku outline → Opus draft) → Review (split-pane + rejection chat) → Publish (subscriber's own domain via Cloudflare) → Fan-out (newsletter + social + visuals, background via scheduled_tasks). Two usage dimensions: published posts/mo + newsletter subscriber cap, framed as outcomes ("1 keyword-researched post per week, sent to up to 500 people, done while you sleep"). Rankability scoring via domain authority heuristic + content gap analysis (fetch + cheerio + Haiku, no new integrations beyond Lead Gen). Template-first visual generation (HTML/CSS + Puppeteer) + AI image fallback (OpenAI Images API) + Remotion video (large tier only). Continuous tier-paced throttle — engine won't generate new draft until current one is reviewed. Three newsletter list-growth channels: CSV import (permission pass), embeddable Brand DNA-styled form, inline blog CTA. Hybrid newsletter format (single post → standalone, multiple → digest). Content-to-outreach matching: published posts matched to Lead Gen prospects, content-forward emails in approval queue with earned autonomy. Chat-first subscriber portal (bartender primary interface). Demo: vertical + location input, full pipeline in SuperBad's voice, "imagine what could happen if our tool knew who you were." Admin at `/lite/content` + fleet overview at `/lite/content/subscribers`. Three onboarding wizard steps (domain → topics → newsletter). GSC optional at medium+, video at large, everything else open. Automated aggressive list hygiene with full CSV export. Subtle inline CTA on SuperBad's own blog posts. **New tables:** `content_topics`, `blog_posts`, `blog_post_feedback`, `social_drafts`, `newsletter_subscribers`, `newsletter_sends`, `ranking_snapshots`, `content_engine_config`. **No new columns on existing tables.** **10 Claude prompts** (2 Opus, 8 Haiku). **`activity_log.kind` gains ~15 values.** `scheduled_tasks.task_type` gains 6 values. **8 new build-time disciplines (43–50).** **New integrations:** OpenAI Images API, Remotion, Google Search Console API (optional). **Sprinkles claimed:** §2 system email subject lines, §6 OG images. **No new motion/sound slots. No new specs spawned. No new memories.** **Content mini-session owed** (large — social visual template library ~10–15 templates + newsletter template + demo page copy + blog CTAs + embeddable form + email subjects + OG image templates + empty/loading states + fleet overview + notification copy + Remotion art direction). **Cross-spec flags:** Lead Gen (content-match in approval queue + earned autonomy + reply auto-enrol), Sales Pipeline (standard SaaS deal creation), Brand DNA (perpetual context + demo voice + seed keyword derivation), Client Management (portal integration + bartender actions), SaaS Billing (productConfig + usage dimensions + feature flags), Onboarding (three wizard steps), S&D (sprinkle claims + suppression), Daily Cockpit (own queue + fleet health), Unified Inbox (email threading + subscriber replies), Client Context Engine (engagement signals). **Phase 5 sizing:** 7 sessions (A: data+research / B: generation+review+publish / C: visual pipeline / D: newsletter / E: social / F: SaaS integration / G: outreach+ranking+fleet). C∥D after B. F after A+SaaS. G after A+B. | [phase-3-content-engine-handoff.md](sessions/phase-3-content-engine-handoff.md)
11. ~~`docs/specs/unified-inbox.md`~~ ✅ **LOCKED 2026-04-13** — 13 questions resolved. Full consolidation of human-to-human comms into one `messages` + `threads` surface. Replaces Outlook as Andy's primary work email client via staged M365 migration (Phase 1: Lite-as-Graph-API-client on existing mailbox, Outlook stays as backup; Phase 2: DNS cutover deferred). Two addresses (`andy@` + `support@`) unified with ticket overlay on support@ only (status: open/waiting/resolved + Claude-assigned type chip). Three independent parallel Haiku classifications per inbound: router (Q5 — known/new-lead/non-client/spam + contact resolution), notification triage (Q10 — urgent/push/silent), signal/noise (Q12 — signal/noise/spam + transactional sub-classification). Topic threads + per-contact Conversation view (flattens all threads chronologically, single query feeds Client Context Engine). Passive-vs-active channel delivery rule (portal chat + task feedback echo to email; IG/FB/SMS/WhatsApp native-only, no echo). Always-drafted Opus replies for client-facing threads with chat-refinement sidecar + retrieval-based voice learning from past sent messages + low-confidence flag chips + never auto-send policy. Single composer with optional AI draft + contact-aware picker + subject auto-generation. Claude-triaged push notifications (web push + PWA push) + 8am morning digest of everything silenced (skipped on quiet mornings) + shared notification dispatcher primitive for all system events. 12-month import on setup with on-demand backfill per older thread + post-import contact routing review. Signal/noise auto-deletion: 30 days general noise, 180 days transactional receipts, 7 days spam; Keep pin overrides; 14-day pre-purge trash bin; keep-sender promotion after 3 pins. PWA mobile with read+quick-reply+triage, Claude draft pre-loaded, compose-new discouraged but available; Outlook mobile stays as escape hatch. **New tables:** `threads`, `messages`, `message_attachments`, `message_calendar_invites`, `message_delivery_attempts`, `notifications`, `classification_corrections`, `inbox_signature_settings`, `graph_api_state`. **New columns on `contacts`:** `relationship_type`, `inbox_alt_emails`, `notification_weight`, `always_keep_noise`. **4 Claude prompts** (1 Opus for reply drafter, 3 Haiku for the three classifiers + ticket-type chip). **`activity_log.kind` gains ~18 values.** `scheduled_tasks.task_type` gains 5 values (`inbox_draft_reply`, `inbox_hygiene_purge`, `inbox_morning_digest`, `inbox_graph_subscription_renew`, `inbox_initial_import`). **14 new build-time disciplines (51–64).** **New integrations:** Microsoft Graph API (OAuth + webhook subscriptions + send-as + delegated Mail scopes), PWA Push (iOS 16.4+ / Android). **Sprinkle claims:** §2 morning digest email copy, §2 low-confidence chip copy, §2 "continue in your portal" echo footer, §2 mobile compose-new nudge, §2 Focus/Noise empty states, §2 trash-bin hygiene copy, §2 setup wizard Graph-API consent step copy, §2 post-import routing-review copy, §2 keep-sender promotion chip, §2 weekly inbox hygiene celebration copy for Daily Cockpit, §2 browser tab title for inbox, §2 "Send & Close ticket" voice, §2 signature default templates (2 variants per address). **New memory:** `feedback_passive_vs_active_channels.md` — email-echo only for passive channels; active channels (IG/FB/SMS/WhatsApp) deliver natively with no cross-channel echo. **Content mini-session owed** (see deferred list below). **Foundations §11 patches owed:** (1) `sendEmail({ transport: 'graph' \| 'resend', classification: 'transactional' \| 'outreach' })` extension so Andy-replies route through Graph and system emails continue via Resend; (2) notification dispatcher primitive as a new shared §11 module; (3) `classification_corrections` as a shared learning-loop primitive available to any classifier across the platform. **Cross-spec flags:** Client Management (`relationship_type` enum on contacts + `createThreadFromPortalChat()` integration point + AI chatbot cancel flow + portal Messages menu), Task Manager (`createThreadFromTaskFeedback()` integration point + task notifications route through shared dispatcher), Lead Gen (`attachMessageToInboxThread()` for outreach replies + campaign_id custom header preserved for fallback threading), Content Engine (`createThreadFromNewsletterReply()` + unreviewed draft notifications via shared dispatcher), SaaS Subscription Billing (support@ threads link to subscription via contact FK + customer context panel + payment-failure notifications + cancel-flow AI chatbot uses inbox threads), Brand DNA (profile read by reply drafter as system context), Client Context Engine (reads `messages`+`threads` directly via `contact_id` + classification outputs are signals + `engagement_signals` array provides behavioural data), Onboarding + Segmentation (Graph API connection is new wizard step + post-import routing review step + noise bulk-purge offer step), Sales Pipeline (Deal pages show "Recent messages" sidebar via `contact_id` join), Surprise & Delight (sprinkle claims + suppression on compose + on setup wizard + on billing threads), Quote Builder + Branded Invoicing (no schema change — their emails still go via Resend; client replies land normally), Daily Cockpit (reads inbox aggregates: unread counts, waiting-on-Andy, open support tickets, tickets >48h, silenced-digest preview, hygiene stats, anomaly detection + absorbs the parked Q7 idea as Andy-facing weekly social-DM digest). **Phase 5 sizing:** 8 sessions (A: Graph API integration / B: data model + threading + routing + 12mo import / C: inbox UI + compose + Conversation view / D: reply drafting + refine-chat + voice learning / E: notifications + PWA + morning digest / F: signal/noise + auto-delete + hygiene / G: support@ ticket overlay + classification / H: cross-spec integration surfaces). A+B must land very early in Phase 5 as foundation-layer builds — multiple other specs depend on the `messages`+`threads` tables. C follows immediately; D–G can parallel after C; H is integration work sprinkled through later sessions. | [phase-3-unified-inbox-handoff.md](sessions/phase-3-unified-inbox-handoff.md)
12. ~~`docs/specs/daily-cockpit.md`~~ ✅ **LOCKED 2026-04-13** — 9 questions resolved. The operator's morning surface at `/lite`. Three-zone default layout: narrative brief at top (SuperBad voice, 2–3 sentences) + tactile attention rail (horizontal, up to 6 chips "waiting on Andy" + `+N more` overflow to `/lite/waiting`) + conditional system-health banner strip (up to 2 banners + overflow to `/lite/health`) + calendar preview band + switchable kanban/list planning view below (inherits Task Manager's locked column rules — do not re-derive). **Three daily briefs** (morning 06:00, midday 12:00, evening 18:30 Melbourne) with chained continuity + compact event trail from `activity_log` filtered via an allow-list (`getAndyFacingActivitySince()`); midday reads morning brief prose + today's event log; evening reads both + expanded log + tomorrow's calendar + tomorrow's scheduled_tasks. **Material-event regen** of current slot's brief via `scheduled_tasks` (new task type `cockpit_brief_regenerate`, 10-min debounce per slot, chain-anchored to the original morning brief to prevent hallucination chains). **Quiet-slot skip** precondition check (zero waiting items + zero material events + zero banners) renders a curated fallback line from a rotation pool instead of calling Opus. **Fleet-scoped chips** fold into the same rail with a small `[Fleet]` tag when firing; no dedicated fleet band. **No chip dismissal or snooze** — rail is purely derived; resolution at the source spec is the only clearing mechanism. **Mobile PWA layout:** vertical stack, horizontal-scroll rail, tabbed kanban (Must Do / Should Do / If Time) with house-spring motion, list toggle icon; cockpit is default PWA route post-install. **New table:** `cockpit_briefs` (unique constraint on user_id + slot + brief_date; regen overwrites with audit trail in activity_log). **1 Claude prompt:** `cockpit-brief` (Opus, slot parameter, three prompt templates morning/midday/evening). **`activity_log.kind` gains ~8 values.** `scheduled_tasks.task_type` gains 1 value (`cockpit_brief_regenerate`). **9 new build-time disciplines (65–73).** **Sprinkle claims:** §2 browser tab title on `/lite` (state-reflecting voice treatments), §2 signed-in admin greeting line above the brief (rotation pool). **New ambient category proposed for S&D closed list (6 → 7):** quiet-slot fallback lines — pending sprinkle-promotion brainstorm gate. **Cross-spec patches owed at Phase 3.5:** 11 locked specs add `getWaitingItems()` contract (Quote Builder, Branded Invoicing, SaaS Billing, Unified Inbox, Task Manager, Client Management, Lead Generation, Intro Funnel, Brand DNA Assessment, Content Engine, Client Context Engine); 8 locked specs add `maybeRegenerateBrief(eventKey, payload)` callsite wiring (SaaS Billing for `subscription_payment_failed` + `subscription_cancelled`, Sales Pipeline for `deal_won` + `deal_lost`, Branded Invoicing for `invoice_paid_large`, Intro Funnel for `intro_funnel_booking_confirmed`, Lead Gen for `outreach_reply_positive`, Content Engine for `content_engine_domain_verification_failed`, Unified Inbox for `graph_api_subscription_lapsed`, Finance Dashboard/Observatory for `cost_anomaly_detected`); 4 specs add `getHealthBanners()` contract (Unified Inbox, Content Engine, Finance Dashboard, SaaS Billing). **No new memories. No new motion slots. No new sound slots. No new specs spawned. No Foundations patches owed beyond what's already flagged.** **Content mini-session owed** (small-medium — three brief prompt templates + quiet-slot fallback pool + admin greeting pool + tab title voice treatments + chip label conventions per source_spec + `/lite/waiting` + `/lite/health` copy + calendar preview empty-state). Must run before Phase 5 Sessions A + B. **Phase 5 sizing:** 5 sessions (A: briefs data + morning slot + cron / B: midday + evening + event trail + chaining + material-event regen + quiet-skip + fallback pool / C: attention rail + `/lite/waiting` / D: banner strip + calendar preview + `/lite/health` / E: mobile PWA layout + kanban tabs). A + B are voice-critical and require the content mini-session. Cockpit is a Phase 5 *middle* build, not late — ship the skeleton early so downstream specs wire their contracts as they land. | [phase-3-daily-cockpit-handoff.md](sessions/phase-3-daily-cockpit-handoff.md)
13. `docs/specs/setup-wizards.md` — cross-cutting primitive
14. `docs/specs/hiring-pipeline.md` — parallel CRM for headhunting, reuses Pipeline's KanbanBoard + activity_log primitives
15. **`docs/specs/surprise-and-delight.md`** — ✅ **PRE-WRITTEN 2026-04-12 mini-brainstorm #3** (see `sessions/phase-3-surprise-and-delight-brainstorm-handoff.md`). Cross-cutting voice + hidden eggs feature. Two-layer model: 80% ambient Claude-generated voice in a closed list of 6 surface categories + 20% rare Metal Gear Solid-tradition platform-awareness eggs (1 admin canonical + 12 public catalogued). Admin = roommate voice, public = bartender voice, never pitches. Public knowledge ceiling (browser-free data only). Cadence: 1 guaranteed first-visit egg for public + max 2/14d thereafter; max 1/7d for authenticated users. Crossover preserves earned state. Context-aware suppression is hard-gated (mid-payment, wizards, errors, first session, etc.). Riddle loop via shared `resolveRiddleAnswer()` resolver. Hybrid wrong-answer branching (pre-generated common + live Claude fallback capped ~100/riddle lifetime). Kill switch in Settings → Display. **New tables:** `riddles`, `riddle_resolutions`, `hidden_egg_fires`, `ambient_copy_cache`. **New user columns:** `last_hidden_egg_fired_at`, `hidden_egg_tricks_enabled`, `fired_egg_ids_recent`. **New Claude primitives:** `generateInVoice()` and `resolveRiddleAnswer()`. **New build-time disciplines:** 19–23. **Closed-list exemptions:** hidden-layer CRT motion + rain sound explicitly exempt from Tier 2 motion and sound registry because they fire ≤1x/month/user. **Silent dependency:** this spec cannot be **built** before `brand-dna-assessment.md` (#5) ships AND Andy has taken SuperBad's own assessment — the drift check needs SuperBad's Brand DNA profile as grading substrate. Phase 5 build order: Brand DNA feature → SuperBad's own assessment run → Surprise & Delight build. **Follow-up brainstorm owed:** 3–5 more admin eggs beyond the canonical CRT turn-off, before Phase 5 build of this feature. **New memory:** `feedback_surprise_and_delight_philosophy.md` (canonical philosophy document — every future spec touching a user-facing surface reads this).
16. **`docs/specs/task-manager.md`** — ✅ **LOCKED 2026-04-12 mini-brainstorm #4** (see `sessions/phase-3-task-manager-brainstorm-handoff.md`). Operational-spine feature with two surfaces: Task Manager module at `/lite/tasks` (list view + detail drawer + filters + bulk delete) and globally-available Braindump primitive (floating button + `Cmd+Shift+D` shortcut on admin surfaces only, modal with in-place Claude parse → edit → commit flow, `⇄` swap affordance on ambiguous entity candidates). **Load-bearing architectural decision:** deliverables are tasks — `docs/specs/client-management.md` references this spec rather than defining its own deliverables data model. **Closed enums:** `kind` (5 values: `personal` / `admin` / `prospect_followup` / `client_deliverable` / `client_task`), `status` (7 values: `todo` / `in_progress` / `blocked` / `awaiting_approval` / `delivered` / `done` / `cancelled`), `recurrence` (6 presets). State machine validated in `lib/tasks/transitions.ts`. Polymorphic-by-convention entity linking via `(entity_type, entity_id)` following the activity_log pattern. **Approval workflow for deliverables:** dual entry — tokenised magic-link email (transactional, bypasses §11.4 quiet window, 48h reminder respects it) + direct portal visit, both routing through single `approveDeliverable()` primitive in `lib/tasks/approve.ts`. Rejection feedback fires as inbound message in comms inbox with `source='task_rejection'`. **Cockpit integration (read-only):** tasks are a cockpit data source with locked column rules (Must Do / Should Do / If Time filters on due date + priority + status); `awaiting_approval` and `blocked` stay off the attention surface and surface as passive counts in the narrative brief. **Notifications intentionally minimal:** zero push on Andy's own tasks; morning digest at 08:00 Melbourne gated by two conditions (Andy not signed in by 08:00 AND something to report); braindump ingestion silent. **Recurrence:** auto-spawn on completion via `markTaskDone()`, not calendar cron; irregular patterns handled manually in v1. **Checklists:** optional inline JSON, parser emits for countable phrasings, `checklist_auto_complete` default true with 10s visible undo window. **New tables:** `tasks`, `braindumps`. **New Claude primitives:** `parseBraindump(rawText, surfaceContext?)` (Haiku-tier, reads Brand DNA + recent contacts for entity matching), `approveDeliverable(taskId, decision, feedback?)`, `generateInVoice('task_digest_subject', context)` (from S&D). **New build-time disciplines:** 24–29. **Cross-spec impacts:** client-management.md gets a deliverables flag (do not define local data model); daily-cockpit.md gets a tasks-as-data-source flag with locked column contract; comms-inbox.md gains `linked_task_id` nullable column + `'task_rejection'` source value; client-portal render rules defined via `getTasksForClientPortal()` only. **Foundations §11 patch owed in Phase 5:** `sendEmail()` gate (§11.2) gains required `classification: 'transactional' | 'outreach'` parameter — transactional bypasses §11.4 quiet window, outreach respects it. **Sprinkle claimed:** §2 browser tab title (dynamic — `"SuperBad Lite — 4 overdue"` vs `"SuperBad Lite — nothing's on fire"`); mark `[CLAIMED by task-manager]` in `docs/candidates/sprinkle-bank.md`. **No silent dependencies** — can build as soon as Phase 5 starts; pre-Brand-DNA/pre-S&D fallback is static dry lines on `generateInVoice()` calls. **No new memories.**

17. ~~`docs/specs/finance-dashboard.md`~~ ✅ **LOCKED 2026-04-13** — 9 questions resolved. Operator-only. Headline: P&L tile + 90-day projection chart side-by-side above fold. Projection = contracted MRR + pipeline-weighted open deals with stage-age decay (halves every 30d past expected dwell); trial shoots at 100%. Expenses: manual quick-add + `recurring_expenses` cron-booking (pending-review gate) + daily Observatory rollup (API costs) + daily Stripe Balance Transactions rollup (Payment processing). Accountant bundle export: BAS summary PDF + P&L PDF + transactions/expenses/invoices/per-client CSVs, zipped, preset BAS quarters + EOFY + custom range, inc/gst/ex on every row. Below fold: Claude-Haiku narrative with clickable number-references (validation + fallback) + tiles (MRR & net run-rate / outstanding invoices / top 5 categories / tax provision) + recent transactions feed. Cockpit banners: `finance_bas_due` + `finance_eofy_due` + `finance_invoice_overdue` only (tax-tight is dashboard-only, state-not-action). One global time-range picker, projection immune (always forward 90d), URL-encoded deep-linkable. Shallow drill-down per tile + row click-through to owning feature. Tax provision: GST owed / income tax provisioned / yours-to-spend (Stripe balance proxy, no open-banking in v1). **New tables:** `expenses`, `recurring_expenses`, `finance_snapshots`, `compliance_milestones`. **1 Haiku LLM job** (`finance-narrative` — structured output with number-reference validation). **2 new Stripe integration jobs** (`stripe-balance-read`, `stripe-balance-transactions-read`). **`scheduled_tasks.task_type` gains 6 values** (`finance_snapshot_take`, `finance_narrative_regenerate`, `finance_observatory_rollup`, `finance_stripe_fee_rollup`, `recurring_expense_book`, `finance_export_generate`). **`activity_log.kind` gains 8 values.** **11 new settings keys** (`finance.*`). **Sprinkle claims:** narrative opening lines rotation pool + `/lite/finance/*` browser tab titles. **No new motion/sound slots. No new memories. No new specs spawned.** **Content mini-session owed** (small-medium — narrative prompt calibration × 8–10 scenarios + tax-tight/projection-cliff/overdue-heavy callout pools + 3 BAS banner variants by days-remaining + empty states + tab titles). **Cross-spec flags for Phase 3.5:** Daily Cockpit (3 new `getHealthBanners()` kinds + `maybeRegenerateBrief()` subject discriminator confirmation), Branded Invoicing (`?filter=overdue` query param + Puppeteer multi-document export confirmation), Observatory (add `finance-narrative` + 2 Stripe jobs to model registry), Setup Wizards (add `finance-tax-rates` onboarding step writing `finance.gst_rate` + `finance.income_tax_rate`). **Phase 5 sizing:** 4 sessions (A: data model + expense form + settings / B: projection logic + roll-up crons + Stripe Balance API / C: dashboard UI + narrative prompt + drill-downs / D: accountant bundle + cockpit banners + onboarding step). | [phase-3-finance-dashboard-handoff.md](sessions/phase-3-finance-dashboard-handoff.md)

18. ~~`docs/specs/cost-usage-observatory.md`~~ ✅ **LOCKED 2026-04-13** — 9 questions resolved. Dual primary job: alarm + attribution co-equal, forensic queryability free. Four-actor model (`internal | external | shared | prospect`) with single-row log + ephemeral hashed `shared_cohort_id` + query-time proration. Three anomaly detectors in parallel (hard thresholds + rate loop-catcher + learned bands with 7-day/50-call warmup). Severity-tiered banners (low dry-calm / mid neutral-louder / severe terse-flat-red with kill-switch). Prospect-phase spend transfers to subscriber/client on conversion via atomic rewrite in conversion handler, preserving `converted_from_candidate_id` for audit — **CPA, CPA-by-channel, CPA-by-ICP-segment, payback period all derivable**. Attribution reoriented around tier health (aggregate per-tier margin + % underwater), with per-subscriber recommendation cards **only rendered for Large-tier** subscribers (Small/Medium negative margin = tier-design bug surfaced at tier level — anchored in new memory `project_tier_limits_protect_margin.md`). Operator-only costs (never expose AUD to subscribers); two separate ledgers (`usage_records` subscriber-facing + `external_call_log` operator-facing) never merged. Claude diagnosis card (Opus prompt `diagnose-cost-anomaly.ts`) on top of raw-data expandable table; one-click band editor on anomaly detail view. No auto-pause, no platform hard cap; Andy-set monthly AUD thresholds (up to 3) + linear run-rate projection alert. **New tables:** `external_call_log`, `cost_anomalies`, `deploy_events`, `observatory_settings`. **Model/integration registry** (`lib/ai/models.ts` + `lib/integrations/registry.ts`) — required bands at registration, ESLint-enforced no-direct-vendor-SDK-imports in feature code. **3 Claude prompts** (2 Opus, 1 Haiku). **`activity_log.kind` gains 10 values.** `scheduled_tasks.task_type` gains 5 values (three detectors + diagnoser + weekly digest). **Foundations §11 patch consolidates** send-gate `classification` parameter (owed from Task Manager), LLM model registry (owed from memory), external-call observability primitive, quarterly model-review ritual — authored by Phase 5 Session A. **Sprinkle claimed:** §5 weekly digest email subject line (dry MTD-posture rotation). **New memories:** `feedback_technical_decisions_claude_calls.md` (don't ask Andy to pick between technical options), `project_tier_limits_protect_margin.md` (tier limits protect margin; negative margin Small/Medium = tier-design bug). **Cross-spec flags for Phase 3.5:** every integration-using spec must specify `actor_type`/`actor_id` at each external-call site (~14 specs); Daily Cockpit patches `getHealthBanners()` to include the 5-kind `ObservatoryBanner` union; SaaS Billing notes `usage_records` vs `external_call_log` ledger separation; Lead Gen specifies conversion-handler atomicity; Intro Funnel specifies trial-shoot Stripe attribution as `prospect`; every LLM-using spec declares `prompt_version_hash` convention. **Content mini-session owed** (small-medium — banner body pools × 3 tiers, diagnosis prompt calibration across 8–10 synthetic scenarios, dashboard empty-states, weekly digest framework, threshold/projection banner copy, kill-switch modal copy). **Phase 5 sizing:** 3 sessions (A: logging primitives + registry + every vendor SDK wrapper + FOUNDATIONS patch + deploy webhook / B: three detectors + anomaly dedupe + diagnosis prompt + Cockpit banner wiring + severe-tier email + kill-switch plumbing + prompt-version tracking / C: observatory dashboard + anomaly detail + job detail + settings + projection tile + tier-health panel + Large-tier recommendation cards + weekly digest). A+B partially parallelisable; C depends on B. | [phase-3-cost-usage-observatory-handoff.md](sessions/phase-3-cost-usage-observatory-handoff.md)

19. `docs/specs/six-week-plan-generator.md` — **NEW (2026-04-13, requested by Andy)**. Autonomous generation of the bespoke 6-week, week-by-week marketing plan included with every $297 trial shoot (per memory `project_trial_shoot_facts.md`). Plan must function as a real retainer alternative — self-runnable by the prospect, not a teaser — because the Intro Funnel's retainer-fit recommendation (§13.4) frames "take-the-plan-and-run" as a credible third outcome alongside retainer and not-now. Inputs: Brand DNA profile (assessment is part of the trial shoot flow) + Lead Gen's 9-signal enrichment + shoot-day context (conversation notes, on-site observations, delivered asset themes) + SuperBad's own Brand DNA for voice grounding. Output: branded weekly plan with per-week concrete tasks, channel mix, content angles, KPIs/success signals, and "what good looks like" framing. Surfaces into Intro Funnel step 8 as a second sub-surface alongside the Pixieset gallery. **Must run before the Intro Funnel trial-shoot-facts patch** (#31 in Deferred tasks) so the patch can reference this spec's pipeline rather than handwaving "Opus-drafted plan from Brand DNA + enrichment + shoot context". Expected artefacts: ≥1 new table (likely `trial_marketing_plans` with versioning for regen), 1–2 Opus prompts (initial draft + material-event regen), Andy review + regenerate affordance before client sees it, branded web view + PDF export via shared renderer, delivery email template, portal sub-surface, ownership of plan after conversion to retainer (becomes early client context). Cross-spec dependencies to watch in brainstorm: Brand DNA (reads profile), Lead Gen (reads enrichment), Intro Funnel (deliverables reveal + retainer-fit framing + cancellation — what happens to plan if client forfeits booking), Client Context Engine (plan becomes perpetual context on retainer conversion), Task Manager (plan weeks may spawn as client deliverables post-conversion), Surprise & Delight (voice & delight treatment + potential sprinkle claim on cover line), Cost & Usage Observatory (`actor_type: 'prospect'` logging, transfers to `client` on conversion), Content Engine (does the plan integrate with Content Engine if prospect converts and takes it?). **Content mini-session likely owed** (plan week-structure framing, per-channel language defaults, delivery email, empty/regen states). Best sequenced before or immediately after Setup Wizards (#13) so Phase 3.5 review sees the complete trial-shoot deliverable surface.

When Andy says "let's go", begin the next Phase 3 session following the protocol in `START_HERE.md`.

---

## Phase roadmap

- [ ] **Phase 0** — Pre-flight scaffolding *(done in the HQ session, before this project opened — see `docs/phase-0-scaffold-note.md`)*
- [x] **Phase 1** — Scope brainstorm → `SCOPE.md` *(locked 2026-04-11)*
- [x] **Phase 2** — Foundations brainstorm → `FOUNDATIONS.md` *(locked 2026-04-11)*
- [x] **Phase 3** — Feature specs → `docs/specs/*.md` *(21 specs locked 2026-04-12 through 2026-04-13)*
- [x] **Phase 3.5** — Spec review (exit gate) → patches applied across all docs, `sessions/phase-3.5-exit-handoff.md` *(locked 2026-04-13)*
- [x] **Phase 4** — Build plan → `BUILD_PLAN.md` + `AUTONOMY_PROTOCOL.md` + `LAUNCH_READY.md` *(locked 2026-04-13 — BUILD_PLAN.md: 23 waves, ~150 atomic sessions, 60+ cron jobs, shared-primitive registry, 5 critical-flow E2E assignments, rollback strategy per wave; AUTONOMY_PROTOCOL.md: 13 non-skippable per-session gates + model-tiering + Opus-quota pause behaviour + kill-switches + observability discipline; LAUNCH_READY.md: 12-section pre-launch gate checklist)*
- [ ] **Phase 5** — Build execution → session by session
- [ ] **Phase 6** — Launch → `superbadmedia.com.au/lite` live

Phase 0 stays unchecked in this tracker because the session that did it (HQ) doesn't count as a Lite session — this tracker begins with Phase 1.

---

## Session log

| Session | Phase | Type | Summary | Handoff note |
|---|---|---|---|---|
| 2026-04-11 | 1 | Brainstorm | Scope locked — 6 core areas all in v1, setup wizards + unified inbox as cross-cutting, no fixed rate card, per-client retainer pricing inside Lite, zero manual media authoring | [phase-1-handoff.md](sessions/phase-1-handoff.md) |
| 2026-04-11 | 2 | Brainstorm | Foundations locked — Next.js App Router on reused HQ Coolify, SQLite+Drizzle+Litestream→R2, Auth.js v5 magic-link, Resend for all email, Stripe everything, shadcn/ui + Framer Motion + use-sound. Lite ships at `lite.superbadmedia.com.au` first, migrates to `/lite` path at launch. GHL gets switched off at cutover — nothing worth preserving. | [phase-2-handoff.md](sessions/phase-2-handoff.md) |
| 2026-04-11 | 3 | Spec | Design system baseline locked — surface strategy (warm stacked tints), 2-tier motion (house spring + 7 closed Tier 2 moments + 2 overlays), 2 density presets, graduated soft radius, BHS closed-list of 8 locations, Settings → Display panel (4 accessibility toggles + 3 theme presets + 3 typeface presets), pink as customer-warmth + focus, full token system, 11 build-time disciplines. New memory: curated brand-canonical customisation pattern. | [phase-3-design-system-baseline-handoff.md](sessions/phase-3-design-system-baseline-handoff.md) |
| 2026-04-11 | 3 | Spec | Sales Pipeline locked — 8 stages (Lead → Contacted → Conversation → Trial Shoot → Quoted → Negotiating → Won → Lost), three-entity CRM (Company + Contact + Deal), `billing_mode` flag for manual-billed companies, append-only activity log with three nullable FKs, two-tier hover card (desktop only), per-stage stale halo + snooze, Loss Reason modal with closed list of 7, Trial Shoot panel on Company profile, full Drizzle schema + validation rules, Stripe + Resend webhook idempotency from day one. **3 new specs spawned:** Branded Invoicing, Intro Funnel, Hiring Pipeline. **SCOPE.md alignment change:** Trial Shoot is now an explicit stage (was Conversation in SCOPE). **New memory:** SuperBad client size diversity (Andy correction). | [phase-3-sales-pipeline-handoff.md](sessions/phase-3-sales-pipeline-handoff.md) |
| 2026-04-11 | 3 | Scope patch | Mini-brainstorm added 3 new features to v1: **Content Engine** (semi-autonomous SEO blog + newsletter + social drafts with stub publish adapters, SerpAPI-driven keyword research, Cloudflare path routing to `/blog`, one-gate review with Claude rejection chat), **Brand DNA Assessment** (NASA-style dual-mode psychological + taste profile, 50–100 indirect-signal MC questions per bank, shape-aware multi-stakeholder blending, signal-tag output, perpetual LLM context across platform), **Premium Onboarding + Revenue Segmentation** (composes Brand DNA into retainer onboarding, standardised fast SaaS onboarding with dashboard-nudged Brand DNA as upsell signal, double segmentation layer). **Non-goal redrawn:** newsletter is now in scope for SuperBad's own owned audience; third-party campaign management remains out. **New memory:** Brand DNA as perpetual LLM context. | [phase-3-scope-patch-handoff.md](sessions/phase-3-scope-patch-handoff.md) |
| 2026-04-12 | 3 | Upgrades integration | Integrated the 7 "easy-fit" upgrades from `where-we-are.html § 4`. **5 cross-cutting primitives** added to `FOUNDATIONS.md` as new §11 (Universal Audit Log, Safe-To-Send Gate, Timezone-Correct Timestamps, Outreach Quiet Window, Brand-Voice Drift Check) with 5 new build-time disciplines (13–17). **2 features** folded into existing planned specs via new `SCOPE.md` "Additional v1 features (2026-04-12 upgrades shortlist)" section: Sentiment-Aware Reply Drafts (into `unified-inbox.md`), One-Click Client Data Export (into `client-management.md`). **No new spec sessions added** — feature additions reference their parent specs. Next Action block updated with the cross-cutting constraints the Lead Gen spec must honour. | [phase-3-upgrades-integration-handoff.md](sessions/phase-3-upgrades-integration-handoff.md) |
| 2026-04-12 | 3 | Spec | Lead Generation locked — 13 questions resolved. Operations-pipeline model: automated daily 3am run, one unified search, two tracks (SaaS + retainer) with winner-takes-all scoring against qualification floors, 9-signal enrichment (Meta Ad Library, Google Maps, Google Ads Transparency, PageSpeed, whois, Instagram BD, YouTube API, website scrape, Maps extras), Hunter.io contact discovery with pattern fallback, single approval queue with per-track earned autonomy graduation (manual → 10 clean approvals → probation → 5 sends → auto_send with rolling-20 at 80% maintenance floor + 4 circuit breakers), non-overrideable 4-week warmup ramp (5/10/15/20/30), engagement-gated indefinite sequences with 4-tier engagement model (click > full open > sub-60s open > none, 3 consecutive non-engagements ends sequence), three-tier DNC with single `isBlockedFromOutreach()` enforcement, single sender `Andy Robinson <hi@contact.superbadmedia.com.au>`, three-tab surface (Queue / Runs log / Metrics with closed list of 4 panels), Candidate → Deal promotion at first-send time via `createDealFromLead()`. **New tables:** `lead_candidates`, `outreach_drafts`, `outreach_sends`, `outreach_sequences`, `lead_runs`, `dnc_emails`, `dnc_domains`, `resend_warmup_state`, `autonomy_state`. **Cross-spec flags:** Pipeline `contacts.email_status` enum + `activity_log.kind` enum need non-breaking additions. **No new specs spawned.** **No new memories.** | [phase-3-lead-generation-handoff.md](sessions/phase-3-lead-generation-handoff.md) |
| 2026-04-12 | 3 | Scope patch | Mini-brainstorm added 1 new v1 **primitive**: **Client Context Engine** — per-contact always-on Claude engine storing comms + activity + action items + deal/delivery/invoice state + Brand DNA as invisible context, surfacing a living human-facing summary on every profile, producing hybrid one-shot-plus-nudge channel-aware draft replies on click, auto-extracting action items (with owners "you" vs "them") from every message with manual override. Active summaries (Haiku-tier, regen on material events) + reactive drafts (Opus-tier) + signals feed the Daily Cockpit, not a second attention surface. Reuses the Content Engine rejection-chat primitive for draft nudging — zero new chat infra. Treated as a primitive, not a feature: composed by Lead Gen, Client Management, Daily Cockpit, Sales Pipeline card hover; Lead Gen's "generate email" button becomes a call into this primitive with empty-history fallback. Pairs architecturally with Brand DNA as the "where you are in the conversation" half of the perpetual-context pair. Spec positioned at #7 in the backlog (after Onboarding + Segmentation, before Client Management). **New memory:** `project_two_perpetual_contexts.md` — Brand DNA + Context Engine must both be read on every client-facing LLM call. | [phase-3-context-engine-scope-patch-handoff.md](sessions/phase-3-context-engine-scope-patch-handoff.md) |
| 2026-04-12 | 3 | Spec | Intro Funnel locked — 16 questions resolved. Customer-facing acquisition surface for the $297 trial shoot, reframed from "sales funnel" to "branded experience that happens to convert." Full flow: on-brand editorial landing page → section 1 contact form (creates Deal + portal session immediately) → fast-dopamine portal access → shape-branched self-persuasion questionnaire (solo_founder / founder_led_team / multi_stakeholder_company × 4 sections = 12 banks) → Stripe Payment Element inline with dynamic amount from Settings → native calendar booking (not Cal.com) with 5-business-day advance notice + 3-per-week cap → trial shoot → Pixieset API native gallery reveal → 24h later post-shoot reflection questionnaire (8 screens, linear arc, Q1 safety valve, Claude synthesis reveal) → Claude-generated retainer-fit recommendation (Opus, structured JSON with recommendation_type + confidence + reasoning + flags) → Daily Cockpit for Andy's review → fork at Won into Client Management portal OR dormant on non-conversion. Aggressive abandon cadence 15m SMS→24h SMS+email→3d email→demote to cold outreach with context flag. Cancel/reschedule policy: 48h refund window, max 2 reschedules, auto-Lost on 3rd attempt; SuperBad-initiated cancel/reschedule always fully refundable regardless of timing. **New tables:** `intro_funnel_submissions`, `intro_funnel_payments`, `intro_funnel_bookings`, `intro_funnel_reflections`, `intro_funnel_retainer_fit`, `intro_funnel_config`, `calendar_bookings` (polymorphic, shared), `calendar_config`, `dnc_phones`, `twilio_sms_log`. **Deal creation at section 1 submit** (not at first-send like Lead Gen — section 1 is strong intent, Trial Shoot is an active stage). **New stack integration:** Twilio (non-breaking FOUNDATIONS addition). **Pixieset integration** via API for native gallery, URL fallback. **Cross-spec flags:** Pipeline needs 6 `deals` cols + 3 `contacts` cols + 21 activity_log.kind values + 7 lost_reason values (closed list 7→14) + 2 Trial Shoot sub-statuses + 8 Trial Shoot panel fields. Design-system-baseline needs revisit for 3 new Tier-2 motion candidates (Payment Element reveal, synthesis reveal, deliverables gallery reveal). **New memory:** `feedback_dont_undershoot_llm_capability.md` — Andy corrected my "defer AI feature to v1.1 for training data" reflex; Claude reasons from rich context + described criteria without labelled data. **Follow-up mini-session owed:** content authoring for landing page + 9 question banks + reflection text + email templates. **No new specs spawned.** | [phase-3-intro-funnel-handoff.md](sessions/phase-3-intro-funnel-handoff.md) |
| 2026-04-12 | 3 | Mini-brainstorm + spec pre-write | Surprise & Delight locked as cross-cutting v1 feature — two-layer model (80% ambient Claude-generated voice in a closed list of 6 surface categories + 20% rare Metal Gear Solid-tradition platform-awareness hidden eggs), tone asymmetry (admin = roommate voice, public = bartender voice, never pitches), public knowledge ceiling (browser-freely-given data only — never IP geoloc / ISP / GPS), cadence (1 guaranteed first-visit egg for public + max 2/14d thereafter; max 1/7d for authenticated users; crossover preserves earned state), context-aware suppression hard-gated during mid-payment/wizards/errors/first-session, riddle loop via shared `resolveRiddleAnswer()` resolver with hybrid wrong-answer branching (pre-generated + live Claude fallback capped ~100/riddle), kill switch in Settings → Display. Canonical admin egg locked (CRT turn-off after three 2am sessions). 12 public eggs catalogued and approved. **Pre-written as `docs/specs/surprise-and-delight.md` at backlog position #15** — unusual for a scope patch session, but the brainstorm surfaced enough locked detail that the spec work was near-complete. Still has a silent dependency on `brand-dna-assessment.md` for build ordering: Phase 5 must ship Brand DNA first, then have Andy take SuperBad's own assessment, then build Surprise & Delight. Hidden-layer effects (CRT motion + rain ambient sound) are **explicitly exempt** from the Tier 2 motion closed list and the sound registry because they fire ≤1x/month/user; exemptions are named in the spec. 5 new build-time disciplines added (19–23). **Follow-up brainstorm owed:** 3–5 more admin eggs before Phase 5 build. **New memory:** `feedback_surprise_and_delight_philosophy.md` — every future spec touching a user-facing surface reads this. | [phase-3-surprise-and-delight-brainstorm-handoff.md](sessions/phase-3-surprise-and-delight-brainstorm-handoff.md) |
| 2026-04-12 | 3 | Spec | Quote Builder locked — 21 questions resolved. The most load-bearing Phase 3 spec by margin: first feature where money actually moves (Stripe Payment Intent + Subscription + webhook idempotency end-to-end), introduces the shared `scheduled_tasks` primitive + single Node cron worker on Coolify that every subsequent spec inherits, and carries a large cross-spec flag list. Stage-gated proposal structure (5 scroll-snap sections), Claude draft-from-context default + template + blank, retainer/project/mixed structures via Stripe `add_invoice_items`, per-quote term + expiry, GST-inclusive canonical storage (surfaced the "felt experience wins" principle), 7-state machine with view tracking, structured catalogue + `tier_rank`, two-pane editor with live preview, Claude-drafted send email + drift check + Andy review, deliberate-second-artefact PDF (not web-page compression), **same-page cinematic Payment Element reveal via Framer `layoutId`** inheriting Intro Funnel's existing Tier-2 slot (no new motion slot spent), shared-shell billing-mode-specific confirmation, pyramid-synthesis intro paragraph applying `feedback_client_doc_source_hierarchy`, supersede-via-new-row edit-after-send with hash-based proof-of-acceptance. **Q21 cancel flow is the longest lock:** Billing Portal cancel disabled, custom Lite-UX-layer flow at `/lite/portal/subscription` (owned by future Client Portal spec) with pre-term retainer options (*Let's chat* / pay remainder / 50% buyout), pre-term SaaS options (pay remainder / 1-month pause / continue), post-term retention (upgrade / downgrade / "here's what you'd be losing" / cancel through — no discounts ever). **Scheduled-tasks primitive:** polymorphic `task_type` with handler map, idempotency keys on every row, stale-row reclaim every 15 min, heartbeat table read by cockpit for red-banner alerting, exponential retry backoff, all handlers inherit §11.4 quiet window via `sendEmail()`. 5 Quote Builder handler types enumerated (`quote_expire` / `quote_reminder_3d` / `manual_invoice_send` / `subscription_pause_resume_reminder` / `subscription_pause_resume`). **New tables:** `quotes`, `catalogue_items`, `quote_templates`, `scheduled_tasks`, `worker_heartbeats`. **Pipeline cross-spec flags:** `won_outcome = 'project'` + 5 new `deals` columns + `companies.gst_applicable` + 18 new `activity_log.kind` values. **Sound registry:** 1 new slot `sound:quote_accepted` (admin-cockpit-only, visibility-gated). **Sprinkle claimed:** §3 Quote PDF cover line. **Motion slots spent:** 0 new (Payment Element reveal inherits). **Memory promotion flagged:** `feedback_felt_experience_wins` — when convention and felt experience conflict, felt experience wins unless compliance forces the convention. Surfaced twice in the brainstorm (Q6 GST, Q21 cancel flow). **Content mini-session owed:** catalogue content + all client-facing copy + email templates + cancel flow copy + PDF cover line decision + terms page. **Phase 5 sizing:** 3–4 sessions minimum (Data+Stripe / Scheduled-tasks / Draft editor+send / Client page+confirmation+cancel flow). **No new specs spawned.** | [phase-3-quote-builder-handoff.md](sessions/phase-3-quote-builder-handoff.md) |
| 2026-04-12 | 3 | Spec | Branded Invoicing locked — 25 questions resolved. Manual-billed-company invoice generation with ATO-compliant tax invoices, two-step generate/send primitive, 3-day pre-send review window, self-perpetuating monthly task chain (two tasks per cycle), per-company payment terms, bank transfer + Stripe online payment via inline Payment Element (inherits Tier-2 slot), full branded two-section scroll-snap web view, shared PDF renderer with Quote Builder, 5-state lifecycle, automated + manual reminders, standalone index + company billing tab, void + reissue via supersede. Refines Quote Builder's handler integration (first cycle enqueue → `manual_invoice_generate`, send handler narrows to send-only). **New table:** `invoices`. **Sprinkle claimed:** §3 Invoice PDF footer (split treatment). **No new motion/sound slots. No new specs spawned. No new memories.** Memory promotion completed: `feedback_felt_experience_wins.md` (owed from Quote Builder). | [phase-3-branded-invoicing-handoff.md](sessions/phase-3-branded-invoicing-handoff.md) |
| 2026-04-12 | 3 | Spec | Brand DNA Assessment locked — 30 questions resolved. Full psychological + aesthetic + communication profile. Three tracks (Founder / Business / Founder + Supplement) routed by alignment gate. Shape-aware sectioned banks (31 total). Card-per-question UI with ambient visual evolution. Between-section Opus insights (×4, human register). Cinematic reveal: first impression → beat → section-by-section build. Multi-stakeholder blending with divergence flags. Hard portal lock for retainer/SaaS, optional for trial shoot. Tiered downstream injection (full for Opus, tags-only for Haiku). SuperBad's own profile as Phase 5 milestone. **4 new tables.** **5 Opus prompts.** **1 new Tier-2 motion slot + 1 new sound.** **New memory:** `feedback_no_lite_on_client_facing.md`. **Content mini-session owed** — largest in the project. | [phase-3-brand-dna-assessment-handoff.md](sessions/phase-3-brand-dna-assessment-handoff.md) |
| 2026-04-12 | 3 | Spec | Task Manager (+ Braindump primitive) locked — 10 questions resolved through mini-brainstorm #4. Operational-spine feature with two surfaces: Task Manager module at `/lite/tasks` with list view + right-side detail drawer + filters + bulk delete, and a globally-available Braindump primitive (floating button + `Cmd+Shift+D` shortcut on admin surfaces only, in-place Claude parse → edit → commit modal flow, `⇄` swap affordance for ambiguous entity candidates, raw text persisted in `braindumps` table). **Load-bearing architectural decision:** deliverables are tasks — Client Management references this spec rather than defining its own deliverables data model. Single universal task model with closed `kind` enum (5 values) + closed `status` enum (7 values) validated via state machine in `lib/tasks/transitions.ts` + preset `recurrence` enum (6 values) auto-spawning on completion via `markTaskDone()` not calendar cron. Polymorphic-by-convention entity linking via `(entity_type, entity_id)` following the `activity_log` pattern. Approval workflow via single `approveDeliverable()` primitive with dual entry (tokenised magic-link email bypassing §11.4 quiet window + direct portal visit); rejection feedback fires as inbound message in comms inbox with new `source='task_rejection'`. Notifications intentionally minimal: zero push on Andy's own tasks, cockpit is primary surface, morning digest at 08:00 Melbourne gated by two conditions (Andy not signed in by 08:00 AND something to report), braindump ingestion silent. Cockpit integration is read-only with locked column contract (Must Do / Should Do / If Time filter rules; `awaiting_approval` + `blocked` stay off attention surface, surface as passive counts in narrative brief). **New tables:** `tasks`, `braindumps`. **New Claude primitives:** `parseBraindump(rawText, surfaceContext?)` (Haiku-tier, reads Brand DNA + recent contacts for entity matching), `approveDeliverable(taskId, decision, feedback?)`. **New build-time disciplines:** 24–29. **Cross-spec flags:** client-management.md (deliverables via this spec, no local model), daily-cockpit.md (tasks as data source with locked column contract), comms-inbox.md (gains `linked_task_id` + `'task_rejection'` source). **Foundations §11 patch owed in Phase 5:** `sendEmail()` gate gains required `classification: 'transactional' \| 'outreach'` parameter. **Sprinkle claimed:** §2 browser tab title for `/lite/tasks`. **No new specs spawned. No new memories.** | [phase-3-task-manager-brainstorm-handoff.md](sessions/phase-3-task-manager-brainstorm-handoff.md) |
| 2026-04-12 | 3 | Spec | Client Management locked — 33 questions resolved. Dual-entry company+contact profiles (7 and 5 tabs). Chat-first client portal: full-page AI chat with bartender register as portal home, menu bubble → clean overlay menu, 3-step tour, fallback chain (chat → Pixieset gallery → branded error page). AI chat: Opus Q&A + escalation + limited safe actions, persistent history, full log on admin Portal Chat tabs. Portal sections via menu. Real-time notifications: email + badge + SSE push. Individual isolated portals. Clients index with health scores + relationship stage filters. Global search (`Cmd+K`). One-click data export via `scheduled_tasks`. Intro Funnel migration job. **New table:** `portal_chat_messages`. **2 new contacts columns.** **3 Claude prompts.** **8 activity_log.kind values.** **2 scheduled_tasks.task_type values.** **Sprinkle claimed:** browser tab titles. **No new motion/sound/specs/memories.** | [phase-3-client-management-handoff.md](sessions/phase-3-client-management-handoff.md) |

| 2026-04-12 | 3 | Spec | SaaS Subscription Billing locked — 29 questions resolved. Generic multi-product billing infrastructure for Lite's self-serve SaaS arm. Identity-based pricing (setup fee on monthly, annual waives it — no discounts, commitment framing). Popcorn tiers within products (usage limits) and across products (Full Suite). Interactive demo landing pages (fully open, no gate, convention-break callout). Cold outreach meta-demonstration + pre-populated dashboard. Brand DNA pre-seeded for cold prospects. SaaS cancel flow: AI chatbot contact, motivational reality check, pay-remainder retains access, product-switch soft step. Fully native account management. Multiple independent subscriptions. Hard cap + upgrade prompt + personality usage bar. 5 new tables, 3 new deal columns, 11 activity_log.kind values, 3 scheduled_tasks types, 5 build-time disciplines, 12 voice-treated surfaces, 9 email templates. **No new motion/sound/specs/memories.** Content mini-session owed (large). | [phase-3-saas-subscription-billing-handoff.md](sessions/phase-3-saas-subscription-billing-handoff.md) |
| 2026-04-13 | 3 | Spec | Cost & Usage Observatory locked — 9 questions resolved. Operator-only cost-attribution + anomaly-detection surface. Dual primary job (alarm + attribution co-equal, forensic free). Four-actor model (`internal | external | shared | prospect`) with single-row log + ephemeral hashed cohort id + query-time proration. Three detectors in parallel (hard thresholds + rate loop-catcher + learned bands with 7-day/50-call warmup). Severity-tiered banners (low dry-calm / mid neutral-louder / severe terse-flat-red with kill-switch). Prospect spend transfers on conversion — CPA + CPA-by-channel + payback period all derivable. Attribution reoriented around tier health as primary signal; per-subscriber recommendation cards only for Large-tier (Small/Medium negative margin = tier-design bug surfaced at tier level). Operator-only costs (never expose AUD to subscribers); two separate ledgers. Claude diagnosis card + raw-data expandable; one-click band editor. No auto-pause, no platform hard cap; Andy-set monthly thresholds + linear run-rate projection alert. **4 new tables** (`external_call_log`, `cost_anomalies`, `deploy_events`, `observatory_settings`). Model/integration registry with ESLint-enforced no-direct-vendor-SDK-imports. **3 Claude prompts** (2 Opus, 1 Haiku). **`activity_log.kind` gains 10 values; `scheduled_tasks.task_type` gains 5 values.** **FOUNDATIONS §11 patch consolidates** send-gate classification + LLM model registry + external-call observability + quarterly review ritual into one amendment authored by Phase 5 Session A. **Sprinkle claimed:** weekly digest subject line. **2 new memories:** `feedback_technical_decisions_claude_calls.md` + `project_tier_limits_protect_margin.md`. **Phase 3.5 backward-pass load:** ~14 integration specs need actor-attribution review at call sites. Content mini-session owed (small-medium). **Phase 5 sizing:** 3 sessions (A: logging + registry + wrappers + FOUNDATIONS / B: detectors + diagnosis + banner wiring / C: dashboard + settings + digest). | [phase-3-cost-usage-observatory-handoff.md](sessions/phase-3-cost-usage-observatory-handoff.md) |
| 2026-04-13 | 3 | Spec | Setup Wizards locked — 11 questions resolved. Cross-cutting foundational primitive: library-as-primitive shell + 10-type step library + custom escape hatch + completion-contract-enforced artefacts. Slideover default, dedicated-route exception for flagship. Auto-save + 24h nudge + 7d cockpit banner + 30-day expiry. Single shell, `audience: admin \| client` tone prop. First-run admin critical flight (Stripe → Resend → Graph API) with capstone ceremony. Observatory registration automatic on completion + visible post-completion summary; vendor manifests at `lib/integrations/vendors/<vendor>.ts` co-locate SDK + band registration (ESLint-enforced shared with Observatory). Help escalation after 2 consecutive failures → portal bartender (client) or dedicated Claude chat (admin, new Opus job `admin-setup-assistant`, persistent thread). Curated 5 client-facing completions surface as Daily Cockpit chips; 7d-idle admin wizards as health banner. **4 gates enforce wizard functionality** (completion contract + per-session build + real-vendor verification + LAUNCH_READY row). **3 new tables** (`wizard_progress`, `wizard_completions`, `integration_connections`) + 1 more from admin chat (`admin_support_threads`). **12-wizard inventory** with completion contracts each. **6 activity_log.kind, 3 scheduled_tasks.task_type, 6 settings keys.** **Sprinkle claimed:** browser tab titles (phase-stratified, audience-differentiated). **1 new Tier-2 motion + 1 new Tier-1 candidate + 1 new sound** flagged for design-system revisit. **Cross-spec patches owed on 6 specs** (SaaS, Content Engine, Unified Inbox, Onboarding+Segmentation, Brand DNA, Intro Funnel) + Daily Cockpit contract extensions + Observatory model registry addition. Content mini-session owed. **Phase 5 sizing:** 3 sessions (A: shell + step library + data model / B: critical flight + capstone + admin Claude chat / C: integrations hub + remaining admin wizards). Client-facing wizards built within their parent spec's build sessions. **No new specs spawned. No new memories.** Mid-session feedback reinforcement: from Q6 onward, locked technical decisions silently per `feedback_technical_decisions_claude_calls.md`. | [phase-3-setup-wizards-handoff.md](sessions/phase-3-setup-wizards-handoff.md) |
| 2026-04-13 | 3 | Spec | Six-Week Plan Generator locked — 16 questions resolved. Final Phase 3 spec. Autonomous generation of the bespoke 6-week marketing plan bundled with the $297 trial shoot, framed as "what we'd implement if we were your agency — here, you run it." Two-stage Opus pipeline (strategy outline → per-week elaboration) + Haiku self-review, Andy's two-tier review with per-week regen, released to shared Client Management portal as read-only section + PDF takeaway (Puppeteer). Non-converter path: prospect clicks "Start Week 1" → plan becomes live tracker on portal for the remaining 60-day access window; at day 60 portal archives, PDF stays. Retainer path: first retainer payment fires Week 1 start; plan migrates into Client Context as `active_strategy` artefact on Deal Won with Andy's refresh-review at kickoff. One free prospect-side revision request routes into Andy's review queue. Scope wider than content — full marketing plan (email list, lead magnets, Meta ads, local SEO, etc.). Shoot-day notes captured on Trial Shoot panel: structured Marketing Infrastructure checklist (enrichment-prefilled) + Goals + 4 Shoot-Day Signals + Observations. Chat primitive NOT owned here — consumed from Client Management with pre-retainer rate-limit. **3 new tables** (`six_week_plans`, `six_week_plan_task_progress`, `trial_shoot_notes`). **17 new `activity_log.kind` values. 3 new `scheduled_tasks` task types. 3 new `sendEmail()` classifications. 9 new settings keys (`plan.*`). 4 new LLM jobs. 4 prompt files.** **2 new Tier-2 motion candidates** for design-system revisit. **Sprinkle claimed:** PDF footer dry line (seed placeholder). **Patches owed on 6 locked specs** (Intro Funnel ×3, Client Management ×2, Client Context Engine, Daily Cockpit, Cost & Usage Observatory, Branded Invoicing/SaaS Billing Stripe-hook, Surprise & Delight sprinkle bank). **Success metrics:** revision-request rate < 10% + regen count ≤ 2 + median review time < 10 min + retainer conversion lift. **v1.0 explicitly scope-cut:** no end-of-Week-6 re-planning, no mid-cycle retainer re-planning, no generation for direct/referral onboarding entries — all deferred to Strategic Planning v1.1. **Content mini-session owed** (medium — 2 Opus prompt calibration + self-review checklist + portal copy + PDF + emails + tab titles). **Phase 5 sizing:** 4 sessions (A: data model + migrations + helpers + prompt-file stubs / B: generator pipeline + Andy review UI / C: portal surface + PDF + revision flow / D: retainer migration + non-converter expiry + Stripe hook). **2 new memories:** `project_six_week_plan_is_the_retainer_plan.md`, `project_non_converter_portal_lifecycle.md`. **Closes Phase 3; Phase 3.5 is next.** | [phase-3-six-week-plan-generator-handoff.md](sessions/phase-3-six-week-plan-generator-handoff.md) |

| 2026-04-13 | 3.5 | Review | Step 2 — cross-spec flag reconciliation. Sub-agent scanned 20 specs: 95 ACK / 1 MISSING / 3 PARTIAL / 0 CONFLICT. Patches applied: `active_strategy` artefact added to Client Context Engine (§11.1, §11.2, §11.3, §12.5 — function library + consuming-specs table + lifecycle prose + table schema); 4 new `activity_log.kind` values in sales-pipeline.md; Brand DNA §3.2 existing-profile gate-skip note. PARTIALs deferred as accepted forward flags (Client Mgmt SSE/WebSocket choice = Phase 5 build call; Observatory actor-attribution = step 5/7 audit). No mop-up needed. Next: step 2a (spec self-containment inlining — heaviest sub-step). | [phase-3.5-step-2-flag-reconciliation-handoff.md](sessions/phase-3.5-step-2-flag-reconciliation-handoff.md) |
| 2026-04-13 | 4 | Build plan (partial) | BUILD_PLAN.md landed — 23 waves, ~150 atomic build sessions, dependency-strict ordering with aggregators last, Foundation A/B split, Wave 0 Pixieset spike, §C 60+ cron table, §R shared-primitive registry, §E 5 critical-flow E2E assignments, rollback strategy per wave. AUTONOMY_PROTOCOL.md + LAUNCH_READY.md deferred to fresh session per mid-session context-budget discipline. | [phase-4-partial-handoff.md](sessions/phase-4-partial-handoff.md) |
| 2026-04-13 | 5 | Spike (P0) | Pixieset API feasibility — **outcome B**. No public API, no webhook, no iframe support. Reverse-engineered endpoints unsupportable. Path B (on-brand link-out + manual URL paste trigger) locked for v1.0; alternatives mop-up declined (Pic-Time/Cloudspot/ShootProof share the same closed posture). Patches: intro-funnel §15.2/§15.3, BUILD_PLAN Wave 0 + IF-2, PATCHES_OWED F2.c section. | [p0-pixieset-spike-handoff.md](sessions/p0-pixieset-spike-handoff.md) |
| 2026-04-13 | 4 | Build plan (close) | AUTONOMY_PROTOCOL.md + LAUNCH_READY.md locked in fresh session. Autonomy protocol: 13 non-skippable per-session gates (G0 kickoff → G12 tracker+commit) including preflight precondition verification, mid-session 70% checkpoint, settings-literal grep, universal motion review, rollback declaration, artefact verification, typecheck+tests, E2E on 5 critical flows, model tiering via `/quick /normal /deep`, Opus-quota → pause (not silent degrade), cache-aware batching, kill-switch safety net, token-budget alerts. LAUNCH_READY.md: 12-section pre-launch gate (infra/email/payments/auth/compliance/observability/kill-switches/E2E-in-prod/synthetic-client dry-run/shadow-period readiness/docs/final sign-off). Phase 4 complete — tracker flipped to Phase 5. | [phase-4-handoff.md](sessions/phase-4-handoff.md) |
| 2026-04-13 | 5 | Build (A1) | Wave 1 A1 — project initialisation. Next.js 16.2.3 App Router + React 19.2.4 + TypeScript 5 + Tailwind v4 + shadcn (`base-nova` / neutral) scaffolded via temp-dir create-next-app merge. Dev server pinned to `:3001`. Env validator (zod, `lib/env.ts`) wired through `instrumentation.ts` — fires loud at boot. `.env.example` declares `NEXT_PUBLIC_APP_URL` / `NEXTAUTH_URL` / `NEXTAUTH_SECRET`. Typecheck + production build both clean. Rollback: git-revertable, no data shape change. Two PATCHES_OWED rows added (A5 owes Vitest harness; A2 owes public-SVG replacement). | [a1-handoff.md](sessions/a1-handoff.md) |
| 2026-04-13 | 5 | Build (A2) | Wave 1 A2 — design system tokens + theme/typeface presets. Full SuperBad token set live in `app/globals.css` (warm-neutral 8-step scale, brand palette, surfaces, semantic, spacing, 3 radii, 8-size type scale, motion constants, shadcn compatibility shims). Typed mirror at `lib/design-tokens.ts` with preset axes + closed-list types + preference enum shells. 9 Google fonts via `next/font/google` in `lib/fonts.ts`. Cookie-driven preset selection via `lib/presets.ts` + server actions. `/lite/design` reference route (renamed from `_design` — Next.js private-folder convention). Vitest harness installed; `tests/tokens.test.ts` enforces three-layer token parity. **Scope divergence locked silently** (`feedback_technical_decisions_claude_calls`): BUILD_PLAN A2's user-preference columns → rolled to A5 (Drizzle infra dependency); Settings → Display UI → rolled to post-A8 session (auth + primitives dependency). Typecheck + `npm test` (6/6) + `npm run build` all green. Rollback: git-revertable, no data shape change. 6 PATCHES_OWED rows added, 1 closed. | [a2-handoff.md](sessions/a2-handoff.md) |
| 2026-04-14 | 5 | Build (A3) | Wave 1 A3 — design system primitives. 35 shadcn primitives in `components/ui/` (32 via `npx shadcn add` + hand-rolled `form.tsx` and `date-picker.tsx`). 8 custom Lite wrappers in `components/lite/` (ThemeProvider, MotionProvider, SoundProvider, AdminShell, BrandHero, SkeletonTile, EmptyState, Tier2Reveal) — each with JSDoc listing consumed tokens. `BrandHero` enforces BHS closed list at type level. `/lite/design` extended with primitives gallery + 6-axis accessibility panel (10-axis coverage). Provider stubs for A4 (MotionProvider CSS-only; SoundProvider no-op; Tier2Reveal CSS keyframes). Typecheck + `npm test` (17/2) + `npm run build` clean. 3 PATCHES_OWED rows added. | [a3-handoff.md](sessions/a3-handoff.md) |
| 2026-04-14 | 5 | Build (A4) | Wave 1 A4 — motion + sound registry. `houseSpring` wired through Framer `MotionConfig` at `components/lite/motion-provider.tsx` with preference-derived transition (full → spring, reduced → 180ms linear, off → instant). 7 locked Tier 2 choreographies in `lib/motion/choreographies.ts` + `pdfRenderOverlay` Tier 1 token. 7-item sound registry in `lib/sounds.ts` with Howler-backed lazy-instanced `play()` at `components/lite/sound-provider.tsx`. `Tier2Reveal` rewritten on Framer with required typed `choreography` prop. `/lite/design` extended with picker-driven Tier 2 demo + 7 sound triggers. `SoundKey` union corrected to spec's canonical kebab-cased keys. `framer-motion` + `howler` + `@types/howler` installed; `use-sound` explicitly rejected. Typecheck + `npm test` (25/25, +8 new) + `npm run build` clean; `/lite/design` HTTP 200. 4 PATCHES_OWED rows added. | [a4-handoff.md](sessions/a4-handoff.md) |
| 2026-04-14 | 5 | Build (A5) | Wave 1 A5 — settings + permissions + kill-switches + ESLint rules. Drizzle client singleton + `settings` table + seed migration (68 rows: Finance 11, Wizards 6, Plan 10, Portal 5, Intro Funnel 1, Hiring 28, Email 4 new, Alerts 3 new — all `INSERT OR IGNORE` idempotent). `settings.get(key)` helper with typed key map + per-key zod parser + in-memory cache. `user` table with `role` + `timezone` + 6 display-preference columns + `first_signed_in_at_ms` (Auth.js-compatible; A8 wires NextAuth against it). `lib/auth/permissions.ts` `can(role, action, resource)` default-deny helper seeded with Finance Dashboard §13 matrix only (wave-extension pattern — every subsequent wave owes its own rules). `lib/kill-switches.ts` 4 flags default-disabled. `lib/types/glossary.ts` branded TS aliases for 11 entity types. 3 custom ESLint rules (`no-direct-anthropic-import`, `no-direct-stripe-customer-create`, `no-direct-resend-send`) loaded via jiti from `.mjs` config. `DATABASE_URL` env var added. Registry Totals reconciled to 68 (pre-A5 was 61; historical "60" was arithmetic error). Typecheck + `npm test` (42/42, +17 new) + `npm run build` + `npm run lint` all clean. Rollback: migration reversible. 4 PATCHES_OWED rows added. | [a5-handoff.md](sessions/a5-handoff.md) |

When a session completes, add a row here with a link to its handoff note.

---

## How to update this file

At the end of every session:

1. Tick the phase box if the whole phase is now complete.
2. Add a row to the session log.
3. Rewrite the **🧭 Next Action** block to point at whatever comes next (the next phase, or the next item inside the current multi-session phase).
4. Commit only if Andy asks — otherwise leave uncommitted.
