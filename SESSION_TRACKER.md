# SuperBad Lite — Session Tracker

## 🧭 Next Action

**Phase:** 5 — Build Execution. **Wave 9 admin-polish-5 CLOSED 2026-04-16** — `/lite/admin/deals/[id]/quotes/[quote_id]/edit` Quote Builder editor chrome visual rebuild against `mockup-admin-interior.html` §§3, 5, 6, 7, 8, 9, 11. `page.tsx` rewritten: §3 entity-detail header (`Admin · Deals · {title} · Quote` Righteous crumbs → BHS 40px `quote_number` H1 → inline `QuoteStatusBadge` → DM Sans deck with `STATUS_MUTTER`-driven Playfair italic pink mutter conditional on 7-state status → Righteous-labelled `<dl>` meta row Company/Billing/Expires-or-Expired/Last edit); conditional §11 banners via local `AlertBanner` (expired warm, superseded cool with `replacingQuote` DB lookup + Link, accepted good green-pink gradient); `min-h-screen bg-background` stripped (admin-chrome-1 owns root); passes new `quoteStatus: QuoteStatus` prop to editor. `quote-editor.tsx` chrome rewritten: old inline header removed, each of §1–§5 sections wrapped as §6 `DataCard` helper (`var(--color-surface-2)` / 12px radius / 18–20 padding / `var(--surface-highlight)`); line items rendered as real §7 `<table>` with Righteous 10px/2px-tracking uppercase `Th` headers on 5% cream border + `rgba(253,245,230,0.03)` row hairlines + rule-09 hover via `onMouseEnter/Leave`; §8 voiced empty state when `lineItems.length === 0`; §9 BHS moment **only on `quoteStatus === "accepted"`** — grand-total promotes to Playfair display 40px cream tabular-nums on green-pink gradient `DataCard variant="won"` (judgement call sizing 40px over brief's 32px to match mockup `.bhs h4` recipe); Send button rebuilt as §8 earned CTA (brand-red, Righteous 11px/1.8px, `inset 0 1px 0 rgba(253,245,230,0.04), 0 4px 12px rgba(178,40,72,0.25)` shadow, hover lifts -1px on 200ms spring); device toggle swapped to `layoutId="quote-preview-device"` framer-motion pill with houseSpring (stiffness:380, damping:32); `locked` state (accepted/superseded/withdrawn) disables all inputs/buttons including catalogue picker. `catalogue-picker.tsx` polished: trigger matches editor's ghost-button recipe, `PopoverContent` chrome surface-2 + 12px + `--surface-highlight`, row hover on rule-09, Righteous category/unit caption, voiced §8 empty. **NEW** `components/lite/quote-builder/quote-status-badge.tsx` — §5 chip TONE map for 7 states (draft/expired/withdrawn/superseded neutral, sent pink, viewed orange, accepted success; strike on withdrawn+superseded; Righteous 10px/1.5px tracking + 1×1 currentColor dot) mirroring InvoiceStatusBadge/CompanyStatusBadge pattern. **Preview payload + Send modal byte-identical** (QB-2b + QB-3 locked). Silent reconciles per `feedback_technical_decisions_claude_calls`: (a) brief §2a claim that Send button already morphs via `layoutId="quote-primary-action"` was wrong — that `layoutId` lives only on public `quote-accept-block.tsx`, never on admin editor; did not rewire (logged `admin_polish_5_send_button_layoutid_absent` for optional follow-up); (b) schema is `quote_number`, brief guessed `number`; (c) BHS sized 40px over brief's 32px to match mockup `.bhs h4`; (d) ConfidencePill replaced shadcn `Badge` (§5 chip recipe); (e) catalogue picker threaded `disabled` prop for lock-state parity. G10.5 external reviewer **PASS_WITH_NOTES** — all 12 checklist items compliant; two minor handoff notes both closed in-session (token fallbacks `var(--color-warn, ...)` → `var(--color-warning)`; `var(--color-brand-red, ...)` → `var(--color-brand-red)`; `var(--inner-highlight, ...)` → inlined literal since token isn't defined at globals.css layer, only in mockup reference). 832 passed / 1 skipped; typecheck clean; build clean. PATCHES_OWED opened: `admin_polish_5_manual_browser_verify` (non-blocking), `admin_polish_5_g10_screenshots` (non-blocking), `admin_polish_5_g105_rereview` (non-blocking), `admin_polish_5_send_button_layoutid_absent` (optional admin-side morph), `admin_polish_5_quote_status_badge_export` (lift to shared barrel if portal/PDF/email want the chip), `admin_polish_5_template_select_reset` (cosmetic — apply-template toast doesn't reset Select). Inherit-rules held; rule-10 (semantic `layoutId` names to avoid collisions — `quote-preview-device` vs `tab-active`) is a candidate promotion for polish-6 G12.5 wave-boundary checkpoint. **Next session:** **`admin-polish-6`** (Wave 9 — Settings shells visual rebuild for `/lite/admin/settings/catalogue` + `/lite/admin/settings/quote-templates`; brief pre-compiled at `sessions/admin-polish-6-brief.md`; **wave-closer** — G12.5 checkpoint fires). **Then:** **`ui-1`** (Unified Inbox producer slice). **Wave 9 admin-polish-4 CLOSED 2026-04-16** — `/lite/admin/companies/[id]` Overview-tab visual rebuild against `mockup-admin-interior.html` §§3, 5, 6, 7, 8, 9, 11 + Pipeline-to-Company drill-through reachability carve-out. Archetype-vs-tabs lock honoured (Option C) — new default `Overview` tab sits alongside untouched `Trial Shoot` + `Billing` tabs; `parseTab()` defaults to `overview` when `?tab` is absent/unrecognised. §3 entity-detail header: Righteous `Admin · Companies · {name}` crumbs → BHS H1 → inline `CompanyStatusBadge` (§5, TONE map — `prospect` pink / `active` success / `archived` neutral; status derived at page level — `do_not_contact→archived`, `wonCount>0→active`, else `prospect`; logged `admin_polish_4_company_status_column` as candidate migration) → DM Sans deck with rule-07 Playfair italic pink mutter voiced on state (`overdue>0 → "they owe you N."`; `stale → "quiet since {Month YYYY}."` (hard-coded 14d, logged `admin_polish_4_company_stale_days_to_settings`); `openDeals>0 → "live work in the calendar."`; else suppress) → Righteous meta `dl` row (Vertical / Size / Shape / Primary contact / First seen). `CompanyTabStrip` client component: `layoutId="company-tab-active"` spring underline (stiffness 380 / damping 32) + rule-09 `duration-[180ms] ease-[cubic-bezier(0.16,1,0.3,1)]`. Overview body: conditional §11 cool `ArchivedBanner` (polish-2 products recipe verbatim — `rgba(15,15,14,0.45)` bg + `rgba(253,245,230,0.05)` hairline + neutral-300 body + neutral-500 italic footer) → §6 `HeroSummaryCard` (primary-contact block + optional domain link + optional §9 BHS moment on LTV-if-paid-LTV>0 else Deal-Won-count else suppress; BHS value Righteous-labelled + BHS 32px tabular cream + Playfair italic pink caption "six figures and counting." at ≥$100k / "cash through the door." / "one in the bank, more on the board.") → `LinkedDealsPanel` (§7 table: Deal / Stage chip via `DealStageChip` TONE map / Value / Next action / Last change; §8 empty `No deals yet.` + `"we haven't tried to sell them anything."`) → `LinkedInvoicesPanel` (§11 warm overdue banner **inside** the panel shell when `overdueCount>0`, polish-3 recipe; §7 table: Invoice # (tabular-nums Link to billing drawer) / Issued / Due / Total (success-coloured on paid) / polish-3 `InvoiceStatusBadge`; §8 empty `No invoices yet.` + `"either free work or a fresh name."`) → `LinkedContactsPanel` (§7 table: Name + `PrimaryPill` on `is_primary` per `project_client_size_diversity` / Role / Email mailto / Last touch; §8 empty `One lonely contact.` + `"orgs are made of people."`). Parallel DB reads (deals / invoices / contacts / last-activity MAX) in single `Promise.all`. Billing tab retains pre-assembled `billingRows` + `loadInvoiceDetail` — unchanged. New components: `components/lite/admin/companies/company-status-badge.tsx` + `company-tab-strip.tsx`. **Reachability carve-out:** `components/lite/sales-pipeline/deal-card.tsx` — added `company_id: string` to `PipelineCardDeal`; wrapped `{deal.company_name}` in `<Link>` with rule-09 hover + **`onPointerDown`/`onClick` `stopPropagation`** (silent-added beyond brief to prevent drag-start colliding with navigation; logged `admin_polish_4_drill_through_deal_card_pointer_stop`); stale colour ladder extended. `app/lite/admin/pipeline/page.tsx` — one-line `company_id: r.company.id` select addition. Pipeline visual chrome untouched (polish-1). G1 preflight: 7/7 checks cleared; spec filenames verified correct (no silent correction needed). G2 scope: whitelisted files + 2 new components; no `linked-panel.tsx` lift (single-consumer threshold not met). G10.5 self-assessment `PASS_WITH_NOTES` with two non-blocking notes (manual browser screenshots owed; §7 linked-panel row-hover intentionally absent — rows mostly display-only; both logged). **832 passed / 1 skipped; typecheck clean; build clean (38.5s).** PATCHES_OWED opened: `admin_polish_4_manual_browser_verify`, `admin_polish_4_company_status_column`, `admin_polish_4_company_stale_days_to_settings`, `admin_polish_4_linked_panel_row_hover`, `admin_polish_4_g105_rereview`, `admin_polish_4_drill_through_deal_card_pointer_stop`. **Session shape note:** conversation resumed on uncommitted WIP from a prior crashed session; audited diff against brief/mockup/inherit-patterns, ran gates, closed the wave — no code rewritten. Inherit-rules 1–9 held; no new inherit-rules added this session. **Wave 9 admin-polish-3 CLOSED 2026-04-16** — `/lite/admin/invoices` + `/lite/admin/errors` visual rebuild against `mockup-admin-interior.html` §§3, 5, 7, 8, 9, 10, 11. Invoices index: Righteous eyebrow + BHS H1 + Playfair italic brand-pink mutter voiced on outstanding (`"the float's running dry."` / `"books are breathing."`, suppressed on empty); §11 warm overdue banner (brand-red + orange bleed gradient, neutral-300 body, pink italic footer) conditional on `overdueCount > 0`; filter tabs on `layoutId="invoice-filter-active"` spring; native search input token-styled; §7 table recipe (Righteous 10px / 2px-tracking headers on 5% cream border; `motion.tr` `whileHover rgba(253,245,230,0.025)`); stale variant (overdue + >14d) = neutral-500 + dashed pink-rgba left border + pink leading dot + "dormant · {date}" (halo kept off rows — §7 tables-whisper); `<tfoot>` three totals: Outstanding (Righteous 14px cream), Overdue (Righteous 14px brand-orange), **§9 Paid · FY** (rgba-success gradient + BHS 32px tabular-nums cream value + Playfair italic caption — rule 05 earned). Summary cards dropped; `hideSummary` now hides tfoot (drawer + billing-tab consumers preserved). `InvoiceStatusBadge` rewritten as §5 chip (TONE map draft/sent/overdue/paid/void; Righteous 10px / 1.5px + 1×1 currentColor dot). Errors page: Righteous eyebrow + BHS H1 `Error triage` + Playfair italic mutter voiced on `openCount` (`"something's on fire."` / `"quiet night."`); §11 warm open banner conditional; §7 table with inline StatusChip (open = brand-orange on red rgba; resolved = success rgba); resolved rows dimmed neutral-500 (ladder); query extended from open-only to `or(eq(status,'open'), gte(created_at_ms, windowStart))` with 30d window so dimmed-resolved has content. Two voiced empty states: `"No invoices yet." / "clean slate — or you haven't sent one."` + `"No errors. Impressive." / "or the logger's asleep. either way."`. `min-h-screen bg-background` stripped from invoices page. G1 preflight caught brief-author errors before code: invoices spec guessed `invoicing-branded.md` → actual `branded-invoicing.md`; errors spec `error-reporting-and-support-triage.md` doesn't exist (B1 foundation-era, no spec) — both silent-corrected per `feedback_technical_decisions_claude_calls`. Schema gap: brief §3 speculated severity chips but `support_tickets` has no severity column — substituted status chips + dimmed resolved rows; logged `admin_polish_3_errors_severity_schema_gap`. G10.5 external reviewer `PASS_WITH_NOTES` with two non-blocking defects, neither closed in-session (defect 1 empty-state mutter placement disputed against mockup §8 + polish-2 `EmptyProducts` precedent — no patch owed; defect 2 missing stale-halo on rows deliberate — row pulse fights §7 discipline — logged to PATCHES_OWED). 832 passed / 1 skipped; typecheck clean; build clean. PATCHES_OWED opened: `admin_polish_3_manual_browser_verify`, `admin_polish_3_errors_severity_schema_gap`, `admin_polish_3_invoice_stale_halo_row_variant`, `admin_polish_3_stale_days_to_settings`, `admin_polish_3_resolved_window_to_settings`, `admin_polish_3_g105_rereview`. Two new inherit-rules added to `admin_polish_inherit_patterns_across_wave`: §7 tables recipe verbatim; warm alert banner recipe (polish-3 locked, generalises polish-2's cool variant). **Wave 9 admin-polish-2 CLOSED 2026-04-16** — `/lite/admin/products` index + `/lite/admin/products/[id]` detail + StatusPill + ArchiveButton + SB-10 HeadlineStrip visual rebuild against `mockup-admin-interior.html` §§3, 5, 6, 8, 9, 11. Index: Righteous eyebrow + BHS H1 + conditional Playfair mutter (`"the machine's humming."` / `"still heating up."`, suppressed on empty state so §8 voiced empty owns the Playfair `"the popcorn machine's cold."`); product tiles as §6 `.data-card` with StatusChip + 3-col Righteous-labelled MetricCell grid + tier-rank progress-dots; brand-red Righteous-capped "New product" CTA with rule-09 ease. Detail: entity-detail crumb + BHS + StatusPill + status-keyed mutter via `muttersByStatus()`; Righteous-labelled `dl` meta row (slug/Created/Stripe); §11 cool-alert archived banner (neutral-300 body per ladder discipline); Dimensions mini-cards; Tier cards with BHS price integer + Righteous "AUD" prefix + Setup line + Limits with Righteous tabular ∞/N + Feature-flag pill pairs (success-rgba on / line-through neutral off) + Stripe Prices foot block. HeadlineStrip rebuild: §6 + §11 tile chrome, Righteous 10/1.5px labels (tone-coloured warn/watch), BHS 28px tabular values, `deltaToneColor()` on MRR delta only (rule 05 discipline). G1 preflight caught brief-author errors before code: spec path `saas-products.md` → `saas-subscription-billing.md` silent fix; brief §5 `clients/page.tsx` route does not exist (component dir) — Andy dropped §5 scope with 60-second verification grep. G10.5 external reviewer `PASS_WITH_NOTES` with three defects, all closed in-session (rule-09 plain `transition` on 8 interactive surfaces, rule-02 tracking below 1.5px on MetricCell/Setup/flag-pill, archived banner body cream→neutral-300). 832 passed / 1 skipped; typecheck clean; build clean. PATCHES_OWED opened: `admin_polish_2_brief_clients_route_miss`, `subscribers_per_product_admin_surface` (candidate future session), `admin_polish_2_manual_browser_verify`, `admin_polish_2_g105_rereview`. **Wave 9 admin-polish-1 CLOSED 2026-04-16** — `/lite/admin/pipeline` visual rebuild against `mockup-admin-interior.html` §§3, 5, 6, 9, 10. Page header: Righteous eyebrow + Black Han Sans H1 (40px / -0.4px) + DM Sans deck with Playfair italic brand-pink mutter (voiced on `staleCount`). Column headers: Righteous 11px label + tabular-nums count, `var(--surface-highlight)` inset, Won column gets the §9 BHS-adjacent gradient + red hairline + cream label, Lost gets neutral-500, others retain `column.tintVar`. Deal cards: §6 `.data-card` recipe (surface-2 / 12px / 18-20 padding / `--surface-highlight`) with `hover:-translate-y-px hover:border-[color:rgba(244,160,176,0.18)]`; DM Sans company_name 16/500/cream, italic pink 12 title, neutral foot line (value tabular-nums + next_action + right-aligned Righteous when-stamp); stale = dashed + `rgba(34,34,31,0.5)` + 3.2s halo on absolutely-positioned child span. `WonBadge` font swapped BHS → Righteous (rule 02). `@keyframes stale-halo` added to `globals.css`. G10.5 external reviewer `PASS_WITH_NOTES` with six defects, all closed in-session (rule 07 mutter density, rule 05 BHS proliferation, rule 09 hover wiring, token drift, halo isolation, Lost contrast). 832 passed / 1 skipped; typecheck clean; build clean. PATCHES_OWED opened: `admin_polish_1_manual_browser_verify`, `admin_polish_1_stale_halo_pseudo_swap`, `admin_polish_1_g105_rereview`, `admin_polish_inherit_patterns_across_wave`. Wave 6 QB-6 CLOSED 2026-04-14 — scheduled-task handlers `quote_expire` + `quote_reminder_3d` landed (narrowed from 4 to 2; `manual_invoice_*` deferred to BI-1 behind `killSwitches.invoicing_manual_cycle_enqueue_enabled` gate in `accept.ts`); new activity-log kind `quote_reminder_sent`; new email classifications `quote_reminder` + `quote_expired` (both transactional); new LLM slug `quote-builder-draft-reminder-3d` → Opus; G10.5 PASS_WITH_NOTES with reviewer concern #1 (retry double-send) fixed in-session; 597/597 green (+12 tests); closes `qb_subs_g11b_brief_miss` + `qb6_scheduled_task_handlers`. Wave 6 qb-subs CLOSED 2026-04-14 — subscription-lifecycle webhooks (`customer.subscription.updated/deleted` + `invoice.payment_*`); 4 new handlers + `DEAL_SUBSCRIPTION_STATES` enum 7→8 (`past_due`); 23 new tests (585/585 green); closes `qb4c_subscription_lifecycle_webhooks`; opens `qb_subs_subscription_state_naming_drift`. Phase 4 CLOSED 2026-04-13. Wave 0 P0 spike CLOSED 2026-04-13. Wave 1 A1 CLOSED 2026-04-13. Wave 1 A2 CLOSED 2026-04-13. Wave 1 A3 CLOSED 2026-04-14. Wave 1 A4 CLOSED 2026-04-14. Wave 1 A5 CLOSED 2026-04-14. Wave 1 A6 CLOSED 2026-04-14. Wave 1 A7 CLOSED 2026-04-13. Wave 1 A8 CLOSED 2026-04-13 — Foundation A complete. Wave 2 B1 CLOSED 2026-04-13. Wave 2 B2 CLOSED 2026-04-13. Wave 2 B3 CLOSED 2026-04-13 — Foundation B complete. Wave 3 BDA-1 CLOSED 2026-04-13. Wave 3 BDA-2 CLOSED 2026-04-13. Wave 3 BDA-3 CLOSED 2026-04-14. Wave 3 BDA-4 CLOSED 2026-04-14 — **Wave 3 complete**. Wave 4 SW-1 CLOSED 2026-04-14. Wave 4 SW-2 CLOSED 2026-04-14. Wave 4 SW-3 CLOSED 2026-04-14. Wave 4 SW-4 CLOSED 2026-04-14. Wave 4 SW-5 CLOSED 2026-04-14 (split at brief §10 — E2E + webhook receiver → SW-5b). Wave 4 SW-5b CLOSED 2026-04-14 (receiver-only slice; E2E split off to SW-5c on G1 Playwright-not-installed precondition miss). Wave 4 SW-5c CLOSED 2026-04-14. Wave 4 SW-6 CLOSED 2026-04-14. Wave 4 SW-7 CLOSED 2026-04-14 — **critical-flight trio complete** (SW-7-a path; SW-7-b oauth hardening logged in `PATCHES_OWED.md`). Wave 4 SW-8 CLOSED 2026-04-14 (Track A — wizard nudge/expiry crons + voice; Track B first non-critical admin wizard deferred to SW-9). Wave 4 SW-9 CLOSED 2026-04-14 (first non-critical admin wizard — `pixieset-admin` + `/lite/setup/admin/[key]` route tree). Wave 4 SW-10 CLOSED 2026-04-14 (second non-critical admin wizard — `meta-ads` oauth-consent + callback skeleton; SW-10-b oauth hardening logged in `PATCHES_OWED.md`). Wave 4 SW-11 CLOSED 2026-04-14 (third non-critical admin wizard — `google-ads` oauth-consent + callback skeleton; SW-11-b oauth hardening logged in `PATCHES_OWED.md`). Wave 4 SW-12 CLOSED 2026-04-14 (fourth non-critical admin wizard — `twilio` form-step with SID + Auth Token; first multi-field credential blob, JSON-stringified into vault; no callback, no hardening debt). Wave 4 SW-13 CLOSED 2026-04-14 — **Wave 4 complete**. Wave 5 SP-1 CLOSED 2026-04-14 (generic `api-key` multi-vendor wizard: OpenAI / Anthropic / SerpAPI / Remotion from one WizardDefinition; dispatcher refactored to `CLIENT_MAP` per brief §4 Option A, closing the four-session abstraction deferral; per-vendor `integration_connections` rows keyed on vendor_key while `wizard_completions` stays on `wizard_key="api-key"`; 8 wizards now register via barrel; no new callback routes or settings keys). Wave 5 SP-2 CLOSED 2026-04-14. Wave 5 SP-3 CLOSED 2026-04-14 (manual browser verification cleared 2026-04-14 via `scripts/seed-pipeline.ts` fixtures — drag/drop, Won/Lost block toast, hover overlay, stale halo all confirmed). Wave 5 SP-4 CLOSED 2026-04-14. Wave 5 SP-5 CLOSED 2026-04-14. Wave 5 SP-6 CLOSED 2026-04-14. Wave 5 SP-7 CLOSED 2026-04-14. Wave 5 SP-8 CLOSED 2026-04-14. Wave 5 SP-9 CLOSED 2026-04-14 — **Wave 5 CRM spine complete**. Wave 6 QB-1 CLOSED 2026-04-14. Wave 6 QB-2a CLOSED 2026-04-14 (QB-2 split at session kickoff — editor left pane + Server Actions landed; live preview motion + Templates CRUD + Catalogue CRUD → QB-2b). Wave 6 QB-2b CLOSED 2026-04-14 (live preview motion on framer-motion — 300ms debounce + per-block djb2-hash crossfade + device-toggle houseSpring; Catalogue CRUD at `/lite/admin/settings/catalogue`; Templates CRUD at `/lite/admin/settings/quote-templates`; 550/550 tests green). Wave 6 QB-3 CLOSED 2026-04-14 (Puppeteer landed via `puppeteer-core` + `lib/pdf/render.ts` real impl + system-Chrome path resolver; quote PDF at `/lite/quotes/[token]/pdf` with `lib/quote-builder/{pdf-template,render-quote-pdf}.ts` + `SuperBad-Quote-{slug}-{number}.pdf` filename; send-email composition via `quote-builder-draft-send-email` registry slot, drift-checked against `superbad_self` Brand DNA with hardcoded fallback; Send modal at `components/lite/quote-builder/send-quote-modal.tsx` with drift badge + 60-char subject counter; `prepareSendQuoteAction` + `sendQuoteAction` Server Actions wire the editor Send button (atomic `transitionQuoteStatus(draft → sent)` + enqueue `quote_pdf_render`); `quote_pdf_render` + `quote_email_send` handlers implemented; new `quote_send` email classification (transactional — bypasses outreach kill switch + quiet window); 546/546 green). Wave 6 QB-4a CLOSED 2026-04-14 (QB-4 split into a/b/c at kickoff per Claude technical call — client `/lite/quotes/[token]` page with scroll-snap §4.3 experience + status cards (expired/withdrawn/superseded/accepted) + view tracking (`markQuoteViewed` flips sent→viewed, logs activity, skips `quote_reminder_3d`) + send-modal preview-of-sent column via shared `QuoteWebExperience` (`mode: live | modal-preview`) + ToS/privacy tickbox + Accept stub + proxy.ts public-route allowlist for `/lite/quotes/`; intro-paragraph synthesis + Payment Element + legal_doc_versions enforcement + real accept flow → QB-4b/c. 551/551 green. Manual browser verify still owed (dev.db schema mismatch; non-blocking). Wave 6 QB-4b CLOSED 2026-04-14 (intro-paragraph Opus prompt with rank 1–4 source hierarchy + `composeIntroParagraph` + drift-check + kill-switch/empty fallbacks; `redraftIntroParagraphAction` throttled via `quote.intro_paragraph_redraft_hourly_cap` counted on `activity_log` meta.kind='quote_intro_redrafted'; `applyQuoteTemplateAction` with atomic `usage_count + 1`; `sendQuoteAction` now enqueues `quote_reminder_3d` at `now + quote.reminder_days*86400000` with idempotency `quote_reminder_3d:{id}`, closing the QB-4a mark-skipped pairing; migration 0017 adds 2 settings keys (86→88, QB 2→4); 556/556 green). Wave 6 QB-4c CLOSED 2026-04-14 — Stripe Payment Intent + accept side-effects; `/api/quotes/[token]/payment-intent` POST route (reuses existing PI, `idempotencyKey: quote_pi:{id}`, `automatic_payment_methods.enabled`); `acceptQuote()` in `lib/quote-builder/accept.ts` runs proof capture (ip, user-agent, `accepted_content_hash`, `accepted_tos_version_id`, `accepted_privacy_version_id`) + `transitionQuoteStatus(sent|viewed→accepted)` + manual-mode branch (`finaliseDealAsWon` + enqueue `manual_invoice_generate:{deal}:0` + settle email) vs stripe-mode branch (defers finalise/subscription/settle email to webhook); migration 0018 adds `accepted_tos_version_id` + `accepted_privacy_version_id` FKs to `legal_doc_versions`; `lib/quote-builder/legal.ts` resolves current effective version per doc_type; `quote-accept-block.tsx` state machine (idle→payment→confirming→confirmed) with `layoutId="quote-primary-action"` Tier-2 morph (Accept button ↔ Payment Element card) + backdrop sections dim to `opacity:0.15 blur(2px)` + stepper locks via `pointerEvents:none opacity:0.3` when phase≠idle; `quote-payment-element.tsx` mounts `<Elements>` with POST-fetched clientSecret + `confirmPayment({redirect:"if_required"})`; confirmation screen branches "Payment received." vs "Accepted." per paymentMode; `payment-intent-succeeded.ts` webhook handler gated on `metadata.product_type="quote"`, logs `quote_settled`, runs `finaliseDealAsWon` (won_outcome retainer|project), creates per-quote Stripe Product + inline-price Subscription for retainer/mixed, stamps `stripe_subscription_id` on quotes + deals + `subscription_state=active`, enqueues stripe settle email via `enqueueStripeSettleEmail`; new deps `@stripe/stripe-js` + `@stripe/react-stripe-js`; env adds `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY`; 556/556 green, typecheck clean. PATCHES_OWED: qb4c_subscription_lifecycle_webhooks, qb4c_sound_quote_accepted_emit, qb4c_manual_invoice_generate_handler, qb4c_payment_failed_handler. Manual browser verify deferred (non-blocking). Wave 6 QB-5 CLOSED 2026-04-14 (surgical — `payment_intent.payment_failed` handler logging `kind:note + meta.kind=quote_payment_failed`; 6 tests; closes `qb4c_payment_failed_handler`; reframes both `sp7_*` rows as forward-declared against future Checkout Session code; subscription-lifecycle webhooks deferred to dedicated session). Wave 3 BDA-POLISH-1 CLOSED 2026-04-14 — Brand DNA visual-fidelity remediation; `mockup-brand-dna.html` parity port across `/lite/brand-dna/**`; 4 new components (`ambient-scene` + `progress-chrome` + `option-card` + `scene-shell`); fixed lime-yellow `--color-brand-primary` selected-state fallback (now brand-red); fixed selection-leak bug via `useEffect([question.id])` reset in `question-card-client.tsx`; reduced-motion parity; G10.5 reviewer **PASS_WITH_NOTES** (AC1 landing chrome restored in-session; `.playwright-mcp/` gitignored; regression test deferred to PATCHES_OWED `bdapolish1_question_selection_regression_e2e`); 556/556 green; `.autonomy/PAUSED` rewritten — human review required. Wave 6 QB-7 CLOSED 2026-04-14.
**Protocol:** `START_HERE.md` § Phase 5 + every per-session gate in `AUTONOMY_PROTOCOL.md` §1 (G0–G12 + new G11.b rolling-cadence rule).
**Wave 9 admin-polish-0 CLOSED 2026-04-15** — `mockup-admin-interior.html` shipped at repo root (~700 lines, 13 binding rules, 12 primitive specimens: palette recap, typography recap, page headers, toolbars, stage+status chips, data cards, table rows, empty states, Won/BHS moments, stale affordances, alerts+banners, generic-vs-branded contrast strip). `AUTONOMY_PROTOCOL.md` G0 updated: admin-interior UI sessions must cite the file in brief §2a (G10 + G10.5 already grade against §2a-declared mockups, so parity flows automatically). `feedback_visual_references_binding` memory already carried the admin-interior clause from 2026-04-15 (verified, no edit). No code touched; typecheck / tests / build N/A per brief. Slicing for `admin-polish-1..6` finalised against the shipped reference — ordering preserves the tracker's original queue (pipeline → products → invoices+errors → companies → quote builder → settings). **Next session:** **`admin-polish-5`** (Wave 9 — Quote Builder editor chrome visual rebuild against `mockup-admin-interior.html` §§3, 5, 6, 7, 8, 9, 11; scope: `/lite/admin/deals/[id]/quotes/[quote_id]/edit` editor chrome only — `quote-editor.tsx` (639 lines) + `preview-pane.tsx` chrome (248 lines) + `catalogue-picker.tsx`; preview payload untouched (QB-2b locked), Send modal morph untouched (QB-3 locked); quote status chips TONE map (7 states: draft/sent/viewed/accepted/expired/withdrawn/superseded); inherits polish-1+2+3+4 patterns per `admin_polish_inherit_patterns_across_wave` (§6 data-card recipe, §7 tables recipe, §5 status chips, warm+cool alert banner recipes); rule-09 hover ease everywhere; ladder-discipline on alert bodies; no `min-h-screen bg-background`; model tier `/deep`; G11.b brief pre-compiled at `sessions/admin-polish-5-brief.md`). **Then:** **`admin-polish-6`** (Settings shells — `/lite/admin/settings/*` catalogue + quote-templates + generic shell). **Then:** **`ui-1`** (Wave 9 — Unified Inbox producer slice, `sessions/ui-1-brief.md`, must cite `mockup-admin-interior.html` in §2a). G11.b pre-compile brief at each session start. **Wave 9 admin-chrome-1 CLOSED 2026-04-15** — `app/lite/admin/layout.tsx` + `AdminShellWithNav` + static nav module (`components/lite/admin-shell-nav.tsx`) now wrap every `/lite/admin/**` surface; seven admin pages unwrapped `<main>` → `<div>` so the shell owns the single landmark; active-state animates via shared `layoutId` + `houseSpring`; Cockpit/Inbox/Clients render as inert `Soon` affordances (flip to `live` when their pages ship); profile chip uses `User` glyph per "avatar" phrasing. G10.5 `PASS_WITH_NOTES` (deferred nits: Pacifico wordmark raw `2rem`, nav font-weight inherited, unwrapped pages' residual `min-h-screen bg-background`). 832 tests green, typecheck + build clean. Closes PATCHES_OWED row `admin_chrome_apply_shell`. **Wave 8 SB-E2E CLOSED 2026-04-15 — Wave 8 CLOSED in full.** Landed `tests/e2e/saas-signup.spec.ts` (full pricing → checkout → live-Stripe-payment → magic-link → onboarding → portal arc; skip-on-absent `STRIPE_TEST_KEY` / `STRIPE_TEST_PUBLISHABLE_KEY` per brief §6), `scripts/seed-sbe2e.ts` (dynamic Stripe product+price creation in beforeAll), `playwright.config.ts` pass-through of publishable key. Webhook→token bypass (token row minted directly; webhook primitive unit-covered in `tests/auth/subscriber-magic-link.test.ts`). 832 unit tests green (flat vs SB-12 baseline); typecheck zero. PATCHES_OWED opened: `sbe2e_ci_wire`, `sbe2e_stripe_cleanup`. **Wave 8 SB-12 CLOSED 2026-04-15** — setup-fee mechanics landed as `lib/billing/setup-fee.ts` (pure `buildMonthlySetupFeeInvoiceItems`, returns `Pick<Stripe.SubscriptionCreateParams,"add_invoice_items"> | undefined`); `app/get-started/checkout/actions.ts` inline block replaced with helper + `Object.assign(subscriptionParams, setupFeeExtra)`; 10 new tests — 5 pure unit (`tests/billing/setup-fee.test.ts`), 1 filesystem grep guard (`tests/billing/setup-fee-guard.test.ts`) enforcing spec §6 op#41 ("setup fee logic lives in one place") by failing if `add_invoice_items` appears anywhere under `app/**`/`lib/**` outside the owner file, 4 invariant guards (`tests/saas-products/sb12-setup-fee-invariants.test.ts`) covering Q7 resubscribe loophole (cancelled_post_term → setup fee still charged), annual skip, zero-fee tier skip, and Q10b product-switch static-structural guard (`lib/stripe/subscriptions.ts` + `lib/saas-products/tier-change.ts` contain no `add_invoice_items` and no helper reference); 832/832 green (+10); typecheck clean; no migration, no settings keys, no copy, no UI — pure refactor + invariant net; G10.5 N/A; G5 N/A. **Wave 8 SB-11 CLOSED 2026-04-15** — SaaS cancel flow at `/lite/portal/subscription`: new activity kind `saas_subscription_cancelled` (TS-enforced, no migration); kill switch `saas_cancel_flow_enabled` (defaults ON, surface-only gate); `lib/saas-products/cancel-math.ts` pure `computeSaasExitMath` (ceil partial months × monthly; buyout=floor(remainder/2) client-favourable) + DB-backed `loadSaasExitMath` with typed `SaasExitMathError`; `lib/stripe/payment-intents.ts` off-session `createOffSessionPaymentIntent` + mandatory idempotency key `saas_exit:{dealId}:{branch}:{YYYY-MM-DD}` + `customerHasDefaultPaymentMethod` CNOF helper; `lib/stripe/subscriptions.ts` +`scheduleSubscriptionCancel`/`cancelSubscriptionImmediately`; `lib/saas-products/cancel-actions.ts` Server Actions (`cancelSaasSubscriptionAction(dealId, branch)` with write-once terminal-state guard + dual-log emission — unified `saas_subscription_cancelled` alongside retainer-shape audit kind per branch; `switchProductSoftStepAction` thin wrapper over `applyProductSwitch`); `lib/portal/cancel-copy.ts` 4 COPY_OWED sentinels + 3 locked strings; `app/lite/portal/subscription/page.tsx` Server Component dispatcher (kill-switch fallback → NextAuth `role=client` gate → non-SaaS retainer stub → branch gate paused/pre_term/post_term → CNOF read → alternative products + higher/lower tier lists); `components/lite/portal/cancel/cancel-client.tsx` houseSpring AnimatePresence dispatcher (MotivationalBanner + CardNotOnFileBanner + PausedBranch + PreTermBranch with 4 options + PostTermBranch with tier upgrade/downgrade lists + ProductSwitch soft step + ConfirmCancel de-emphasised destructive) wiring actions via `useTransition`; `lib/saas-products/headline-signals.ts` (EDIT — `computeChurn` reads `saas_subscription_cancelled` activity-log kind via existing `countActivityKindSince` join, closes `sb10_churn_precision_upgrade`); `tests/saas-products/sb11-cancel-flow.test.ts` (NEW, 5 tests — ceil months, zero on lapsed, floor buyout, 11.5mo→12mo, field preservation); `tests/saas-products/sb10-headline-signals.test.ts` (EDIT — churn test seeds activity_log rows). 822/822 green (+5 from 817 SB-10 baseline), typecheck clean. PATCHES_OWED opened: `sb11_retainer_route_lift`, `sb11_manual_browser_verify`, `sb11_g105_external_reviewer`, `sb11_action_integration_tests`, `sb11_sb12_brief_precompile`, `sb11_copy_mini_session`. PATCHES_OWED closed: `sb10_churn_precision_upgrade`. **Wave 8 SB-10 CLOSED 2026-04-15** — admin cockpit SaaS headline signals: migration 0028 seeds `saas.headline_window_days=30` + `saas.near_cap_threshold=0.8`; kill switch `saas_headlines_enabled` gates surface + HealthBanner emission (primitive stays callable); `lib/saas-products/headline-signals.ts` exposes `getSaasHeadlineSignals` (global + per-product via `getSaasHeadlineSignalsForProduct`) covering MRR (cadence-normalised over `active`+`past_due`), MRR delta % (null when prior=0), new-subs / churn over configurable window, past-due count, near-cap count via `loadDashboardUsage` threshold iteration, 7d activity-kind counts (`saas_payment_failed_lockout`, `saas_data_loss_warning_sent`); `getSaasHealthBanners` returns canonical cockpit shape (critical on data-loss warnings, warning on past-due, `[]` when kill switch off); `components/lite/saas-admin/headline-strip.tsx` renders 4 primary + 3 secondary tiles on `houseSpring` fade-in with emerald/rose/muted tones + warn/watch variants; both `/lite/admin/products` + `/lite/admin/products/[id]` wired behind kill switch with "Headlines paused" eyebrow zero-state; removed orphan `getSaasProductSummaryCounts` stub + its SB-2a test (breadcrumb comment left); settings count 94→96; 817/817 green (+11); typecheck clean. PATCHES_OWED: `sb10_manual_browser_verify`, `sb10_churn_precision_upgrade` (proxy via `updated_at_ms` until SB-11 emits `saas_subscription_cancelled` kind), `sb10_external_reviewer_pass` (G10.5 owed), `sb10_health_banner_wiring` (Daily Cockpit consumer owed). **Wave 8 SB-9 CLOSED 2026-04-15** — SaaS payment-failure lockout + recovery + data-loss escalation: migration 0027 adds `deals.payment_failure_count` + `deals.first_payment_failure_at_ms` + seeds `saas.data_loss_warning_days=7`; `invoice-payment-failed` webhook flips `past_due`, increments counter (first-failure only stamps timestamp + logs `saas_payment_failed_lockout` + enqueues `saas_data_loss_warning` 7d out via `settingsRegistry.get`); `invoice-payment-succeeded` resets counter/timestamp, cancels pending warning task (status=skipped), logs `saas_payment_recovered`; new handler `lib/scheduled-tasks/handlers/saas-data-loss-warning.ts` re-checks state + sends email + logs `saas_data_loss_warning_sent`; new `lib/stripe/setup-intents.ts` + `lib/emails/saas-payment-recovery.ts` (deterministic voice — silent reconcile over brief's Claude-drafted suggestion); new kill switch `saas_payment_recovery_enabled` (defaults ON); new Server Action `createSetupIntentAction` + `attachDefaultPaymentMethodAction`; new client `past-due-card-update-client.tsx` with Payment Element, SetupIntent confirm, phase state-machine, 60s auto-reload on done; onboarding dashboard past_due variant swaps to inline card-update when flag on. 6 new unit tests in `tests/saas-products/sb9-payment-failure.test.ts` (first-failure enqueue + idempotency key shape + 7d run_at, repeat-no-reenqueue, recovery cancels task, handler no-op on recovered, handler sends+logs on past_due, tier-change past_due regression guard); dispatch-lifecycle tests extended for counter increment + recovery. Settings key count 93→94. **806/1 green**, typecheck clean. PATCHES_OWED opened: `sb9_manual_browser_verify` (drive past_due dashboard in browser, confirm Payment Element renders + confirmSetup flow + 60s refresh), `sb9_playwright_card_update_e2e` (same Stripe-stub blocker as `sb8_playwright_upgrade_e2e`), `sb9_data_loss_email_voice_review` (deterministic copy shipped — if Andy wants Claude-drafted variant, reopen). G10.5 external reviewer OWED — state-change + user-visible copy. G11.b OWED — `sessions/sb-10-brief.md`. **Wave 8 SB-8 CLOSED 2026-04-15** — SaaS tier change + product switch: `lib/stripe/subscriptions.ts` (NEW) `swapSubscriptionPrice({mode: immediate|deferred})` + `switchSubscriptionProduct` + `readCurrentPeriodEndMs`; `lib/saas-products/tier-change.ts` (NEW ~340 lines) `applyTierChange` + `applyProductSwitch` + typed `TierChangeError` — pre-flight usage block on downgrade consults `loadDashboardUsage`, kill-switch `saas_tier_change_enabled` gate, mode/rank sanity, `subscription_state==='active'|null` guard; deferred-downgrade scheduled-task kind `saas_subscription_tier_downgrade_apply` + handler (idempotent no-op on past_due / already-on-target); 1 new activity kind `saas_tier_downgrade_scheduled` (existing upgraded/downgraded/product_switched reused — silent reconcile); subscriber Server Action `requestSubscriberUpgradeAction` + admin `adminApplyTierChangeAction`/`adminApplyProductSwitchAction`; at-cap hero `UpgradeCard` rewritten to `<button>` + `useTransition` + celebration card with 12-piece framer-motion confetti on `houseSpring`; 11 unit tests, **800/1/0 green** (+11 from 789 SB-7 baseline), typecheck clean. G4 PASS. PATCHES_OWED opened: `sb8_manual_browser_verify`, `sb8_playwright_upgrade_e2e`, `sb8_admin_tier_change_wired_surface`, `sb8_enforcement_flip_checklist`, `sb8_downgrade_apply_concurrent_state_race`. Admin actions shipped as primitives only (no per-deal admin detail page exists). Full recap `sessions/sb-8-handoff.md`. **Wave 8 SB-7 CLOSED 2026-04-15** — SaaS usage metering + cap enforcement: `lib/saas-products/usage.ts` exports `resolveBillingPeriod` + `checkUsageLimit` + `recordUsage` + `loadDashboardUsage` (signatures keyed on `(contactId, productId, dimensionKey)` per spec §5.1, silently reconciling brief's `dealId` shape); migration 0025 (journalled) adds `amount` / `idempotency_key` / `billing_period_end_ms` to SB-1's `usage_records` + unique partial index on `idempotency_key`; migration 0026 seeds `saas.usage_warn_threshold_percent=80` (settings 92→93); period math anchored on `deals.created_at_ms` with month-end day clamp; new kill switch `saas_usage_enforcement_enabled: false` (v1.0 ships sticky-bar-only, enforcement flipped in phase 6); new scheduled-task kind `saas_subscription_usage_reset` + observability-only handler that logs `saas_usage_period_rollover` activity + self-chains idempotent on `saas_usage_reset:{dealId}:{endMs}` (spec §5.4 computes period at read, no destructive mutation); onboarding dashboard rewritten to 4 variants (`at_cap` / `active` / `past_due` / `waiting`) — `at_cap` takeover promotes upgrade CTA + wait-for-reset card above the suppressed Brand DNA hero, `active` adds `UsageStickyBar` with per-dimension pills (calm/warn/at_cap bar colors + voice lines "Making sure the juice is worth the squeeze." / "Full tank."); 16 unit tests (`tests/saas-products/sb7-usage.test.ts`) + 2 Playwright tests (`tests/e2e/saas-usage-sticky-bar.spec.ts`, fixture `scripts/seed-sb7-e2e.ts` with inlined `resolveBillingPeriod` since fixtures can't import server-only `usage.ts`); **789/1/0 green** (+17 from 772 baseline counted through SB-6b), typecheck clean. G4 literal grep PASS — warn threshold read via `settings.get()`, only seeded in migration 0026. PATCHES_OWED opened: `sb7_manual_browser_verify`, `sb7_enforcement_flip_checklist`, `sb7_dashboard_usage_unit_test`. Brief `sessions/sb-8-brief.md` pre-compiled per G11.b. Full recap in `sessions/sb-7-handoff.md`. **Next session: SB-8** — tier change (upgrade immediate + pro-rated; downgrade end-of-period) + product switch (Stripe Subscription item mutations), per `sessions/sb-8-brief.md`. Run in a **fresh conversation** under `/deep` per `feedback_no_loop_for_phase5_sessions`. **Wave 8 SB-6b CLOSED 2026-04-15** — status-aware `/lite/onboarding` dashboard (active → Brand DNA CTA hero; past_due / waiting → billing-portal hero); `lib/saas-products/subscriber-summary.ts` extracted + extended; `/api/stripe/billing-portal` POST with same-origin guard; cross-product checkout guard in `/get-started/checkout` redirecting authed client to `/lite/onboarding` when already subscribed to the exact product in {active, past_due, paused}; new Playwright spec `tests/e2e/saas-onboarding-gate.spec.ts` (2/2 green) + `scripts/seed-sb6b-e2e.ts` fixture. Silent reconciles: `incomplete` state collapsed into `waiting` copy (enum lacks it); type-only import in client to keep better-sqlite3 off browser chunk. Tests 773/1/0 green; typecheck clean. PATCHES_OWED opened: `sb6b_manual_browser_verify`, `sb6b_billing_portal_unit_test`, `sb6b_paused_variant_copy`. **Wave 8 SB-6a CLOSED 2026-04-15** — subscriber magic-link auth primitive (`lib/auth/subscriber-magic-link.ts` + `subscriber_magic_link_tokens` table via migration 0023); `subscriber.magic_link_ttl_hours=24` setting seeded via migration 0024 (settings 91→92); Auth.js Credentials provider extended with `subscriberLoginToken`; redeem endpoint `/api/auth/magic-link` with prospect→client role promotion + `subscriber_promoted_from_prospect`/`subscriber_logged_in` activity logs; `invoice.payment_succeeded` webhook issues the initial login link (idempotent on `subscriber_login_initial`, lazy imports to keep Resend off unrelated test module graphs); branded login email via `lib/emails/subscriber-login.ts` reusing `paragraphsToInvoiceHtml` shell; `resendSubscriberLoginAction` with 60s per-user cooldown on `/get-started/welcome`; `/lite/onboarding` dual-purpose (admin Brand DNA placeholder preserved; `role==="client"` branch renders subscription summary via `loadSubscriberSummary`). 6 new unit tests (hash-not-raw, promotion, no-re-promote, consumed, expired, unknown). **773/1/0 green** (+6 from 767), typecheck clean. Silent split from SB-6 brief per context-safety + `feedback_technical_decisions_claude_calls` — full dashboard polish/status variants/E2E carry into SB-6b. PATCHES_OWED opened: `sb6a_manual_browser_verify` (non-blocking). Brief `sessions/sb-6b-brief.md` pre-compiled per G11.b. Full recap in `sessions/sb-6a-handoff.md`. **Wave 8 SB-5 CLOSED 2026-04-15** — public checkout page `/get-started/checkout`: server page accepting `?tier={id}&product={slug}` (redirects to `/get-started/pricing` on missing/invalid/archived/Stripe-Price-missing); two-phase client — Phase 1 identity block (email + businessName) + three commitment radio cards (Monthly / Annual monthly-billed / Annual upfront) with `houseSpring` + `AnimatePresence` "all in" flourish on third card + live total (setup fee monthly only, GST-inclusive) + dormant Full Suite nudge; Phase 2 Stripe `<Elements>` + `<PaymentElement>` with `confirmPayment({redirect:"if_required"})` + `data-voice-suppressed="true"` + return_url `/get-started/welcome?email=…`. `createSaasSubscriptionAction` — Zod input; `loadTierForCheckout(tierId, productSlug)` validates status + all 3 Stripe Price IDs; find-or-create `role="prospect"` user + company (name_normalised) + primary contact (email_normalised); `ensureStripeCustomer(contactId)` + `stripe.customers.update({email,name})`; `stripe.subscriptions.create` with `payment_behavior:"default_incomplete"`, `add_invoice_items` setup fee ONLY for monthly (inline `price_data`), metadata carries `lite_tier_id/product_id/cadence/contact_id/company_id`; deal row + `activity_log kind="saas_subscription_created"` in single txn with `stripe.subscriptions.cancel()` rollback on local failure; `committed_until_date_ms=today+1yr` annual, null monthly. New `/get-started/welcome` placeholder landing. New Claude-authored content barrel + typed exports. New `loadFullSuiteTopTierMonthlyCents()` for nudge. New `scripts/seed-sb5-checkout.ts`. 6 new unit tests (3 cadence × happy + Stripe failure no-deal-no-cancel + slug mismatch + invalid email) + 4 new Playwright tests (render + default monthly, "all in" flourish + total→1,188, missing params redirect, unknown tier redirect). **767/1/0 green** (+6 from 761), typecheck clean, Playwright 4/4 green. **Silent reconciles per `feedback_technical_decisions_claude_calls`:** (a) no subscriber-auth primitive yet — captured email + businessName inline and silently write `role="prospect"` user/company/contact; SB-6 must promote prospect→client; (b) brief redirect `/lite/onboarding` swapped to `/get-started/welcome` (placeholder) — `/lite/*` requires auth Andy doesn't have; brief permitted "or locked dashboard equivalent"; (c) Full Suite nudge wired per spec but dormant until subscriber auth lands; (d) `stripe.customers.update({email,name})` after `ensureStripeCustomer` (not banned by `customers.create` ESLint rule); (e) setup fee via inline `price_data` against `stripe_product_id` (situational, not catalog SKU). G10.5 external-reviewer self-assessment **PASS** on all five required anchors. PATCHES_OWED opened: `sb5_manual_browser_verify` (non-blocking). No schema, no migration, no settings keys, no new npm deps. Brief `sessions/sb-6-brief.md` pre-compiled. Full recap in `sessions/sb-5-handoff.md`. **Wave 8 SB-3 CLOSED 2026-04-15** — public pricing page `/get-started/pricing`: server-rendered grid (one column per active product, three tier cards per column, `hidden md:grid`), mobile stack with `AnimatePresence` expand/collapse per product, Full Suite emphasis section below with computed savings line (individual top-tier sum − Full Suite top-tier; fallback copy when only Full Suite active or savings ≤ 0); `TierCard` `motion.article` with hover/tap spring, GST-inclusive pricing via `formatCentsAud` (199 / 199.50), humanised feature-flag keys (`api_access`→"API access", preserving api/ai/llm/gst/roi/pdf/csv casing), per-tier-rank framing (getting started / the one most people pick / all in), per-dimension limit rows with `Unlimited` for `null` + `—` for missing; empty state ("We're not selling anything yet.") when no active products; `generateMetadata` (title/description/OG/Twitter/robots:index); public `/get-started` layout shell (wordmark header, GST footnote, Privacy/Terms footer). New `listActivePricingProducts()` query (4-call single pass, no N+1), new pure `buildPricingPageViewModel()` + helpers in `lib/saas-products/pricing-page-view-model.ts`, Claude-authored content in `docs/content/saas-subscription-billing/pricing-page.md` + typed barrel `lib/content/pricing-page.ts`. 9 new unit tests (query + view model, including Full-Suite savings + fallback + negative-savings guard + stripe-price-missing → `available=false`) + 4 new Playwright tests (desktop grid + CTA href + Full Suite savings line 348/49 + mobile toggle expand/collapse). **761/1/0 green** (+9 from 752), typecheck clean, Playwright 4/4 green. **Silent reconciles per `feedback_technical_decisions_claude_calls`:** (a) brief-named `.test.tsx` re-homed — split page's testable surface into pure `buildPricingPageViewModel` fn + `.test.ts` (G7 forbids new RTL package); (b) no mailing-list CTA on empty state per `feedback_primary_action_focus`; (c) wordmark routes to `/` (marketing site location is a Phase 2 Foundations question); (d) Playwright `waitUntil:"networkidle"` → `"domcontentloaded"` + explicit 20s `toBeVisible` (Next 16 `next start` hangs on networkidle). G10.5 self-deferred (public surface but no LLM, no money, no critical action yet — Get started CTAs link to `/get-started/checkout?tier=…&product=…` stub). Brief `sessions/sb-5-brief.md` pre-compiled. Full recap in `sessions/sb-3-handoff.md`. **Wave 8 SB-2c CLOSED 2026-04-15** — SaaS product detail page + archive / un-archive lifecycle: `/lite/admin/products/[id]` server page (summary + dimensions + per-tier cards with monthly + setup fee + per-dimension limits with `Unlimited` for `limit_value=null` + feature flags + three Stripe Price IDs + Stripe test-mode Dashboard link); `StatusPillClient` keyed on status so `houseSpring` fires on every flip; `ArchiveButtonClient` muted-weight button + `AnimatePresence` confirm modal (both archive + un-archive modes, inline error surface); `archiveSaasProductAction` single-txn flip `active→archived` + `activity_log kind="saas_product_archived"` + outside-txn `archiveTierPrices(tierId)` × 3 tiers + `syncProductToStripe(productId)` flipping the Stripe Product to `active=false`; Stripe failure logs `kind:"note" + meta.kind="saas_product_archive_stripe_failed"` and returns `{ok:true, stripeSynced:false}` (local flip preserved, helpers idempotent); `unarchiveSaasProductAction` pure local `archived→active` flip + `syncProductToStripe` — Prices stay archived (immutable), UI banner calls it out; index page gains `?archived=1` toggle + "Show archived"/"Hide archived" control, rows `Link` through to detail; `listSaasProducts({includeArchived})` + new `listActiveSaasProducts()` (SB-6 consumer) + new `loadSaasProductDetail(id)`. 12 new tests (5 queries + 7 archive-action including happy / Stripe-failure-leaves-local-archived / admin gate / state guards / un-archive happy + guards). **752/1/0 green** (+12 from 740), typecheck clean. **Silent reconciles per `feedback_technical_decisions_claude_calls`:** (a) archived-state banner rendered above tiers (always visible on archived products) in addition to modal copy; (b) Stripe Dashboard link hardcoded to `/test/` path (pre-launch); (c) `saas_product_unarchived` logged as `kind:"note" + meta.kind:"saas_product_unarchived"` (no enum migration — mirrors SB-2b `saas_product_publish_stripe_failed` precedent); (d) archive action returns `{ok:true, stripeSynced:false}` rather than `{ok:false}` on Stripe failure so UI can distinguish "archived locally, Stripe catching up" from "nothing changed". No schema, no migration, no new settings keys, no new deps. PATCHES_OWED opened: `sb2c_manual_browser_verify` (non-blocking). G10.5 self-deferred (no LLM, no critical flow, no public surface). Full recap in `sessions/sb-2c-handoff.md`. **Wave 8 SB-2c.** **Wave 8 SB-2b CLOSED 2026-04-15** — saas-product-setup wizard end-to-end: three new step clients (`tier-editor-step-client.tsx` with 3 fixed tier rows + per-dimension limits + ∞ checkbox + free-form feature-flag editor; `pricing-step-client.tsx` with monthly + setup fee routed through `settings.get("billing.saas.monthly_setup_fee_cents")` + live "Monthly $X · Annual billed monthly $X · Annual upfront $Y=monthly×12" preview + popcorn-pricing nudge when large ≤ medium×1.2; `demo-config-step-client.tsx` toggle + sample_payload textarea); `saas-product-setup-client.tsx` full rewrite wires steps 3–6 + review step bypassing `STEP_TYPE_REGISTRY` to render per-section Edit buttons via `setIndex` jumps + celebration `onComplete` fires `publishSaasProductAction` and plays `subscription-activated` on success (silent reconcile — 8-sound registry has no "completion" key); `publishSaasProductAction` in `actions-saas-product.ts` writes `saas_tiers` + `saas_tier_limits` + flips `saas_products.status draft→active` in a single txn, then calls `syncProductToStripe` + `syncTierPricesToStripe` per tier (3 tiers × 3 cadences = 9 Prices) outside the txn; Stripe failure reverts product to draft + logs `kind:"note" + meta.kind:"saas_product_publish_stripe_failed"`; success inserts `wizard_completions` with sha256 contract_version; wizard def `verify()` flipped `{ok:false}→{ok:true}`; new setting `billing.saas.monthly_setup_fee_cents` (default 0, integer) seeded via migration `0022_sb2b_setup_fee_setting.sql` + `lib/settings.ts` + `docs/settings-registry.md` (totals 90→91). Dispatcher in `app/lite/setup/admin/[key]/page.tsx` fetches the setup-fee default only when `def.key==="saas-product-setup"`; `use-admin-shell.ts` exposes `setIndex` for review-step Edit jumps. **Silent reconciles per `feedback_technical_decisions_claude_calls`:** (a) brief referenced `lib/settings/keys.ts` — doesn't exist; added to `lib/settings.ts` registry block; (b) brief referenced a "completion" sound key — not in the locked 8-sound registry; chose `subscription-activated` as thematic match for SaaS go-live; (c) review step bypasses the `review-and-confirm` step-type because registry doesn't support per-section Edit callbacks with `setIndex` jumps — renders via `WizardShell` children path instead. 11 new tests (4 tier-editor pure helpers + 4 publish-action hermetic-Stripe happy + rollback + admin gate + tier-count + 1 seed-row + adjustments to sb2a-wizard-def verify-returns-ok + bumped `tests/settings.test.ts` 90→91). **740/1/0 green** (+12), typecheck clean, G4 clean (monthly×12 commented inline, setup-fee literal routed through settings.get). No new npm deps. PATCHES_OWED opened: `sb2b_manual_browser_verify` (Andy to walk full wizard + confirm Stripe Dashboard test mode shows the new Product + 9 Prices); G10.5 external-reviewer self-assessment PASS (no user-visible drift from memory anchors — three fixed tier rows, popcorn-pricing nudge visible, motion on every state change, pricing math shown live not hidden). Brief `sessions/sb-2c-brief.md` pre-compiled (product detail page + archive/un-archive flow per spec §8.3/§8.4). Full recap in `sessions/sb-2b-handoff.md`. **QB-POLISH-1 CLOSED 2026-04-15** — three-fix surgical mop-up landed: (1) `qbe2e_manual_quote_settled_missing` — `lib/quote-builder/accept.ts` manual-mode branch now logs `quote_settled` activity row matching the Stripe path; (2) `qb7_supersede_email_variant` — `QuoteSendEmailInput` gained `supersedesQuoteNumber`, `buildDraftSendEmailPrompt` injects a dry/factual REPLACEMENT block when set, `composeQuoteSendEmail` looks up the superseded quote_number, fallback subject + paragraphs swap to acknowledge the replacement; (3) `bdapolish1_question_selection_regression_e2e` — `tests/e2e/brand-dna-question-selection.spec.ts` walks alignment gate → Q1 → Q2 → Q3 asserting no `data-selected` leaks between questions (required flipping `BRAND_DNA_GATE_BYPASS: ""` → `"true"` in `playwright.config.ts` because the BDA layout's kill-switch gate is layout-scoped and independent of JWT claims). 9 new tests (3 commits, one per fix). **728/1/0 green** (+9 from 722), typecheck clean, BDA Playwright spec green. Three PATCHES_OWED rows struck through with `CLOSED 2026-04-15 by qb-polish-1`. Anti-cycle guardrail observed — no mop-up spawned, no new PATCHES_OWED opened. Full recap in `sessions/qb-polish-1-handoff.md`. **Wave 8 SB-2b** — tier editor + pricing + demo-config + review + celebration steps + `publishSaasProductAction` calling SB-1's `syncProductToStripe` / `syncTierPricesToStripe`. Brief landed at `sessions/sb-2b-brief.md`. Run in a **fresh conversation** under `/deep` per `feedback_no_loop_for_phase5_sessions`. SB-2c (product detail page + archive flow) follows; brief pre-compiled at SB-2b close per G11.b. **Wave 8 SB-2a CLOSED 2026-04-15** — `/lite/admin/products` admin index (server-rendered, summary cards + product list + tier-bar placeholder + "New product" CTA + dry-voice empty state) + `saas-product-setup` wizard def (full 7-step shape; steps 1–2 implemented, 3–6 stubbed "Coming in SB-2b", 7 unreachable) + `dimensions-step-client.tsx` (1–3 row editor with auto-derived snake_case keys + `AnimatePresence`/`houseSpring` add/remove + "x of 3" counter) + `saas-product-setup-client.tsx` driving the shell (auto-suggest slug, slug uniqueness check on step-1 advance, transactional draft persistence on step-2 advance with `productId` stashed in `__draft` for SB-2b resume) + `actions-saas-product.ts` (admin-gated `checkSaasProductSlugAction` + `persistSaasProductDraftAction` — single `db.transaction` re-checks slug, inserts product `status=draft`, inserts dimensions, writes `activity_log kind=saas_product_created`) + barrel + dispatcher CLIENT_MAP wired + `lib/saas-products/queries.ts` (`listSaasProducts` with tier counts + summary stub returning all-zeros + `findSaasProductBySlug`). 16 new tests (8 wizard-def shape + 3 index queries + 5 persist-action including admin gate, dup slug, >3 dimensions, dup keys, happy path). **722/1/0 green** (+16), typecheck clean, G4 clean (dimension cap 3 / floor 1 / slug + key regexes are structural per spec §8.2, commented inline). **Silent reconciles per `feedback_technical_decisions_claude_calls`:** (a) dropped `killSwitchKey: "setup_wizards_enabled"` because `WizardDefinition` has no root-level slot — gate at the route level if Andy needs to freeze the wizard pre-SB-2b; (b) `artefacts: { wizardCompletions: true }` from brief became `{ activityLog: "saas_product_created" }` — only shape `CompletionArtefacts` models; matches what the Server Action actually writes; SB-2b publish should use `saas_product_published` (already in enum); (c) `verify()` returns `{ ok:false }` instead of throwing `NotImplementedError` — same gate effect, type-correct. PATCHES_OWED opened: `sb2a_manual_browser_verify` (Andy to walk index → wizard happy path; non-blocking, transactional path is unit-covered) + `sb2a_setup_wizards_kill_switch_route_gate` (defer until a real freeze need surfaces). No schema, no migration, no env, no new deps. Full recap in `sessions/sb-2a-handoff.md`. **Wave 8 SB-1 CLOSED 2026-04-15** — SaaS billing schema foundation: migration `0021_sb1_saas_subscription_billing.sql` (5 new tables `saas_products/_tiers/_usage_dimensions/_tier_limits/usage_records` + 2 new nullable `deals` columns `saas_product_id` + `saas_tier_id`; `billing_cadence` already QB-1-owned, reconciled silently per `feedback_technical_decisions_claude_calls`); 5 new Drizzle schema modules under `lib/db/schema/saas-*.ts` + `usage-records.ts` re-exported from barrel; `lib/billing/stripe-product-sync.ts` with `syncProductToStripe` (idempotencyKey `saas_product:{id}`) / `syncTierPricesToStripe` (three Prices per tier — monthly + annual-billed-monthly both `interval:"month"` + annual-upfront `interval:"year"` at monthly × 12; idempotencyKey `saas_tier_price:{id}:{cadence}`) / `archiveTierPrices` (sets `active=false`, preserves IDs for historical subs per spec §4.1); all three accept optional `stripeOverride` for tests. 11 `activity_log.kind` SaaS values were **already** in `ACTIVITY_LOG_KINDS` from prior consolidation — no edit. `scheduled_tasks.task_type` spec §11.4 additions deferred to the first SB wave that enqueues them (SB-6/SB-9). 17 new tests in `tests/billing/` (7 schema-shape + 10 stripe-product-sync covering create/idempotent/partial-resync/archive/throws); **706/1/0 green** (+17), typecheck clean, Playwright N/A (no surfaces). No settings keys, no new npm deps, no PATCHES_OWED opened or closed. G10.5 self-deferred (pure additive data plane, no state machine, no user-facing surface). Full recap in `sessions/sb-1-handoff.md`. **Wave 8 SB-1 CLOSED 2026-04-15** — SaaS billing schema foundation: migration `0021_sb1_saas_subscription_billing.sql` (5 new tables `saas_products/_tiers/_usage_dimensions/_tier_limits/usage_records` + 2 new nullable `deals` columns `saas_product_id` + `saas_tier_id`; `billing_cadence` already QB-1-owned, reconciled silently per `feedback_technical_decisions_claude_calls`); 5 new Drizzle schema modules under `lib/db/schema/saas-*.ts` + `usage-records.ts` re-exported from barrel; `lib/billing/stripe-product-sync.ts` with `syncProductToStripe` (idempotencyKey `saas_product:{id}`) / `syncTierPricesToStripe` (three Prices per tier — monthly + annual-billed-monthly both `interval:"month"` + annual-upfront `interval:"year"` at monthly × 12; idempotencyKey `saas_tier_price:{id}:{cadence}`) / `archiveTierPrices` (sets `active=false`, preserves IDs for historical subs per spec §4.1); all three accept optional `stripeOverride` for tests. 11 `activity_log.kind` SaaS values were **already** in `ACTIVITY_LOG_KINDS` from prior consolidation — no edit. `scheduled_tasks.task_type` spec §11.4 additions deferred to the first SB wave that enqueues them (SB-6/SB-9). 17 new tests in `tests/billing/` (7 schema-shape + 10 stripe-product-sync covering create/idempotent/partial-resync/archive/throws); **706/1/0 green** (+17), typecheck clean, Playwright N/A (no surfaces). No settings keys, no new npm deps, no PATCHES_OWED opened or closed. G10.5 self-deferred (pure additive data plane, no state machine, no user-facing surface). Full recap in `sessions/sb-1-handoff.md`. **Wave 7 BI-E2E CLOSED 2026-04-15** — Playwright critical-flow shipped: 5/5 BI-E2E green + 3/3 QB-E2E unaffected; new `scripts/seed-bi-e2e.ts` (idempotent fixture: company + contact + won-retainer deal + draft invoice + hard-suppression row stubbing Resend send so admin send still flips draft→sent without a live API key); new `tests/e2e/bi-e2e.spec.ts` (5 serial tests covering precondition kill switch, admin drawer Send now, public Payment Element render, signed `payment_intent.succeeded` → paid + idempotent replay, post-paid confirmation page); two infra fixes in `tests/e2e/fixtures/seed-db.ts` that unblock every future webhook-touching spec — (1) wipe schema in-place via `DROP TABLE` loop instead of `fs.unlinkSync` because Next 16 `next start` opens the DB from at least one route worker before globalSetup fires (unlinking left workers pointing at orphaned inodes → "no such table: webhook_events" inside the Stripe webhook handler); (2) post-`drizzleMigrate` loop scans `lib/db/migrations/*.sql` and executes any tag absent from `meta/_journal.json` (drizzle's migrator skips pure-data seed migrations like `0001_seed_settings`, `0013_sp7_webhook_dispatch_setting`, `0014`, `0015`); `playwright.config.ts` `RESEND_API_KEY: ""` → `"re_test_placeholder"` (Resend SDK validates format at construction time). 689/1/0 unit tests unchanged; typecheck clean; full Playwright 8 active green + 8 skipped (gated). Spec-vs-code gap surfaced (not patched here): direct admin `sendInvoice()` doesn't enqueue `invoice_overdue_reminder` cron — only `handleManualInvoiceSend` does; spec §6.1 wants both. Brief step 4 (live Stripe PI test under `BI_E2E_STRIPE=1`) deferred — hermetic dispatcher coverage already exists in `tests/stripe/dispatch-payment-intent-invoice.test.ts`. Full recap in `sessions/bi-e2e-handoff.md`. **Wave 7 complete.** BI-2b-ii CLOSED 2026-04-15 — three Claude prompts under `lib/ai/prompts/branded-invoicing/` (send / reminder covering both automated+manual via `reminderCount` / supersede-notification; aligned with spec §6 over brief's 4-prompt list per `feedback_technical_decisions_claude_calls`); three composer wrappers under `lib/invoicing/compose-{send-email,reminder-email,supersede-email}.ts` with drift-check + deterministic fallback on kill-switch/parse-fail/empty-response; spec §6.2 first-reminder "overdue" sanitiser (subject-regex → forces fallback); shared `email-html.ts#paragraphsToInvoiceHtml()` (SuperBad red CTA, server-owned link); `sendInvoiceReminder()` gained `draftOverride?` for Andy-edited drafts; `prepareReminderAction` + extended `sendReminderAction({draft?})` Server Actions; full Claude-draft Send modal on the admin drawer mirroring `send-quote-modal.tsx` (drift badge, 60-char subject counter, editable paragraphs, fallback warning); 6 new tests in `tests/bi2b-ii-compose-emails.test.ts`. **689/1/0 green** (+6), typecheck clean, G4 clean. No schema, no migration, no env, no new deps, no PATCHES_OWED opened or closed. Full recap in `sessions/bi-2b-ii-handoff.md`. **BI-2b-i CLOSED 2026-04-15** — Stripe Payment Element + webhook invoice branches + scroll-snap client experience: `lib/invoicing/load-public-invoice.ts` (public token loader + inverse supersede), `app/api/invoices/[token]/payment-intent/route.ts` (PI creation with `ensureStripeCustomer` + idempotencyKey `invoice_pi:{id}` + metadata `product_type:"invoice"` + reuse existing PI when amount unchanged + back-fill `deals.stripe_customer_id`), `payment-intent-succeeded` + `payment-intent-failed` webhook handlers gain `product_type === "invoice"` branches (succeeded calls `markInvoicePaid` with idempotent `alreadyPaid` short-circuit; failed logs `invoice_payment_failed` activity), `components/lite/invoices/invoice-web-experience.tsx` (two-section scroll-snap §4.4 with fixed dot-indicator, bank transfer + Payment Element in `motion.div layoutId="quote-primary-action"` Tier-2 morph, overdue banner, paid confirmation), `components/lite/invoices/superseded-invoice-card.tsx` (void-state takeover with replacement link variant), `app/lite/invoices/[token]/page.tsx` rewritten from BI-1b min-viable, `PaymentElementHost` reused via `paymentIntentEndpoint` prop (no duplication), `.env.example` seeds `SUPERBAD_ABN` + `SUPERBAD_BILLING_EMAIL` + bank details (pre-launch populate in Coolify), 8 new tests in `tests/stripe/dispatch-payment-intent-invoice.test.ts` (missing/unknown invoice_id, happy-path flip, idempotent re-delivery, customer back-fill + preservation, failure-branch logging). **683/1/0 green** (+8), typecheck clean. No schema, no migration, no settings keys, no new deps. Full recap in `sessions/bi-2b-i-handoff.md`. Manual browser check deferred to bi-2b-ii wave close. `qbe2e_manual_quote_settled_missing` PATCHES_OWED row unchanged. BI-2a CLOSED 2026-04-15 — admin surfaces shipped: `/lite/admin/invoices` index (summary cards, motion-powered filter tabs, search, row→drawer), invoice detail drawer with status-driven action matrix (draft|sent|overdue|paid|void), 10 Server Actions covering create-manual / line-item add-remove / due-date / send-now / reminder / mark-paid / void / supersede / update-payment-terms, business-logic primitives in `lib/invoicing/admin-mutations.ts` (supersede = void source + spawn draft carrying forward line items; deterministic reminder bumps `reminder_count` + cancels pending overdue cron), `cancel-scheduled.ts` idempotency-key cancellation helper, company profile tab nav (Trial Shoot | Billing) with payment-terms select + read-only bank-details card + new-invoice button, `loadInvoiceDetail()` detail query with inverse supersede lookup (no migration). 8 new tests (`bi2a-index-query.test.ts` FY/month math + `bi2a-supersede.test.ts` supersede/void/reminder). 675/1/0 green. G4 literal grep PASS; G12 typecheck PASS. Deferred to BI-2b per brief: Stripe Payment Element, Claude email drafting, scroll-snap upgrade, env-seed, `ensureStripeCustomer`. **BI-2 was split 2026-04-15** into BI-2a (admin, now CLOSED) + BI-2b (integrations). BI-1b CLOSED 2026-04-15 — `lib/invoicing/pdf-template.tsx` (ATO tax-invoice, GST-itemised, PAID-stamp variant, status banners) + `lib/invoicing/render-invoice-pdf.ts` (reproducible from id, reuses `lib/pdf/render.ts`) + `/api/invoices/[token]/pdf` (token-gated, streams inline) + `/lite/invoices/[token]` minimum-viable public read-only view + `proxy.ts` allowlist for `/lite/invoices/` + kill-switch **flipped** `invoicing_manual_cycle_enqueue_enabled: false → true` + QB-E2E precondition + post-accept `manualInvoiceRows.toHaveLength(...)` assertion both flipped to match; 8 new tests (668/1/0); G10.5 PASS_WITH_NOTES (SUPERBAD_ABN env-fallback + token-miss audit-logging noted, both deferred to BI-2/launch). Narrowed PATCHES_OWED `ensureStripeCustomer()` row to BI-2 (BI-1a/b ship no Stripe-touching paths). BI-1a CLOSED 2026-04-15 — schema + primitives + handlers + sweep + mark-paid + 24 tests. **Previously — Wave 7 BI-1** — Branded Invoicing kickoff. Original brief at `sessions/bi-1-brief.md`. QB-E2E CLOSED 2026-04-14 — full `deal → quote (sent) → viewed → accepted` Playwright suite green (3/3, 636 unit tests still green); webServer switched to `next build && next start` because Next 16 Turbopack dev-server silently dropped better-sqlite3 writes from RSC (fix applied; previous `qb4c_stripe_client_eager_build_break` also closed in-session); opened `qbe2e_manual_quote_settled_missing` in PATCHES_OWED (manual accept path doesn't log `quote_settled` activity row — spec-vs-code gap). QB-8 CLOSED 2026-04-14 — `lib/subscription/{remainder,early-cancel}.ts` pure helpers + state-flip helpers (`computeEarlyCancelRemainder`, `beginEarlyCancelIntent`, `abandonEarlyCancelIntent`, `finaliseEarlyCancelPaidRemainder`, `finaliseEarlyCancelBuyout`); 636/1 green (+17 tests); no schema, no migration, no env, no kill-switch. QB-7 CLOSED 2026-04-14. Manual browser verification for QB-2b/3/4a/4b/4c/QB-6/QB-7 still owed (non-blocking).

**Previously — Wave 6 QB-5 (surgical)** — Stripe `payment_intent.payment_failed` handler + metadata-contract reframing. Landed: `lib/stripe/webhook-handlers/payment-intent-failed.ts` (logs `kind:"note" + meta.kind:"quote_payment_failed"` with Stripe failure code/message; quote status stays `accepted` per spec §7.1; non-quote PIs skipped `covered_by_checkout_session`; uses `database.insert(activity_log)` directly for dbArg test-db threading). Registered in `dispatchStripeEvent()`. 6 new tests in `tests/stripe/dispatch-payment-intent-failed.test.ts` (hermetic SQLite, mirrors SP-7 harness). **562/562 tests green** (was 556), typecheck clean, G4 clean. No schema change, no migration, no settings keys, no new npm deps. PATCHES_OWED updates: `qb4c_payment_failed_handler` ✅ CLOSED; `sp7_if_stripe_metadata_contract` reframed (Intro Funnel is PaymentIntent-only per spec §11.2 — Checkout-Session contract forward-declared); `sp7_qb_stripe_metadata_contract` reframed (QB-4c PaymentIntent satisfies the metadata contract; Checkout-Session flavour carries forward to any future SaaS/retainer Checkout code path). Subscription-lifecycle webhooks (`customer.subscription.*` + `invoice.payment_*`) explicitly deferred to a dedicated session — too large to bundle. `acceptQuote()` + PI-succeeded quote-branch coverage tests still thin (QB-4c carry carried forward). Full recap in `sessions/qb-5-handoff.md`.

**Previously — Wave 6 QB-2b** — live preview motion (300ms debounce + houseSpring crossfade on affected preview block + mobile/desktop frame-width animation per spec §4.1 motion block) + **Templates CRUD** at `/lite/admin/settings/quote-templates` (spec §4.5) + **Catalogue CRUD** at `/lite/admin/settings/catalogue` (spec §4.6). Medium-context session — each of the three is self-contained. Drop-in point for the preview upgrade: swap `components/lite/quote-builder/preview-pane.tsx` (props contract stable: `content`, `totals`, `structure`, `quoteNumber`, `companyName`, `device`). Consider extracting sections §1–§4 of `components/lite/quote-builder/quote-editor.tsx` into a shared `QuoteBodyFields` sub-component before writing the templates editor so both surfaces render identical fields. QB-2a landed: `lib/quote-builder/content-shape.ts` (`QuoteContent` v1 type + `emptyQuoteContent()` / `computeTotals()` / `inferStructure()`); `lib/quote-builder/draft.ts` (`createDraftQuote` idempotent per deal, `findOpenDraftForDeal`, `updateDraftQuote` with `QuoteNotDraftError` status-guard, totals/structure recompute on update); routes `/lite/admin/deals/[id]/quotes/new` (creates-or-reuses → redirects) + `.../[quote_id]/edit` (admin-gated editor); Server Action `updateDraftQuoteAction`; client components `quote-editor.tsx` (two-pane 40/60, 5 sections + sidebar, toast on save), `catalogue-picker.tsx` (BaseUI popover, search + category filter + retainer/one-off kind), `preview-pane.tsx` (static approximation). Canonical total rule locked: `total_cents_inc_gst = retainer_monthly + one_off` (first-invoice framing). 13 new tests (9 pure helper + 4 integration with mocked settings + seeded user/company/deal). **542/542 tests green** (was 529/529), typecheck clean, G4 clean (only tunable is `quote.default_expiry_days` routed through `settings.get()`). No schema change, no migration, no settings writes, no new npm deps, no PATCHES_OWED opened or closed. Full recap in `sessions/qb-2a-handoff.md`.

**Previously — Wave 6 QB-1** — QB-1 landed: 4 new tables (`quotes`, `catalogue_items`, `quote_templates`, `sequences`) + companies `gst_applicable`/`abn` via migration `0016_qb1_quote_builder_schema.sql` (journal-tracked idx 16); `lib/quote-builder/sequences.ts#allocateQuoteNumber()` atomic year-scoped `SB-YYYY-NNNN` allocator; `lib/quote-builder/transitions.ts` state-machine guard (§5.1 matrix + terminal helpers); `lib/scheduled-tasks/handlers/` registry with 6 QB stubs throwing `NotImplementedError` (filled by QB-6); 2 settings keys seeded (`quote.default_expiry_days`=14, `quote.setup_fee_monthly_saas`=0), registry 84 → 86. **529/529 tests green** (was 514), typecheck clean, G4 clean. Two PATCHES_OWED stay open (`sp7_if_stripe_metadata_contract`, `sp7_qb_stripe_metadata_contract`) — both gate on QB-5. Catalogue content mini-session still owed (structural shape only shipped). Full recap in `sessions/qb-1-handoff.md`. Pre-session for QB-2 should (re-)pick up `BUILD_PLAN.md` Wave 6 + `docs/specs/quote-builder.md` §4.1 (editor UI).

**Previously — Wave 5 SP-9** — per `BUILD_PLAN.md` Wave 5 (last row of the CRM spine). Pre-session should pick up `sp7_if_stripe_metadata_contract` + `sp7_qb_stripe_metadata_contract` (Intro Funnel + Quote Builder Checkout Session metadata). SP-9 landed: `lib/copy/empty-states.ts` bank (10 entries) + `stage-config.ts` refactor to `emptyKey`; `useToastWithSound()` hook at `components/lite/toast-with-sound.tsx` (the locked §11A.2 toast primitive); pipeline toast copy pass per §11A.2 (Moved/Won-stripe/Won-manual/Lost/snooze — sound keys mapped against the locked 8-sound registry via comments, name-reconciliation owed in PATCHES_OWED `sp9_sound_registry_name_map`); S&D admin egg #3 "Three Wons in a session" — client ref-counter + server ≤once/30-day gate at `app/lite/admin/pipeline/three-wons-egg.ts` + new key `pipeline.sd_three_wons_last_fired_ms` (migration `0015_sp9_three_wons_egg_setting.sql`, registry 83 → 84); S&D spec admin catalogue patched with egg #3. Four PATCHES_OWED opened: `sp9_three_wons_migrate_to_hidden_egg_fires` (gate: S&D `hidden_egg_fires` table landing), `sp9_bounce_rollback_toast` + `sp9_payment_failed_urgent_toast` (gate: realtime channel), `sp9_sound_registry_name_map` (gate: design-system-baseline revisit). 10 new tests (5 empty-state bank + 5 three-wons gate). **514/514 tests green**, typecheck clean, G4 clean. Manual browser verification for SP-5 + SP-6 + SP-9 still owed (non-blocking; no regression). Full recap in `sessions/sp-9-handoff.md`.
**Previously earlier — Wave 5 SP-9** — per `BUILD_PLAN.md` Wave 5 (last row of the CRM spine). Confirm scope from build plan at session start. SP-8 landed: `lib/resend/webhook-handlers/` module (`dispatchResendEvent` + `email-bounced` + `email-complained` + `types`); kill-switch `pipeline.resend_webhook_dispatch_enabled` (seed migration `0014_sp8_resend_webhook_setting.sql`, registry 81 → 82); `app/api/resend/webhook/route.ts` — Svix-verified receiver mirroring the SP-7 Stripe route: `external_call_log` + `webhook_events` insert-on-conflict-do-nothing → dispatch → stamp outcome → always 200; replay returns `{dispatch:'replay'}`; bad sig / missing headers → 400. Hard bounce → `contacts.email_status='invalid'` + `email_suppressions` (kind=bounce) + rollback `contacted → lead` via `transitionDealStage` (safety rule: still in `contacted`). Soft bounce → `soft_bounce` + suppression, no rollback. Complaint → `complained` + `companies.do_not_contact=true` + rollback `contacted|conversation → lead`; freeze enforced by DNC gate in `canSendTo`. Contact-not-found still writes suppression and returns `skipped:contact_not_found`. `RESEND_WEBHOOK_SECRET` added to `.env.example`. 12 new tests (5 bounced + 4 complained + 3 kill-switch). **504/504 tests green**, typecheck clean, G4 clean (kill switch via `settings.get()`; bounce type strings + event types are schema, not tunables). No new route other than the webhook receiver; no new schema (reused SP-1 tables). One PATCHES_OWED opened: **SP-8-b** (signed-Resend-webhook E2E smoke spec, opt-in via `SP8_WEBHOOK_E2E=1`). Multi-contact picker UI on hard bounce deferred (pipeline-card concern, not webhook). `email.sent` auto-advance `lead → contacted` deferred to Lead Gen / Outreach (needs per-send→deal association). Manual browser verification for SP-5 + SP-6 still owed (not regressed; seed instructions in those handoffs). Full recap in `sessions/sp-8-handoff.md`.
**Previously — SP-7 landed:** `lib/stripe/webhook-handlers/` module (`dispatchStripeEvent` + `checkout-session-completed` + `payment-intent-succeeded` + `types`); kill-switch `pipeline.stripe_webhook_dispatch_enabled` (seed migration `0013_sp7_webhook_dispatch_setting.sql`); `markCompanyTrialShootBooked` intro-path helper (one tx: deal→`trial_shoot` + company→`booked` + `trial_shoot_booked` activity row); `finaliseDealAsWon` signature extended with optional `value_cents` (stamps + flips `value_estimated=false`; SP-6 callers unchanged); `app/api/stripe/webhook/route.ts` rewritten to insert-on-conflict-do-nothing into `webhook_events` (PK = Stripe event id) → dispatch → update outcome → always 200; replay returns `{dispatch:'replay'}`. Branches on `session.metadata.product_type ∈ intro|retainer|saas`; missing/unknown metadata records `webhook_events.result='error'` with diagnostic reason (no 5xx — Stripe retries are worse than recorded failures). `payment_intent.succeeded` registered as known-skipped (`covered_by_checkout_session`). 13 new tests (9 dispatch + 2 PI-succeeded + 2 kill-switch) — **492/492 tests green**, typecheck clean, G4 clean (kill switch via `settings.get()`; enums aren't tunables). E2E spec `tests/e2e/stripe-webhook-critical-flow.spec.ts` ships opt-in via `SP7_WEBHOOK_E2E=1`; `buildSignedWebhook` fixture extended to accept custom data objects. Two PATCHES_OWED opened (`sp7_if_stripe_metadata_contract` gate: Intro Funnel build; `sp7_qb_stripe_metadata_contract` gate: Quote Builder build) — these specs' Checkout Session creation must stamp `metadata.deal_id` + `metadata.product_type`. Manual browser verification for SP-5 + SP-6 still owed (not regressed by SP-7; seed instructions in sp-5-handoff.md + sp-6-handoff.md). Full recap in `sessions/sp-7-handoff.md`.
**Previously earlier — SP-6 landed:** generic `DestructiveConfirmModal` primitive (simple + type-to-confirm modes), sales-pipeline `WonConfirmModal` (outcome picker + billing_mode-branched confirm — `stripe` → type-the-company-name per §3.3 safety valve; `manual` → simple confirm) and `LossReasonModal` (7-reason closed list, notes required on `other`), transactional `finaliseDealAsWon()` / `finaliseDealAsLost()` in `lib/crm/finalise-deal.ts` (atomic: finalisation fields + `transitionDealStage` in one `database.transaction`; Won path clears stale `loss_reason`/`loss_notes`; Lost path pre-transaction guards `'other'` requires trimmed notes — direct callers can't bypass), two new Server Actions (`finaliseWonAction` / `finaliseLostAction`) each with admin re-check + `revalidatePath`, `transitionDealAction` now rejects `won`/`lost` with a route-to-finalisation-action error. `PipelineBoard` Won/Lost drop toast-block removed — drop now opens the matching modal carrying the card; optimistic update after `{ok:true}`. `PipelineCardDeal.billing_mode` added + page query selects `companies.billing_mode`. 9 new unit tests covering atomicity, meta propagation, stale-loss-field clearing, all three outcomes, null-notes acceptance, `'other'` null + whitespace-only rejection, illegal-transition rollback assertion. **479/479 tests green at SP-6 close**, typecheck clean, G4 clean (closed enums aren't tunables). No schema change, no migration, no new settings keys, no new routes, no PATCHES_OWED opened or closed. Drag-out-of-Won explicitly deferred (Won is terminal in `LEGAL_TRANSITIONS`; reversal story owed before the matrix widens). Recap in `sessions/sp-6-handoff.md`.
**Original prior note — SP-6 next-session block:** <previously pointed at SP-7 with pre-session browser-verification checklist; SP-7 now closed, browser verification still owed but non-blocking.> Pre-session should clear manual browser verification owed from SP-5 + SP-6 (seed company with `trial_shoot_status='booked'` and deals in varying stages; walk the TrialShootPanel stepper + drag deals into Won (both `stripe` and `manual` billing) and Lost (including `Other` with notes required); confirm toasts + activity_log rows + stage_change meta). SP-6 landed: generic `DestructiveConfirmModal` primitive (simple + type-to-confirm modes), sales-pipeline `WonConfirmModal` (outcome picker + billing_mode-branched confirm — `stripe` → type-the-company-name per §3.3 safety valve; `manual` → simple confirm) and `LossReasonModal` (7-reason closed list, notes required on `other`), transactional `finaliseDealAsWon()` / `finaliseDealAsLost()` in `lib/crm/finalise-deal.ts` (atomic: finalisation fields + `transitionDealStage` in one `database.transaction`; Won path clears stale `loss_reason`/`loss_notes`; Lost path pre-transaction guards `'other'` requires trimmed notes — direct callers can't bypass), two new Server Actions (`finaliseWonAction` / `finaliseLostAction`) each with admin re-check + `revalidatePath`, `transitionDealAction` now rejects `won`/`lost` with a route-to-finalisation-action error. `PipelineBoard` Won/Lost drop toast-block removed — drop now opens the matching modal carrying the card; optimistic update after `{ok:true}`. `PipelineCardDeal.billing_mode` added + page query selects `companies.billing_mode`. 9 new unit tests covering atomicity, meta propagation, stale-loss-field clearing, all three outcomes, null-notes acceptance, `'other'` null + whitespace-only rejection, illegal-transition rollback assertion. **479/479 tests green**, typecheck clean, G4 clean (closed enums aren't tunables). No schema change, no migration, no new settings keys, no new routes, no PATCHES_OWED opened or closed. Drag-out-of-Won explicitly deferred (Won is terminal in `LEGAL_TRANSITIONS`; reversal story owed before the matrix widens). Full recap in `sessions/sp-6-handoff.md`.

**Previously — SP-5 landed:** 6-state `trial_shoot_status` sub-machine (`lib/crm/trial-shoot-status.ts` constants + `legalForwardTargets`; `advance-trial-shoot-status.ts` transactional helper with forward-only guard + `completed_at_ms` first-stamp; `advanceTrialShootStatusOnFeedback()` no-op convenience hook for Intro Funnel feedback path), `updateTrialShootPlan()` helper, admin `/lite/admin/companies/[id]` thin route (header + back-to-pipeline + `TrialShootPanel`), and panel UI (stepper, plan textarea with dirty/save, read-only feedback block, completion timestamp Australia/Melbourne). Activity logged as `kind='note'` + `meta.kind='trial_shoot_status_change'`/`'trial_shoot_plan_updated'` (matches SP-4 snooze pattern — no enum widening). Two Server Actions with admin re-check + path revalidate. 18 new unit tests; 470/470 green; typecheck clean; G4 clean; no settings keys, no migration, no PATCHES_OWED. SheetWithSound + full Company profile deferred to Client Management spec. Manual browser verification owed next session (seed a company with `trial_shoot_status='booked'`, step through to completed, save a plan).

**Previously — SP-4 landed:** `snoozeDeal()` + `snoozeDealAction` + `SnoozePopover` (1/3/7d presets + custom end-of-day date input; default reads `pipeline.snooze_default_days`); card optimistically drops halo. Manual browser verification for SP-4 still owed (seed fixtures from `scripts/seed-pipeline.ts` still present — open a stale card → Snooze → confirm halo drop + toast). Wave-4 residue still flaggable: OAuth callback hardening trio (SW-7-b / SW-10-b / SW-11-b) pending Azure/Meta/Google Cloud app registrations; `/lite/integrations` hub page unshipped; shared OAuth E2E harness non-owed.

**SP-3 outcome (2026-04-14):** Sales Pipeline Kanban board landed at `/lite/admin/pipeline`. Ships generic `components/lite/kanban-board.tsx` (dnd-kit `DndContext` + `DragOverlay`, house spring on drag overlay, reduced-motion fallback to opacity-only; zero deal vocabulary per spec §12.5 — ready for Hiring Pipeline reuse), pipeline-specific `components/lite/sales-pipeline/` layer (`stage-config.ts` 8-column warm-tint progression with cream wash on Won + pink wash on Trial Shoot per §5.2; `deal-card.tsx` 5-element compact + 300ms hover-intent overlay with primary contact / last activity / 3 quick-action stubs; `won-badge.tsx` RETAINER pink/cream + SAAS orange/charcoal BHS caption — PATCHES_OWED row opened for the 9th BHS location; `pipeline-board.tsx` client wrapper with pessimistic transitions + `LEGAL_TRANSITIONS` client mirror + Won/Lost drop-block toast "ships in SP-6"). Ships `app/lite/admin/pipeline/page.tsx` admin-gated Server Component (loads deals ⨝ companies ⨝ primary-contact in one query; resolves fallback contact per company via `is_primary DESC, created_at_ms ASC`; computes `isDealStale` server-side with thresholds from `settings.get()`; `max(activity_log.created_at_ms)` per deal via grouped query for hover-overlay "N days ago" label) and `app/lite/admin/pipeline/actions.ts` (`transitionDealAction` with admin re-check + Won/Lost server-side rejection + `revalidatePath`). Ships `lib/crm/is-stale.ts` pure helper (terminal stages never stale; future `snoozed_until_ms` suppresses; expired snooze doesn't; nullish `last_stage_change_at_ms` defensively returns false) + 24 tests. Migration `0012_sp3_pipeline_settings.sql` seeds 7 keys: 6 per-stage stale thresholds (lead 14 / contacted 5 / conversation 7 / trial_shoot 14 / quoted 5 / negotiating 3 days) + `pipeline.snooze_default_days=3`; registry + totals updated to 80 keys; existing `tests/settings.test.ts` seed-count bumped 74→81. Installed `@dnd-kit/core` + `/sortable` + `/utilities` (React 19 compatible; peer `react: >=16.8.0` no upper bound). **447/447 tests green**, typecheck clean, G4 literal-grep clean (hover-intent 300ms + spring constants ruled UI timing not autonomy). Manual browser verification deferred in-session (requires dev server + fixture deals) — owed before SP-4 kickoff per handoff §Verification. SP-4 brief pre-compiled. Full recap in `sessions/sp-3-handoff.md`.

**SP-2 outcome (2026-04-14):** Deal state model + transitions. Ships `lib/crm/validate-deal.ts` (pure `validateDeal()` per spec §4.2 — `won`⇒`won_outcome`, `lost`⇒`loss_reason`, `lost`+`other`⇒`loss_notes`) and `lib/crm/transition-deal-stage.ts` (`LEGAL_TRANSITIONS` matrix collapsing §3.2 forward auto-edges + §3.3 manual backward overrides into one map; `won` terminal `[]`; `lost` terminal *except* `lost → lead` rekindle; `transitionDealStage(dealId, toStage, {by, meta?, nowMs?}, dbArg?)` — loads deal, rejects unknown/identity/illegal/finalisation-incomplete, updates `stage` + `last_stage_change_at_ms` + `updated_at_ms` + clears `next_action_overridden_at_ms`, writes `activity_log.kind="stage_change"` with `meta={from_stage, to_stage, by, ...extra}` all inside `database.transaction(tx)`; reserved meta keys (`from_stage`/`to_stage`/`by`) cannot be shadowed by caller-supplied meta). Re-open from `won` deferred to SP-3+ as explicit helper (not a matrix widening). `lib/crm/index.ts` re-exports both helpers + matrix + types. 25 new tests across `tests/crm/validate-deal.test.ts` (7) and `tests/crm/transition-deal-stage.test.ts` (18 — matrix shape invariants, happy-path activity_log meta assertion incl. extra-meta merge, `next_action_overridden_at_ms` reset, won-with-outcome / lost→lead rekindle, every rejection path, no-activity-row-on-rejection assertion, full legal-edge round-trip + full illegal-edge rejection sweeps). **423/423 tests green**, typecheck clean. No schema change, no migration, no settings keys, no new routes, no env vars. File whitelist adhered to. No PATCHES_OWED opened or closed. Full recap in `sessions/sp-2-handoff.md`.

**SP-1 outcome (2026-04-14):** CRM spine landed — four new schema files (`companies` with canonical 3-tuple `shape` F1.b + `billing_mode` default `stripe` + `trial_shoot_*` + indexed `name_normalised`/`domain`; `contacts` with cascade FK to company + unique `stripe_customer_id` per SaaS §11.2 + indexed `email_normalised`/`phone_normalised`; `deals` owning the locked 8-tuple `DEAL_STAGES` + 3-tuple `DEAL_WON_OUTCOMES` including `project` per Quote Builder §5.6 + 7-tuple `DEAL_LOSS_REASONS` + 7-tuple `DEAL_SUBSCRIPTION_STATES` per QB §5.6 + 3-tuple `DEAL_BILLING_CADENCES` — full subscription lifecycle columns owned on `deals` per BUILD_PLAN directive to keep SaaS wave free of schema churn; `webhook_events` idempotency ledger for Stripe + Resend with composite unique `(provider, event_id)`), plus edited `activity_log` adding FK refs (company cascade, contact set-null, deal cascade). Migration `0011_sp1_crm_spine.sql` **hand-authored** — drizzle-kit's auto-generator tried to recreate 0005–0010 because those migrations were hand-written without paired snapshots; the canonical escape hatch is hand-writing the SQL while keeping 0011's auto-generated snapshot (which correctly captures full schema for future diffs). Ships `lib/crm/normalise.ts` (email trim+lowercase, phone digit-only — E.164 canonicalisation intentionally out of scope per §10.4 "digits only for dedupe", company name squeezed+punctuation-stripped, domain scheme+www+path stripped) and `lib/crm/create-deal-from-lead.ts` (main helper — resolves/creates company by normalised name + optional domain disambiguation, then contact by normalised email primary / phone fallback scoped within the resolved company per §10.4, then creates deal + `stage_change` activity_log row all inside one `database.transaction(tx)`; non-destructive merge never blanks a populated field; source-driven stage override map lands `intro_funnel_contact_submitted` + `intro_funnel_paid` in `trial_shoot`, every other source defaults to `lead`). 27 new tests across `tests/crm/schema.test.ts` (14 — every enum shape locked, normalisation unit tests) and `tests/crm/create-deal-from-lead.test.ts` (13 — happy path, dedupe matrix, non-destructive merge, source override, company dedupe by normalised name+domain, validation errors, FK wiring: orphan rejected + cascade-delete on company). Pre-existing `tests/activity-log.test.ts` updated to seed parent company before activity_log insert (FKs now enforced). **398/398 tests green**, typecheck clean. No settings keys touched, no new routes, no new env vars. No PATCHES_OWED opened or closed. SP-2 brief pre-compiled. Full recap in `sessions/sp-1-handoff.md`.

**SW-13 outcome (2026-04-14):** Generic multi-vendor API-key wizard closes Wave 4 — last row of spec §5.1. **First wizard def serving N vendors from one module.** Ships four vendor manifests (`lib/integrations/vendors/openai.ts`, `anthropic.ts`, `serpapi.ts`, `remotion.ts` — each with `vendorKey` + 1–2 Observatory bands sized to vendor semantics; OpenAI/Anthropic/SerpAPI also export base URLs; Anthropic exports `ANTHROPIC_VERSION_HEADER="2023-06-01"`). `lib/wizards/defs/api-key.ts` — single `WizardDefinition<ApiKeyPayload>` composing `api-key-paste → review-and-confirm → celebration`; exports `ApiKeyVendor` union + `API_KEY_VENDOR_PROFILES` map + `isApiKeyVendor` guard + `getApiKeyVendorProfile` helper; `completionContract.required=["vendor","apiKey","verifiedAt","confirmedAt"]`; `vendorManifest` intentionally undefined (per-vendor manifest resolved at completion time from `payload.vendor`); `verify` dispatches on vendor — live pings for OpenAI `/v1/models` (bearer), Anthropic `/v1/models` (x-api-key + anthropic-version; cheaper than `/v1/messages`, doesn't burn tokens), SerpAPI `/account?api_key=…`; **Remotion format-only check** (length ≥ 20; no hosted endpoint exists for commercial licence keys — documented in manifest `humanDescription`). Barrel imports `./api-key`; registry carries **8 wizards** now. **Dispatcher refactor landed (Option A per brief §4)** — `app/lite/setup/admin/[key]/page.tsx` replaces the 4-branch if/else-if chain with `CLIENT_MAP: Record<string, ClientRenderer>`; renderer args `{ common, allowTestTokenInjection, searchParams }` cover every current wizard; adding a new wizard = add one row. Page now accepts `searchParams: Promise<…>` (Next 15/16 convention). `api-key` row validates `?vendor=` via `isApiKeyVendor`, `notFound()` on unknown; vendor validated defensively in the action too. New files: `.../clients/api-key-client.tsx` (vendor-agnostic per-wizard client receiving `vendor` + `vendorLabel` as props; configures the api-key-paste step's `testCall` via `testApiKeyAction(vendor,key)`; review summary surfaces vendor label + masked key suffix), `.../actions-api-key.ts` (two actions — `testApiKeyAction(vendor,key)` dispatches to profile verify; `completeApiKeyAction(payload)` resolves per-vendor manifest, runs standard `registerIntegration → verifyCompletion → wizard_completions insert`, stores `metadata.vendor` + `metadata.verified_at_ms` alongside vault-encrypted credential). **Architecturally novel:** each vendor writes its own `integration_connections` row keyed on `vendor_key="openai"/"anthropic"/"serpapi"/"remotion"` while `wizard_completions.wizard_key` stays `"api-key"` — consumer feature sessions look up credentials by vendor_key, which is the natural key for "give me the OpenAI token", not by wizard_key. 9 new unit tests (371/371 green — barrel registration asserts 8 wizards; tests cover step composition, contract shape with `vendor` required, undefined vendorManifest, per-vendor profile/manifest wiring, type-guard coverage, Remotion format-only verify, unknown-vendor rejection via completion dispatch, live OpenAI rejection with bogus key). New Playwright spec `tests/e2e/admin-api-key-openai.spec.ts` — **first E2E asserting the wizard-key / vendor-key divergence**; walks `/lite/setup/admin/api-key?vendor=openai` → paste → test-key (live) → masked-suffix → continue → review → celebration; `test.skip()` when `OPENAI_TEST_KEY` unset. `.env.example` documents 4 new `*_TEST_KEY` vars with explicit callout that app code never reads them (distinct from `ANTHROPIC_API_KEY` which is read directly by `lib/ai/models.ts` + kill-switches). Typecheck clean, lint clean, build clean (no new routes — generic api-key lives under existing `/lite/setup/admin/[key]`, no oauth callback), `npm run test:e2e` reports 7 skipped (6 prior + new admin-api-key-openai). No migration, no settings keys touched, no schema change. **No PATCHES_OWED row opened** — no callback (no oauth), no cross-spec flag; dispatcher-abstraction four-session deferral closed (was tracked as "reassess when generic API-key lands", not as a PATCHES_OWED row). Wave 4 complete. Next session kickoff: SP-1 (Wave 5 Sales Pipeline). Full recap in `sessions/sw-13-handoff.md`.

**SW-12 outcome (2026-04-14):** Fourth non-critical admin wizard (`twilio`) ships the form-step pattern with a multi-field credential. `lib/integrations/vendors/twilio.ts` manifest (`vendorKey="twilio"`, two bands: `twilio.account.read` p95 600 / p99 1800 ms, `twilio.message.send` p95 900 / p99 2400 ms; exports `TWILIO_API_BASE="https://api.twilio.com/2010-04-01"`). `lib/wizards/defs/twilio.ts` (`WizardDefinition<TwilioPayload>` composing `form → review-and-confirm → celebration`; two-field Zod schema — SID regex `^AC[a-f0-9]{32}$`, Auth Token regex `^[a-f0-9]{32}$`; `completionContract.verify` hits `GET ${TWILIO_API_BASE}/Accounts/<SID>.json` with HTTP Basic auth; exports `maskTwilioSid` / `maskTwilioToken` helpers; capstone undefined — non-critical). Barrel imports all seven wizards. Dispatcher in `app/lite/setup/admin/[key]/page.tsx` extended to a four-branch if/else-if chain across **two distinct props shapes** (form-only × 2, oauth × 2) — `CLIENT_MAP` abstraction **still deferred** (fourth time); real trigger is SW-13's vendor-picker prop (first dynamically selected shape). New files: `.../clients/twilio-client.tsx` (copied from pixieset-admin-client with two-field form state + masked review summary — first admin wizard masking credentials in review), `.../actions-twilio.ts` (**first wizard storing a multi-field credential blob** — `{ accountSid, authToken }` JSON-stringified into `credentials.plaintext` since the vault column is typed `string`; `account_sid` duplicated in `metadata` for queryability without decryption). 6 new unit tests (362/362 green — includes live `api.twilio.com` bad-credential ping per the three-session precedent). New Playwright spec `tests/e2e/admin-twilio.spec.ts` — **first admin E2E without `?testToken=` injection**, types real creds into both form fields; `test.skip()` when either `TWILIO_TEST_SID` or `TWILIO_TEST_TOKEN` unset. `.env.example` documents `TWILIO_TEST_SID`, `TWILIO_TEST_TOKEN` plus an explicit "no app-registration step, so no CLIENT_ID/SECRET owed" note. Typecheck clean, lint clean, build clean (no new route — form-step, no callback). `npm run test:e2e` reports 6 skipped (5 prior + admin-twilio). No migration, no settings keys touched, no schema change. **No PATCHES_OWED row opened** — form-step wizards have no callback-skeleton debt; first non-critical wizard in three sessions to not owe a `swNb_*_oauth_callback_hardening` follow-up. Shared OAuth E2E harness still non-owed. SW-13 brief pre-compiled (generic API-key wizard + dispatcher abstraction refactor; likely Wave 4 closure). Full recap in `sessions/sw-12-handoff.md`.

**SW-11 outcome (2026-04-14):** Third non-critical admin wizard (`google-ads`) ships the second oauth-consent pattern on the admin tree. `lib/integrations/vendors/google-ads.ts` manifest (`vendorKey="google-ads"`, two bands: `google.identity.read` p95 600 / p99 1800 ms, `google.ads.campaigns.read` p95 900 / p99 2400 ms; exports `GOOGLE_OAUTH_SCOPES=["https://www.googleapis.com/auth/adwords","openid","email","profile"]`, `GOOGLE_OAUTH_AUTHORIZE_URL="https://accounts.google.com/o/oauth2/v2/auth"`, and `GOOGLE_USERINFO_URL="https://www.googleapis.com/oauth2/v3/userinfo"` as design-time constants; `humanDescription` flags the Ads API `developer-token` HTTP header quirk so ad-campaign-builder doesn't rediscover it). `lib/wizards/defs/google-ads.ts` (`WizardDefinition<GoogleAdsPayload>` composing `oauth-consent → review-and-confirm → celebration`; `completionContract.verify` pings `GET /oauth2/v3/userinfo` via `fetch` with bearer-only auth; capstone undefined — non-critical). `lib/wizards/defs/index.ts` barrel now imports all six wizards. Dispatcher in `app/lite/setup/admin/[key]/page.tsx` extended to a three-branch if/else-if chain (pixieset-admin + meta-ads + google-ads); `buildGoogleAuthorizeUrl()` reads `GOOGLE_ADS_CLIENT_ID`/`NEXT_PUBLIC_APP_URL` and falls back to `"#"` when unset; URL builder ships with `access_type=offline` + `prompt=consent` + `include_granted_scopes=true` now so SW-11-b inherits refresh-token plumbing. **Dispatcher abstraction deferred (third reassessment)** — meta-ads and google-ads share props shape but pixieset diverges; `CLIENT_MAP` would collapse that difference behind a cast for minimal gain. Real trigger point: fourth distinct props shape (likely generic API-key wizard's vendor-picker prop at SW-13). New files: `.../clients/google-ads-client.tsx` (1:1 mirror of meta-ads-client with Google labels), `.../actions-google-ads.ts` (`completeGoogleAdsAction` — `registerIntegration → verifyCompletion → wizard_completions insert`; no `unstable_update()` per admin-tree convention), `app/api/oauth/google-ads/callback/route.ts` (**SW-11-a skeleton** — accepts `?code`/`?error`, redirects to wizard with `?oauth=pending`/`?oauth=error`; real exchange deferred to SW-11-b). 5 new unit tests (356/356 green); new Playwright spec `tests/e2e/admin-google-ads.spec.ts` uses `?testToken=` direct injection, `test.skip()` when `GOOGLE_ADS_TEST_TOKEN` unset. `.env.example` documents `GOOGLE_ADS_CLIENT_ID`, `GOOGLE_ADS_CLIENT_SECRET`, `GOOGLE_ADS_TEST_TOKEN` plus the developer-token note. Typecheck clean, lint clean, build clean (all three oauth callbacks compile), `npm run test:e2e` reports 5 skipped (4 prior + admin-google-ads; cleared stale `.next` turbopack persistence cache to unblock first-run webserver start — unrelated to diff). No migration, no settings keys touched, no schema change. Opened `sw11b_google_ads_oauth_callback_hardening` in `PATCHES_OWED.md`; none closed. Shared OAuth E2E harness (`runOAuthWizardE2E`) flagged as non-owed follow-up — extract alongside first vendor app registration, not now. SW-12 brief pre-compiled (recommendation: Twilio — first form-step wizard since Pixieset). Full recap in `sessions/sw-11-handoff.md`.

**SW-10 outcome (2026-04-14):** Second non-critical admin wizard (`meta-ads`) ships the oauth-consent pattern on the admin tree. `lib/integrations/vendors/meta-ads.ts` manifest (`vendorKey="meta-ads"`, two bands: `meta.identity.read` p95 600 / p99 1800 ms, `meta.campaigns.read` p95 900 / p99 2400 ms; `META_OAUTH_SCOPES=["ads_read","ads_management"]` and `META_GRAPH_API_VERSION="v20.0"` exported as design-time constants), `lib/wizards/defs/meta-ads.ts` (`WizardDefinition<MetaAdsPayload>` composing `oauth-consent → review-and-confirm → celebration`; `completionContract.verify` pings `GET https://graph.facebook.com/v20.0/me?fields=id,name` via `fetch`; capstone undefined — non-critical), `lib/wizards/defs/index.ts` barrel (all five wizards now imported). Dispatcher in `app/lite/setup/admin/[key]/page.tsx` extended to a two-branch if/else-if chain (pixieset-admin + meta-ads); `buildMetaAuthorizeUrl()` reads `META_ADS_CLIENT_ID`/`NEXT_PUBLIC_APP_URL` and falls back to `"#"` when unset (dev parity with graph-api-admin); `allowTestTokenInjection` computed from `NODE_ENV !== "production"`. New files: `.../clients/meta-ads-client.tsx` (mirrors graph-api-admin-client on the admin shell — `useAdminShell` hook, `?testToken=` injection, celebration onComplete), `.../actions-meta-ads.ts` (`completeMetaAdsAction` — `registerIntegration → verifyCompletion → wizard_completions insert`; **no `unstable_update()`** per admin-tree convention), `app/api/oauth/meta-ads/callback/route.ts` (**SW-10-a skeleton** — accepts `?code`/`?error`, redirects to wizard with `?oauth=pending`/`?oauth=error`; real exchange deferred to SW-10-b). 5 new unit tests (351/351 green); new Playwright spec `tests/e2e/admin-meta-ads.spec.ts` uses `?testToken=` direct injection, `test.skip()` when `META_ADS_TEST_TOKEN` unset. `.env.example` documents `META_ADS_CLIENT_ID`, `META_ADS_CLIENT_SECRET`, `META_ADS_TEST_TOKEN`. Typecheck clean, lint clean, build clean (both new routes compile), `npm run test:e2e` reports 4 skipped (3 prior + admin-meta-ads). No migration, no settings keys touched, no schema change. Opened `sw10b_meta_ads_oauth_callback_hardening` in `PATCHES_OWED.md`; none closed. SW-11 brief pre-compiled (recommendation: Google Ads — second oauth branch in admin tree). Full recap in `sessions/sw-10-handoff.md`.

**SW-9 outcome (2026-04-14):** First non-critical admin wizard (`pixieset-admin`) + the non-critical admin route tree it anchors. Ships `lib/integrations/vendors/pixieset.ts` manifest (`vendorKey="pixieset"`, single design-time band `pixieset.link.paste` — no live API per P0 spike outcome B, band exists only so `registerBands()` has something to hash; kill-switch shares `setup_wizards_enabled`), `lib/wizards/defs/pixieset-admin.ts` (`WizardDefinition<PixiesetAdminPayload>` composing `form → review-and-confirm → celebration`; form step's Zod schema enforces `https://<slug>.pixieset.com/…` shape, rejects `http://`, bare `pixieset.com`, and hostnames with `.pixieset.com` only as a path-segment lookalike; `extractPixiesetSlug()` helper exported for the review summary; `completionContract.verify` trivially returns `{ok:true}` — there is nothing to ping — but `artefacts.integrationConnections=true` still fires so the `integration_connections` row remains the source of truth). Barrel (`lib/wizards/defs/index.ts`) now imports all four wizards. Route tree home decided in-session: **`/lite/setup/admin/[key]`** (recommendation from brief + SW-8 open thread; spec §5 patched with the route-tree lock block in the same session). Ships `app/lite/setup/admin/[key]/page.tsx` (Server Component — `getWizard(key)` via barrel side-effect, 404 on unknown or non-admin, reads expiry days from `getWizardShellConfig`), `.../clients/pixieset-admin-client.tsx` (per-wizard client file — same pattern SW-7 established on the critical tree; owns form-step → review summary build-up → celebration orchestrator), `.../clients/use-admin-shell.ts` (non-critical counterpart to `use-critical-flight-shell` — cancel/done route to `/lite` cockpit rather than `/lite/first-run`), `.../actions-pixieset.ts` (single Server Action — `completePixiesetAction` orchestrator running `registerIntegration → verifyCompletion → wizard_completions insert` with rollback on failure; **no `unstable_update()` call** because non-critical wizards don't participate in the `critical_flight_complete` JWT claim). 6 new unit tests (346/346 green — imports via the barrel to assert all four wizards register). Typecheck clean, lint clean, build clean (`/lite/setup/admin/[key]` now in route manifest), `npm run test:e2e` reports 3 skipped (unchanged — E2E is optional per AUTONOMY §G12 for non-critical flows). No migration, no settings keys touched, no schema change. No PATCHES_OWED rows opened; none closed. SW-10 brief pre-compiled. Full recap in `sessions/sw-9-handoff.md`.

**SW-8 outcome (2026-04-14):** Track A (wizard nudge/expiry crons + voice). Ships 3 new `ScheduledTaskType` values (`wizard_resume_nudge`, `wizard_expiry_warn`, `wizard_expire` — brief said `wizard_auto_expire`; spec §11 wins per START_HERE); new independent `wizards_nudges_enabled` kill-switch (default off; brief §7 — Phase-6 shadow can pause comms without freezing the lifecycle); `lib/wizards/nudge/` module — `content.ts` (subject pools + HTML bodies, dry voice, mirrored from canonical source `docs/content/setup-wizards/nudge-emails.md`), `handlers.ts` (3 defensive handlers as `wizardNudgeHandlers: HandlerMap`; re-read `wizard_progress` at fire time, skip on abandoned/stale rows; resume-nudge checks `scheduledForLastActiveAtMs === row.last_active_at_ms`; expire handler intentionally bypasses the nudge kill-switch — only mechanism releasing the partial-unique-index slot on `wizard_progress(user_id, wizard_key)`), `enqueue.ts` (`scheduleWizardNudges(row)` + `cancelWizardNudges(progressId)`; resume-nudge idempotency key includes `last_active_at_ms` so activity updates yield fresh rows + stale tasks no-op at fire time; expiry-warn + expire idempotent per progressId alone). 10 new tests (340/340 green). Helpers ship **unwired** — no `wizard_progress` writer exists yet (SW-1 stopped at schema); handler-map also unwired into any top-level `startWorker()` call (Quote Builder QB-6 composes the full HandlerMap). Resume-URL builder uses a small in-file `CRITICAL` set with `/lite/setup/<key>?resume=1` fallback — non-critical admin wizard route tree deferred to SW-9. No migration (SQLite `task_type` is plain `text`, no CHECK constraint). No schema change. No settings keys opened/closed. Typecheck + lint + build + full test suite clean; E2E not required (non-critical flow per AUTONOMY §G12). SW-9 brief pre-compiled (first task: route-tree decision + in-session spec patch; recommendation `/lite/setup/admin/[key]`). No PATCHES_OWED opened or closed. Full recap in `sessions/sw-8-handoff.md`.

**SW-7 outcome (2026-04-14):** Ships `lib/integrations/vendors/graph-api.ts` manifest (`vendorKey="graph-api"`, two bands: `graph.mail.send` p95 900 / p99 2200 ms, `graph.calendar.read` p95 500 / p99 1500 ms; `GRAPH_OAUTH_SCOPES` exported as a design-time constant — stays out of `settings` because oauth scopes aren't autonomy-sensitive per G4/G8 rubric), `lib/wizards/defs/graph-api-admin.ts` (`WizardDefinition<GraphAdminPayload>` composing `oauth-consent → review-and-confirm → celebration`; `completionContract.verify` pings `GET https://graph.microsoft.com/v1.0/me` via `fetch` — no Graph SDK installed, one endpoint doesn't earn a dependency), `lib/wizards/defs/index.ts` barrel (adds `./graph-api-admin` import — all three critical wizards register from one entrypoint), `app/lite/setup/critical-flight/[key]/actions-graph.ts` (`completeGraphAdminAction` celebration orchestrator; no test-key counterpart because the token arrives via oauth-callback, not a paste), `app/api/oauth/graph-api/callback/route.ts` (**skeleton per SW-7-a path** — accepts Microsoft's `?code`/`?state`/`?error` and redirects back to the wizard with `?oauth=pending`/`?oauth=error`; SW-7-b swaps the stub for real code→token exchange + signed-cookie handoff once Andy registers an Azure app). **Client-split taken per brief §11** — prior monolithic `critical-flight-client.tsx` refactored into a dispatcher + four new files under `.../clients/`: `use-critical-flight-shell.ts` (shared hook owning index/state/navigation), `stripe-admin-client.tsx`, `resend-client.tsx`, `graph-api-admin-client.tsx` (the last reads `?testToken=…` for the E2E path, gated server-side on `NODE_ENV !== "production"` via the `allowTestTokenInjection` prop — production pages never set the flag, injection path is closed by construction). `page.tsx` now computes `graphAuthorizeUrl` from `MS_GRAPH_CLIENT_ID`/`MS_GRAPH_TENANT_ID`/`NEXT_PUBLIC_APP_URL` (falls back to `"#"` when env unset) and passes `allowTestTokenInjection` through. 5 new unit tests (330/330 green — `graph-api-admin-wizard.test.ts` imports via the barrel). New Playwright spec `tests/e2e/critical-flight-graph.spec.ts` uses the `?testToken=` direct-injection path; `test.skip()` when `GRAPH_TEST_TOKEN` unset. `.env.example` documents `MS_GRAPH_CLIENT_ID`, `MS_GRAPH_TENANT_ID`, `GRAPH_TEST_TOKEN`. Typecheck clean (cleared 3 stray iCloud `.next/types/` dupes as prep — not committed), lint clean, build clean (both new routes compile), `npm run test:e2e` reports 3 skipped. No migration, no settings keys touched, no schema change. Opened `sw7b_graph_oauth_callback_hardening` in `PATCHES_OWED.md`; no prior rows closed. SW-8 brief pre-compiled (first task is scope selection between Track A nudge/expiry crons and Track B first non-critical admin wizard; Track A recommended). Full recap in `sessions/sw-7-handoff.md`.

**SW-6 outcome (2026-04-14):** Ships `lib/integrations/vendors/resend.ts` manifest (`vendorKey="resend"`, single band `resend.email.send` p95 600 / p99 1500 ms), `lib/wizards/defs/resend.ts` (`WizardDefinition<ResendAdminPayload>` composing `api-key-paste → review-and-confirm → celebration` — one step shorter than stripe-admin because Resend has no provisioning-time handshake; `completionContract.verify` pings `apiKeys.list()` as the cheapest authenticated identity check), `lib/wizards/defs/index.ts` barrel (eagerly imports `./stripe-admin` + `./resend` — closes `sw5_wizard_defs_barrel_owed`), `app/lite/setup/critical-flight/[key]/actions-resend.ts` (`testResendKeyAction` + `completeResendAction` — kept in a separate file not folded into `actions.ts` because the combined module would have crossed the ~200 LOC split-point named in the brief). `page.tsx` now imports the barrel for side-effect registration. `critical-flight-client.tsx` branches by `wizardKey` between stripe-admin and resend (per-wizard initial states, `onComplete` payload builder, review summary, api-key-paste `testCall` dispatch). Decided **not** to split into per-wizard client files this session — two branches still read cleanly; brief recommends revisiting at SW-7 (third wizard's oauth-consent shape is the right pressure point). 5 new unit tests (325/325 green — `resend-wizard.test.ts` imports via the barrel to exercise its registration side-effect). New Playwright spec `tests/e2e/critical-flight-resend.spec.ts` navigates directly to `/lite/setup/critical-flight/resend` (the seeded JWT puts stripe-admin first in the ordering, so a bounce through /lite/first-run would redirect elsewhere); `test.skip()` when `RESEND_TEST_KEY` unset. `.env.example` documents `RESEND_TEST_KEY`. Typecheck clean, lint clean, build clean, `npm run test:e2e` reports 2 skipped. No migration, no settings keys touched, no schema change. Closed `sw5_wizard_defs_barrel_owed`; no new PATCHES_OWED rows. SW-7 brief pre-compiled. Full recap in `sessions/sw-6-handoff.md`.

**SW-5c outcome (2026-04-14):** Ships `playwright.config.ts` + `tests/e2e/` scaffold + first critical-flight E2E smoke. `@playwright/test` installed as direct devDep; Chromium pulled via `npx playwright install`. `test:e2e` script wired. webServer runs `next dev -p 3101` with hermetic env (isolated sqlite DB at `tests/e2e/.test-critical-flight.db`, deterministic `NEXTAUTH_SECRET`, test `STRIPE_WEBHOOK_SECRET`, `CREDENTIAL_VAULT_KEY`, and `KILL_SWITCHES_ON=setup_wizards_enabled`). `tests/e2e/fixtures/seed-db.ts` doubles as Playwright globalSetup: wipes prior state, applies all migrations, seeds admin user, encodes a NextAuth v5 session JWT via `@auth/core/jwt` (`authjs.session-token` cookie name + matching JWE salt) with `brand_dna_complete=true`, writes `storageState`. `tests/e2e/fixtures/stripe-signature.ts` hand-rolls the `t=...,v1=<hmac-sha256>` header to bypass the SDK's overly-strict public type on `generateTestHeaderString`. `tests/e2e/critical-flight-stripe.spec.ts` exercises the full arc (sign-in → /lite/first-run redirect → paste key → POST signed webhook → probe detects → confirm → celebrate → capstone → cockpit) with DB assertions against the Playwright sqlite file; `test.skip()` when `STRIPE_TEST_KEY` is unset so CI stays green without a real key. **Off-whitelist edit (patched in handoff):** `lib/kill-switches.ts` gains a `KILL_SWITCHES_ON` env reader at module load (comma-separated list; unknown keys ignored; defaults unchanged) — legitimate runtime-wiring stop-gap that the existing comment names as owed. `.gitignore` + `.env.example` updated (`STRIPE_TEST_KEY`). Typecheck clean, 320/320 unit green, lint clean, build clean, `npm run test:e2e` reports 1 skipped (scaffold green). No new PATCHES_OWED rows. SW-6 brief pre-compiled. Full recap in `sessions/sw-5c-handoff.md`.

**SW-5b outcome (2026-04-14):** Ships `app/api/stripe/webhook/route.ts` — POST Route Handler (nodejs runtime) that verifies `stripe-signature` via `stripe.webhooks.constructEvent` against `STRIPE_WEBHOOK_SECRET` and emits one `external_call_log` row per valid event (`job="stripe.webhook.receive"`, `actor_type="internal"`, `estimated_cost_aud=0`, `units={event_type,event_id}`). Bad signature → 400; valid → 200 `{received:true}`; insert failures log-and-swallow (never throws). **Lazy per-request `new Stripe()` in `getStripe()`** instead of `@/lib/stripe/client` — the singleton eagerly instantiates at module load and throws at build-time page-data collection when `STRIPE_SECRET_KEY` is empty. Lazy instance with `|| "sk_test_placeholder"` keeps build clean; prod unchanged. 5 new unit tests (320/320 green) using hermetic migrated sqlite mocked in for `@/lib/db` + class-stub mock of the `stripe` module. Typecheck clean, lint clean, build clean (`/api/stripe/webhook` dynamic in route manifest). No migration, no new settings keys, no new env var (`STRIPE_WEBHOOK_SECRET` was already in `.env.example` from A7). **Split at brief §10**: Playwright E2E deferred to SW-5c because G1 precondition failed — `@playwright/test` is not a direct dep (only transitive), no `playwright.config.ts`, no `tests/e2e/`. Installing Playwright + writing the first fixture + writing the E2E spec alongside a webhook build would have blown scope. Closes PATCHES_OWED `sw5_stripe_webhook_receiver_owed`. SW-5c brief pre-compiled. Full recap in `sessions/sw-5b-handoff.md`.

**SW-5 outcome (2026-04-14):** Ships `lib/wizards/defs/stripe-admin.ts` — first `WizardDefinition<StripeAdminPayload>` composing SW-2's api-key-paste → webhook-probe → review-and-confirm → celebration steps via `STEP_TYPE_REGISTRY`, wired to `stripeManifest` + live `balance.retrieve` verify contract + `integrationConnections` artefact gate. `voiceTreatment.capstone = undefined` (arc-level capstone stays at `/lite/first-run` per SW-4, not per-wizard). Ships `/lite/setup/critical-flight/[key]` route tree: Server Component (`page.tsx`) resolves via `getWizard(key)` (side-effect imports the stripe def), 404s unknown keys, hydrates expiry + webhook timeout from `settings`. Client driver (`critical-flight-client.tsx`) tracks step index + accumulates payload; branches by step.type to wire Server-Action configs. Three Server Actions (`actions.ts`): `testStripeKeyAction` delegates to `contract.verify`, `checkStripeWebhookReceivedAction` polls `external_call_log` for `job=stripe.webhook.receive` (receiver owed to SW-5b), `completeStripeAdminAction` runs `registerIntegration → verifyCompletion → wizard_completions insert → unstable_update()` with rollback on any failure. Off-whitelist one-line edit to `lib/auth/auth.ts` to destructure `unstable_update` (brief-required for JWT refresh). 7 new tests (315/315 green — 5 stripe-admin-wizard, 2 critical-flight-route). Typecheck clean, lint clean, build clean (route in manifest). No migration. **Split at brief §10 split-point**: E2E Playwright smoke + `/api/stripe/webhook` receiver deferred to SW-5b (webhook receiver out of SW-5 whitelist; E2E needs it to exercise the full arc realistically). 3 PATCHES_OWED rows (receiver, defs barrel, integration rollback on insert failure). SW-5b brief pre-compiled. Full recap in `sessions/sw-5-handoff.md`.

**SW-4 outcome (2026-04-14):** Ships `lib/wizards/critical-flight.ts` (`getCriticalFlightStatus` + `nextCriticalWizardKey` — settings-driven ordering, injectable db, per-user isolation, tolerant of duplicate completion rows per spec §6.1). Replaces the A8 stub at `lib/auth/has-completed-critical-flight.ts` with a real DB check — kill-switch-off short-circuits to `true` matching SW-2/SW-3 no-op convention. `/lite/first-run` route (Server Component) routes to `/lite/setup/critical-flight/[nextKey]` when the flight is incomplete, renders the capstone otherwise (placeholder copy "SuperBad is open for business.", reused `tier2["wizard-complete"]` entry until the design-system revisit registers `motion:critical_flight_capstone`). Proxy gate 2 wired: reads `req.auth.user.critical_flight_complete` from the JWT and redirects incomplete admins, with `/lite/first-run` narrowly allowlisted out of gate 2 to avoid loops (still gated by Brand DNA). Off-whitelist auth-triplet edits (auth.config.ts / auth.ts / session.ts) lift the async DB check into the Node-side jwt callback on signIn / signUp / session.update() — mirrors BDA-4's `brand_dna_complete` pattern. 11 new tests (308/308 green — 7 critical-flight, 4 has-completed-critical-flight). Typecheck clean, lint clean, build clean (`/lite/first-run` in route manifest). No migration. No PATCHES_OWED rows — auth-triplet edits are contemporaneous (same rationale as SW-3's settings edits). SW-5 brief pre-compiled (scoped to the first consumer wizard, `stripe-admin`, with split-point guidance if E2E pushes past 70% context). Full recap in `sessions/sw-4-handoff.md`.

**SW-3 outcome (2026-04-14):** Ships `lib/integrations/registerIntegration.ts` (object-arg helper; encrypts plaintext credentials via B2 vault with `${vendorKey}.credentials` AAD; SHA-256 `band_registration_hash` of sorted jobs; kill-switch throws rather than silently no-opping so celebration orchestrator can roll back), `lib/integrations/registerBands.ts` (pure Observatory stub returning job names), `lib/integrations/vendors/stripe.ts` (first manifest — `stripe-admin`, 3 job bands: customer.create/invoice.create/webhook.receive; type-only Stripe SDK import, no runtime calls), `lib/wizards/verify-completion.ts` (required-key check → `contract.verify()` with `settings.get('wizards.verify_timeout_ms')` timeout → artefact assertions for `integrationConnections` + `activityLog`). Celebration step extended with optional `config.onComplete` orchestrator (runs once on mount; Done CTA pending → ok → "Try again" on failure); passive-mode byte-compatible with SW-2's test. Migration `0010_sw3_verify_timeout.sql` seeds `wizards.verify_timeout_ms=4000` (journal idx 10; total settings 74). 14 new tests (297/297 green). Typecheck clean, lint clean, build clean. No PATCHES_OWED rows — settings edits contemporaneous with consumer, authorised by brief DoD. SW-4 brief unchanged (pre-compiled by SW-2). Full recap in `sessions/sw-3-handoff.md`.

**SW-2 outcome (2026-04-14):** Ships the 10-step-type library + `custom` escape hatch + `STEP_TYPE_REGISTRY` map (`components/lite/wizard-steps/{form,oauth-consent,api-key-paste,webhook-probe,dns-verify,csv-import,async-check,content-picker,review-confirm,celebration,custom}-step.tsx` + `index.ts`). `lib/wizards/step-types.ts` defines the uniform `StepTypeDefinition<TState>` + `StepComponentProps<TState>` + `invalid()` helper. `<WizardShell>` now short-circuits to a maintenance placeholder when `setup_wizards_enabled=false` (new kill-switch; ships disabled per convention) and accepts an optional `step`/`stepState`/`onStepStateChange`/`onNext` prop set that renders via the registry; `children` fallback preserved so SW-1 chrome tests stay green (with kill-switch flipped in `beforeAll`). Celebration step reads `tier2["wizard-complete"]` directly — A4's choreography is the single source of truth. Three new `settings` keys seeded via migration `0009_sw2_wizard_step_timeouts.sql` (journal idx 9): `wizards.dns_verify_poll_interval_ms=10000`, `wizards.async_check_timeout_ms=600000`, `wizards.webhook_probe_timeout_ms=300000`. Registry doc Wizards section grew 6 → 9 (total 73); `lib/settings.ts` registry updated. `oauth-consent` dropped `<Button asChild>` (prop not supported; styled `<a>` instead). `form` step `validate()` tightened against `values: null`. 20 new tests (283/283 green). Typecheck clean, lint clean, build clean. No PATCHES_OWED rows (one off-whitelist contemporaneous edit — `tests/wizard-shell.test.tsx` kill-switch flip — documented in handoff). SW-4 brief written. Full recap in `sessions/sw-2-handoff.md`.

**SW-1 outcome (2026-04-14):** 3 new tables (`wizard_progress`, `wizard_completions`, `integration_connections`) via migration `0008_sw1_wizards.sql` (journal idx 8) — partial unique index on live wizard_progress rows enforces "one live wizard per user+key" invariant without blocking audit history. `lib/wizards/types.ts` ships `WizardDefinition<T>`, `WizardStepDefinition`, `CompletionContract<T>`, `VendorManifest`, `TabTitlePool`, `CapstoneConfig`, `WizardVoiceTreatment`, `AnyWizardDefinition`. `lib/wizards/registry.ts` empty scaffold with `registerWizard`/`getWizard`/`listWizardKeys` + test-only reset. `<WizardShell>` client component (houseSpring progress segments, cancel trigger, optional help slot, expiry hint reads `expiryDays` prop). Off-whitelist `lib/wizards/shell-config.ts` server helper bundles `settings.get('wizards.expiry_days')` into props — PATCHES_OWED row `sw1_shell_config_helper_added`. `activity-log.ts` NOT edited — the 4 `wizard_*` kinds already landed in Phase 4 consolidation (lines 207-210). 18 new tests (263/263 green). Typecheck clean, lint clean, build clean. 2 PATCHES_OWED rows (brief settings-key drift for SW-6; shell-config off-whitelist rationale). SW-3 brief written. Full recap in `sessions/sw-1-handoff.md`.

**BDA-4 outcome (2026-04-14):** jwt callback in `lib/auth/auth.ts` now overrides the Edge-safe base with a Node-side version that, on `signIn`/`signUp`/`update` triggers, calls `isBrandDnaCompleteForUser(token.id)` and flips `token.brand_dna_complete`. New helper `lib/auth/brand-dna-complete-check.ts` queries `brand_dna_profiles` for `subject_type='superbad_self' AND is_current=true AND status='complete'`; kill-switch (`brand_dna_assessment_enabled`) short-circuits to false so non-Brand-DNA deployments pay no DB cost in the auth hot path. `reveal-client.tsx` split into `RevealClient` (local `SessionProvider` wrapper) + `RevealInner` (consumes `useSession`); post-completion effect awaits `markProfileComplete` then calls `update()` to re-mint the JWT, with a `cancelled` flag guarding unmount. `.env.example` annotates `BRAND_DNA_GATE_BYPASS` as dev/test-only post-BDA-4. 7 new tests (245 total). Typecheck clean, lint clean, build clean (with font mock). No migration. 2 PATCHES_OWED rows (SessionProvider local-scope hoist when Wave 4 SW-4 lands; admin login UI owed for full browser E2E). SW-1 + SW-2 briefs written. Full recap in `sessions/bda-4-handoff.md`.

**BDA-3 outcome (2026-04-14):** Builds `/lite/brand-dna/reveal` cinematic Tier 2 reveal: `page.tsx` (Suspense-wrapped) + `reveal-client.tsx` (3-phase state machine `impression → sections → portrait`). Opus callers `generateFirstImpression` + `generateProsePortrait` (both `llm_calls_enabled` gated, cached on their persisted columns — refreshes never re-bill). Prompt files as standalone `.ts` modules (new convention; PATCHES_OWED row to backfill BDA-2 insight prompt). 8th sound key `brand_dna_reveal` (underscore-cased per spec §10.5; src regex widened to `[a-z_-]+`). Tier 2 choreography `brand-dna-reveal` (durationMs 20000, container stagger 0.6s + delayChildren 3.2s, reduced-motion fade). `markProfileComplete` server action (kill-switch gated, idempotent, logs `onboarding_brand_dna_completed` — `ACTIVITY_LOG_KINDS` now 226). `submitReflection` redirect flipped `section/5/insight` → `/lite/brand-dna/reveal`. 13 new tests (238 total). Out-of-whitelist: `eslint.config.mjs` (LLM-boundary rule extension), `lib/db/schema/activity-log.ts` (new kind), `tests/motion-sound.test.ts` (registry-count extension). 4 PATCHES_OWED rows. Typecheck clean, 238/238 green, lint clean, build clean (with font mock). G10: dev-server curl showed layout-gate 307 — expected (no `BRAND_DNA_GATE_BYPASS` in dev env); BDA-4 makes this moot. BDA-4 brief written. Full recap in `sessions/bda-3-handoff.md`.

[AUTONOMY] Stale lock reclaimed 2026-04-13T22:53:49Z — BDA-2 lock was 22h54m old (worker from prior run did not release). Proceeding with BDA-2.

[AUTONOMY] 2026-04-14T01:14:49Z — BDA-3 paused — Opus tier requires manual run. Worker acquired A7 lock by misreading the tracker (Next Action block is very long; A7 was already closed). Detected BDA-3 as the true next session. BDA-3 is /deep (Opus) — not Sonnet-executable. .autonomy/PAUSED written. Resume: delete .autonomy/PAUSED, run BDA-3 locally with Claude Opus 4.6.

**BDA-2 outcome (2026-04-13):** Builds `/lite/brand-dna` route tree: alignment gate question ("Does your business represent your personality?" → 3-track routing), `app/lite/brand-dna/layout.tsx` (kill-switch gate + dark shell), card-per-question UI (`section/[n]/page.tsx` + `question-card-client.tsx`), between-section Opus insight page (`section/[n]/insight/page.tsx` + `insight-reveal-client.tsx`), optional reflection page (`section/[n]/reflection/page.tsx` + `reflection-client.tsx`), Server Actions (`actions.ts`: `submitAlignmentGate`, `submitAnswer`, `submitReflection`, `getSelfProfileId`). Stub question bank: 15 questions × 5 sections at `lib/brand-dna/question-bank.ts`. `generateSectionInsight()` Opus caller at `lib/brand-dna/generate-insight.ts` (cached in `section_insights`). Signal-tag aggregation in `brand_dna_profiles.signal_tags` JSON map. Save/resume: profile entry re-routes to `section/{current_section}`. `brand_dna_assessment_enabled` kill-switch added. `/lite/brand-dna` added to `isPublicRoute()`. ESLint carve-out for `lib/brand-dna/generate-insight.ts`. 20 new tests (226 total). Fix: non-async exports removed from "use server" file. Build fix: `NEXT_FONT_GOOGLE_MOCKED_RESPONSES` + musl library needed for offline sandbox (pre-existing environment constraint). BDA-3 brief written. Full recap in `sessions/bda-2-handoff.md`.

**BDA-1 outcome (2026-04-13):** Extends `brand_dna_profiles` stub in-place (18 new columns: subject_display_name, contact_id, company_id, version, is_current, is_superbad_self, track, shape, needs_regeneration, section_scores, signal_tags, prose_portrait, first_impression, reflection_text, section_insights, supplement_completed, current_section, completed_at_ms). Creates `brand_dna_answers`, `brand_dna_blends`, `brand_dna_invites` tables. `lib/brand-dna/` module: `issueBrandDnaInvite()` (wraps `issueMagicLink()` with `issued_for='brand_dna_invite'`) + `redeemBrandDnaInvite()` (validates invite, marks used, creates/attaches profile). `ACTIVITY_LOG_KINDS` extended by 2 (`brand_dna_invite_sent`, `brand_dna_invite_redeemed` → 225 total). Migration `0007_bda1_brand_dna.sql` (journal idx 7). 11 new tests (206 total). 3 PATCHES_OWED rows. Typecheck clean, lint clean, build clean (pre-existing Google Fonts 403). BDA-2 brief written. Full recap in `sessions/bda-1-handoff.md`.

**B3 outcome (2026-04-13):** `/lite/legal/{index,terms,privacy,acceptable-use,cookie-policy}` MDX pages (public, no auth gate). `cookie_consents` audit table + migration `0006_b3_legal.sql` (journal idx 6). GDPR cookie consent banner (`components/lite/cookie-consent-banner.tsx`) — EU geo-gated via `isEuIp()` + `GEOIP_TEST_EU_IPS` test override; non-EU footer link only; starts `visible=isEu` for SSR curl verification. `POST /api/cookie-consent` route (SHA-256 IP hash). `legal_doc_versions` seeded 4 v1.0 rows (terms/privacy/aup/cookie-policy). `legal.dsr_email` + `legal.dsr_response_days` settings keys (70 total). `/lite/legal/` added to `isPublicRoute()`. `next-mdx-remote/rsc` for MDX rendering (not `@next/mdx` — Turbopack incompatible). 23 new tests (195 total). 5 PATCHES_OWED rows added. Foundation-B exit checklist complete: B1 Sentry ✓ B2 Litestream/vault ✓ B3 legal pages + GDPR banner ✓. Full recap in `sessions/b3-handoff.md`.

**B2 outcome (2026-04-13):** Litestream → R2 replication config (`litestream.yml`). `lib/crypto/vault.ts` AES-256-GCM vault (`vault.encrypt/decrypt`, 12-byte IV, AAD = context scope). `lib/crypto/index.ts` barrel. `no-direct-crypto` ESLint rule (blocks `createCipheriv`/`createDecipheriv`/`subtle.encrypt` outside `lib/crypto/vault.ts`). `INCIDENT_PLAYBOOK.md` stub (8 sections). `docs/dr-runbook.md` runnable restore procedure. `FOUNDATIONS.md` §5 patched with ATO 7-year retention policy (PATCHES_OWED row closed). `.env.example` extended with `CREDENTIAL_VAULT_KEY`, `R2_BUCKET_NAME`, `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`, `R2_ENDPOINT_URL`, `DB_FILE_PATH`. 10 new vault tests (172 total). Typecheck clean, lint clean. No schema changes / no migration. Full recap in `sessions/b2-handoff.md`.

**B1 outcome (2026-04-13):** Sentry SDK wired (client + server + edge; kill-switch gated; `sentry_enabled=false` default). `lib/support/reportIssue()` Server Action inserts `support_tickets` row + captures Sentry event + logs `support_ticket_created` activity. `lib/support/cost-alerts.ts` checks Anthropic daily spend vs `alerts.anthropic_daily_cap_aud` (settings.get()) and fires transactional alert email. `components/lite/report-issue-button.tsx` global footer button (Dialog + AnimatePresence + houseSpring form ↔ success). `/lite/admin/errors` triage dashboard (Server Component, admin-only). `support_tickets` table + migration `0005_b1_support.sql` (journal idx 5). `sentry_enabled` kill-switch added. `support_ticket_created` activity kind added (now 222 total). `middleware.ts` deleted (stale legacy from A8 that had broken imports; `proxy.ts` is the sole Next.js 16 middleware). `@sentry/nextjs@10.48.0` installed. 13 new tests (162 total). Typecheck clean, lint clean. Build: pre-existing Google Fonts 403 only. 6 PATCHES_OWED rows added. Full recap in `sessions/b1-handoff.md`.

**A8 outcome (2026-04-13):** Portal-guard primitive + Brand DNA Gate + Auth.js v5. `portal_magic_links` table (generalised with nullable `client_id` + `submission_id`). `brand_dna_profiles` stub (gate query target). `lib/portal/guard.ts` (encode/decode/getPortalSession). `lib/portal/issue-magic-link.ts` + `lib/portal/redeem-magic-link.ts`. NextAuth v5 split-config pattern: `lib/auth/auth.config.ts` (Edge-safe, no DB) + `lib/auth/auth.ts` (Node.js, Credentials provider stub). `lib/auth/session.ts` (re-export + Session type augmentation). `proxy.ts` (Next.js 16 renamed `middleware.ts` → `proxy.ts`; Brand DNA Gate + critical-flight layering). `/lite/onboarding` placeholder + `/lite/portal/recover` recovery form (houseSpring animation) + `/lite/portal/r/[token]` magic-link redeem route. `BRAND_DNA_GATE_BYPASS` env-var escape hatch. 35 new tests (portal-guard 11, brand-dna-gate 14, magic-link 8 + 2 schema). Migration `0004_a8_portal_auth.sql`. `ACTIVITY_LOG_KINDS` extended to 221 (`portal_session_started`). Wave 2 B1/B2/B3 briefs written per G11.b. 6 PATCHES_OWED rows added. Typecheck clean, 150/150 green, lint clean. Full recap in `sessions/a8-handoff.md`.

**A7 outcome (2026-04-13):** Resend email adapter + 16-value `EmailClassification` union + `canSendTo()` suppression gate (hard: bounce/complaint blocks all; soft: unsubscribe/manual bypassed for transactional) + `isWithinQuietWindow()` Melbourne tz gate (Intl API, no external lib, 29-holiday JSON) + `checkBrandVoiceDrift()` Haiku grader (dual kill-switch gated) + `ensureStripeCustomer()` idempotent create/search + `renderToPdf()` stub (Buffer placeholder, QB-3 gets Puppeteer). `legal_doc_versions` + `email_suppressions` schema + migration `0003_a7_email_stripe_pdf.sql`. `drift-check-grader` haiku slug added (53 total). `ACTIVITY_LOG_KINDS` extended to 220 (`drift_check_failed` + `portal_magic_link_sent` + `portal_magic_link_redeemed`). Stripe API version: `2026-03-25.dahlia`. `AnyDrizzle` type widened to `Record<string, unknown>` for test dbOverride. 5 new test files: 12 new tests (115 total). Typecheck clean, 115/115 green, lint clean. Build gate: pre-existing Google Fonts 403 in sandbox (A2/A3 fonts, unmodified by A7) — passes on production Coolify. Rollback: feature-flag-gated (`outreach_send_enabled` + `drift_check_enabled` default OFF). Full recap in `sessions/a7-handoff.md`.

**A6 outcome (2026-04-14):** 6 new tables landed (`activity_log`, `scheduled_tasks`, `worker_heartbeats`, `external_call_log`, `threads`, `messages`). Consolidated 217-value `activity_log.kind` enum + 37-value `task_type` enum — A5's PATCHES_OWED row about the missing consolidated source was inaccurate (the consolidation lives in `docs/specs/sales-pipeline.md` §4.1 since Phase 3.5 step 2a). `logActivity()`, `enqueueTask()`, scheduled-tasks worker (gated on `scheduled_tasks_enabled`, 15-min stale reclaim, 3-attempt retry, 5min×6^n backoff, 50-row batch, 60s tick). `formatTimestamp(date, tz)` with 5 format modes. `lib/ai/models.ts` maps all 53 `lib/ai/prompts/INDEX.md` slugs to tiers (opus/sonnet/haiku); `modelFor(slug)` resolves to Claude 4.6 model IDs. Migration `0002_a6_activity_scheduled_inbox.sql`. Typecheck clean, 68/68 tests green (26 new: activity-log 3, scheduled-tasks 7, format-timestamp 8, llm-models 5, messages-schema 3), `npm run build` clean, `npm run lint` clean. 6 new PATCHES_OWED rows (deferred FKs; spec counts drift from consolidated reality; worker runtime parameters live as code-constants). Full recap in `sessions/a6-handoff.md`.

**Read at the start of B1:** `sessions/b1-brief.md` (primary — drives G0 → G12), `CLAUDE.md`, `START_HERE.md` § Phase 5, `AUTONOMY_PROTOCOL.md` (full), `sessions/a8-handoff.md` + `sessions/a7-handoff.md` (last 2 handoffs per CLAUDE.md).

**A5 outcome (2026-04-14):** Drizzle client singleton at `lib/db/index.ts` (better-sqlite3 + WAL + FK pragma). `settings` table (schema `lib/db/schema/settings.ts`; schema migration `0000_init.sql`; seed migration `0001_seed_settings.sql` — 68 rows across Finance/Wizards/Plan/Portal/Intro Funnel/Hiring/Email/Alerts; all INSERTs `INSERT OR IGNORE` so re-runs are idempotent). Migration runner at `lib/db/migrate.ts` applies Drizzle's journal migrations then walks untracked `.sql` seeds. `settings.get(key)` helper at `lib/settings.ts` — typed key map, per-key zod parser, in-memory cache invalidated on write. `user` table at `lib/db/schema/user.ts` with `role` + `timezone` + the six display-preference columns + `first_signed_in_at_ms` (Auth.js-compatible; A8 wires NextAuth against it). `lib/auth/permissions.ts` — `Role` union + `can(role, action, resource)` default-deny helper, seeded with Finance Dashboard §13 matrix only (each subsequent wave extends). `lib/kill-switches.ts` — 4 flags, all default-disabled. `lib/types/glossary.ts` — branded TS aliases for 11 entity types + onboarding entry paths. Three custom ESLint rules at `lib/eslint-rules/*.ts` loaded via jiti from `eslint.config.mjs` — `no-direct-anthropic-import`, `no-direct-stripe-customer-create`, `no-direct-resend-send`. `DATABASE_URL` added to `lib/env.ts` + `.env.example`. Registry doc extended with Email + Alerts sections; Totals reconciled to 68 (pre-A5 was 61; the historical "60" was an arithmetic mistake). Typecheck clean, `npm test` 42/42 green (25 pre-existing + 17 new), `npm run build` clean, `npm run lint` clean. Full recap in `sessions/a5-handoff.md`. Four new PATCHES_OWED rows (access matrix never consolidated → every wave owes its own rules; `activity_log.kind` enum source file missing → A6 escalation risk; `lib/db/migrate.ts` created off-whitelist with rationale; registry Totals arithmetic historical note).

**A4 outcome (2026-04-14):** `houseSpring` wired through Framer `MotionConfig` at `components/lite/motion-provider.tsx` with preference-derived transition (full → spring, reduced → 180ms linear, off → instant). 7 locked Tier 2 choreographies in `lib/motion/choreographies.ts` (`dashboard-first-load`, `morning-brief-open`, `quote-accepted`, `subscription-activated`, `wizard-complete`, `portal-first-load`, `inbox-zero`) — each with variants + transition + reduced fallback; `pdfRenderOverlay` Tier 1 token ready for QB-3/BI-2/SWP-8. 7-item sound registry in `lib/sounds.ts` with Howler-backed lazy-instanced `play()` at `components/lite/sound-provider.tsx`; hard-muted when `!soundsEnabled || motion !== 'full'`. `Tier2Reveal` rewritten on Framer with a required typed `choreography` prop. `/lite/design` extended with a picker-driven Tier 2 demo + 7 sound triggers. `SoundKey` union fixed to spec's canonical kebab-cased keys (A3 had speculative names — A4's handoff explains). `framer-motion` + `howler` + `@types/howler` installed; `use-sound` explicitly NOT installed (Howler directly fits provider API — see A4 handoff decision #1). Typecheck clean, `npm test` 25 / 25 green (+ 8 new tests), `npm run build` clean, `/lite/design` HTTP 200. Full recap in `sessions/a4-handoff.md`. Four new PATCHES_OWED rows (loaderror warning swap, sound-attributions file, BUILD_PLAN wording update, Dialog modal-fade carve-out verification).

**A3 outcome (2026-04-14):** 35 primitives in `components/ui/` (32 via `npx shadcn add`, plus hand-rolled `form.tsx` and `date-picker.tsx` because base-nova doesn't ship either from the registry). 8 custom Lite wrappers in `components/lite/` (`ThemeProvider`, `MotionProvider`, `SoundProvider`, `AdminShell`, `BrandHero`, `SkeletonTile`, `EmptyState`, `Tier2Reveal`) — each with JSDoc listing consumed tokens per spec rule 11. `BrandHero` enforces the BHS closed list at type level via `location: BhsLocation`. `/lite/design` extended with a primitives gallery + 6-axis accessibility panel (10-axis coverage via toggles + documented axes). Layout now wraps in `ThemeProvider → MotionProvider → SoundProvider` with `<Toaster />` mounted globally. Typecheck clean, `npm test` green (17 tests / 2 files), `npm run build` clean, `/lite/design` HTTP 200. **Provider stubs for A4:** `MotionProvider` is CSS-attribute-only; `SoundProvider.play()` is a no-op; `Tier2Reveal` uses CSS keyframes. A4 replaces each under the same public APIs. Three new PATCHES_OWED rows (Slot library upgrade, `next-themes` removal audit, `lib/copy/empty-states.ts` bank). Full recap in `sessions/a3-handoff.md`.

**A2 outcome (2026-04-13) [superseded by A3 note above]:** Full SuperBad token set in `app/globals.css` + typed mirror at `lib/design-tokens.ts`; 3 theme presets + 3 typeface presets (cookie-selected via `lib/presets.ts`); all nine fonts wired through `lib/fonts.ts`; `/lite/design` reference route live (note: renamed from `/lite/_design` because Next.js treats underscore-prefixed folders as private); Vitest harness + `tests/tokens.test.ts` (6 green) asserts CSS↔TS parity. Typecheck + build clean. **Scope descope:** BUILD_PLAN-A2 claims re: `user`-table preference columns (moved to A5) and Settings → Display UI (moved to post-A8 session) — neither buildable without preceding infra. Full recap in `sessions/a2-handoff.md`. Five new PATCHES_OWED rows (user columns → A5, Settings UI → post-A8, BUILD_PLAN `/lite/_design` rename, lazy-font optimisation, General Sans swap, admin gate → A8); one closed (Vitest harness satisfied).

**A1 outcome (2026-04-13):** Next.js 16.2.3 + React 19.2.4 + Tailwind v4 + shadcn (`base-nova` default preset, neutral base colour) scaffolded. Dev server pinned to `:3001`. Env validator via `instrumentation.ts` → `lib/env.ts` (zod) fires loud on boot. Typecheck + `npm run build` both clean. Full recap in `sessions/a1-handoff.md`.

**P0 outcome (2026-04-13):** **B — no public Pixieset API, no webhook, no iframe support.** Path B (on-brand link-out launch card + manual URL-paste trigger for `gallery_ready_at`) locked for v1.0. Alternatives mop-up declined (Pic-Time, Cloudspot, ShootProof share the same closed posture — switching wouldn't unlock Path A). IF-2 (Wave 14) builds Path B only. Follow-on rows in `PATCHES_OWED.md` under F2.c section for IF-2 + SW-5. Full recap in `sessions/p0-pixieset-spike-handoff.md`.

**After A1:** Wave 1 Foundation A continues A2 → A3 → ... → A8 per `BUILD_PLAN.md` Wave 1. Foundation A must complete before Foundation B (Wave 2); Foundation B must complete before Wave 3.

**Phase 4 closed 2026-04-13.** See `sessions/phase-4-handoff.md` for full recap (final handoff) and `sessions/phase-4-partial-handoff.md` (BUILD_PLAN.md landing).

**Phase 3.5 closed 2026-04-13.** Full recap in `sessions/phase-3.5-exit-handoff.md`.

**Step 11 is COMPLETE (all 4 stages done 2026-04-13).** Stage 1 (Entry → Booking) — `sessions/phase-3.5-step-11-stage-1-handoff.md`. Stage 2 (Trial Shoot → Reflection → Retainer-Fit) — `sessions/phase-3.5-step-11-stage-2-handoff.md`. Stage 3 (Six-Week Plan flow) — `sessions/phase-3.5-step-11-stage-3-handoff.md`. Stage 4 (Retainer conversion + F1.a closure) — `sessions/phase-3.5-step-11-stage-4-handoff.md`. Stage 1 flags: F1.a (portal session durability — closed at Stage 4), F1.b, F1.c, F1.d. Stage 2 flags: F2.a, F2.b, F2.c, F2.d, F2.e. Stage 3 flags: F3.a, F3.b, F3.c, F3.d, F3.e, F3.f. Stage 4 flags: F4.a (retainer-payment-before-refresh-review queue + retroactive Week 1 fire; new `plan.refresh_review_block_escalation_hours` settings key), F4.b (Brand DNA gate-first → bartender-led kickoff on gate-clear; new `retainer_kickoff_bartender_said_at` column + 2 activity_log kinds), F4.c (Onboarding §8.1 welcome screen branches: trial-shoot graduates bypass, direct/referral render; new `onboarding_welcome_seen_at` column), F1.a closure (portal-guard primitive + magic-link-in-every-email inline at intro-funnel §10.1 + client-management §10.1; new `portal_magic_links` table; new `portal.magic_link_ttl_hours` + `portal.session_cookie_ttl_days` keys).

Settings registry now at **60 keys** (Plan 10, Portal 5, others unchanged).

**Batch A DONE 2026-04-13** (steps 3, 3a, 3b, 4, 5). See `sessions/phase-3.5-batch-a-handoff.md`.
**Batch B DONE 2026-04-13** (steps 6, 7, 7a, 10). See `sessions/phase-3.5-batch-b-handoff.md`.
- Step 6 (enum audit): `deals.won_outcome` extended to `['retainer','saas','project']`. No other enum gaps.
- Step 7 (Phase 4/5 coordination items): 3 items recorded in PATCHES_OWED → Pending (Observatory prompt-cardinality, `canSendTo`/`renderToPdf`/`checkBrandVoiceDrift` runtime sigs, cockpit source-spec enumeration). None block Phase 4.
- Step 7a (settings registry): `docs/settings-registry.md` created with 56 keys across Finance/Wizards/Plan/Portal/Hiring. Phase 5 Session A seeds from this file.
- Step 10 (subscription state machine): `FOUNDATIONS.md` §12 canonicalised (13 states, 2 billing paths, transition owners, activity_log rule). 3 reference patches applied (Quote Builder §7.1 past_due transition, Branded Invoicing §3.1 step 11 chain-stop, Client Management §2.3 pause-action visibility gate).

**SCOPE stop-point RESOLVED 2026-04-13.** All four items promoted to v1.0: Branded Invoicing §17, Intro Funnel §18, Six-Week Plan Generator §19, Hiring Pipeline §20.

**Next actions (in order):**
1. ~~**Step 11 walkthrough Stage 1 (Entry → Booking)** — DONE 2026-04-13.~~ See `sessions/phase-3.5-step-11-stage-1-handoff.md`.
2. ~~**Step 11 walkthrough Stage 2 (Trial Shoot → Reflection → Retainer-Fit)** — DONE 2026-04-13.~~ See `sessions/phase-3.5-step-11-stage-2-handoff.md`.
3. ~~**Step 11 walkthrough Stage 3 (Six-Week Plan flow)** — DONE 2026-04-13.~~ See `sessions/phase-3.5-step-11-stage-3-handoff.md`.
4. ~~**Step 11 walkthrough Stage 4 (Retainer conversion + F1.a portal-guard/magic-link closure)** — DONE 2026-04-13.~~ See `sessions/phase-3.5-step-11-stage-4-handoff.md`.
5. ~~**Batch C (steps 8, 9, 12, 13, 15)** — autonomous.~~ **DONE 2026-04-13.** See `sessions/phase-3.5-batch-c-handoff.md`. Step 8 added FOUNDATIONS §13 Glossary + 22 literal replacements across 6 specs (F1.c `stripe.Customer` ≠ Client terminology lock APPLIED). Step 9 rewrote Observatory §4.2 registered-jobs inventory against `lib/ai/prompts/INDEX.md` slugs + added F4.b kickoff-variant to `client-mgmt-bartender-opening-line`. Step 12 confirmed GHL cutover assumption consistent across 21 specs, no patches. Step 13 legal/compliance sweep surfaced 6 orphan patches owed (static legal pages, ToS/Privacy acceptance at SaaS signup, tax record retention, credential vault primitive, DSR flow, Quote terms link target) — logged to PATCHES_OWED Pending. Step 15 final literal-grep clean; per-spec success metrics deferred to v1.1 Strategic Planning (acknowledged, no patches).
6. ~~**Stop 14** — product-judgement (4 legal/compliance Qs from Batch C step 13) — DONE 2026-04-13.~~ See `sessions/phase-3.5-stop-14-handoff.md`. All 4 resolved (option A on each): L1 legal-pages = standalone mini-spec `docs/specs/legal-pages.md`; L2 DSR = email-only at v1.0; L3 cookie banner = geo-gated EU + universal footer link, rolled into legal-pages spec; L4 signup acceptance = single tickbox + inline links + two timestamps. 4 resolution rows added to PATCHES_OWED Stop 14 section.
7. ~~**Stop 16** — Phase 3.5 exit approval from Andy — DONE 2026-04-13.~~ Andy acked Phase 3.5 closed; tracker flipped to Phase 4.

---

---

**Phase 3 spec backlog (updated 2026-04-13 — 18 specs total, 15 locked + 1 pre-written, 3 remaining; Daily Cockpit at #12 newly locked — 9 questions, narrative brief + attention rail + banner strip + calendar preview + switchable kanban/list, three daily briefs with chained continuity + event trail + material-event regen + quiet-slot skip, fleet-scoped chips folded into rail, no dismissal/snooze, mobile PWA with tabbed kanban, 1 new table (`cockpit_briefs`), 1 Opus prompt, 9 build-time disciplines (65–73), 3 cross-spec contract patches owed at Phase 3.5 (`getWaitingItems()` × 11 specs, `maybeRegenerateBrief()` × 8 specs, `getHealthBanners()` × 4 specs), 5 build sessions; Cost & Usage Observatory at #18 is the recommended next session — Cockpit's cost-anomaly banner source depends on Observatory's primitives; Observatory's own sequencing note places it immediately after Cockpit and before Setup Wizards):**

*Recommended ordering groups related specs and respects dependencies. Final order is Andy's call at each session start.*

1. ~~`docs/specs/lead-generation.md`~~ ✅ **LOCKED 2026-04-12** — daily run operations pipeline, two tracks (SaaS + retainer) with winner-takes-all scoring, 9-signal enrichment, single approval queue with earned autonomy per track, 4-week warmup ramp non-overrideable, 4-tier engagement-gated indefinite sequences, three-tier DNC with single enforcement function, single sender `Andy Robinson <hi@contact.superbadmedia.com.au>`, Candidate → Deal promotion at first-send time. See `docs/specs/lead-generation.md` + `sessions/phase-3-lead-generation-handoff.md`. Flagged for Pipeline spec update: `contacts.email_status` enum gains `'unsubscribed'`; `activity_log.kind` enum gains 10 Lead Gen values.
2. ~~`docs/specs/intro-funnel.md`~~ ✅ **LOCKED 2026-04-12** — customer-facing acquisition surface for the $297 trial shoot. Branded editorial landing page (not conversion funnel), fast-dopamine portal after section 1 contact submit (not after all 4 sections), shape-branched self-persuasion questionnaire (3 shape variants × 4 sections = 12 banks), Stripe Payment Element inline with dynamic amount, native calendar (not Cal.com) with 5-business-day advance notice + 3-per-week cap, Pixieset API native gallery for deliverables, 8-screen reflection questionnaire with safety-valve Q1 + Claude synthesis reveal, Claude-generated retainer-fit recommendation (Opus, structured JSON), aggressive abandon cadence 15m SMS→24h SMS+email→3d email→demote (Twilio integration — new stack addition), cancel/reschedule policy D + SuperBad-side always-refundable. Deal creation at section 1 submit (direct to Trial Shoot stage, not Lead). See `docs/specs/intro-funnel.md` + `sessions/phase-3-intro-funnel-handoff.md`. **Cross-spec flags:** Pipeline `deals` gains 6 nullable columns + `contacts` gains 3 nullable columns + `activity_log.kind` gains 21 values + `deals.lost_reason` closed list grows 7→14 + Trial Shoot sub-status gains 2 values + new Trial Shoot panel sub-surfaces. Design-system-baseline needs revisit to absorb 3 new Tier-2 motion candidates. FOUNDATIONS gains Twilio as new stack integration. **New memory:** `feedback_dont_undershoot_llm_capability.md`. **Follow-up mini-session owed:** content authoring for landing page + question banks + reflection + copy.
3. ~~`docs/specs/quote-builder.md`~~ ✅ **LOCKED 2026-04-12** — 21 questions resolved. Stage-gated proposal structure (5 named sections rendered as scroll-snap panels), three-mode drafting (Claude draft-from-context default + template load + blank), `structure` enum supporting retainer/project/mixed via Stripe `add_invoice_items`, per-quote term length picker, per-quote expiry picker, **GST-inclusive canonical storage** (surfaced the "felt experience wins" principle — flagged for memory promotion), 7-state state machine with view tracking via token-URL first-fetch, structured catalogue with category + unit + `tier_rank` for retainers, **two-pane split draft editor** with 40/60 live preview, **Claude-drafted send email** with drift check and Andy review in a send modal (link-only delivery, no PDF attachment), **deliberate second-artefact PDF** as invoice-style one-pager (not compressed web-page mirror), **same-page cinematic Payment Element reveal** via Framer `layoutId` inheriting Intro Funnel's Tier-2 motion slot (no new slot spent), **shared-shell billing-mode-specific confirmation screen**, **pyramid synthesis** for the "What you told us" intro paragraph applying the client doc source hierarchy memory, **supersede-via-new-row** for edit-after-send with hash-based proof-of-acceptance, **custom cancel flow at Lite UX layer** (Billing Portal cancel disabled) with three pre-term retainer options (*Let's chat* / pay remainder / 50% buyout), three pre-term SaaS options (pay remainder / 1-month pause / continue), and post-term retention page (upgrade / downgrade / "here's what you'd be losing" / cancel through — no discounts ever). **New shared primitive introduced: `scheduled_tasks` table + single Node cron worker on Coolify** — polymorphic `task_type` with handler map, idempotency keys, stale-row reclaim, exponential retry, heartbeat table. Every subsequent Phase 3 spec can schedule work via this. **New tables:** `quotes`, `catalogue_items`, `quote_templates`, `scheduled_tasks`, `worker_heartbeats`. **New Claude prompts:** `draft-quote-from-context.ts` (Opus), `draft-intro-paragraph.ts` (Opus), `draft-send-email.ts` (Opus), `draft-scope-summary.ts` (Haiku), `draft-pdf-cover-line.ts` (Opus or rotation — content session decides), `draft-settle-email.ts` (Opus), `draft-cancel-intercept-email.ts` (Opus). **Sound registry grows 7 → 8:** one new slot `sound:quote_accepted`, admin-cockpit-only, visibility-gated, idempotent per quote. **Sprinkle claimed:** §3 Quote PDF cover line. **Cross-spec flags:** Pipeline gains `won_outcome = 'project'` + `deals.committed_until_date` + `deals.subscription_state` (7 values) + `deals.pause_used_this_commitment` + `deals.stripe_subscription_id` + `deals.stripe_customer_id` + `companies.gst_applicable` + 18 new `activity_log.kind` values; Branded Invoicing must expose invoice-generation primitive callable by `handleManualInvoiceSend`; Client Management must surface manual-billed "my invoices" page + subscription state on profile; **Client Portal spec** (rolled into Client Management until split) owes the full `/lite/portal/subscription` cancel surface with pre-term + post-term branches; SaaS Subscription Billing must implement tier pricing with commitment-length discounts and close the "cancel-long-resubscribe-short" loophole; Daily Cockpit must subscribe to quote real-time events and render scheduled-tasks pane + worker heartbeat banner; Comms Inbox must thread quote emails via `In-Reply-To`/`References`. **Foundations §11.2 patch** (already owed from Task Manager) gains the `classification: 'transactional' | 'outreach'` parameter — Quote Builder relies on this for its ~13 outbound emails. **New memory owed:** `feedback_felt_experience_wins.md` (flagged for promotion — see handoff). **Follow-up content mini-session owed** — scope listed in deferred tasks above. **Phase 5 sizing note:** expect 3–4 build sessions minimum (Data+Stripe / Scheduled-tasks / Draft editor+send / Client web page+confirmation+cancel flow). Sessions A and B can run in parallel. | [phase-3-quote-builder-handoff.md](sessions/phase-3-quote-builder-handoff.md)
4. ~~`docs/specs/branded-invoicing.md`~~ ✅ **LOCKED 2026-04-12** — 25 questions resolved. Manual-billed-company invoice generation with ATO-compliant tax invoices, year-prefixed sequential numbering (`SB-INV-2026-0001`), two-step generate/send primitive, 3-day pre-send review window with cockpit notification for add-on line items, self-perpetuating monthly task chain (two tasks per cycle: `manual_invoice_generate` + `manual_invoice_send`), per-company payment terms (default 14 days), bank transfer primary + optional Stripe online payment via inline Payment Element (inherits Tier-2 slot, no new motion spent), full branded two-section scroll-snap web view at `/lite/invoices/[token]`, shared PDF renderer with Quote Builder, 5-state lifecycle (draft/sent/overdue/paid/void), automated 3-day-post-due reminder + manual follow-ups, standalone index at `/lite/invoices` with summary cards + company profile billing tab, void + reissue via supersede (no credit notes in v1), one-click mark-as-paid for bank transfers, full hidden-egg suppression on all invoice surfaces. **Sprinkle claimed:** §3 Invoice PDF footer — split treatment: dry line on unpaid PDF (content mini-session), *"paid in full. pleasure doing business."* on web view paid confirmation. **Refines Quote Builder's handler integration:** first cycle enqueue changes from `manual_invoice_send` to `manual_invoice_generate`; handler map gains 1 new entry; `manual_invoice_send` narrows to send-only. **New table:** `invoices`. **New column:** `companies.payment_terms_days`. **`activity_log.kind` gains 8 values.** `scheduled_tasks.task_type` gains 2 values (`manual_invoice_generate`, `invoice_overdue_reminder`). **3 Claude prompts:** `draft-invoice-email.ts` (Opus), `draft-invoice-reminder.ts` (Opus), `draft-supersede-notification.ts` (Haiku). **Cross-spec flags:** Quote Builder (handler refinement), Pipeline (companies column + activity log), Daily Cockpit (draft review + overdue + failed task cards), Client Management (billing tab), Client Portal (read-only invoice list), Comms Inbox (email threading), Foundations (transactional classification dependency). **No new motion slots. No new sound slots. No new specs spawned. No new memories.** **Content mini-session owed** (small — can fold into Quote Builder's). **Phase 5 sizing:** 2 sessions (Data+primitives+handlers+PDF / Web view+Payment Element+index+billing tab). Session A must land before Quote Builder Session C. | [phase-3-branded-invoicing-handoff.md](sessions/phase-3-branded-invoicing-handoff.md)
5. ~~`docs/specs/brand-dna-assessment.md`~~ ✅ **LOCKED 2026-04-12** — 30 questions resolved. Full psychological + aesthetic + communication profile. Three tracks routed by alignment gate (Founder / Business / Founder + Supplement). Shape-aware sectioned banks (5 sections × 3 shapes × 2 framings = 30 core banks + 1 supplement = 31 total). 4 MC options per question, 1–3 tags per option, unweighted. Two-level signal tag taxonomy (domain → tag, ~40–60 tags across 5 domains). Profile output: structured tags JSON + Opus prose portrait + first impression paragraph. Between-section Opus insights (×4) — must sound human, never AI. Cinematic reveal: first impression lead-in → beat → section-by-section profile build. Multi-stakeholder: individual profiles + Claude-blended company profile with divergence flags. Hard portal lock with visible preview for retainer/SaaS; optional for trial shoot. Tokenised invite links for prospects. Full retake from scratch with side-by-side Claude-highlighted comparison. Tiered downstream injection: full profile for Opus, tags-only for Haiku. Card-per-question UI with section-driven ambient visual evolution. ~20–30% visual options (autonomously generated). SuperBad's own profile as Phase 5 milestone (Andy takes assessment, cross-references brand skills). **New tables:** `brand_dna_profiles`, `brand_dna_answers`, `brand_dna_blends`, `brand_dna_invites`. **5 Opus prompts.** **activity_log.kind gains ~8 values.** **New Tier-2 motion slot:** cinematic reveal (list grows to 11). **New sound:** `sound:brand_dna_reveal` (registry grows to 9). **Sprinkle claimed:** §2 browser tab titles. **New memory:** `feedback_no_lite_on_client_facing.md` — client-facing surfaces say "SuperBad", never "SuperBad Lite." **Content mini-session owed** — largest in the project (31 banks + taxonomy + prompts + visual assets + section art direction). **Cross-spec flags:** Onboarding (#6 — composes Brand DNA), Client Context Engine (#7 — reads as perpetual context), Intro Funnel (LOCKED — trial shoot nudge), Client Management (#8 — profile status), S&D (LOCKED — silent build dependency), Content Engine (#10 — reads for voice), Daily Cockpit (#12 — morning brief tone), Sales Pipeline (LOCKED — `brand_dna_status` field), Design System Baseline (LOCKED — Tier-2 + sound additions). **Phase 5 sizing:** 3–4 sessions (A: data+invites, B: card UI+transitions+insights, C: reveal+generation+sound, D: blend+retake+portal gate). A and B parallel. | [phase-3-brand-dna-assessment-handoff.md](sessions/phase-3-brand-dna-assessment-handoff.md)
6. ~~`docs/specs/onboarding-and-segmentation.md`~~ ✅ **LOCKED 2026-04-12** — 34 questions resolved. Composition layer orchestrating Brand DNA + Revenue Segmentation + setup wizards + SaaS product config into audience-specific onboarding. Retainer: quote acceptance auto-triggers → welcome email (Claude-drafted, personalised) → welcome screen (with "what we already know" Claude summary) → Brand DNA (hard gate) → practical setup (return-when-ready: contacts, ad accounts, content archive; 24h/72h/weekly nudge). SaaS: payment → welcome email (graceful degradation) → welcome screen → Brand DNA (hard gate, resume screen on abandon) → Revenue Segmentation (5 MC questions, SaaS-only, ~2 min) → product config (interface contract: `productConfig` + `firstRunView` + metadata) → credentials (magic link only) → product unlocked. Revenue Segmentation stored as 5 enum columns on `companies` + `location` + `revenue_segmentation_completed_at`. Company auto-created for all SaaS signups. Two-tier upsell targeting: Warm (budget OR engagement) / Hot (budget AND engagement AND goal) with Victoria location gate (configurable). Cockpit alerts for Hot, pipeline filter for Warm+Hot. Brand DNA retake nudge (12-month threshold) replaces first-time-completion nudge. Onboarding progress derived from primitives — no dedicated state. Token-based portal access at `/portal/[token]`, credentials at end. All retainer clients enter via quote acceptance — no alternative trigger, no legacy migration. **IMPORTANT BUILD NOTE: Brand DNA is a hard gate for retainer AND SaaS. Optional for trial shoot only.** **Sprinkles claimed:** welcome email first line (§3), client portal footer (§7). **New columns on `companies`:** `revenue_range`, `team_size`, `biggest_constraint`, `twelve_month_goal`, `industry_vertical`, `industry_vertical_other`, `location`, `revenue_segmentation_completed_at`. **No new tables.** **`activity_log.kind` gains ~8 values.** **Content mini-session owed** (small — can fold into another spec's). **No new memories.** **Phase 5 sizing:** 2 sessions. | [phase-3-onboarding-and-segmentation-handoff.md](sessions/phase-3-onboarding-and-segmentation-handoff.md)
7. ~~`docs/specs/client-context-engine.md`~~ ✅ **LOCKED 2026-04-12** — 31 questions resolved. Per-contact always-on Claude engine — the "where you are" companion to Brand DNA's "who they are." Compose-at-read-time architecture (no dual-write obligation on other features), sectioned summary cache (narrative + signals computed on read + inline draft fields), explicit closed material event list mapped to regeneration sections, dedicated `action_items` table with `you`/`them` ownership and soft-status lifecycle, rule-based composite health score (`healthy`/`cooling`/`at_risk`/`stale` — no Claude call), background regeneration via `scheduled_tasks` with deduplication at enqueue time, draft prompt architecture with context as silent system knowledge (never recaps — reply targets latest message only), hybrid one-shot + nudge field reusing Content Engine's rejection-chat primitive, channel auto-picked from `preferred_channel` column with one-click switcher (email v1, SMS-ready via adapter interface), right-side draft drawer consistent with Task Manager pattern, auto-saved unsent drafts (one per contact, persist until sent/discarded), separate `private_notes` table with physical module boundary (engine has no import path — enforced at code review), cold prospect fallback using SuperBad's own Brand DNA + enrichment data, fixed typed signal set for cockpit consumption, exported function library in `lib/context-engine/` (no API routes for inter-feature calls), per-action token caps + `llm_usage_log` table for cost visibility, flat factual summary voice (working tool, not personality surface). **New tables:** `context_summaries`, `action_items`, `private_notes`, `llm_usage_log`. **New column:** `contacts.preferred_channel`. **5 Claude prompts:** `summarise-contact-context.ts` (Haiku), `extract-action-items.ts` (Haiku), `generate-draft.ts` (Opus), `regenerate-draft-with-nudge.ts` (Opus), `reformat-draft-for-channel.ts` (Haiku). **`activity_log.kind` gains 11 values.** `scheduled_tasks.task_type` gains 2 values (`context_summary_regenerate`, `context_action_item_extract`). **4 new build-time disciplines (30–33):** no private-notes import in engine module, material event list verification, `assembleContext()` as sole context path, dual-context requirement on draft prompts. **No new motion slots. No new sound slots. No new sprinkle claims.** **Content mini-session owed** (small — prompt calibration + empty/loading copy, can fold into another spec's session). **Cross-spec flags:** Client Management (#8 — embeds summary tile + action items + draft drawer + private notes timeline), Lead Gen (LOCKED — `generateDraft()` cold fallback), Daily Cockpit (#12 — `getSignalsForAllContacts()` + aggregated action items), Sales Pipeline (LOCKED — card hover summary + `preferred_channel` column), Unified Inbox (#11 — must provide `messages` table fulfilling abstract schema), Quote Builder (LOCKED — quote events in signals-only event list), Branded Invoicing (LOCKED — invoice events in signals-only event list), Brand DNA (LOCKED — profile as tiered draft input), Task Manager (LOCKED — deliverable events in signals-only event list, action items and tasks are separate tables), Onboarding (LOCKED — welcome email reads summary), Content Engine (#10 — aggregated client signals), S&D (LOCKED — suppression on draft drawer + action items). **Phase 5 sizing:** 3 sessions (A: data model + assembleContext + health score + handlers, B: prompts + regeneration pipeline + deduplication + usage logging, C: draft drawer + action items panel + profile tile + empty/loading states). Sequential — A before B before C. | [phase-3-client-context-engine-handoff.md](sessions/phase-3-client-context-engine-handoff.md)
8. ~~`docs/specs/client-management.md`~~ ✅ **LOCKED 2026-04-12** — 33 questions resolved. Dual-entry model (companies + contacts both first-class). Company profile: 7 tabs (Overview / Deliverables / Billing / Brand DNA / Comms / Portal Chat / Activity). Contact profile: 5 tabs (Overview / Comms / Brand DNA / Portal Chat / Activity). Contact Overview: two-column layout with Context Engine summary tile + action items + tasks + private notes + deal snapshot + draft drawer. Client portal: **chat-first design** — portal home is a full-page AI chat with bartender register (unnamed, warm, knows-your-name voice from S&D spec), menu bubble expands to full-page clean overlay menu, first-visit 3-step tour, fallback chain (chat → Pixieset gallery → SuperBad-branded error page). AI chat: Opus-powered read-only Q&A + escalation to Andy + limited safe actions (approve deliverable, trigger export, view invoice, Stripe Billing Portal link). Chat persistent per contact, escalations to comms inbox, full log viewable by Andy on admin profiles. Portal sections: Deliverables, Invoices, Brand DNA, Package, Messages, Gallery, Data Export. Real-time portal notifications: email + badge on load + WebSocket/SSE push. Individual portals isolated per contact. Clients index at `/lite/clients` with summary cards + compact list + health score dots + Active/Completed/Churned filters. Global search (`Cmd+K` + header) across entities + documents + tasks. One-click data export via `scheduled_tasks` (admin-triggered or client-triggered via chat). Intro Funnel fork-at-Won portal migration job. Deliverables via Task Manager (no fork). Cancel flow pages at `/lite/portal/subscription` consuming Quote Builder's defined branches. **New table:** `portal_chat_messages`. **New columns on `contacts`:** `portal_chat_last_seen_at`, `portal_last_visited_at`. **3 Claude prompts:** bartender opening line (Haiku), chat responses (Opus), escalation summary (Haiku). **`activity_log.kind` gains 8 values.** `scheduled_tasks.task_type` gains 2 values (`client_data_export`, `intro_funnel_portal_migration`). **4 new build-time disciplines (34–37).** **Sprinkle claimed:** §2 browser tab titles for admin profiles + portal pages. **No new motion/sound slots. No new specs spawned. No new memories.** **Content mini-session owed** (small — portal tour copy, error page, empty/loading states, notification email). **Cross-spec flags:** Unified Inbox (must support `source='portal_chat_escalation'`), Daily Cockpit (new client/churned/escalation alerts), Setup Wizards (external links in practical setup), SaaS Billing (cancel flow variant). **Phase 5 sizing:** 4 sessions (A: admin profiles + search, B: portal chat, C: portal sections + notifications, D: export + migration + cancel flow). A before B before C; D parallels B or C. | [phase-3-client-management-handoff.md](sessions/phase-3-client-management-handoff.md)
9. ~~`docs/specs/saas-subscription-billing.md`~~ ✅ **LOCKED 2026-04-12** — 29 questions resolved. Generic multi-product subscription infrastructure for Lite's self-serve SaaS arm. Identity-based pricing (same per-month rate everywhere; monthly pays one-time setup fee, annual waives it; three commitment cards: getting started / committed / all in). Popcorn at two levels: within products (small/medium/large tiers differentiated by usage limits) and across products (Full Suite priced to make stacking obviously worse). Multiple named usage dimensions per product + feature flags. Hard cap with one-click pro-rated upgrade + persistent usage sticky bar with evolving voice personality (calm→dry→dead-pan at limit). Two entry paths: warm traffic (interactive demo landing page, fully open no email gate, convention-break callout) and cold outreach (meta-demonstration email + pre-populated dashboard from enrichment data). Brand DNA hard gate after payment, pre-seeded from enrichment for cold prospects. SaaS cancel flow diverges from retainer: AI chatbot contact (not Andy's email, max escalation only), motivational reality check about entrepreneurship, pay-remainder retains access until anniversary, product-switch as soft first step. Fully native account management (no Stripe Billing Portal exposure). Multiple independent subscriptions per customer (one deal per product subscription). Setup fee per new monthly subscription naturally closes cancel-long-resubscribe-short loophole. Product admin at `/lite/products` with setup wizard, archive only (no duplicate — "that's how small details get missed"), three-level analytics zoom (cockpit → aggregate → per-product). Immediate payment failure lockout with data preservation, 3x failure → data-loss warning escalation. 9 billing email templates. 12 voice-treated surfaces. **New tables:** `saas_products`, `saas_tiers`, `saas_usage_dimensions`, `saas_tier_limits`, `usage_records`. **New columns on `deals`:** `saas_product_id`, `saas_tier_id`, `billing_cadence`. **`activity_log.kind` gains 11 values.** `scheduled_tasks.task_type` gains 3 values (`saas_data_loss_warning`, `saas_annual_renewal_reminder`, `saas_card_expiry_warning`). **5 new build-time disciplines (38–42).** **Sprinkles claimed:** §3 "Subscription cancelled" email, §2 browser tab titles for `/lite/products`. **No new motion/sound slots. No new specs spawned. No new memories.** **Content mini-session owed** (large — 12 voice surfaces + 9 emails + admin empty states). **Cross-spec flags:** Pipeline (3 new deal columns, multiple deals per contact), Lead Gen (meta-demo outreach, enrichment data access), Brand DNA (pre-seeding mechanism), Client Management (native account management, SaaS cancel branches, product menu items), Onboarding (Brand DNA pre-seeding trigger), Daily Cockpit (SaaS health signals), Finance Dashboard (SaaS revenue data), Quote Builder (shared cancel flow, shared scheduled_tasks). **Phase 5 sizing:** 5 sessions (A: data+Stripe / B: product admin / C: public signup / D: subscriber experience / E: cancel+failures). B and C parallel after A. | [phase-3-saas-subscription-billing-handoff.md](sessions/phase-3-saas-subscription-billing-handoff.md)
10. ~~`docs/specs/content-engine.md`~~ ✅ **LOCKED 2026-04-13** — 26 questions resolved. Dual-use from day one: SuperBad's own SEO content engine AND a SaaS product sold to subscribers (first product on the billing infrastructure). Six-stage pipeline: Research → Topic Queue (visible, passive, with outlines) → Generation (Haiku outline → Opus draft) → Review (split-pane + rejection chat) → Publish (subscriber's own domain via Cloudflare) → Fan-out (newsletter + social + visuals, background via scheduled_tasks). Two usage dimensions: published posts/mo + newsletter subscriber cap, framed as outcomes ("1 keyword-researched post per week, sent to up to 500 people, done while you sleep"). Rankability scoring via domain authority heuristic + content gap analysis (fetch + cheerio + Haiku, no new integrations beyond Lead Gen). Template-first visual generation (HTML/CSS + Puppeteer) + AI image fallback (OpenAI Images API) + Remotion video (large tier only). Continuous tier-paced throttle — engine won't generate new draft until current one is reviewed. Three newsletter list-growth channels: CSV import (permission pass), embeddable Brand DNA-styled form, inline blog CTA. Hybrid newsletter format (single post → standalone, multiple → digest). Content-to-outreach matching: published posts matched to Lead Gen prospects, content-forward emails in approval queue with earned autonomy. Chat-first subscriber portal (bartender primary interface). Demo: vertical + location input, full pipeline in SuperBad's voice, "imagine what could happen if our tool knew who you were." Admin at `/lite/content` + fleet overview at `/lite/content/subscribers`. Three onboarding wizard steps (domain → topics → newsletter). GSC optional at medium+, video at large, everything else open. Automated aggressive list hygiene with full CSV export. Subtle inline CTA on SuperBad's own blog posts. **New tables:** `content_topics`, `blog_posts`, `blog_post_feedback`, `social_drafts`, `newsletter_subscribers`, `newsletter_sends`, `ranking_snapshots`, `content_engine_config`. **No new columns on existing tables.** **10 Claude prompts** (2 Opus, 8 Haiku). **`activity_log.kind` gains ~15 values.** `scheduled_tasks.task_type` gains 6 values. **8 new build-time disciplines (43–50).** **New integrations:** OpenAI Images API, Remotion, Google Search Console API (optional). **Sprinkles claimed:** §2 system email subject lines, §6 OG images. **No new motion/sound slots. No new specs spawned. No new memories.** **Content mini-session owed** (large — social visual template library ~10–15 templates + newsletter template + demo page copy + blog CTAs + embeddable form + email subjects + OG image templates + empty/loading states + fleet overview + notification copy + Remotion art direction). **Cross-spec flags:** Lead Gen (content-match in approval queue + earned autonomy + reply auto-enrol), Sales Pipeline (standard SaaS deal creation), Brand DNA (perpetual context + demo voice + seed keyword derivation), Client Management (portal integration + bartender actions), SaaS Billing (productConfig + usage dimensions + feature flags), Onboarding (three wizard steps), S&D (sprinkle claims + suppression), Daily Cockpit (own queue + fleet health), Unified Inbox (email threading + subscriber replies), Client Context Engine (engagement signals). **Phase 5 sizing:** 7 sessions (A: data+research / B: generation+review+publish / C: visual pipeline / D: newsletter / E: social / F: SaaS integration / G: outreach+ranking+fleet). C∥D after B. F after A+SaaS. G after A+B. | [phase-3-content-engine-handoff.md](sessions/phase-3-content-engine-handoff.md)
11. ~~`docs/specs/unified-inbox.md`~~ ✅ **LOCKED 2026-04-13** — 13 questions resolved. Full consolidation of human-to-human comms into one `messages` + `threads` surface. Replaces Outlook as Andy's primary work email client via staged M365 migration (Phase 1: Lite-as-Graph-API-client on existing mailbox, Outlook stays as backup; Phase 2: DNS cutover deferred). Two addresses (`andy@` + `support@`) unified with ticket overlay on support@ only (status: open/waiting/resolved + Claude-assigned type chip). Three independent parallel Haiku classifications per inbound: router (Q5 — known/new-lead/non-client/spam + contact resolution), notification triage (Q10 — urgent/push/silent), signal/noise (Q12 — signal/noise/spam + transactional sub-classification). Topic threads + per-contact Conversation view (flattens all threads chronologically, single query feeds Client Context Engine). Passive-vs-active channel delivery rule (portal chat + task feedback echo to email; IG/FB/SMS/WhatsApp native-only, no echo). Always-drafted Opus replies for client-facing threads with chat-refinement sidecar + retrieval-based voice learning from past sent messages + low-confidence flag chips + never auto-send policy. Single composer with optional AI draft + contact-aware picker + subject auto-generation. Claude-triaged push notifications (web push + PWA push) + 8am morning digest of everything silenced (skipped on quiet mornings) + shared notification dispatcher primitive for all system events. 12-month import on setup with on-demand backfill per older thread + post-import contact routing review. Signal/noise auto-deletion: 30 days general noise, 180 days transactional receipts, 7 days spam; Keep pin overrides; 14-day pre-purge trash bin; keep-sender promotion after 3 pins. PWA mobile with read+quick-reply+triage, Claude draft pre-loaded, compose-new discouraged but available; Outlook mobile stays as escape hatch. **New tables:** `threads`, `messages`, `message_attachments`, `message_calendar_invites`, `message_delivery_attempts`, `notifications`, `classification_corrections`, `inbox_signature_settings`, `graph_api_state`. **New columns on `contacts`:** `relationship_type`, `inbox_alt_emails`, `notification_weight`, `always_keep_noise`. **4 Claude prompts** (1 Opus for reply drafter, 3 Haiku for the three classifiers + ticket-type chip). **`activity_log.kind` gains ~18 values.** `scheduled_tasks.task_type` gains 5 values (`inbox_draft_reply`, `inbox_hygiene_purge`, `inbox_morning_digest`, `inbox_graph_subscription_renew`, `inbox_initial_import`). **14 new build-time disciplines (51–64).** **New integrations:** Microsoft Graph API (OAuth + webhook subscriptions + send-as + delegated Mail scopes), PWA Push (iOS 16.4+ / Android). **Sprinkle claims:** §2 morning digest email copy, §2 low-confidence chip copy, §2 "continue in your portal" echo footer, §2 mobile compose-new nudge, §2 Focus/Noise empty states, §2 trash-bin hygiene copy, §2 setup wizard Graph-API consent step copy, §2 post-import routing-review copy, §2 keep-sender promotion chip, §2 weekly inbox hygiene celebration copy for Daily Cockpit, §2 browser tab title for inbox, §2 "Send & Close ticket" voice, §2 signature default templates (2 variants per address). **New memory:** `feedback_passive_vs_active_channels.md` — email-echo only for passive channels; active channels (IG/FB/SMS/WhatsApp) deliver natively with no cross-channel echo. **Content mini-session owed** (see deferred list below). **Foundations §11 patches owed:** (1) `sendEmail({ transport: 'graph' \| 'resend', classification: 'transactional' \| 'outreach' })` extension so Andy-replies route through Graph and system emails continue via Resend; (2) notification dispatcher primitive as a new shared §11 module; (3) `classification_corrections` as a shared learning-loop primitive available to any classifier across the platform. **Cross-spec flags:** Client Management (`relationship_type` enum on contacts + `createThreadFromPortalChat()` integration point + AI chatbot cancel flow + portal Messages menu), Task Manager (`createThreadFromTaskFeedback()` integration point + task notifications route through shared dispatcher), Lead Gen (`attachMessageToInboxThread()` for outreach replies + campaign_id custom header preserved for fallback threading), Content Engine (`createThreadFromNewsletterReply()` + unreviewed draft notifications via shared dispatcher), SaaS Subscription Billing (support@ threads link to subscription via contact FK + customer context panel + payment-failure notifications + cancel-flow AI chatbot uses inbox threads), Brand DNA (profile read by reply drafter as system context), Client Context Engine (reads `messages`+`threads` directly via `contact_id` + classification outputs are signals + `engagement_signals` array provides behavioural data), Onboarding + Segmentation (Graph API connection is new wizard step + post-import routing review step + noise bulk-purge offer step), Sales Pipeline (Deal pages show "Recent messages" sidebar via `contact_id` join), Surprise & Delight (sprinkle claims + suppression on compose + on setup wizard + on billing threads), Quote Builder + Branded Invoicing (no schema change — their emails still go via Resend; client replies land normally), Daily Cockpit (reads inbox aggregates: unread counts, waiting-on-Andy, open support tickets, tickets >48h, silenced-digest preview, hygiene stats, anomaly detection + absorbs the parked Q7 idea as Andy-facing weekly social-DM digest). **Phase 5 sizing:** 8 sessions (A: Graph API integration / B: data model + threading + routing + 12mo import / C: inbox UI + compose + Conversation view / D: reply drafting + refine-chat + voice learning / E: notifications + PWA + morning digest / F: signal/noise + auto-delete + hygiene / G: support@ ticket overlay + classification / H: cross-spec integration surfaces). A+B must land very early in Phase 5 as foundation-layer builds — multiple other specs depend on the `messages`+`threads` tables. C follows immediately; D–G can parallel after C; H is integration work sprinkled through later sessions. | [phase-3-unified-inbox-handoff.md](sessions/phase-3-unified-inbox-handoff.md)
12. ~~`docs/specs/daily-cockpit.md`~~ ✅ **LOCKED 2026-04-13** — 9 questions resolved. The operator's morning surface at `/lite`. Three-zone default layout: narrative brief at top (SuperBad voice, 2–3 sentences) + tactile attention rail (horizontal, up to 6 chips "waiting on Andy" + `+N more` overflow to `/lite/waiting`) + conditional system-health banner strip (up to 2 banners + overflow to `/lite/health`) + calendar preview band + switchable kanban/list planning view below (inherits Task Manager's locked column rules — do not re-derive). **Three daily briefs** (morning 06:00, midday 12:00, evening 18:30 Melbourne) with chained continuity + compact event trail from `activity_log` filtered via an allow-list (`getAndyFacingActivitySince()`); midday reads morning brief prose + today's event log; evening reads both + expanded log + tomorrow's calendar + tomorrow's scheduled_tasks. **Material-event regen** of current slot's brief via `scheduled_tasks` (new task type `cockpit_brief_regenerate`, 10-min debounce per slot, chain-anchored to the original morning brief to prevent hallucination chains). **Quiet-slot skip** precondition check (zero waiting items + zero material events + zero banners) renders a curated fallback line from a rotation pool instead of calling Opus. **Fleet-scoped chips** fold into the same rail with a small `[Fleet]` tag when firing; no dedicated fleet band. **No chip dismissal or snooze** — rail is purely derived; resolution at the source spec is the only clearing mechanism. **Mobile PWA layout:** vertical stack, horizontal-scroll rail, tabbed kanban (Must Do / Should Do / If Time) with house-spring motion, list toggle icon; cockpit is default PWA route post-install. **New table:** `cockpit_briefs` (unique constraint on user_id + slot + brief_date; regen overwrites with audit trail in activity_log). **1 Claude prompt:** `cockpit-brief` (Opus, slot parameter, three prompt templates morning/midday/evening). **`activity_log.kind` gains ~8 values.** `scheduled_tasks.task_type` gains 1 value (`cockpit_brief_regenerate`). **9 new build-time disciplines (65–73).** **Sprinkle claims:** §2 browser tab title on `/lite` (state-reflecting voice treatments), §2 signed-in admin greeting line above the brief (rotation pool). **New ambient category proposed for S&D closed list (6 → 7):** quiet-slot fallback lines — pending sprinkle-promotion brainstorm gate. **Cross-spec patches owed at Phase 3.5:** 11 locked specs add `getWaitingItems()` contract (Quote Builder, Branded Invoicing, SaaS Billing, Unified Inbox, Task Manager, Client Management, Lead Generation, Intro Funnel, Brand DNA Assessment, Content Engine, Client Context Engine); 8 locked specs add `maybeRegenerateBrief(eventKey, payload)` callsite wiring (SaaS Billing for `subscription_payment_failed` + `subscription_cancelled`, Sales Pipeline for `deal_won` + `deal_lost`, Branded Invoicing for `invoice_paid_large`, Intro Funnel for `intro_funnel_booking_confirmed`, Lead Gen for `outreach_reply_positive`, Content Engine for `content_engine_domain_verification_failed`, Unified Inbox for `graph_api_subscription_lapsed`, Finance Dashboard/Observatory for `cost_anomaly_detected`); 4 specs add `getHealthBanners()` contract (Unified Inbox, Content Engine, Finance Dashboard, SaaS Billing). **No new memories. No new motion slots. No new sound slots. No new specs spawned. No Foundations patches owed beyond what's already flagged.** **Content mini-session owed** (small-medium — three brief prompt templates + quiet-slot fallback pool + admin greeting pool + tab title voice treatments + chip label conventions per source_spec + `/lite/waiting` + `/lite/health` copy + calendar preview empty-state). Must run before Phase 5 Sessions A + B. **Phase 5 sizing:** 5 sessions (A: briefs data + morning slot + cron / B: midday + evening + event trail + chaining + material-event regen + quiet-skip + fallback pool / C: attention rail + `/lite/waiting` / D: banner strip + calendar preview + `/lite/health` / E: mobile PWA layout + kanban tabs). A + B are voice-critical and require the content mini-session. Cockpit is a Phase 5 *middle* build, not late — ship the skeleton early so downstream specs wire their contracts as they land. | [phase-3-daily-cockpit-handoff.md](sessions/phase-3-daily-cockpit-handoff.md)
13. `docs/specs/setup-wizards.md` — cross-cutting primitive
14. `docs/specs/hiring-pipeline.md` — parallel CRM for headhunting, reuses Pipeline's KanbanBoard + activity_log primitives
15. **`docs/specs/surprise-and-delight.md`** — ✅ **PRE-WRITTEN 2026-04-12 mini-brainstorm #3** (see `sessions/phase-3-surprise-and-delight-brainstorm-handoff.md`). Cross-cutting voice + hidden eggs feature. Two-layer model: 80% ambient Claude-generated voice in a closed list of 6 surface categories + 20% rare Metal Gear Solid-tradition platform-awareness eggs (1 admin canonical + 12 public catalogued). Admin = roommate voice, public = bartender voice, never pitches. Public knowledge ceiling (browser-free data only). Cadence: 1 guaranteed first-visit egg for public + max 2/14d thereafter; max 1/7d for authenticated users. Crossover preserves earned state. Context-aware suppression is hard-gated (mid-payment, wizards, errors, first session, etc.). Riddle loop via shared `resolveRiddleAnswer()` resolver. Hybrid wrong-answer branching (pre-generated common + live Claude fallback capped ~100/riddle lifetime). Kill switch in Settings → Display. **New tables:** `riddles`, `riddle_resolutions`, `hidden_egg_fires`, `ambient_copy_cache`. **New user columns:** `last_hidden_egg_fired_at`, `hidden_egg_tricks_enabled`, `fired_egg_ids_recent`. **New Claude primitives:** `generateInVoice()` and `resolveRiddleAnswer()`. **New build-time disciplines:** 19–23. **Closed-list exemptions:** hidden-layer CRT motion + rain sound explicitly exempt from Tier 2 motion and sound registry because they fire ≤1x/month/user. **Silent dependency:** this spec cannot be **built** before `brand-dna-assessment.md` (#5) ships AND Andy has taken SuperBad's own assessment — the drift check needs SuperBad's Brand DNA profile as grading substrate. Phase 5 build order: Brand DNA feature → SuperBad's own assessment run → Surprise & Delight build. **Follow-up brainstorm owed:** 3–5 more admin eggs beyond the canonical CRT turn-off, before Phase 5 build of this feature. **New memory:** `feedback_surprise_and_delight_philosophy.md` (canonical philosophy document — every future spec touching a user-facing surface reads this).
16. **`docs/specs/task-manager.md`** — ✅ **LOCKED 2026-04-12 mini-brainstorm #4** (see `sessions/phase-3-task-manager-brainstorm-handoff.md`). Operational-spine feature with two surfaces: Task Manager module at `/lite/tasks` (list view + detail drawer + filters + bulk delete) and globally-available Braindump primitive (floating button + `Cmd+Shift+D` shortcut on admin surfaces only, modal with in-place Claude parse → edit → commit flow, `⇄` swap affordance on ambiguous entity candidates). **Load-bearing architectural decision:** deliverables are tasks — `docs/specs/client-management.md` references this spec rather than defining its own deliverables data model. **Closed enums:** `kind` (5 values: `personal` / `admin` / `prospect_followup` / `client_deliverable` / `client_task`), `status` (7 values: `todo` / `in_progress` / `blocked` / `awaiting_approval` / `delivered` / `done` / `cancelled`), `recurrence` (6 presets). State machine validated in `lib/tasks/transitions.ts`. Polymorphic-by-convention entity linking via `(entity_type, entity_id)` following the activity_log pattern. **Approval workflow for deliverables:** dual entry — tokenised magic-link email (transactional, bypasses §11.4 quiet window, 48h reminder respects it) + direct portal visit, both routing through single `approveDeliverable()` primitive in `lib/tasks/approve.ts`. Rejection feedback fires as inbound message in comms inbox with `source='task_rejection'`. **Cockpit integration (read-only):** tasks are a cockpit data source with locked column rules (Must Do / Should Do / If Time filters on due date + priority + status); `awaiting_approval` and `blocked` stay off the attention surface and surface as passive counts in the narrative brief. **Notifications intentionally minimal:** zero push on Andy's own tasks; morning digest at 08:00 Melbourne gated by two conditions (Andy not signed in by 08:00 AND something to report); braindump ingestion silent. **Recurrence:** auto-spawn on completion via `markTaskDone()`, not calendar cron; irregular patterns handled manually in v1. **Checklists:** optional inline JSON, parser emits for countable phrasings, `checklist_auto_complete` default true with 10s visible undo window. **New tables:** `tasks`, `braindumps`. **New Claude primitives:** `parseBraindump(rawText, surfaceContext?)` (Haiku-tier, reads Brand DNA + recent contacts for entity matching), `approveDeliverable(taskId, decision, feedback?)`, `generateInVoice('task_digest_subject', context)` (from S&D). **New build-time disciplines:** 24–29. **Cross-spec impacts:** client-management.md gets a deliverables flag (do not define local data model); daily-cockpit.md gets a tasks-as-data-source flag with locked column contract; comms-inbox.md gains `linked_task_id` nullable column + `'task_rejection'` source value; client-portal render rules defined via `getTasksForClientPortal()` only. **Foundations §11 patch owed in Phase 5:** `sendEmail()` gate (§11.2) gains required `classification: 'transactional' | 'outreach'` parameter — transactional bypasses §11.4 quiet window, outreach respects it. **Sprinkle claimed:** §2 browser tab title (dynamic — `"SuperBad Lite — 4 overdue"` vs `"SuperBad Lite — nothing's on fire"`); mark `[CLAIMED by task-manager]` in `docs/candidates/sprinkle-bank.md`. **No silent dependencies** — can build as soon as Phase 5 starts; pre-Brand-DNA/pre-S&D fallback is static dry lines on `generateInVoice()` calls. **No new memories.**

17. ~~`docs/specs/finance-dashboard.md`~~ ✅ **LOCKED 2026-04-13** — 9 questions resolved. Operator-only. Headline: P&L tile + 90-day projection chart side-by-side above fold. Projection = contracted MRR + pipeline-weighted open deals with stage-age decay (halves every 30d past expected dwell); trial shoots at 100%. Expenses: manual quick-add + `recurring_expenses` cron-booking (pending-review gate) + daily Observatory rollup (API costs) + daily Stripe Balance Transactions rollup (Payment processing). Accountant bundle export: BAS summary PDF + P&L PDF + transactions/expenses/invoices/per-client CSVs, zipped, preset BAS quarters + EOFY + custom range, inc/gst/ex on every row. Below fold: Claude-Haiku narrative with clickable number-references (validation + fallback) + tiles (MRR & net run-rate / outstanding invoices / top 5 categories / tax provision) + recent transactions feed. Cockpit banners: `finance_bas_due` + `finance_eofy_due` + `finance_invoice_overdue` only (tax-tight is dashboard-only, state-not-action). One global time-range picker, projection immune (always forward 90d), URL-encoded deep-linkable. Shallow drill-down per tile + row click-through to owning feature. Tax provision: GST owed / income tax provisioned / yours-to-spend (Stripe balance proxy, no open-banking in v1). **New tables:** `expenses`, `recurring_expenses`, `finance_snapshots`, `compliance_milestones`. **1 Haiku LLM job** (`finance-narrative` — structured output with number-reference validation). **2 new Stripe integration jobs** (`stripe-balance-read`, `stripe-balance-transactions-read`). **`scheduled_tasks.task_type` gains 6 values** (`finance_snapshot_take`, `finance_narrative_regenerate`, `finance_observatory_rollup`, `finance_stripe_fee_rollup`, `recurring_expense_book`, `finance_export_generate`). **`activity_log.kind` gains 8 values.** **11 new settings keys** (`finance.*`). **Sprinkle claims:** narrative opening lines rotation pool + `/lite/finance/*` browser tab titles. **No new motion/sound slots. No new memories. No new specs spawned.** **Content mini-session owed** (small-medium — narrative prompt calibration × 8–10 scenarios + tax-tight/projection-cliff/overdue-heavy callout pools + 3 BAS banner variants by days-remaining + empty states + tab titles). **Cross-spec flags for Phase 3.5:** Daily Cockpit (3 new `getHealthBanners()` kinds + `maybeRegenerateBrief()` subject discriminator confirmation), Branded Invoicing (`?filter=overdue` query param + Puppeteer multi-document export confirmation), Observatory (add `finance-narrative` + 2 Stripe jobs to model registry), Setup Wizards (add `finance-tax-rates` onboarding step writing `finance.gst_rate` + `finance.income_tax_rate`). **Phase 5 sizing:** 4 sessions (A: data model + expense form + settings / B: projection logic + roll-up crons + Stripe Balance API / C: dashboard UI + narrative prompt + drill-downs / D: accountant bundle + cockpit banners + onboarding step). | [phase-3-finance-dashboard-handoff.md](sessions/phase-3-finance-dashboard-handoff.md)

18. ~~`docs/specs/cost-usage-observatory.md`~~ ✅ **LOCKED 2026-04-13** — 9 questions resolved. Dual primary job: alarm + attribution co-equal, forensic queryability free. Four-actor model (`internal | external | shared | prospect`) with single-row log + ephemeral hashed `shared_cohort_id` + query-time proration. Three anomaly detectors in parallel (hard thresholds + rate loop-catcher + learned bands with 7-day/50-call warmup). Severity-tiered banners (low dry-calm / mid neutral-louder / severe terse-flat-red with kill-switch). Prospect-phase spend transfers to subscriber/client on conversion via atomic rewrite in conversion handler, preserving `converted_from_candidate_id` for audit — **CPA, CPA-by-channel, CPA-by-ICP-segment, payback period all derivable**. Attribution reoriented around tier health (aggregate per-tier margin + % underwater), with per-subscriber recommendation cards **only rendered for Large-tier** subscribers (Small/Medium negative margin = tier-design bug surfaced at tier level — anchored in new memory `project_tier_limits_protect_margin.md`). Operator-only costs (never expose AUD to subscribers); two separate ledgers (`usage_records` subscriber-facing + `external_call_log` operator-facing) never merged. Claude diagnosis card (Opus prompt `diagnose-cost-anomaly.ts`) on top of raw-data expandable table; one-click band editor on anomaly detail view. No auto-pause, no platform hard cap; Andy-set monthly AUD thresholds (up to 3) + linear run-rate projection alert. **New tables:** `external_call_log`, `cost_anomalies`, `deploy_events`, `observatory_settings`. **Model/integration registry** (`lib/ai/models.ts` + `lib/integrations/registry.ts`) — required bands at registration, ESLint-enforced no-direct-vendor-SDK-imports in feature code. **3 Claude prompts** (2 Opus, 1 Haiku). **`activity_log.kind` gains 10 values.** `scheduled_tasks.task_type` gains 5 values (three detectors + diagnoser + weekly digest). **Foundations §11 patch consolidates** send-gate `classification` parameter (owed from Task Manager), LLM model registry (owed from memory), external-call observability primitive, quarterly model-review ritual — authored by Phase 5 Session A. **Sprinkle claimed:** §5 weekly digest email subject line (dry MTD-posture rotation). **New memories:** `feedback_technical_decisions_claude_calls.md` (don't ask Andy to pick between technical options), `project_tier_limits_protect_margin.md` (tier limits protect margin; negative margin Small/Medium = tier-design bug). **Cross-spec flags for Phase 3.5:** every integration-using spec must specify `actor_type`/`actor_id` at each external-call site (~14 specs); Daily Cockpit patches `getHealthBanners()` to include the 5-kind `ObservatoryBanner` union; SaaS Billing notes `usage_records` vs `external_call_log` ledger separation; Lead Gen specifies conversion-handler atomicity; Intro Funnel specifies trial-shoot Stripe attribution as `prospect`; every LLM-using spec declares `prompt_version_hash` convention. **Content mini-session owed** (small-medium — banner body pools × 3 tiers, diagnosis prompt calibration across 8–10 synthetic scenarios, dashboard empty-states, weekly digest framework, threshold/projection banner copy, kill-switch modal copy). **Phase 5 sizing:** 3 sessions (A: logging primitives + registry + every vendor SDK wrapper + FOUNDATIONS patch + deploy webhook / B: three detectors + anomaly dedupe + diagnosis prompt + Cockpit banner wiring + severe-tier email + kill-switch plumbing + prompt-version tracking / C: observatory dashboard + anomaly detail + job detail + settings + projection tile + tier-health panel + Large-tier recommendation cards + weekly digest). A+B partially parallelisable; C depends on B. | [phase-3-cost-usage-observatory-handoff.md](sessions/phase-3-cost-usage-observatory-handoff.md)

19. `docs/specs/six-week-plan-generator.md` — **NEW (2026-04-13, requested by Andy)**. Autonomous generation of the bespoke 6-week, week-by-week marketing plan included with every $297 trial shoot (per memory `project_trial_shoot_facts.md`). Plan must function as a real retainer alternative — self-runnable by the prospect, not a teaser — because the Intro Funnel's retainer-fit recommendation (§13.4) frames "take-the-plan-and-run" as a credible third outcome alongside retainer and not-now. Inputs: Brand DNA profile (assessment is part of the trial shoot flow) + Lead Gen's 9-signal enrichment + shoot-day context (conversation notes, on-site observations, delivered asset themes) + SuperBad's own Brand DNA for voice grounding. Output: branded weekly plan with per-week concrete tasks, channel mix, content angles, KPIs/success signals, and "what good looks like" framing. Surfaces into Intro Funnel step 8 as a second sub-surface alongside the Pixieset gallery. **Must run before the Intro Funnel trial-shoot-facts patch** (#31 in Deferred tasks) so the patch can reference this spec's pipeline rather than handwaving "Opus-drafted plan from Brand DNA + enrichment + shoot context". Expected artefacts: ≥1 new table (likely `trial_marketing_plans` with versioning for regen), 1–2 Opus prompts (initial draft + material-event regen), Andy review + regenerate affordance before client sees it, branded web view + PDF export via shared renderer, delivery email template, portal sub-surface, ownership of plan after conversion to retainer (becomes early client context). Cross-spec dependencies to watch in brainstorm: Brand DNA (reads profile), Lead Gen (reads enrichment), Intro Funnel (deliverables reveal + retainer-fit framing + cancellation — what happens to plan if client forfeits booking), Client Context Engine (plan becomes perpetual context on retainer conversion), Task Manager (plan weeks may spawn as client deliverables post-conversion), Surprise & Delight (voice & delight treatment + potential sprinkle claim on cover line), Cost & Usage Observatory (`actor_type: 'prospect'` logging, transfers to `client` on conversion), Content Engine (does the plan integrate with Content Engine if prospect converts and takes it?). **Content mini-session likely owed** (plan week-structure framing, per-channel language defaults, delivery email, empty/regen states). Best sequenced before or immediately after Setup Wizards (#13) so Phase 3.5 review sees the complete trial-shoot deliverable surface.

When Andy says "let's go", begin the next Phase 3 session following the protocol in `START_HERE.md`.

---

## Phase roadmap

- [ ] **Phase 0** — Pre-flight scaffolding *(done in the HQ session, before this project opened — see `docs/phase-0-scaffold-note.md`)*
- [x] **Phase 1** — Scope brainstorm → `SCOPE.md` *(locked 2026-04-11)*
- [x] **Phase 2** — Foundations brainstorm → `FOUNDATIONS.md` *(locked 2026-04-11)*
- [x] **Phase 3** — Feature specs → `docs/specs/*.md` *(21 specs locked 2026-04-12 through 2026-04-13)*
- [x] **Phase 3.5** — Spec review (exit gate) → patches applied across all docs, `sessions/phase-3.5-exit-handoff.md` *(locked 2026-04-13)*
- [x] **Phase 4** — Build plan → `BUILD_PLAN.md` + `AUTONOMY_PROTOCOL.md` + `LAUNCH_READY.md` *(locked 2026-04-13 — BUILD_PLAN.md: 23 waves, ~150 atomic sessions, 60+ cron jobs, shared-primitive registry, 5 critical-flow E2E assignments, rollback strategy per wave; AUTONOMY_PROTOCOL.md: 13 non-skippable per-session gates + model-tiering + Opus-quota pause behaviour + kill-switches + observability discipline; LAUNCH_READY.md: 12-section pre-launch gate checklist)*
- [ ] **Phase 5** — Build execution → session by session
- [ ] **Phase 6** — Launch → `superbadmedia.com.au/lite` live

Phase 0 stays unchecked in this tracker because the session that did it (HQ) doesn't count as a Lite session — this tracker begins with Phase 1.

---

## Session log

| Session | Phase | Type | Summary | Handoff note |
|---|---|---|---|---|
| 2026-04-11 | 1 | Brainstorm | Scope locked — 6 core areas all in v1, setup wizards + unified inbox as cross-cutting, no fixed rate card, per-client retainer pricing inside Lite, zero manual media authoring | [phase-1-handoff.md](sessions/phase-1-handoff.md) |
| 2026-04-11 | 2 | Brainstorm | Foundations locked — Next.js App Router on reused HQ Coolify, SQLite+Drizzle+Litestream→R2, Auth.js v5 magic-link, Resend for all email, Stripe everything, shadcn/ui + Framer Motion + use-sound. Lite ships at `lite.superbadmedia.com.au` first, migrates to `/lite` path at launch. GHL gets switched off at cutover — nothing worth preserving. | [phase-2-handoff.md](sessions/phase-2-handoff.md) |
| 2026-04-11 | 3 | Spec | Design system baseline locked — surface strategy (warm stacked tints), 2-tier motion (house spring + 7 closed Tier 2 moments + 2 overlays), 2 density presets, graduated soft radius, BHS closed-list of 8 locations, Settings → Display panel (4 accessibility toggles + 3 theme presets + 3 typeface presets), pink as customer-warmth + focus, full token system, 11 build-time disciplines. New memory: curated brand-canonical customisation pattern. | [phase-3-design-system-baseline-handoff.md](sessions/phase-3-design-system-baseline-handoff.md) |
| 2026-04-11 | 3 | Spec | Sales Pipeline locked — 8 stages (Lead → Contacted → Conversation → Trial Shoot → Quoted → Negotiating → Won → Lost), three-entity CRM (Company + Contact + Deal), `billing_mode` flag for manual-billed companies, append-only activity log with three nullable FKs, two-tier hover card (desktop only), per-stage stale halo + snooze, Loss Reason modal with closed list of 7, Trial Shoot panel on Company profile, full Drizzle schema + validation rules, Stripe + Resend webhook idempotency from day one. **3 new specs spawned:** Branded Invoicing, Intro Funnel, Hiring Pipeline. **SCOPE.md alignment change:** Trial Shoot is now an explicit stage (was Conversation in SCOPE). **New memory:** SuperBad client size diversity (Andy correction). | [phase-3-sales-pipeline-handoff.md](sessions/phase-3-sales-pipeline-handoff.md) |
| 2026-04-11 | 3 | Scope patch | Mini-brainstorm added 3 new features to v1: **Content Engine** (semi-autonomous SEO blog + newsletter + social drafts with stub publish adapters, SerpAPI-driven keyword research, Cloudflare path routing to `/blog`, one-gate review with Claude rejection chat), **Brand DNA Assessment** (NASA-style dual-mode psychological + taste profile, 50–100 indirect-signal MC questions per bank, shape-aware multi-stakeholder blending, signal-tag output, perpetual LLM context across platform), **Premium Onboarding + Revenue Segmentation** (composes Brand DNA into retainer onboarding, standardised fast SaaS onboarding with dashboard-nudged Brand DNA as upsell signal, double segmentation layer). **Non-goal redrawn:** newsletter is now in scope for SuperBad's own owned audience; third-party campaign management remains out. **New memory:** Brand DNA as perpetual LLM context. | [phase-3-scope-patch-handoff.md](sessions/phase-3-scope-patch-handoff.md) |
| 2026-04-12 | 3 | Upgrades integration | Integrated the 7 "easy-fit" upgrades from `where-we-are.html § 4`. **5 cross-cutting primitives** added to `FOUNDATIONS.md` as new §11 (Universal Audit Log, Safe-To-Send Gate, Timezone-Correct Timestamps, Outreach Quiet Window, Brand-Voice Drift Check) with 5 new build-time disciplines (13–17). **2 features** folded into existing planned specs via new `SCOPE.md` "Additional v1 features (2026-04-12 upgrades shortlist)" section: Sentiment-Aware Reply Drafts (into `unified-inbox.md`), One-Click Client Data Export (into `client-management.md`). **No new spec sessions added** — feature additions reference their parent specs. Next Action block updated with the cross-cutting constraints the Lead Gen spec must honour. | [phase-3-upgrades-integration-handoff.md](sessions/phase-3-upgrades-integration-handoff.md) |
| 2026-04-12 | 3 | Spec | Lead Generation locked — 13 questions resolved. Operations-pipeline model: automated daily 3am run, one unified search, two tracks (SaaS + retainer) with winner-takes-all scoring against qualification floors, 9-signal enrichment (Meta Ad Library, Google Maps, Google Ads Transparency, PageSpeed, whois, Instagram BD, YouTube API, website scrape, Maps extras), Hunter.io contact discovery with pattern fallback, single approval queue with per-track earned autonomy graduation (manual → 10 clean approvals → probation → 5 sends → auto_send with rolling-20 at 80% maintenance floor + 4 circuit breakers), non-overrideable 4-week warmup ramp (5/10/15/20/30), engagement-gated indefinite sequences with 4-tier engagement model (click > full open > sub-60s open > none, 3 consecutive non-engagements ends sequence), three-tier DNC with single `isBlockedFromOutreach()` enforcement, single sender `Andy Robinson <hi@contact.superbadmedia.com.au>`, three-tab surface (Queue / Runs log / Metrics with closed list of 4 panels), Candidate → Deal promotion at first-send time via `createDealFromLead()`. **New tables:** `lead_candidates`, `outreach_drafts`, `outreach_sends`, `outreach_sequences`, `lead_runs`, `dnc_emails`, `dnc_domains`, `resend_warmup_state`, `autonomy_state`. **Cross-spec flags:** Pipeline `contacts.email_status` enum + `activity_log.kind` enum need non-breaking additions. **No new specs spawned.** **No new memories.** | [phase-3-lead-generation-handoff.md](sessions/phase-3-lead-generation-handoff.md) |
| 2026-04-12 | 3 | Scope patch | Mini-brainstorm added 1 new v1 **primitive**: **Client Context Engine** — per-contact always-on Claude engine storing comms + activity + action items + deal/delivery/invoice state + Brand DNA as invisible context, surfacing a living human-facing summary on every profile, producing hybrid one-shot-plus-nudge channel-aware draft replies on click, auto-extracting action items (with owners "you" vs "them") from every message with manual override. Active summaries (Haiku-tier, regen on material events) + reactive drafts (Opus-tier) + signals feed the Daily Cockpit, not a second attention surface. Reuses the Content Engine rejection-chat primitive for draft nudging — zero new chat infra. Treated as a primitive, not a feature: composed by Lead Gen, Client Management, Daily Cockpit, Sales Pipeline card hover; Lead Gen's "generate email" button becomes a call into this primitive with empty-history fallback. Pairs architecturally with Brand DNA as the "where you are in the conversation" half of the perpetual-context pair. Spec positioned at #7 in the backlog (after Onboarding + Segmentation, before Client Management). **New memory:** `project_two_perpetual_contexts.md` — Brand DNA + Context Engine must both be read on every client-facing LLM call. | [phase-3-context-engine-scope-patch-handoff.md](sessions/phase-3-context-engine-scope-patch-handoff.md) |
| 2026-04-12 | 3 | Spec | Intro Funnel locked — 16 questions resolved. Customer-facing acquisition surface for the $297 trial shoot, reframed from "sales funnel" to "branded experience that happens to convert." Full flow: on-brand editorial landing page → section 1 contact form (creates Deal + portal session immediately) → fast-dopamine portal access → shape-branched self-persuasion questionnaire (solo_founder / founder_led_team / multi_stakeholder_company × 4 sections = 12 banks) → Stripe Payment Element inline with dynamic amount from Settings → native calendar booking (not Cal.com) with 5-business-day advance notice + 3-per-week cap → trial shoot → Pixieset API native gallery reveal → 24h later post-shoot reflection questionnaire (8 screens, linear arc, Q1 safety valve, Claude synthesis reveal) → Claude-generated retainer-fit recommendation (Opus, structured JSON with recommendation_type + confidence + reasoning + flags) → Daily Cockpit for Andy's review → fork at Won into Client Management portal OR dormant on non-conversion. Aggressive abandon cadence 15m SMS→24h SMS+email→3d email→demote to cold outreach with context flag. Cancel/reschedule policy: 48h refund window, max 2 reschedules, auto-Lost on 3rd attempt; SuperBad-initiated cancel/reschedule always fully refundable regardless of timing. **New tables:** `intro_funnel_submissions`, `intro_funnel_payments`, `intro_funnel_bookings`, `intro_funnel_reflections`, `intro_funnel_retainer_fit`, `intro_funnel_config`, `calendar_bookings` (polymorphic, shared), `calendar_config`, `dnc_phones`, `twilio_sms_log`. **Deal creation at section 1 submit** (not at first-send like Lead Gen — section 1 is strong intent, Trial Shoot is an active stage). **New stack integration:** Twilio (non-breaking FOUNDATIONS addition). **Pixieset integration** via API for native gallery, URL fallback. **Cross-spec flags:** Pipeline needs 6 `deals` cols + 3 `contacts` cols + 21 activity_log.kind values + 7 lost_reason values (closed list 7→14) + 2 Trial Shoot sub-statuses + 8 Trial Shoot panel fields. Design-system-baseline needs revisit for 3 new Tier-2 motion candidates (Payment Element reveal, synthesis reveal, deliverables gallery reveal). **New memory:** `feedback_dont_undershoot_llm_capability.md` — Andy corrected my "defer AI feature to v1.1 for training data" reflex; Claude reasons from rich context + described criteria without labelled data. **Follow-up mini-session owed:** content authoring for landing page + 9 question banks + reflection text + email templates. **No new specs spawned.** | [phase-3-intro-funnel-handoff.md](sessions/phase-3-intro-funnel-handoff.md) |
| 2026-04-12 | 3 | Mini-brainstorm + spec pre-write | Surprise & Delight locked as cross-cutting v1 feature — two-layer model (80% ambient Claude-generated voice in a closed list of 6 surface categories + 20% rare Metal Gear Solid-tradition platform-awareness hidden eggs), tone asymmetry (admin = roommate voice, public = bartender voice, never pitches), public knowledge ceiling (browser-freely-given data only — never IP geoloc / ISP / GPS), cadence (1 guaranteed first-visit egg for public + max 2/14d thereafter; max 1/7d for authenticated users; crossover preserves earned state), context-aware suppression hard-gated during mid-payment/wizards/errors/first-session, riddle loop via shared `resolveRiddleAnswer()` resolver with hybrid wrong-answer branching (pre-generated + live Claude fallback capped ~100/riddle), kill switch in Settings → Display. Canonical admin egg locked (CRT turn-off after three 2am sessions). 12 public eggs catalogued and approved. **Pre-written as `docs/specs/surprise-and-delight.md` at backlog position #15** — unusual for a scope patch session, but the brainstorm surfaced enough locked detail that the spec work was near-complete. Still has a silent dependency on `brand-dna-assessment.md` for build ordering: Phase 5 must ship Brand DNA first, then have Andy take SuperBad's own assessment, then build Surprise & Delight. Hidden-layer effects (CRT motion + rain ambient sound) are **explicitly exempt** from the Tier 2 motion closed list and the sound registry because they fire ≤1x/month/user; exemptions are named in the spec. 5 new build-time disciplines added (19–23). **Follow-up brainstorm owed:** 3–5 more admin eggs before Phase 5 build. **New memory:** `feedback_surprise_and_delight_philosophy.md` — every future spec touching a user-facing surface reads this. | [phase-3-surprise-and-delight-brainstorm-handoff.md](sessions/phase-3-surprise-and-delight-brainstorm-handoff.md) |
| 2026-04-12 | 3 | Spec | Quote Builder locked — 21 questions resolved. The most load-bearing Phase 3 spec by margin: first feature where money actually moves (Stripe Payment Intent + Subscription + webhook idempotency end-to-end), introduces the shared `scheduled_tasks` primitive + single Node cron worker on Coolify that every subsequent spec inherits, and carries a large cross-spec flag list. Stage-gated proposal structure (5 scroll-snap sections), Claude draft-from-context default + template + blank, retainer/project/mixed structures via Stripe `add_invoice_items`, per-quote term + expiry, GST-inclusive canonical storage (surfaced the "felt experience wins" principle), 7-state machine with view tracking, structured catalogue + `tier_rank`, two-pane editor with live preview, Claude-drafted send email + drift check + Andy review, deliberate-second-artefact PDF (not web-page compression), **same-page cinematic Payment Element reveal via Framer `layoutId`** inheriting Intro Funnel's existing Tier-2 slot (no new motion slot spent), shared-shell billing-mode-specific confirmation, pyramid-synthesis intro paragraph applying `feedback_client_doc_source_hierarchy`, supersede-via-new-row edit-after-send with hash-based proof-of-acceptance. **Q21 cancel flow is the longest lock:** Billing Portal cancel disabled, custom Lite-UX-layer flow at `/lite/portal/subscription` (owned by future Client Portal spec) with pre-term retainer options (*Let's chat* / pay remainder / 50% buyout), pre-term SaaS options (pay remainder / 1-month pause / continue), post-term retention (upgrade / downgrade / "here's what you'd be losing" / cancel through — no discounts ever). **Scheduled-tasks primitive:** polymorphic `task_type` with handler map, idempotency keys on every row, stale-row reclaim every 15 min, heartbeat table read by cockpit for red-banner alerting, exponential retry backoff, all handlers inherit §11.4 quiet window via `sendEmail()`. 5 Quote Builder handler types enumerated (`quote_expire` / `quote_reminder_3d` / `manual_invoice_send` / `subscription_pause_resume_reminder` / `subscription_pause_resume`). **New tables:** `quotes`, `catalogue_items`, `quote_templates`, `scheduled_tasks`, `worker_heartbeats`. **Pipeline cross-spec flags:** `won_outcome = 'project'` + 5 new `deals` columns + `companies.gst_applicable` + 18 new `activity_log.kind` values. **Sound registry:** 1 new slot `sound:quote_accepted` (admin-cockpit-only, visibility-gated). **Sprinkle claimed:** §3 Quote PDF cover line. **Motion slots spent:** 0 new (Payment Element reveal inherits). **Memory promotion flagged:** `feedback_felt_experience_wins` — when convention and felt experience conflict, felt experience wins unless compliance forces the convention. Surfaced twice in the brainstorm (Q6 GST, Q21 cancel flow). **Content mini-session owed:** catalogue content + all client-facing copy + email templates + cancel flow copy + PDF cover line decision + terms page. **Phase 5 sizing:** 3–4 sessions minimum (Data+Stripe / Scheduled-tasks / Draft editor+send / Client page+confirmation+cancel flow). **No new specs spawned.** | [phase-3-quote-builder-handoff.md](sessions/phase-3-quote-builder-handoff.md) |
| 2026-04-12 | 3 | Spec | Branded Invoicing locked — 25 questions resolved. Manual-billed-company invoice generation with ATO-compliant tax invoices, two-step generate/send primitive, 3-day pre-send review window, self-perpetuating monthly task chain (two tasks per cycle), per-company payment terms, bank transfer + Stripe online payment via inline Payment Element (inherits Tier-2 slot), full branded two-section scroll-snap web view, shared PDF renderer with Quote Builder, 5-state lifecycle, automated + manual reminders, standalone index + company billing tab, void + reissue via supersede. Refines Quote Builder's handler integration (first cycle enqueue → `manual_invoice_generate`, send handler narrows to send-only). **New table:** `invoices`. **Sprinkle claimed:** §3 Invoice PDF footer (split treatment). **No new motion/sound slots. No new specs spawned. No new memories.** Memory promotion completed: `feedback_felt_experience_wins.md` (owed from Quote Builder). | [phase-3-branded-invoicing-handoff.md](sessions/phase-3-branded-invoicing-handoff.md) |
| 2026-04-12 | 3 | Spec | Brand DNA Assessment locked — 30 questions resolved. Full psychological + aesthetic + communication profile. Three tracks (Founder / Business / Founder + Supplement) routed by alignment gate. Shape-aware sectioned banks (31 total). Card-per-question UI with ambient visual evolution. Between-section Opus insights (×4, human register). Cinematic reveal: first impression → beat → section-by-section build. Multi-stakeholder blending with divergence flags. Hard portal lock for retainer/SaaS, optional for trial shoot. Tiered downstream injection (full for Opus, tags-only for Haiku). SuperBad's own profile as Phase 5 milestone. **4 new tables.** **5 Opus prompts.** **1 new Tier-2 motion slot + 1 new sound.** **New memory:** `feedback_no_lite_on_client_facing.md`. **Content mini-session owed** — largest in the project. | [phase-3-brand-dna-assessment-handoff.md](sessions/phase-3-brand-dna-assessment-handoff.md) |
| 2026-04-12 | 3 | Spec | Task Manager (+ Braindump primitive) locked — 10 questions resolved through mini-brainstorm #4. Operational-spine feature with two surfaces: Task Manager module at `/lite/tasks` with list view + right-side detail drawer + filters + bulk delete, and a globally-available Braindump primitive (floating button + `Cmd+Shift+D` shortcut on admin surfaces only, in-place Claude parse → edit → commit modal flow, `⇄` swap affordance for ambiguous entity candidates, raw text persisted in `braindumps` table). **Load-bearing architectural decision:** deliverables are tasks — Client Management references this spec rather than defining its own deliverables data model. Single universal task model with closed `kind` enum (5 values) + closed `status` enum (7 values) validated via state machine in `lib/tasks/transitions.ts` + preset `recurrence` enum (6 values) auto-spawning on completion via `markTaskDone()` not calendar cron. Polymorphic-by-convention entity linking via `(entity_type, entity_id)` following the `activity_log` pattern. Approval workflow via single `approveDeliverable()` primitive with dual entry (tokenised magic-link email bypassing §11.4 quiet window + direct portal visit); rejection feedback fires as inbound message in comms inbox with new `source='task_rejection'`. Notifications intentionally minimal: zero push on Andy's own tasks, cockpit is primary surface, morning digest at 08:00 Melbourne gated by two conditions (Andy not signed in by 08:00 AND something to report), braindump ingestion silent. Cockpit integration is read-only with locked column contract (Must Do / Should Do / If Time filter rules; `awaiting_approval` + `blocked` stay off attention surface, surface as passive counts in narrative brief). **New tables:** `tasks`, `braindumps`. **New Claude primitives:** `parseBraindump(rawText, surfaceContext?)` (Haiku-tier, reads Brand DNA + recent contacts for entity matching), `approveDeliverable(taskId, decision, feedback?)`. **New build-time disciplines:** 24–29. **Cross-spec flags:** client-management.md (deliverables via this spec, no local model), daily-cockpit.md (tasks as data source with locked column contract), comms-inbox.md (gains `linked_task_id` + `'task_rejection'` source). **Foundations §11 patch owed in Phase 5:** `sendEmail()` gate gains required `classification: 'transactional' \| 'outreach'` parameter. **Sprinkle claimed:** §2 browser tab title for `/lite/tasks`. **No new specs spawned. No new memories.** | [phase-3-task-manager-brainstorm-handoff.md](sessions/phase-3-task-manager-brainstorm-handoff.md) |
| 2026-04-12 | 3 | Spec | Client Management locked — 33 questions resolved. Dual-entry company+contact profiles (7 and 5 tabs). Chat-first client portal: full-page AI chat with bartender register as portal home, menu bubble → clean overlay menu, 3-step tour, fallback chain (chat → Pixieset gallery → branded error page). AI chat: Opus Q&A + escalation + limited safe actions, persistent history, full log on admin Portal Chat tabs. Portal sections via menu. Real-time notifications: email + badge + SSE push. Individual isolated portals. Clients index with health scores + relationship stage filters. Global search (`Cmd+K`). One-click data export via `scheduled_tasks`. Intro Funnel migration job. **New table:** `portal_chat_messages`. **2 new contacts columns.** **3 Claude prompts.** **8 activity_log.kind values.** **2 scheduled_tasks.task_type values.** **Sprinkle claimed:** browser tab titles. **No new motion/sound/specs/memories.** | [phase-3-client-management-handoff.md](sessions/phase-3-client-management-handoff.md) |

| 2026-04-12 | 3 | Spec | SaaS Subscription Billing locked — 29 questions resolved. Generic multi-product billing infrastructure for Lite's self-serve SaaS arm. Identity-based pricing (setup fee on monthly, annual waives it — no discounts, commitment framing). Popcorn tiers within products (usage limits) and across products (Full Suite). Interactive demo landing pages (fully open, no gate, convention-break callout). Cold outreach meta-demonstration + pre-populated dashboard. Brand DNA pre-seeded for cold prospects. SaaS cancel flow: AI chatbot contact, motivational reality check, pay-remainder retains access, product-switch soft step. Fully native account management. Multiple independent subscriptions. Hard cap + upgrade prompt + personality usage bar. 5 new tables, 3 new deal columns, 11 activity_log.kind values, 3 scheduled_tasks types, 5 build-time disciplines, 12 voice-treated surfaces, 9 email templates. **No new motion/sound/specs/memories.** Content mini-session owed (large). | [phase-3-saas-subscription-billing-handoff.md](sessions/phase-3-saas-subscription-billing-handoff.md) |
| 2026-04-13 | 3 | Spec | Cost & Usage Observatory locked — 9 questions resolved. Operator-only cost-attribution + anomaly-detection surface. Dual primary job (alarm + attribution co-equal, forensic free). Four-actor model (`internal | external | shared | prospect`) with single-row log + ephemeral hashed cohort id + query-time proration. Three detectors in parallel (hard thresholds + rate loop-catcher + learned bands with 7-day/50-call warmup). Severity-tiered banners (low dry-calm / mid neutral-louder / severe terse-flat-red with kill-switch). Prospect spend transfers on conversion — CPA + CPA-by-channel + payback period all derivable. Attribution reoriented around tier health as primary signal; per-subscriber recommendation cards only for Large-tier (Small/Medium negative margin = tier-design bug surfaced at tier level). Operator-only costs (never expose AUD to subscribers); two separate ledgers. Claude diagnosis card + raw-data expandable; one-click band editor. No auto-pause, no platform hard cap; Andy-set monthly thresholds + linear run-rate projection alert. **4 new tables** (`external_call_log`, `cost_anomalies`, `deploy_events`, `observatory_settings`). Model/integration registry with ESLint-enforced no-direct-vendor-SDK-imports. **3 Claude prompts** (2 Opus, 1 Haiku). **`activity_log.kind` gains 10 values; `scheduled_tasks.task_type` gains 5 values.** **FOUNDATIONS §11 patch consolidates** send-gate classification + LLM model registry + external-call observability + quarterly review ritual into one amendment authored by Phase 5 Session A. **Sprinkle claimed:** weekly digest subject line. **2 new memories:** `feedback_technical_decisions_claude_calls.md` + `project_tier_limits_protect_margin.md`. **Phase 3.5 backward-pass load:** ~14 integration specs need actor-attribution review at call sites. Content mini-session owed (small-medium). **Phase 5 sizing:** 3 sessions (A: logging + registry + wrappers + FOUNDATIONS / B: detectors + diagnosis + banner wiring / C: dashboard + settings + digest). | [phase-3-cost-usage-observatory-handoff.md](sessions/phase-3-cost-usage-observatory-handoff.md) |
| 2026-04-13 | 3 | Spec | Setup Wizards locked — 11 questions resolved. Cross-cutting foundational primitive: library-as-primitive shell + 10-type step library + custom escape hatch + completion-contract-enforced artefacts. Slideover default, dedicated-route exception for flagship. Auto-save + 24h nudge + 7d cockpit banner + 30-day expiry. Single shell, `audience: admin \| client` tone prop. First-run admin critical flight (Stripe → Resend → Graph API) with capstone ceremony. Observatory registration automatic on completion + visible post-completion summary; vendor manifests at `lib/integrations/vendors/<vendor>.ts` co-locate SDK + band registration (ESLint-enforced shared with Observatory). Help escalation after 2 consecutive failures → portal bartender (client) or dedicated Claude chat (admin, new Opus job `admin-setup-assistant`, persistent thread). Curated 5 client-facing completions surface as Daily Cockpit chips; 7d-idle admin wizards as health banner. **4 gates enforce wizard functionality** (completion contract + per-session build + real-vendor verification + LAUNCH_READY row). **3 new tables** (`wizard_progress`, `wizard_completions`, `integration_connections`) + 1 more from admin chat (`admin_support_threads`). **12-wizard inventory** with completion contracts each. **6 activity_log.kind, 3 scheduled_tasks.task_type, 6 settings keys.** **Sprinkle claimed:** browser tab titles (phase-stratified, audience-differentiated). **1 new Tier-2 motion + 1 new Tier-1 candidate + 1 new sound** flagged for design-system revisit. **Cross-spec patches owed on 6 specs** (SaaS, Content Engine, Unified Inbox, Onboarding+Segmentation, Brand DNA, Intro Funnel) + Daily Cockpit contract extensions + Observatory model registry addition. Content mini-session owed. **Phase 5 sizing:** 3 sessions (A: shell + step library + data model / B: critical flight + capstone + admin Claude chat / C: integrations hub + remaining admin wizards). Client-facing wizards built within their parent spec's build sessions. **No new specs spawned. No new memories.** Mid-session feedback reinforcement: from Q6 onward, locked technical decisions silently per `feedback_technical_decisions_claude_calls.md`. | [phase-3-setup-wizards-handoff.md](sessions/phase-3-setup-wizards-handoff.md) |
| 2026-04-13 | 3 | Spec | Six-Week Plan Generator locked — 16 questions resolved. Final Phase 3 spec. Autonomous generation of the bespoke 6-week marketing plan bundled with the $297 trial shoot, framed as "what we'd implement if we were your agency — here, you run it." Two-stage Opus pipeline (strategy outline → per-week elaboration) + Haiku self-review, Andy's two-tier review with per-week regen, released to shared Client Management portal as read-only section + PDF takeaway (Puppeteer). Non-converter path: prospect clicks "Start Week 1" → plan becomes live tracker on portal for the remaining 60-day access window; at day 60 portal archives, PDF stays. Retainer path: first retainer payment fires Week 1 start; plan migrates into Client Context as `active_strategy` artefact on Deal Won with Andy's refresh-review at kickoff. One free prospect-side revision request routes into Andy's review queue. Scope wider than content — full marketing plan (email list, lead magnets, Meta ads, local SEO, etc.). Shoot-day notes captured on Trial Shoot panel: structured Marketing Infrastructure checklist (enrichment-prefilled) + Goals + 4 Shoot-Day Signals + Observations. Chat primitive NOT owned here — consumed from Client Management with pre-retainer rate-limit. **3 new tables** (`six_week_plans`, `six_week_plan_task_progress`, `trial_shoot_notes`). **17 new `activity_log.kind` values. 3 new `scheduled_tasks` task types. 3 new `sendEmail()` classifications. 9 new settings keys (`plan.*`). 4 new LLM jobs. 4 prompt files.** **2 new Tier-2 motion candidates** for design-system revisit. **Sprinkle claimed:** PDF footer dry line (seed placeholder). **Patches owed on 6 locked specs** (Intro Funnel ×3, Client Management ×2, Client Context Engine, Daily Cockpit, Cost & Usage Observatory, Branded Invoicing/SaaS Billing Stripe-hook, Surprise & Delight sprinkle bank). **Success metrics:** revision-request rate < 10% + regen count ≤ 2 + median review time < 10 min + retainer conversion lift. **v1.0 explicitly scope-cut:** no end-of-Week-6 re-planning, no mid-cycle retainer re-planning, no generation for direct/referral onboarding entries — all deferred to Strategic Planning v1.1. **Content mini-session owed** (medium — 2 Opus prompt calibration + self-review checklist + portal copy + PDF + emails + tab titles). **Phase 5 sizing:** 4 sessions (A: data model + migrations + helpers + prompt-file stubs / B: generator pipeline + Andy review UI / C: portal surface + PDF + revision flow / D: retainer migration + non-converter expiry + Stripe hook). **2 new memories:** `project_six_week_plan_is_the_retainer_plan.md`, `project_non_converter_portal_lifecycle.md`. **Closes Phase 3; Phase 3.5 is next.** | [phase-3-six-week-plan-generator-handoff.md](sessions/phase-3-six-week-plan-generator-handoff.md) |

| 2026-04-13 | 3.5 | Review | Step 2 — cross-spec flag reconciliation. Sub-agent scanned 20 specs: 95 ACK / 1 MISSING / 3 PARTIAL / 0 CONFLICT. Patches applied: `active_strategy` artefact added to Client Context Engine (§11.1, §11.2, §11.3, §12.5 — function library + consuming-specs table + lifecycle prose + table schema); 4 new `activity_log.kind` values in sales-pipeline.md; Brand DNA §3.2 existing-profile gate-skip note. PARTIALs deferred as accepted forward flags (Client Mgmt SSE/WebSocket choice = Phase 5 build call; Observatory actor-attribution = step 5/7 audit). No mop-up needed. Next: step 2a (spec self-containment inlining — heaviest sub-step). | [phase-3.5-step-2-flag-reconciliation-handoff.md](sessions/phase-3.5-step-2-flag-reconciliation-handoff.md) |
| 2026-04-13 | 4 | Build plan (partial) | BUILD_PLAN.md landed — 23 waves, ~150 atomic build sessions, dependency-strict ordering with aggregators last, Foundation A/B split, Wave 0 Pixieset spike, §C 60+ cron table, §R shared-primitive registry, §E 5 critical-flow E2E assignments, rollback strategy per wave. AUTONOMY_PROTOCOL.md + LAUNCH_READY.md deferred to fresh session per mid-session context-budget discipline. | [phase-4-partial-handoff.md](sessions/phase-4-partial-handoff.md) |
| 2026-04-13 | 5 | Spike (P0) | Pixieset API feasibility — **outcome B**. No public API, no webhook, no iframe support. Reverse-engineered endpoints unsupportable. Path B (on-brand link-out + manual URL paste trigger) locked for v1.0; alternatives mop-up declined (Pic-Time/Cloudspot/ShootProof share the same closed posture). Patches: intro-funnel §15.2/§15.3, BUILD_PLAN Wave 0 + IF-2, PATCHES_OWED F2.c section. | [p0-pixieset-spike-handoff.md](sessions/p0-pixieset-spike-handoff.md) |
| 2026-04-13 | 4 | Build plan (close) | AUTONOMY_PROTOCOL.md + LAUNCH_READY.md locked in fresh session. Autonomy protocol: 13 non-skippable per-session gates (G0 kickoff → G12 tracker+commit) including preflight precondition verification, mid-session 70% checkpoint, settings-literal grep, universal motion review, rollback declaration, artefact verification, typecheck+tests, E2E on 5 critical flows, model tiering via `/quick /normal /deep`, Opus-quota → pause (not silent degrade), cache-aware batching, kill-switch safety net, token-budget alerts. LAUNCH_READY.md: 12-section pre-launch gate (infra/email/payments/auth/compliance/observability/kill-switches/E2E-in-prod/synthetic-client dry-run/shadow-period readiness/docs/final sign-off). Phase 4 complete — tracker flipped to Phase 5. | [phase-4-handoff.md](sessions/phase-4-handoff.md) |
| 2026-04-13 | 5 | Build (A1) | Wave 1 A1 — project initialisation. Next.js 16.2.3 App Router + React 19.2.4 + TypeScript 5 + Tailwind v4 + shadcn (`base-nova` / neutral) scaffolded via temp-dir create-next-app merge. Dev server pinned to `:3001`. Env validator (zod, `lib/env.ts`) wired through `instrumentation.ts` — fires loud at boot. `.env.example` declares `NEXT_PUBLIC_APP_URL` / `NEXTAUTH_URL` / `NEXTAUTH_SECRET`. Typecheck + production build both clean. Rollback: git-revertable, no data shape change. Two PATCHES_OWED rows added (A5 owes Vitest harness; A2 owes public-SVG replacement). | [a1-handoff.md](sessions/a1-handoff.md) |
| 2026-04-13 | 5 | Build (A2) | Wave 1 A2 — design system tokens + theme/typeface presets. Full SuperBad token set live in `app/globals.css` (warm-neutral 8-step scale, brand palette, surfaces, semantic, spacing, 3 radii, 8-size type scale, motion constants, shadcn compatibility shims). Typed mirror at `lib/design-tokens.ts` with preset axes + closed-list types + preference enum shells. 9 Google fonts via `next/font/google` in `lib/fonts.ts`. Cookie-driven preset selection via `lib/presets.ts` + server actions. `/lite/design` reference route (renamed from `_design` — Next.js private-folder convention). Vitest harness installed; `tests/tokens.test.ts` enforces three-layer token parity. **Scope divergence locked silently** (`feedback_technical_decisions_claude_calls`): BUILD_PLAN A2's user-preference columns → rolled to A5 (Drizzle infra dependency); Settings → Display UI → rolled to post-A8 session (auth + primitives dependency). Typecheck + `npm test` (6/6) + `npm run build` all green. Rollback: git-revertable, no data shape change. 6 PATCHES_OWED rows added, 1 closed. | [a2-handoff.md](sessions/a2-handoff.md) |
| 2026-04-14 | 5 | Build (A3) | Wave 1 A3 — design system primitives. 35 shadcn primitives in `components/ui/` (32 via `npx shadcn add` + hand-rolled `form.tsx` and `date-picker.tsx`). 8 custom Lite wrappers in `components/lite/` (ThemeProvider, MotionProvider, SoundProvider, AdminShell, BrandHero, SkeletonTile, EmptyState, Tier2Reveal) — each with JSDoc listing consumed tokens. `BrandHero` enforces BHS closed list at type level. `/lite/design` extended with primitives gallery + 6-axis accessibility panel (10-axis coverage). Provider stubs for A4 (MotionProvider CSS-only; SoundProvider no-op; Tier2Reveal CSS keyframes). Typecheck + `npm test` (17/2) + `npm run build` clean. 3 PATCHES_OWED rows added. | [a3-handoff.md](sessions/a3-handoff.md) |
| 2026-04-14 | 5 | Build (A4) | Wave 1 A4 — motion + sound registry. `houseSpring` wired through Framer `MotionConfig` at `components/lite/motion-provider.tsx` with preference-derived transition (full → spring, reduced → 180ms linear, off → instant). 7 locked Tier 2 choreographies in `lib/motion/choreographies.ts` + `pdfRenderOverlay` Tier 1 token. 7-item sound registry in `lib/sounds.ts` with Howler-backed lazy-instanced `play()` at `components/lite/sound-provider.tsx`. `Tier2Reveal` rewritten on Framer with required typed `choreography` prop. `/lite/design` extended with picker-driven Tier 2 demo + 7 sound triggers. `SoundKey` union corrected to spec's canonical kebab-cased keys. `framer-motion` + `howler` + `@types/howler` installed; `use-sound` explicitly rejected. Typecheck + `npm test` (25/25, +8 new) + `npm run build` clean; `/lite/design` HTTP 200. 4 PATCHES_OWED rows added. | [a4-handoff.md](sessions/a4-handoff.md) |
| 2026-04-14 | 5 | Build (A5) | Wave 1 A5 — settings + permissions + kill-switches + ESLint rules. Drizzle client singleton + `settings` table + seed migration (68 rows: Finance 11, Wizards 6, Plan 10, Portal 5, Intro Funnel 1, Hiring 28, Email 4 new, Alerts 3 new — all `INSERT OR IGNORE` idempotent). `settings.get(key)` helper with typed key map + per-key zod parser + in-memory cache. `user` table with `role` + `timezone` + 6 display-preference columns + `first_signed_in_at_ms` (Auth.js-compatible; A8 wires NextAuth against it). `lib/auth/permissions.ts` `can(role, action, resource)` default-deny helper seeded with Finance Dashboard §13 matrix only (wave-extension pattern — every subsequent wave owes its own rules). `lib/kill-switches.ts` 4 flags default-disabled. `lib/types/glossary.ts` branded TS aliases for 11 entity types. 3 custom ESLint rules (`no-direct-anthropic-import`, `no-direct-stripe-customer-create`, `no-direct-resend-send`) loaded via jiti from `.mjs` config. `DATABASE_URL` env var added. Registry Totals reconciled to 68 (pre-A5 was 61; historical "60" was arithmetic error). Typecheck + `npm test` (42/42, +17 new) + `npm run build` + `npm run lint` all clean. Rollback: migration reversible. 4 PATCHES_OWED rows added. | [a5-handoff.md](sessions/a5-handoff.md) |
| 2026-04-14 | 5 | Build (A6) | Wave 1 A6 — activity log + scheduled tasks + inbox schema. 6 new tables: `activity_log`, `scheduled_tasks`, `worker_heartbeats`, `external_call_log`, `threads`, `messages`. Consolidated 217-value `activity_log.kind` enum + 37-value `task_type` enum. `logActivity()` + `enqueueTask()` + scheduled-tasks worker (gated, 15-min stale reclaim, 3-attempt retry, 50-row batch). `formatTimestamp(date, tz)` with 5 format modes. `lib/ai/models.ts` slug→model-ID registry (53 slugs). Migration `0002_a6_activity_scheduled_inbox.sql`. Typecheck + 68/68 tests + build + lint all clean. 6 PATCHES_OWED rows added. | [a6-handoff.md](sessions/a6-handoff.md) |
| 2026-04-13 | 5 | Build (A7) | Wave 1 A7 — email adapter + Stripe + PDF stub. Resend channel adapter with 16-value `EmailClassification` enum + `canSendTo()` suppression gate + `isWithinQuietWindow()` Melbourne tz gate + `checkBrandVoiceDrift()` Haiku grader + `ensureStripeCustomer()` + `renderToPdf()` stub. `legal_doc_versions` + `email_suppressions` schema. Migration `0003_a7_email_stripe_pdf.sql`. `ACTIVITY_LOG_KINDS` extended to 220. 115/115 tests green. Rollback: feature-flag-gated. 5 PATCHES_OWED rows added. | [a7-handoff.md](sessions/a7-handoff.md) |
| 2026-04-13 | 5 | Build (A8) | Wave 1 A8 — Portal-guard + Brand DNA Gate + Auth.js v5. **Foundation A closing session.** `portal_magic_links` + `brand_dna_profiles` tables. `lib/portal/guard.ts` + `issue-magic-link.ts` + `redeem-magic-link.ts`. NextAuth v5 split-config (Edge-safe `auth.config.ts` + Node.js `auth.ts`). `proxy.ts` Brand DNA Gate middleware (Next.js 16 renamed `middleware.ts` → `proxy.ts`). `/lite/onboarding` placeholder + `/lite/portal/recover` recovery form + `/lite/portal/r/[token]` redeem endpoint. Migration `0004_a8_portal_auth.sql`. `ACTIVITY_LOG_KINDS` → 221. Wave 2 B1/B2/B3 briefs written per G11.b. 150/150 tests green. 6 PATCHES_OWED rows added. | [a8-handoff.md](sessions/a8-handoff.md) |
| 2026-04-13 | 5 | Build (B1) | Wave 2 B1 — Sentry + reportIssue + cost alerts. Sentry SDK wired (client + server + edge; `sentry_enabled` kill-switch, default off). `lib/support/reportIssue()` + `lib/support/cost-alerts.ts`. `ReportIssueButton` global footer. `/lite/admin/errors` triage dashboard. `support_tickets` table + migration idx 5. `middleware.ts` deleted. `@sentry/nextjs` installed. 13 new tests (162 total). 6 PATCHES_OWED rows added. | [b1-handoff.md](sessions/b1-handoff.md) |
| 2026-04-13 | 5 | Build (B2) | Wave 2 B2 — Backups + DR + credential vault. `litestream.yml` → R2 replication. `lib/crypto/vault.ts` AES-256-GCM + `lib/crypto/index.ts` barrel. `no-direct-crypto` ESLint rule. `INCIDENT_PLAYBOOK.md` stub (8 sections). `docs/dr-runbook.md` runnable restore procedure. `FOUNDATIONS.md` §5 ATO 7-year retention patch. `.env.example` extended (CREDENTIAL_VAULT_KEY + R2 vars + DB_FILE_PATH). 10 new vault tests (172 total). No schema changes. Typecheck + lint + test clean. | [b2-handoff.md](sessions/b2-handoff.md) |
| 2026-04-13 | 5 | Build (B3) | Wave 2 B3 — Legal pages + cookie consent. **Foundation B closing session.** `/lite/legal/{index,terms,privacy,acceptable-use,cookie-policy}` MDX pages (public). `cookie_consents` table + migration `0006_b3_legal.sql`. GDPR consent banner (EU geo-gated, starts visible for SSR). `POST /api/cookie-consent` (SHA-256 IP hash). `legal_doc_versions` seeded 4 v1.0 rows. `legal.dsr_email` + `legal.dsr_response_days` settings (70 total). `next-mdx-remote/rsc` MDX rendering. 23 new tests (195 total). 5 PATCHES_OWED rows. BDA-1 brief written. Foundation-B exit checklist complete. | [b3-handoff.md](sessions/b3-handoff.md) |
| 2026-04-13 | 5 | Build (BDA-1) | Wave 3 BDA-1 — Brand DNA data model + invite token. **Opens Wave 3.** Extends `brand_dna_profiles` stub in-place (18 new columns). Creates `brand_dna_answers`, `brand_dna_blends`, `brand_dna_invites` tables. `lib/brand-dna/` module: `issueBrandDnaInvite()` + `redeemBrandDnaInvite()`. `ACTIVITY_LOG_KINDS` → 225. Migration `0007_bda1_brand_dna.sql` (journal idx 7). 11 new tests (206 total). 3 PATCHES_OWED rows. BDA-2 brief written. | [bda-1-handoff.md](sessions/bda-1-handoff.md) |
| 2026-04-14 | 5 | Build (SW-1) | Wave 4 SW-1 — Setup Wizards foundation. **Opens Wave 4.** 3 new tables (`wizard_progress` with partial unique index on live rows, `wizard_completions`, `integration_connections`) via migration `0008_sw1_wizards.sql` (journal idx 8). `lib/wizards/types.ts` (`WizardDefinition<T>`, `VendorManifest`, `CompletionContract<T>`, etc.). `lib/wizards/registry.ts` empty scaffold. `<WizardShell>` client chrome (houseSpring progress, cancel, help slot, expiry hint from prop). Off-whitelist server helper `lib/wizards/shell-config.ts` bundles `wizards.expiry_days` into props. Activity-log `wizard_*` kinds already present from Phase 4 consolidation — not re-added. 18 new tests (263 total). Typecheck + lint + build clean. 2 PATCHES_OWED rows. SW-3 brief written. | [sw-1-handoff.md](sessions/sw-1-handoff.md) |
| 2026-04-14 | 5 | Build (SW-8) | Wave 4 SW-8 — wizard nudge/expiry crons + voice (Track A). 3 new `ScheduledTaskType`s (`wizard_resume_nudge`, `wizard_expiry_warn`, `wizard_expire`); new independent `wizards_nudges_enabled` kill-switch (expire handler bypasses it — releases partial-unique slot regardless). `lib/wizards/nudge/` module: `content.ts` + `handlers.ts` (defensive, re-read row at fire time, stale resume-nudge no-ops) + `enqueue.ts` (`scheduleWizardNudges` / `cancelWizardNudges` — unwired; `wizard_progress` writer doesn't exist yet). Canonical email copy at `docs/content/setup-wizards/nudge-emails.md`. 10 new tests (340 total). No migration, no schema change, no settings keys touched. Typecheck + lint + build clean. SW-9 brief pre-compiled. | [sw-8-handoff.md](sessions/sw-8-handoff.md) |
| 2026-04-16 | 5 | Build (admin-polish-3) | Wave 9 admin-polish-3 — Invoices + Errors visual rebuild against `mockup-admin-interior.html` §§3, 5, 7, 8, 9, 10, 11. Invoices index: Righteous eyebrow + BHS H1 + Playfair italic mutter voiced on outstanding; §11 warm overdue banner (brand-red + orange bleed gradient, neutral-300 body) conditional; filter tabs `layoutId` spring; token-styled native search; §7 table (Righteous 10px/2px headers, `motion.tr` `whileHover`); stale variant (overdue+>14d) = neutral-500 + dashed pink-rgba left border + pink leading dot + "dormant · {date}"; `<tfoot>` three totals (Outstanding / Overdue / §9 Paid·FY with BHS 32px cream on success-rgba gradient — rule 05 earned). `InvoiceStatusBadge` rewritten as §5 chip (TONE map + 1×1 dot). Errors page: BHS `Error triage` + mutter on `openCount`; §11 warm open banner conditional; §7 table inline StatusChip (open brand-orange/resolved success); resolved rows dimmed neutral-500; query extended to open + resolved<30d so dimmed ladder has content. Voiced empties: `"No invoices yet." / "clean slate — or you haven't sent one."` + `"No errors. Impressive." / "or the logger's asleep. either way."`. `min-h-screen bg-background` stripped. G1 caught two brief-author spec-path errors (silent-corrected per `feedback_technical_decisions_claude_calls`); schema gap on errors severity (substituted status chips, logged). G10.5 `PASS_WITH_NOTES` two non-blocking defects (empty-state-mutter placement disputed against §8 + polish-2 precedent; stale-halo on rows deliberate non-adoption — row pulse fights §7). 832/1 green; typecheck + build clean. PATCHES_OWED: `admin_polish_3_manual_browser_verify`, `admin_polish_3_errors_severity_schema_gap`, `admin_polish_3_invoice_stale_halo_row_variant`, `admin_polish_3_stale_days_to_settings`, `admin_polish_3_resolved_window_to_settings`, `admin_polish_3_g105_rereview`. Two inherit-rules added to wave ledger (§7 tables recipe; warm alert banner recipe). admin-polish-4 brief pre-compiled. | [admin-polish-3-handoff.md](sessions/admin-polish-3-handoff.md) |

When a session completes, add a row here with a link to its handoff note.

---

## How to update this file

At the end of every session:

1. Tick the phase box if the whole phase is now complete.
2. Add a row to the session log.
3. Rewrite the **🧭 Next Action** block to point at whatever comes next (the next phase, or the next item inside the current multi-session phase).
4. Commit only if Andy asks — otherwise leave uncommitted.
